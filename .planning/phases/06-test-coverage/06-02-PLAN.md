---
phase: 06-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSControl.sol
  - test/unit/GNUSControlStorage.test.ts
  - diamond-typechain-types/GeniusDiamond.ts
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "D-04: GNUSControl.sol facet exposes external view getBannedTransferor(uint256 tokenId, address transferor) returns (bool) — single getter, not on the storage library"
    - "The getter delegates to GNUSControlStorage.isBannedTransferor via the using-for directive (checks both global and per-token ban maps)"
    - "The getter is callable through the diamond proxy after yarn compile regenerates diamond ABI + typechain"
    - "D-05/D-06: Tests in the extended test/unit/GNUSControlStorage.test.ts prove: banTransferorBatch → true, allowTransferorBatch → false, global ban via banTransferorForAll visible at tokenId 0 AND at other tokenIds, batch round-trips across multiple pairs"
    - "The facet change is committed inside the nested contracts/gnus-ai repo BEFORE the parent repo bumps the submodule pointer"
  artifacts:
    - path: "contracts/gnus-ai/GNUSControl.sol"
      provides: "getBannedTransferor external view getter"
      contains: "function getBannedTransferor(uint256 tokenId, address transferor) external view returns (bool)"
    - path: "test/unit/GNUSControlStorage.test.ts"
      provides: "Getter test coverage in a new describe block"
      contains: "getBannedTransferor"
    - path: "diamond-typechain-types/GeniusDiamond.ts"
      provides: "Regenerated typechain including the new selector"
      contains: "getBannedTransferor"
  key_links:
    - from: "contracts/gnus-ai/GNUSControl.sol"
      to: "contracts/gnus-ai/GNUSControlStorage.sol"
      via: "GNUSControlStorage.layout().isBannedTransferor(tokenId, transferor)"
      pattern: "getBannedTransferor"
    - from: "test/unit/GNUSControlStorage.test.ts"
      to: "GeniusDiamond proxy"
      via: "geniusDiamond.getBannedTransferor(...)"
      pattern: "getBannedTransferor\\("
---

<objective>
Complete TEST-03: add the missing banned-transferor read path. Add external view
`getBannedTransferor(uint256 tokenId, address transferor) returns (bool)` to the
GNUSControl facet (D-04), mirroring the `isEmergencyPaused()` pattern, delegating
to the existing internal `GNUSControlStorage.isBannedTransferor`. Extend the
existing GNUSControlStorage unit test file with getter coverage (D-06): ban →
true, allow → false, global ban visible via tokenId 0 (D-05), and batch
ban/allow round-trips.

Purpose: The ban maps (`gBannedTransferors`, `bannedTransferors[tokenId]`) are
write-only today — no external read exists. Tests and integrators cannot verify
ban state without this getter. tokenId 0 (GNUS_TOKEN_ID) is the caller-side
global-ban query convention (D-05).

Output: Modified facet in the nested `contracts/gnus-ai` submodule (committed
there first), regenerated diamond ABI + typechain, extended unit tests in the
parent repo, parent-repo submodule pointer bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-coverage/06-CONTEXT.md
@.planning/phases/06-test-coverage/06-PATTERNS.md

<interfaces>
<!-- Locked decisions implemented by this plan -->
- D-04: getter lives on the GNUSControl FACET (not the storage library), named
  getBannedTransferor, signature (uint256 tokenId, address transferor) returns
  (bool). Single getter — no split per-token/global getters.
- D-05: getBannedTransferor(0, addr) is the global-ban check convention
  (GNUS_TOKEN_ID = 0, GNUSConstants.sol:29).
- D-06: tests EXTEND test/unit/GNUSControlStorage.test.ts — no new test file.

<!-- Pattern to mirror — GNUSControl.sol:84-89 (isEmergencyPaused) -->
  /**
   * @notice Returns whether the diamond is currently emergency-paused.
   */
  function isEmergencyPaused() external view returns (bool) {
      return GNUSControlStorage.layout().paused;
  }

<!-- Library target — GNUSControlStorage.sol:45-53 (internal view, already combined) -->
  function isBannedTransferor(uint256 tokenId, address sender) internal view returns (bool) {
      return layout().gBannedTransferors[sender] || layout().bannedTransferors[tokenId][sender];
  }

<!-- using-for directive already present at GNUSControl.sol:20 -->
  using GNUSControlStorage for GNUSControlStorage.Layout;

<!-- Test file constants already declared (GNUSControlStorage.test.ts:20-24) -->
  const GNUS_TOKEN_ID = 0;
  const NFT_TOKEN_ID_1 = 1;
  const NFT_TOKEN_ID_2 = 2;
  Signers in scope: owner (SUPER_ADMIN), user1, user2, user3.
  Setter calls available on geniusDiamond proxy:
    banTransferorForAll(address), allowTransferorForAll(address),
    banTransferorBatch(uint256[], address[]), allowTransferorBatch(uint256[], address[])

<!-- Typechain regeneration -->
  `yarn compile` chains: npx hardhat compile → diamond:generate-proxy-abi-typechain
  → diamond:generate-gnus-abi-typechain (package.json:11). This refreshes
  diamond-typechain-types/GeniusDiamond.ts with the new selector.
  HAZARD: do NOT run `hardhat clean` — it deletes
  typechain-types/factories/diamond-abi/GeniusDiamond__factory.ts which the
  diamond-abi index re-exports. Plain `yarn compile` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getBannedTransferor view to GNUSControl facet, compile, commit nested submodule (TEST-03, D-04)</name>
  <files>contracts/gnus-ai/GNUSControl.sol, diamond-typechain-types/GeniusDiamond.ts</files>
  <read_first>
    - contracts/gnus-ai/GNUSControl.sol (full file, 185 lines — see
      isEmergencyPaused at lines 84-89, protocolInfo at ~166-176, and the
      banTransferor/allowTransferor setters at ~96-145)
    - contracts/gnus-ai/GNUSControlStorage.sol lines 40-60 (the internal
      isBannedTransferor target)
    - contracts/gnus-ai/GNUSConstants.sol line 29 (GNUS_TOKEN_ID = 0)
    - .planning/phases/06-test-coverage/06-PATTERNS.md (function skeleton and
      placement convention)
  </read_first>
  <action>
    Per D-04, add ONE new external view function to
    contracts/gnus-ai/GNUSControl.sol. Placement: immediately after
    isEmergencyPaused() (after line 89), before banTransferorForAll. No new
    imports; the using-for directive at line 20 already makes the library
    function callable on the layout. Do not gate behind onlySuperAdminRole —
    view getters on this facet are unauthenticated (confirmed by
    isEmergencyPaused and protocolInfo).

    Function contract: `function getBannedTransferor(uint256 tokenId, address
    transferor) external view returns (bool)` with body
    `return GNUSControlStorage.layout().isBannedTransferor(tokenId, transferor);`.
    Doxygen header: @notice "Returns whether an address is banned from
    transferring a given token ID." @dev noting delegation to
    GNUSControlStorage.isBannedTransferor and that it checks both the global
    ban map (gBannedTransferors) and the per-token ban map
    (bannedTransferors[tokenId]); passing GNUS_TOKEN_ID (0) is the caller-side
    convention for querying global-ban status (per D-05). @param tokenId
    "(0 = GNUS, used as the global-ban check)". @param transferor. @return.

    Multi-repo commit protocol (MANDATORY ordering):
    1. Edit the file inside the nested submodule working tree
       (contracts/gnus-ai/ is its own git repo — remote
       GeniusVentures/gnus-ai-contracts; confirm current branch with
       `git -C contracts/gnus-ai branch --show-current`, expected develop).
    2. From the PARENT repo root, run `yarn compile` (NOT `yarn hardhat clean`
       first — clean deletes typechain-types/factories/diamond-abi/
       GeniusDiamond__factory.ts). Verify exit 0 and that
       diamond-typechain-types/GeniusDiamond.ts now contains
       getBannedTransferor.
    3. Commit the facet change INSIDE the nested repo:
       `git -C contracts/gnus-ai add GNUSControl.sol` then
       `git -C contracts/gnus-ai commit -m "feat: add getBannedTransferor view
       (TEST-03)"`. Do not commit anything else in the nested repo. Do not add
       a Co-Authored-By trailer.
    4. Do NOT yet commit the parent-repo pointer bump — that happens in Task 2
       after tests pass, so the parent never points at an unverified facet.
  </action>
  <verify>
    <automated>grep -n "function getBannedTransferor(uint256 tokenId, address transferor) external view returns (bool)" contracts/gnus-ai/GNUSControl.sol &amp;&amp; grep -c "getBannedTransferor" diamond-typechain-types/GeniusDiamond.ts &amp;&amp; git -C contracts/gnus-ai log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function getBannedTransferor(uint256 tokenId, address transferor) external view returns (bool)" contracts/gnus-ai/GNUSControl.sol` returns exactly 1 match.
    - `grep -c "isBannedTransferor(tokenId, transferor)" contracts/gnus-ai/GNUSControl.sol` returns at least 1 (delegation present).
    - New function is positioned between isEmergencyPaused and banTransferorForAll (grep -n line numbers confirm ordering).
    - `yarn compile` exits 0; `grep -c "getBannedTransferor" diamond-typechain-types/GeniusDiamond.ts` returns at least 1.
    - `git -C contracts/gnus-ai status --porcelain` is empty after the nested commit (facet change committed inside nested repo).
    - `git -C contracts/gnus-ai log -1 --format=%s` contains "getBannedTransferor".
    - Parent repo `git status --porcelain contracts/gnus-ai` shows the submodule pointer as modified (not yet committed — Task 2 owns that).
  </acceptance_criteria>
  <done>
    Getter exists on the facet with correct signature/delegation/Doxygen,
    yarn compile regenerated typechain with the new selector, and the change is
    committed inside the nested contracts/gnus-ai repo. Parent pointer bump
    deferred to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend GNUSControlStorage.test.ts with getBannedTransferor tests, run suite, bump submodule pointer (TEST-03, D-05, D-06)</name>
  <files>test/unit/GNUSControlStorage.test.ts, contracts/gnus-ai</files>
  <read_first>
    - test/unit/GNUSControlStorage.test.ts (full file, 335 lines — fixture at
      lines 26-62, constants at 20-24, 'Global banned transferors' describe at
      128-178, 'Token-specific banned transferors' describe at 180-259; the
      file ends at line 335 with the closing of the top-level describe)
    - .planning/phases/06-test-coverage/06-PATTERNS.md (assertion pattern and
      the four required coverage cases)
  </read_first>
  <action>
    Per D-06, add ONE new `describe('getBannedTransferor view', ...)` block
    inside the existing top-level describe('GNUSControlStorage Tests'), placed
    immediately after the 'Token-specific banned transferors' describe block
    (i.e., before the 'Protocol initialization' describe at line 261). No new
    imports, no new fixture — reuse the existing before/beforeEach/afterEach
    snapshot fixture and the constants GNUS_TOKEN_ID, NFT_TOKEN_ID_1,
    NFT_TOKEN_ID_2 and signers owner, user1, user2. Assertion style:
    `expect(await geniusDiamond.getBannedTransferor(id, addr)).to.equal(bool)`
    (scalar read style per protocolInfo tests).

    Write exactly four it() tests covering D-06:
    1. 'should report per-token ban set by banTransferorBatch' — ban
       [NFT_TOKEN_ID_1] for user1 via banTransferorBatch; assert
       getBannedTransferor(NFT_TOKEN_ID_1, user1) === true AND
       getBannedTransferor(NFT_TOKEN_ID_2, user1) === false (per-token
       isolation; global map untouched so other tokenIds are false).
    2. 'should report unbanned after allowTransferorBatch' — ban then allow
       [NFT_TOKEN_ID_1] for user1; assert final
       getBannedTransferor(NFT_TOKEN_ID_1, user1) === false.
    3. 'should report global ban via tokenId 0 and via any other tokenId' —
       assert getBannedTransferor(GNUS_TOKEN_ID, user1) === false initially;
       banTransferorForAll(user1); assert getBannedTransferor(GNUS_TOKEN_ID,
       user1) === true (D-05 convention) AND getBannedTransferor(NFT_TOKEN_ID_1,
       user1) === true (global map OR'd into every tokenId by
       isBannedTransferor); allowTransferorForAll(user1); assert both return
       false.
    4. 'should round-trip batch bans across multiple token/address pairs' —
       banTransferorBatch([NFT_TOKEN_ID_1, NFT_TOKEN_ID_2], [user1, user2]);
       assert all four combinations return true for the banned pairs and false
       for the cross pairs (user1/token2, user2/token1 — note per-token map is
       keyed [tokenId][address], so pair (token1,user2) is false); then
       allowTransferorBatch with the same arrays and assert all four pairs
       return false.

    All addresses via `await userN.getAddress()`. No changes to any other
    describe block.

    Then run the test file and the broader suite:
    1. `npx hardhat test test/unit/GNUSControlStorage.test.ts` — must exit 0
       with the 4 new tests passing.
    2. `yarn test` (full suite) — baseline is 416 passing / 6 failing where the
       6 failures are PRE-EXISTING Safe SDK mock issues in
       scripts/safe/proposeSafeTransaction.ts tests (protocolKit.isOwner is not
       a function). Acceptance: the 4 new tests pass, the pre-existing failure
       count does not increase beyond 6, and no previously-passing test newly
       fails. Do NOT attempt to fix the 6 Safe SDK failures — out of scope.
    3. After tests pass, bump the parent-repo submodule pointer:
       `git add contracts/gnus-ai` from the parent repo root and commit with
       message "chore: bump gnus-ai-contracts submodule — getBannedTransferor
       view (TEST-03)" together with the test file change. No Co-Authored-By
       trailer.
  </action>
  <verify>
    <automated>npx hardhat test test/unit/GNUSControlStorage.test.ts 2&gt;&amp;1 | grep -E "getBannedTransferor view|passing|failing"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "describe('getBannedTransferor view'" test/unit/GNUSControlStorage.test.ts` returns exactly 1 match.
    - `grep -c "getBannedTransferor(" test/unit/GNUSControlStorage.test.ts` returns at least 12 (four tests with multiple assertions each).
    - New describe block is inside describe('GNUSControlStorage Tests') and before 'Protocol initialization' (line-number ordering confirms).
    - `npx hardhat test test/unit/GNUSControlStorage.test.ts` exits 0; output shows 4 new passing tests under 'getBannedTransferor view'.
    - Full `yarn test`: failing count ≤ 6 and every failure is in the Safe SDK proposeSafeTransaction tests (pre-existing); the new getter tests all pass.
    - `git -C contracts/gnus-ai status --porcelain` is clean; parent `git log -1 --format=%s` references the submodule bump / TEST-03; `git diff HEAD~1 --submodule=short -- contracts/gnus-ai` shows the pointer moved to the Task 1 nested commit.
  </acceptance_criteria>
  <done>
    Four getter tests exist and pass through the diamond proxy, full suite
    failure count unchanged from baseline (6 pre-existing Safe SDK failures
    only), nested submodule pointer bumped and committed in the parent repo.
  </done>
</task>

</tasks>

<verification>
- `grep -n "getBannedTransferor" contracts/gnus-ai/GNUSControl.sol` → 1 function definition
- `grep -c "getBannedTransferor" diamond-typechain-types/GeniusDiamond.ts` → ≥ 1
- `npx hardhat test test/unit/GNUSControlStorage.test.ts` → all pass including 4 new tests
- `yarn test` → no new failures beyond the 6 pre-existing Safe SDK mock failures
- Nested repo clean, parent pointer committed
</verification>

<success_criteria>
- TEST-03 satisfied: GNUSControl exposes getBannedTransferor(tokenId,
  transferor) delegating to GNUSControlStorage.isBannedTransferor (D-04);
  tokenId 0 serves as global-ban query (D-05); existing control test file
  extended with ban/allow/global/batch coverage (D-06); diamond ABI + typechain
  regenerated; nested-submodule commit ordering honored.
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage/06-02-SUMMARY.md` when done
</output>
