---
phase: 06-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/foundry/fuzz/ExampleFuzz.t.sol
  - test/unit/NFTFactory.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "D-01: test/foundry/fuzz/ExampleFuzz.t.sol no longer exists in the repo"
    - "Forge fuzz suite still passes with the 12 remaining real fuzz files"
    - "The 2nd-gen child mint transaction in NFTFactory.test.ts has an explicit success assertion (receipt status === 1)"
    - "The supply-delta block is its own it() test, separate from the mint-success test"
    - "D-02: NFTFactory.test.ts explicitly asserts burntSupply === 0n for 2nd-gen child mints, with a comment noting Phase 9 will change this invariant"
    - "D-03: No production code in contracts/ was modified by this plan (GNUSNFTFactory burn logic untouched)"
  artifacts:
    - path: "test/unit/NFTFactory.test.ts"
      provides: "Completed 2nd-gen child mint assertions per D-02"
      contains: "receipt.status === 1"
  key_links:
    - from: "test/unit/NFTFactory.test.ts"
      to: "GeniusDiamond (via signer1Diamond mint)"
      via: "receipt status assertion on the mint transaction"
      pattern: "receipt.*status.*===.*1"
---

<objective>
Complete TEST-01 and TEST-02: delete the stub fuzz scaffolding file and finish the
three incomplete assertion sites in `test/unit/NFTFactory.test.ts` — asserting
CURRENT contract behavior only. No production code changes (D-03).

Purpose: Remove placeholder test scaffolding that provides zero coverage, and
convert three TODO/commented sites into real, passing assertions that lock in
current 2nd-gen mint behavior ahead of Phase 9 reserve accounting.

Output: Deleted `test/foundry/fuzz/ExampleFuzz.t.sol`; updated
`test/unit/NFTFactory.test.ts` with (a) mint receipt success assertion,
(b) supply-delta assertions split into their own `it()` test, (c) explicit
`burntSupply === 0n` no-burn assertion replacing the commented-out BN-style
burn assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-coverage/06-CONTEXT.md
@.planning/phases/06-test-coverage/06-PATTERNS.md

<interfaces>
<!-- Locked decisions implemented by this plan -->
- D-01: DELETE test/foundry/fuzz/ExampleFuzz.t.sol entirely. No replacement tests —
  12 real fuzz files already exist in test/foundry/fuzz/.
- D-02: Assert current behavior; do NOT change GNUSNFTFactory burn semantics.
- D-03: No changes to GNUSNFTFactory._beforeTokenTransfer / mint burn logic.

<!-- Existing in-file patterns the executor reuses (from 06-PATTERNS.md) -->
NFTFactory.test.ts already uses native BigInt literal style: `50n`, `1n`, `0n`
(lines ~502-504, ~546-548) and `BigInt(burntSupply) === toWei(...)` (line ~384).
Use the same `assert(x === y, 'msg')` chai style. Do NOT use ethers v5 BN style
(`.eq(...)`) — that is the style of the commented-out code being replaced.

The file already imports `{ assert, expect }` from 'chai', `utils` from ethers,
and `toWei` from '../../scripts/utils/helpers'. No new imports needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stub fuzz file ExampleFuzz.t.sol (TEST-01, D-01)</name>
  <files>test/foundry/fuzz/ExampleFuzz.t.sol</files>
  <read_first>
    - test/foundry/fuzz/ExampleFuzz.t.sol (confirm it is the 5-stub file with
      assertTrue(true, "Replace with actual fuzz test") placeholders)
    - Directory listing of test/foundry/fuzz/ (confirm the 12 real sibling fuzz
      files remain: AccessControlFuzz, BridgeFuzz, DiamondAccessControl,
      DiamondCoreFuzz, DiamondInvariants, DiamondOwnership, DiamondRouting,
      ERC1155Fuzz, ERC20Fuzz, GNUSWithdrawLimiterFuzz, NFTFactoryFuzz,
      SecurityFuzz)
  </read_first>
  <action>
    Per D-01: delete the file with `git rm test/foundry/fuzz/ExampleFuzz.t.sol`.
    Do not create any replacement file and do not modify any other fuzz file.
    Do not modify foundry.toml, remappings, or package.json — the forge test
    runner discovers *.t.sol files dynamically, so no config references the
    deleted file. Verify no other file imports or references ExampleFuzz
    (grep for "ExampleFuzz" across the repo; expected result: only the deleted
    file itself matched, so after deletion zero matches remain).
  </action>
  <verify>
    <automated>test ! -f test/foundry/fuzz/ExampleFuzz.t.sol &amp;&amp; ! grep -rq "ExampleFuzz" --include="*.sol" --include="*.ts" --include="*.toml" --include="*.json" . 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test/foundry/fuzz/ExampleFuzz.t.sol` does not exist on disk and is staged
      as deleted in git (`git status` shows `deleted:` for the path).
    - `grep -r "ExampleFuzz" test/ foundry.toml remappings.txt package.json`
      returns zero matches.
    - Exactly 12 `*.t.sol` files remain in `test/foundry/fuzz/`
      (`ls test/foundry/fuzz/*.t.sol | wc -l` outputs 12).
  </acceptance_criteria>
  <done>File deleted via git rm, no dangling references, 12 real fuzz files remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Complete 2nd-gen child mint assertions in NFTFactory.test.ts (TEST-02, D-02)</name>
  <files>test/unit/NFTFactory.test.ts</files>
  <read_first>
    - test/unit/NFTFactory.test.ts (full file — specifically the it() block
      'Should mint child NFTS (tokens) of GNUS with address 1 and burn GNUS
      tokens at exchange rate' starting at line 315, and the it() block
      'should mint batch (multiple) child NFTs, fail for unauthorized, succeed
      for authorized &amp; burn correct amount of GNUS' starting at line 430)
    - .planning/phases/06-test-coverage/06-PATTERNS.md sections on
      NFTFactory.test.ts (assertion style, BigInt conventions, line-371/375/522
      patterns)
  </read_first>
  <action>
    Three surgical edits to test/unit/NFTFactory.test.ts, per D-02. Do not touch
    any other test block. Do not modify any file under contracts/.

    Edit A (current line 371 — the TODO "// TODO This needs an assert to check
    the transaction is successful."): replace the TODO comment with a receipt
    success assertion on the existing `tx` (a ContractTransactionResponse from
    signer1Diamond['mint(address,uint256,uint256,bytes)'](signer2,
    newParentNFTID, toWei(5), '0x')): `const receipt = await tx.wait();` then
    `assert(receipt !== null &amp;&amp; receipt.status === 1, 'Child NFT mint
    transaction should succeed');`. Keep the existing `await logEvents(tx);`
    line after the assertion.

    Edit B (current lines 375-389 — the block beginning with the TODO
    "// TODO This begins is a different test that needs to be added." through
    the debuglog of total GNUS burned): move this block OUT of the current
    it() into its own it() test placed immediately after the current one,
    titled 'Should burn correct GNUS supply for 2nd gen child NFT mint'. The
    moved block references `startingSupply`, `geniusDiamond`, and
    `GNUS_TOKEN_ID`, which are in scope in the parent describe — the new it()
    must re-fetch what it needs rather than relying on variables local to the
    previous test: re-read starting/ending supply inside the new test is NOT
    possible post-hoc, so instead structure the split as: the parent test keeps
    everything through the receipt assertion + logEvents; the new it() re-mints
    nothing — it re-declares the supply-delta measurement by reading
    `geniusDiamond['totalSupply(uint256)'](GNUS_TOKEN_ID)` before and after a
    fresh identical mint (signer2 recipient, newParentNFTID, toWei(5), '0x')
    and asserts `BigInt(burntSupply) === toWei(5.0 * 2.0)` with the same
    message string as the original. This keeps each test self-contained
    (mocha tests must not share mutable locals). Keep the original assertion
    text: `Burnt Supply should equal minted * exchange rate (5.0*2.0), but
    equals ${burntSupply.toString()}`. Note: `newParentNFTID` is declared in
    the parent describe scope (visible at line ~336 usage), so the new it() can
    reference it; verify this by reading the enclosing describe — if it is
    block-scoped to the original it(), hoist nothing and instead re-derive it
    from `signer1Diamond.getNFTInfo` state exactly as the original test did.
    Preserve the existing `debuglog(...)` call at the end of the moved block.

    Edit C (current lines 522-525 — the TODO comment plus commented-out
    BN-style burn assertion in the mintBatch test): delete the four lines
    (TODO comment, '// Assert the correct amount...' comment, and the two
    commented-out assert lines) and replace with an explicit current-behavior
    assertion using native BigInt style (matching `balance1 === 0n` usage
    already in this file): a two-line comment reading "NOTE: GNUSNFTFactory
    does not currently burn GNUS for 2nd gen child tokens. Phase 9
    (Treasury/Reserve) will replace this with explicit reserve accounting and
    restore the burn invariant." followed by
    `assert(burntSupply === 0n, \`2nd gen child mint should not burn GNUS
    (Phase 9 will change this), but burnt ${utils.formatEther(burntSupply)}\`);`.
    Keep the surrounding `expectedBurn` computation and console.log lines
    unchanged (they are diagnostic logging, not assertions). Keep the existing
    `debuglog(`Total GNUS burned: ...`)` line after the new assertion.

    Do NOT uncomment or restore the `.eq(expectedBurn)` BN-style assertion —
    it documents behavior Phase 9 will introduce, not current behavior (D-02,
    D-03).
  </action>
  <verify>
    <automated>npx hardhat test test/unit/NFTFactory.test.ts --grep "NFT Factory" 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TODO" test/unit/NFTFactory.test.ts` returns zero matches at the
      three target sites (lines ~371, ~375, ~522 in the original file); any
      TODOs elsewhere in the file are untouched.
    - `grep -c "receipt.status === 1" test/unit/NFTFactory.test.ts` returns
      at least 1.
    - `grep -n "Should burn correct GNUS supply for 2nd gen child NFT mint"
      test/unit/NFTFactory.test.ts` matches exactly one it() title.
    - `grep -n "burntSupply === 0n" test/unit/NFTFactory.test.ts` returns
      exactly 1 match.
    - `grep -n "burntSupply.eq" test/unit/NFTFactory.test.ts` returns zero
      matches (BN-style assertion not restored).
    - `grep -n "Phase 9" test/unit/NFTFactory.test.ts` matches the new comment
      at the no-burn assertion site.
    - `npx hardhat test test/unit/NFTFactory.test.ts` exits 0 with all tests in
      the file passing, including the two new/renamed tests (receipt-success
      assertion test and the split supply-delta test). No test count decrease
      versus the pre-edit run of this file; expect +1 test from the split.
    - `git diff --stat contracts/` shows zero changes.
  </acceptance_criteria>
  <done>
    NFTFactory.test.ts has: receipt success assertion after the 2nd-gen mint,
    supply-delta assertions in their own it() test, and an explicit
    burntSupply === 0n assertion with Phase 9 comment. All tests in the file
    pass. No production contracts modified.
  </done>
</task>

</tasks>

<verification>
- `ls test/foundry/fuzz/*.t.sol | wc -l` → 12
- `npx hardhat test test/unit/NFTFactory.test.ts` → all tests pass (this file
  has no dependency on the 6 pre-existing Safe SDK mock failures, which live in
  scripts/safe/proposeSafeTransaction.ts tests)
- `git diff --stat contracts/ gnus-ai/contracts/` → empty (test-only plan)
</verification>

<success_criteria>
- TEST-01 satisfied: ExampleFuzz.t.sol deleted, zero placeholder assertions
  remain in the repo (grep for "Replace with actual fuzz test" returns nothing).
- TEST-02 satisfied: the three sites (line 371, 375, 522-525) are completed per
  D-02 with current-behavior assertions; full NFTFactory.test.ts suite passes.
- Zero changes under contracts/ (D-03 honored).
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage/06-01-SUMMARY.md` when done
</output>
