---
phase: 11-erc-20-proxy-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - test/unit/GNUSRedeemAdapter.test.ts
  - contracts/gnus-ai/testing/MockERC20Proxy.sol
autonomous: true
requirements:
  - PROXY-03
must_haves:
  truths:
    - "Happy path: user approves diamond as ERC-1155 operator, proxy contract calls redeem(user, childId, amount, recipient), user child balance decreases by amount, recipient GNUS balance increases by amount, diamond child balance is 0 after the tx, Converted-equivalent state change verified, RedeemedViaAdapter event emitted"
    - "Direct-EOA path: user calls redeem(themselves, childId, amount, recipient) with no proxy and it succeeds (the adapter is not proxy-gated — D-05 generic)"
    - "redeem(0, ...) reverts with 'Cannot redeem GNUS itself'"
    - "redeem with amount == 0 reverts with 'Amount must be greater than zero'"
    - "redeem with recipient == address(0) reverts with 'ERC1155: mint to the zero address'"
    - "redeem with from == address(0) reverts with 'ERC1155: transfer from the zero address'"
    - "redeem on an uncreated childId reverts with 'Token not created.'"
    - "redeem on a nonConvertible child token reverts with 'Token is non-convertible' (D-09, AI Credits forward-compat)"
    - "redeem when from has insufficient child balance reverts with 'ERC1155: insufficient balance for transfer'"
    - "redeem when from has NOT approved the diamond as operator reverts with 'ERC1155: caller is not token owner or approved'"
    - "The WR-07 limiter is charged against `from` (the user), NOT against the proxy contract and NOT against the diamond (Pitfall 2 regression test)"
    - "Super-admin bypass: when from is the contract owner, the limiter is NOT charged and SuperAdminBypass is emitted with context 'GNUSRedeemAdapter.redeem' (raw topic assertion — library-declared event)"
    - "The diamond's redeems succeed through the receiver hook (onERC1155Received) — happy path passing IS the Pitfall 1 regression test"
    - "Batch transfers to the diamond revert with 'GNUSRedeemAdapter: batch transfers not accepted'"
    - "The redeem selector is present on the diamond post-upgrade (loupe check)"
    - "The diamond never holds child tokens after a redeem tx (balanceOf(diamond, childId) == 0)"
    - "Full unit suite + Foundry suite remain green (no Phase 11 regressions beyond the documented baseline)"
  artifacts:
    - path: "test/unit/GNUSRedeemAdapter.test.ts"
      provides: "Full unit test suite for the redeem adapter (13 test cases)"
      contains: "describe('GNUS Redeem Adapter Tests'"
    - path: "contracts/gnus-ai/testing/MockERC20Proxy.sol"
      provides: "Minimal mock proxy contract that calls diamond.redeem on behalf of a user — proves the adapter is callable by an arbitrary external contract (D-05)"
      contains: "contract MockERC20Proxy"
  key_links:
    - from: "test/unit/GNUSRedeemAdapter.test.ts"
      to: "contracts/gnus-ai/GNUSRedeemAdapter.sol::redeem"
      via: "signer-connected diamond contract call"
      pattern: "\\.redeem\\("
    - from: "test/unit/GNUSRedeemAdapter.test.ts"
      to: "contracts/gnus-ai/testing/MockERC20Proxy.sol::redeemOnBehalf"
      via: "proxy-mediated call in the contract-caller test"
      pattern: "mockProxy\\.redeemOnBehalf\\("
    - from: "test/unit/GNUSRedeemAdapter.test.ts (limiter test)"
      to: "GNUSWithdrawLimiter.getAccountWithdrawStatus"
      via: "currentUsage delta assertion keyed to `from`"
      pattern: "getAccountWithdrawStatus\\("
---

<objective>
Create the complete unit test suite for the GNUSRedeemAdapter facet (Plan 11-01 output) plus a minimal mock external proxy contract that proves the adapter is callable by an arbitrary external contract (D-05 generic requirement). This plan delivers the behavioral verification for PROXY-03.

Purpose: Every truth in Plan 11-01's must_haves gets an executable assertion. The suite mirrors the GNUSTreasury.test.ts pattern (LocalDiamondDeployer fixture, snapshot isolation, raw-topic assertion for library-declared events) per 11-PATTERNS.md.

Output: New file `test/unit/GNUSRedeemAdapter.test.ts` (13 test cases across 4 describe blocks), new file `contracts/gnus-ai/testing/MockERC20Proxy.sol` (test helper). No production code modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-erc-20-proxy-hardening/11-CONTEXT.md
@.planning/phases/11-erc-20-proxy-hardening/11-RESEARCH.md
@.planning/phases/11-erc-20-proxy-hardening/11-PATTERNS.md
@.planning/phases/11-erc-20-proxy-hardening/11-VALIDATION.md
@.planning/phases/11-erc-20-proxy-hardening/11-01-SUMMARY.md

@contracts/gnus-ai/GNUSRedeemAdapter.sol
@test/unit/GNUSTreasury.test.ts

<interfaces>
<!-- Test infrastructure contracts; extracted from the live codebase. -->

From test/unit/GNUSTreasury.test.ts (the fixture pattern this suite copies — read it in full):
- LocalDiamondDeployer config shape (diamondName, networkName, provider, chainId, writeDeployedDiamondData: false, configFilePath)
- multichain.getProviders() / networkProviders map pattern
- beforeEach/afterEach snapshot isolation via evm_snapshot / evm_revert
- seedProvenanceIfNeeded() storage-probe pattern (TREASURY_STORAGE_SLOT + 1n, eth_getStorageAt)
- bootWithChild() fixture: mint GNUS to owner, createNFT(GNUS_TOKEN_ID, ...) at rate 2e18, mint child to signer1
- Raw-topic assertion for SuperAdminBypass: ethers.id('SuperAdminBypass(address,uint256,string)') + receipt.logs.find
- getAccountWithdrawStatus(address).currentUsage for limiter assertions

From contracts/gnus-ai/GNUSRedeemAdapter.sol (Plan 11-01 output — the system under test):
```solidity
function redeem(address from, uint256 childId, uint256 amount, address recipient) external
event RedeemedViaAdapter(address indexed caller, address indexed from, uint256 indexed childId, uint256 amount, address recipient)
// Revert strings to assert exactly:
//   "Cannot redeem GNUS itself"
//   "Amount must be greater than zero"
//   "ERC1155: mint to the zero address"
//   "ERC1155: transfer from the zero address"
//   "Token not created."
//   "Token is non-convertible"
//   "GNUSRedeemAdapter: batch transfers not accepted" (onERC1155BatchReceived)
// Inherited reverts that fire through the pull:
//   "ERC1155: insufficient balance for transfer" (from ERC1155Upgradeable)
//   "ERC1155: caller is not token owner or approved" (from ERC1155Upgradeable)
```

From diamond-typechain-types (GeniusDiamond):
- The `redeem` selector appears on GeniusDiamond after Plan 11-01's config change + typechain regeneration (`yarn hardhat run` the diamond-abi regen, or the LocalDiamondDeployer does it automatically on deploy — verify in setup)
- `setApprovalForAll(operator, approved)` — user approves diamondAddress as operator
- `balanceOf(account, id)`, `totalSupply(id)`
- `getAccountWithdrawStatus(account)` returns struct with `currentUsage` field
- `createNFT(parentId, name, symbol, exchangeRate, maxSupply, uri)` — owner-only child creation
- `mint(address,uint256)` and `mint(address,uint256,uint256,bytes)` overloads

Setting a child token's nonConvertible flag for the D-09 test:
- GNUSNFTFactory does NOT expose a public setter for nonConvertible on an existing token (Phase 9 design: flag set at creation or via storage write in tests)
- Use the storage-slot write pattern from GNUSTreasury.test.ts: `nftNonConvertibleSlot(tokenId)` computes keccak256(abi.encode(tokenId, FACTORY_STORAGE_SLOT)) + 8n; write via `provider.send('hardhat_setStorageAt', [diamondAddress, slot, '0x...01'])`
- FACTORY_STORAGE_SLOT = keccak256("gnus.ai.nft.factory.storage")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create MockERC20Proxy.sol test helper contract</name>
  <files>contracts/gnus-ai/testing/MockERC20Proxy.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSRedeemAdapter.sol (the interface the mock calls — redeem's four-parameter signature)
    - contracts/gnus-ai/testing/ (check whether this directory exists; if not, this task creates it. Look at existing test-helper contracts elsewhere in the repo for placement convention — e.g. is there a contracts/testing or contracts/mocks directory already?)
  </read_first>
  <action>
    Create `contracts/gnus-ai/testing/MockERC20Proxy.sol` — a minimal stand-in for an external ERC-20 proxy contract. Its ONLY job is to receive a call from a test and forward it to `diamond.redeem(from, childId, amount, recipient)`, proving that an arbitrary external contract can invoke the adapter (D-05: "any conforming external ERC-20 proxy").

    If `contracts/gnus-ai/testing/` does not exist, create it. If the repo already has a conventional location for test-only contracts (check for `contracts/mocks/`, `contracts/test/`, or similar first — use Grep/Glob), place the file there instead and note the deviation in the SUMMARY.

    File contents:
    - SPDX-License-Identifier: MIT; pragma solidity ^0.8.19;
    - Import `IGNUSRedeemAdapter` inline as a local interface (do NOT import the facet contract itself — the mock should depend only on the function signature, mirroring how a real external proxy would integrate):
      ```
      interface IGNUSRedeemAdapter {
          function redeem(address from, uint256 childId, uint256 amount, address recipient) external;
      }
      ```
    - `contract MockERC20Proxy` with:
      - `IGNUSRedeemAdapter public immutable diamond;` — set in constructor
      - `constructor(address _diamond) { diamond = IGNUSRedeemAdapter(_diamond); }`
      - `function redeemOnBehalf(address from, uint256 childId, uint256 amount, address recipient) external { diamond.redeem(from, childId, amount, recipient); }` — the proxy passes the user through as `from`; msg.sender of the outer call (the test) is irrelevant to the adapter
    - NatSpec on the contract: `@notice Minimal mock of an external ERC-20 proxy contract for testing GNUSRedeemAdapter. Intentionally NOT the real erc20-gnus-proxy contract — this proves the adapter is generic (D-05): any contract that knows the user address can drive a redemption.`
    - NatSpec on redeemOnBehalf explaining: in the real deployment, the proxy would call this after its own ERC-20 allowance bookkeeping (PROXY-01 work, erc20-gnus-proxy workstream); here we test only the diamond-side contract-caller path.

    Constraints:
    - No state beyond the immutable diamond reference
    - No access control on redeemOnBehalf (it's a mock; the adapter's own approval check is the security boundary)
    - No events (the adapter's RedeemedViaAdapter is the audit trail)
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -10 && test -f contracts/gnus-ai/testing/MockERC20Proxy.sol && grep -q "contract MockERC20Proxy" contracts/gnus-ai/testing/MockERC20Proxy.sol && grep -q "function redeemOnBehalf(address from, uint256 childId, uint256 amount, address recipient) external" contracts/gnus-ai/testing/MockERC20Proxy.sol && grep -q "diamond.redeem(from, childId, amount, recipient)" contracts/gnus-ai/testing/MockERC20Proxy.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` succeeds with no errors in MockERC20Proxy.sol
    - File exists at `contracts/gnus-ai/testing/MockERC20Proxy.sol` (or the repo's conventional test-helper location if one exists — documented in SUMMARY)
    - Contract has exactly one external function beyond the constructor: `redeemOnBehalf(address, uint256, uint256, address)`
    - The internal call is `diamond.redeem(from, childId, amount, recipient)` with parameters passed through unmodified
    - The interface is declared locally in the file (no import of GNUSRedeemAdapter.sol)
    - No `onlyOwner`, no events, no mutable state
  </acceptance_criteria>
  <done>
    A minimal external-contract caller exists that the test suite can deploy and use to prove the adapter works when msg.sender is a contract (the motivating D-05 scenario).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create GNUSRedeemAdapter.test.ts with the full 13-case suite</name>
  <files>test/unit/GNUSRedeemAdapter.test.ts</files>
  <read_first>
    - test/unit/GNUSTreasury.test.ts (read IN FULL — this is the structural template: imports, describe nesting, fixture functions, snapshot isolation, raw-topic assertion, limiter assertions. Copy its patterns, not its test logic.)
    - contracts/gnus-ai/GNUSRedeemAdapter.sol (the system under test — read to confirm revert strings and event signature match what Plan 11-01 shipped)
    - .planning/phases/11-erc-20-proxy-hardening/11-VALIDATION.md (the Per-Task Verification Map — suite names in this test file are grep targets for it; do NOT deviate from the mapped names)
    - .planning/phases/11-erc-20-proxy-hardening/11-PATTERNS.md §"test/unit/GNUSRedeemAdapter.test.ts" (authoritative test pattern)
  </read_first>
  <action>
    Create `test/unit/GNUSRedeemAdapter.test.ts` mirroring `test/unit/GNUSTreasury.test.ts` structure exactly. TypeScript, tabs for indentation, single quotes, semicolons — match the existing file's style.

    **File header comment:** Phase 11 — GNUSRedeemAdapter unit tests (Plan 11-02). State that the suite pins PROXY-03 behaviorally: happy path (direct + proxy-mediated), full revert matrix, WR-07 limiter attribution to `from`, super-admin bypass, receiver-hook enablement, batch rejection, loupe selector presence, no-custody invariant. Note that suite names are literal grep targets for 11-VALIDATION.md — do not rename.

    **Imports + setup (copy GNUSTreasury.test.ts:1-60 pattern):** same chai/chai-as-promised setup, same import list (drop nothing, add nothing except `MockERC20Proxy` typechain type if needed — check `diamond-typechain-types` or use ethers.Contract with a minimal ABI if the mock isn't in the diamond typechain). The top-level describe is `describe('GNUS Redeem Adapter Tests', async function () {` — exact string, grep target.

    **Constants:** `GNUS_TOKEN_ID = 0n`, `FACTORY_STORAGE_SLOT` (same as GNUSTreasury.test.ts:62), and the `nftNonConvertibleSlot` helper function copied verbatim from GNUSTreasury.test.ts:80-85.

    **Fixture (per-network describe block, copy GNUSTreasury.test.ts:87-155):** same before/beforeEach/afterEach structure. The `bootWithChild()` fixture is copied from GNUSTreasury.test.ts:184-211 with ONE addition: after minting the child to signer1, add `await signer1Diamond.setApprovalForAll(diamondAddress, true);` so the pull in redeem has its operator approval. Rename the local variable from `signer1` semantics where needed — in this suite the "user" is signer1, the "recipient" is signer2, the "owner/super-admin" is owner. Keep variable names consistent with the source file where possible.

    **Additional fixture:** `bootWithNonConvertibleChild()` — same as bootWithChild but after createNFT, write `nonConvertible = true` via `provider.send('hardhat_setStorageAt', [diamondAddress, nftNonConvertibleSlot(childId), ethers.zeroPadValue('0x01', 32)])`. Read it back via `eth_getStorageAt` and assert it's 1 before proceeding (defensive — catches slot-layout drift).

    **Test cases — 13 total, organized in 4 nested describe blocks. Suite/test names are grep targets from 11-VALIDATION.md; use them EXACTLY:**

    describe('happy path', ...):
    1. `'redeems child tokens for GNUS via the adapter (happy path)'` — bootWithChild; signer1 (user) calls `signer1Diamond.redeem(signer1, childId, toWei('30'), signer2)`; assert: (a) tx emits `RedeemedViaAdapter` with args (signer1, signer1, childId, toWei('30'), signer2); (b) signer1 child balance decreased by exactly 30e18; (c) signer2 GNUS balance increased by exactly 30e18; (d) `balanceOf(diamondAddress, childId) == 0` (no-custody invariant); (e) totalSupply(childId) decreased by 30e18 and totalSupply(GNUS_TOKEN_ID) increased by 30e18 (supply-neutral reallocation).
    2. `'is callable by a contract (simulated external proxy)'` — bootWithChild; deploy MockERC20Proxy with diamondAddress; user (signer1) has already approved the diamond as operator in the fixture; call `mockProxy.connect(signers[9]).redeemOnBehalf(signer1, childId, toWei('20'), signer2)` — note the CALLER of the mock is a third party (signers[9]), proving the adapter doesn't care who calls as long as `from` has approved; assert: RedeemedViaAdapter emitted with caller == mockProxy.address, from == signer1; signer1 child down 20; signer2 GNUS up 20; diamond child balance 0.
    3. `'redeem selector present on diamond (loupe check)'` — call the diamond loupe `facetFunctionSelectors` or `facets()` and assert that at least one facet exposes the `redeem(address,uint256,uint256,address)` selector (compute via `ethers.id('redeem(address,uint256,uint256,address)').slice(0, 10)`). Use whatever loupe interface GeniusDiamond exposes (check GNUSTreasury.test.ts or GNUSBridgeIn.test.ts for an existing loupe-call pattern to copy).

    describe('revert matrix', ...):
    4. `'reverts when childId is GNUS_TOKEN_ID'` — bootWithChild; `await expect(signer1Diamond.redeem(signer1, GNUS_TOKEN_ID, toWei('10'), signer2)).to.be.revertedWith('Cannot redeem GNUS itself');`
    5. `'reverts when amount is zero'` — `revertedWith('Amount must be greater than zero')`
    6. `'reverts when recipient is zero address'` — `revertedWith('ERC1155: mint to the zero address')`
    7. `'reverts when from is zero address'` — `revertedWith('ERC1155: transfer from the zero address')`
    8. `'reverts when child token is not created'` — use childId 999n (never created); `revertedWith('Token not created.')` — note the trailing period, match exactly
    9. `'reverts when child token is nonConvertible'` — bootWithNonConvertibleChild; `revertedWith('Token is non-convertible')` — this is the D-09 / AI Credits forward-compat test
    10. `'reverts when caller has insufficient balance'` — bootWithChild (signer1 has 100 child); attempt redeem of toWei('200'); `revertedWith('ERC1155: insufficient balance for transfer')`
    11. `'reverts when from has not approved the diamond as operator'` — bootWithChild BUT revoke the approval first: `await signer1Diamond.setApprovalForAll(diamondAddress, false);` then attempt redeem; `revertedWith('ERC1155: caller is not token owner or approved')`
    12. `'reverts on batch transfer to the diamond'` — signer1 calls `safeBatchTransferFrom(signer1, diamondAddress, [childId], [toWei('10')], '0x')` directly on the diamond; `revertedWith('GNUSRedeemAdapter: batch transfers not accepted')` — proves the batch receiver hook rejects (stranded-custody defense, T-11-05)

    describe('withdrawal limiter (WR-07)', ...):
    13. `'charges the withdrawal limiter against from, not the proxy or the diamond'` — bootWithChild; deploy MockERC20Proxy; snapshot `getAccountWithdrawStatus(signer1).currentUsage`, `getAccountWithdrawStatus(mockProxy.address).currentUsage`, `getAccountWithdrawStatus(diamondAddress).currentUsage`; run `mockProxy.redeemOnBehalf(signer1, childId, toWei('25'), signer2)`; assert: signer1's currentUsage increased by exactly toWei('25'); mockProxy's currentUsage UNCHANGED; diamondAddress's currentUsage UNCHANGED. This is the Pitfall 2 regression test — the whole reason the adapter doesn't use `this.convert()`.
    14. `'emits SuperAdminBypass when from is super admin (raw topic)'` — bootWithChild; ALSO `await ownerDiamond['mint(address,uint256,uint256,bytes)'](owner, childId, toWei('50'), '0x')` to give the owner child tokens, and `await ownerDiamond.setApprovalForAll(diamondAddress, true)`; snapshot owner's currentUsage; call `ownerDiamond.redeem(owner, childId, toWei('10'), owner)`; assert via RAW TOPIC (copy GNUSTreasury.test.ts:265-276 pattern): receipt.logs contains topic `ethers.id('SuperAdminBypass(address,uint256,string)')`, topics[1] == zeroPadValue(owner, 32), decoded data == [toWei('10'), 'GNUSRedeemAdapter.redeem']; owner's currentUsage UNCHANGED. NOTE: the context string 'GNUSRedeemAdapter.redeem' must match what Plan 11-01 shipped — verify against the contract source when writing this assertion.

    **Constraints:**
    - Every revert string asserted character-for-character against the contract source
    - The `bootWithChild` fixture's `setApprovalForAll(diamondAddress, true)` is the ONLY operator approval in the suite (except test 11 which revokes it, and test 14 which approves for owner) — never approve the mock proxy as operator; the approval model is user-approves-diamond directly (Open Question 3 resolution)
    - No `std::this_thread::sleep_for` equivalent — no `setTimeout`-based waits; all assertions are on mined tx receipts
    - `this.timeout(0)` at the top describe (diamond deployment time)
    - Snapshot isolation on every test (beforeEach/afterEach)
    - The suite MUST pass against the Plan 11-01 diamond config — if `redeem` isn't on the diamond, the LocalDiamondDeployer fixture is broken and that's a Plan 11-01 bug, not a test bug
  </action>
  <verify>
    <automated>npx hardhat test test/unit/GNUSRedeemAdapter.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test test/unit/GNUSRedeemAdapter.test.ts` runs to completion
    - All 14 test cases pass (13 from the validation map + the batch-rejection case — if the count differs, the SUMMARY must explain the delta)
    - Zero tests are skipped or pending
    - The output shows the describe name `GNUS Redeem Adapter Tests` (grep target)
    - Test file contains the exact revert strings: 'Cannot redeem GNUS itself', 'Amount must be greater than zero', 'ERC1155: mint to the zero address', 'ERC1155: transfer from the zero address', 'Token not created.', 'Token is non-convertible', 'ERC1155: insufficient balance for transfer', 'ERC1155: caller is not token owner or approved', 'GNUSRedeemAdapter: batch transfers not accepted'
    - The limiter test (case 13) asserts `currentUsage` delta on `signer1` == the redeemed amount AND `currentUsage` delta on the mock proxy address == 0 AND on the diamond address == 0 — all three assertions present in source
    - The SuperAdminBypass test (case 14) uses the raw-topic pattern (`receipt.logs.find(log => log.topics[0] === ...)`) — NOT chai `.to.emit` — because the event is library-declared
    - The proxy-mediated test (case 2) has the mock called by a THIRD signer (not the user, not the owner) — proving caller-agnosticism
    - Every test uses snapshot isolation (beforeEach evm_snapshot / afterEach evm_revert present in the file)
    - File uses tabs, single quotes, semicolons — consistent with GNUSTreasury.test.ts
    - Full unit suite (`npx hardhat test`) shows no NEW failures beyond the documented baseline (1 pre-existing GNUSControlStorage chainID failure per STATE.md); Foundry suite (`yarn forge:test`) shows no new failures beyond the 2 documented Phase 08.1 baseline failures
  </acceptance_criteria>
  <done>
    The complete PROXY-03 behavioral suite is green: happy path (direct and proxy-mediated), full revert matrix with exact strings, limiter attribution to `from`, super-admin bypass with correct context string, receiver-hook enablement, batch rejection, loupe selector presence, and the no-custody invariant. No regressions in the wider unit or Foundry suites.
  </done>
</task>

</tasks>

<verification>
- `npx hardhat test test/unit/GNUSRedeemAdapter.test.ts` — all cases green.
- `npx hardhat test` — full unit suite green except documented baseline (GNUSControlStorage chainID, 1 failure per STATE.md).
- `yarn forge:test` — Foundry suite green except documented baseline (2 Phase 08.1 Safe setUp reverts per STATE.md).
</verification>

<success_criteria>
- 14 test cases across 4 describe blocks, all passing.
- Every revert string from Plan 11-01 asserted exactly.
- Limiter attribution proven: user charged, proxy NOT charged, diamond NOT charged.
- SuperAdminBypass emitted with context 'GNUSRedeemAdapter.redeem' (raw topic).
- Proxy-contract caller path proven via MockERC20Proxy driven by a third-party signer.
- No-custody invariant asserted (diamond child balance == 0 post-redeem).
- No regressions beyond the STATE.md documented baseline.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → diamond | Test harness deploys a local diamond via LocalDiamondDeployer; all assertions are against on-chain state of that deployment. |
| test → MockERC20Proxy | The mock is deployed fresh per test; it holds no state and grants no approvals. |
| storage-slot writes (hardhat_setStorageAt) | Used ONLY for the nonConvertible flag setup (test 9). This is test-only manipulation of diamond storage, acceptable in unit tests on a local chain; never in production scripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-T01 | Tampering | Test asserts wrong revert string (paraphrase drift) | mitigate | Acceptance criteria require exact strings; strings cross-checked against contract source in read_first |
| T-11-T02 | Tampering | Storage-slot layout drift breaks nftNonConvertibleSlot | mitigate | Test writes then READS BACK the slot and asserts == 1 before proceeding (defensive read-back in bootWithNonConvertibleChild) |
| T-11-T03 | Elevation of Privilege | Test fixture accidentally approves the mock proxy as operator, masking the approval model | mitigate | Constraint in Task 2: the ONLY setApprovalForAll calls target diamondAddress; acceptance criteria grep for this |
| T-11-T04 | Denial of Service | Flaky tests via shared diamond state pollution | mitigate | Snapshot isolation (evm_snapshot/evm_revert) on every test — same pattern as GNUSTreasury.test.ts |
| T-11-T05 | Repudiation | SuperAdminBypass asserted via .to.emit fails silently (event absent from ABI) | mitigate | Raw-topic assertion pattern mandated (PATTERNS §Shared Patterns — library-declared events) |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — tests use existing Hardhat/Chai/ethers v6 stack. Slopcheck N/A. |

## ASVS L1 Mapping

- V5 Input Validation: revert matrix cases 4-12 pin every input-validation require
- V7 Error Handling: exact-string assertions on all revert paths
- V13 API: case 2 proves the external-contract caller path (the D-05 generic requirement)
- V14 Configuration: case 3 (loupe check) proves the facet is correctly registered post-upgrade
</threat_model>

<output>
Create `.planning/phases/11-erc-20-proxy-hardening/11-02-SUMMARY.md` when done.
</output>
