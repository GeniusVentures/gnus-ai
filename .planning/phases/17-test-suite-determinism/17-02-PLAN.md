---
phase: 17-test-suite-determinism
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - test/unit/GNUSBridgeIn.test.ts
  - test/unit/GNUSBridgeAttestorIn.test.ts
  - test/unit/GNUSBridgeAttestorUpgrade.test.ts
  - test/unit/GNUSBridgeEnhanced.test.ts
  - test/unit/GNUSNFTFactoryEnhanced.test.ts
  - test/unit/DiamondInitFacet-limiter.test.ts
  - test/unit/ERC1155ProxyOperator.test.ts
  - test/unit/ERC20TransferBatch.test.ts
  - test/unit/GNUSContractAssets.test.ts
  - test/unit/GNUSWithdrawLimiter.test.ts
  - test/unit/GNUSWithdrawLimiterStorage.test.ts
  - test/unit/GeniusOwnershipFacet.test.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "Every test/unit Tier-A scaffold calls ensureDiamondTestBaseline() BEFORE its initialSnapshotId is taken"
    - "Bridge suites (GNUSBridgeIn, GNUSBridgeAttestorIn) still run green with their setChainID(localChainId) re-alias applied AFTER the baseline call, inside their own snapshot window"
    - "Duplicated probe-guard blocks and their TREASURY_STORAGE_SLOT constants are gone from these files — the helper is the only copy"
    - "Full `yarn test` after the sweep reports 0 failing"
  artifacts:
    - path: "test/unit/GNUSBridgeIn.test.ts"
      provides: "bridge scaffold: helper replaces probe block, re-alias preserved"
      contains: "ensureDiamondTestBaseline("
    - path: "test/unit/GNUSWithdrawLimiterStorage.test.ts"
      provides: "plain scaffold with baseline call (limiter-slot probes untouched)"
      contains: "ensureDiamondTestBaseline("
  key_links:
    - from: "test/unit/GNUSBridgeIn.test.ts"
      to: "test/utils/diamond-baseline.ts"
      via: "helper call replacing probe block :259-265, before re-alias :272 and snapshot :281"
      pattern: "ensureDiamondTestBaseline\\("
    - from: "test/unit/GNUSBridgeIn.test.ts"
      to: "diamond chainID 31337"
      via: "setChainID(localChainId) AFTER baseline, BEFORE evm_snapshot"
      pattern: "setChainID\\(localChainId\\)"
---

<objective>
Sweep the 12 remaining test/unit Tier-A scaffold-snapshot suites (per D-03 and the operator's binding scope ruling: all 17 snapshot-carrying scaffold files get the baseline call — GNUSControlStorage was 17-01, the 4 non-unit files are 17-03).

Purpose: Make every unit suite order-independent against the shared cached diamond, and delete the duplicated probe-guard bodies in the 5 unit files that carry them (the helper from 17-01 becomes the only copy).
Output: 12 files with the baseline call inserted before their snapshot; 5 of them with probe blocks/unused slot constants folded away; full suite still 0-failing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-test-suite-determinism/17-CONTEXT.md
@.planning/phases/17-test-suite-determinism/17-PATTERNS.md
@.planning/phases/17-test-suite-determinism/17-01-SUMMARY.md

<interfaces>
<!-- From 17-01 (must exist before this plan runs) -->

From test/utils/diamond-baseline.ts:
```typescript
export const TREASURY_STORAGE_SLOT = /* keccak256("gnus.ai.treasury.storage") */;
export async function ensureDiamondTestBaseline(
    geniusDiamond: GeniusDiamond,
    diamondAddress: string,
): Promise<void;  // probe-guarded seed + setChainID(0) + updateBridgeFee(0)
```

Import convention from test/unit/: `import { ensureDiamondTestBaseline } from '../utils/diamond-baseline';`

Verified scaffold shapes on the working tree (17-PATTERNS.md, re-verified this session):
- Plain: loadDiamondContract(...) → "// Take initial snapshot" → initialSnapshotId = evm_snapshot
- Bridge (GNUSBridgeIn :259-265 probe → :270-272 re-alias → :281 snapshot; GNUSBridgeAttestorIn :396-402 → :407-409 → :419)
- Probe-fold plain: GNUSBridgeAttestorUpgrade (probe at :145-region, slot const :54, snapshot :148), GNUSBridgeEnhanced (probe :49-59, slot const :19, snapshot :62), GNUSNFTFactoryEnhanced (probe :57-region, slot const :16, snapshot :61)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge scaffolds — replace probe block, keep re-alias AFTER baseline</name>
  <files>test/unit/GNUSBridgeIn.test.ts, test/unit/GNUSBridgeAttestorIn.test.ts</files>
  <read_first>
  - test/unit/GNUSBridgeIn.test.ts (before() :239-294 — probe :259-265, re-alias :270-272, snapshot :281, slot const :61)
  - test/unit/GNUSBridgeAttestorIn.test.ts (before() :391-424 — probe :396-402, re-alias :407-409, snapshot :419, slot const :173)
  - test/utils/diamond-baseline.ts (the helper being wired)
  </read_first>
  <action>
  In both files (per D-03):

  1. Add `import { ensureDiamondTestBaseline } from '../utils/diamond-baseline';`
  2. REPLACE the entire probe block (GNUSBridgeIn :259-265; GNUSBridgeAttestorIn :396-402 — the `eth_getStorageAt` send, the `if (BigInt(initialized) === 0n)` and the guarded `GNUSTreasury_SetSeedSupply(0n)`) with the single line `await ensureDiamondTestBaseline(geniusDiamond, diamondAddress);`
  3. KEEP the re-alias block UNCHANGED and positioned AFTER the helper call (GNUSBridgeIn :270-272 / GNUSBridgeAttestorIn :407-409): `const network = await ethers.provider.getNetwork(); localChainId = network.chainId; await geniusDiamond.setChainID(localChainId);` — bridge suites NEED chainID=31337 for bridgeIn's destination-chain guard; only the inherited-state leak dies, not the alias (17-CONTEXT.md Specific Ideas)
  4. KEEP the snapshot statement last (:281 / :419)
  5. DELETE the now-unused local `TREASURY_STORAGE_SLOT` constant (:61 / :173) — the helper owns the slot from now on. Only delete it if no other reference remains in the file (check `grep -n TREASURY_STORAGE_SLOT <file>` after the fold).

  Ordering after the edit, in both files: load diamond → helper call → re-alias to 31337 → attestor setup → evm_snapshot.
  </action>
  <verify>
    <automated>cd gnus-ai && for f in test/unit/GNUSBridgeIn.test.ts test/unit/GNUSBridgeAttestorIn.test.ts; do b=$(grep -n "ensureDiamondTestBaseline(" "$f" | head -1 | cut -d: -f1); a=$(grep -n "setChainID(localChainId)" "$f" | head -1 | cut -d: -f1); s=$(grep -n "evm_snapshot" "$f" | head -1 | cut -d: -f1); echo "$f baseline=$b alias=$a snap=$s"; [ -n "$b" ] && [ "$b" -lt "$a" ] && [ "$a" -lt "$s" ] && echo OK || echo FAIL; done && npx hardhat test test/unit/GNUSBridgeIn.test.ts test/unit/GNUSBridgeAttestorIn.test.ts test/unit/GNUSControlStorage.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
  - In both files: line(ensureDiamondTestBaseline() < line(setChainID(localChainId)) < line(first evm_snapshot)
  - `grep -c "GNUSTreasury_SetSeedSupply" test/unit/GNUSBridgeIn.test.ts` returns 0 and same for GNUSBridgeAttestorIn.test.ts (probe block fully folded)
  - `grep -c "TREASURY_STORAGE_SLOT" <file>` returns 0 for both files after the fold
  - Targeted run `npx hardhat test test/unit/GNUSBridgeIn.test.ts test/unit/GNUSBridgeAttestorIn.test.ts test/unit/GNUSControlStorage.test.ts` reports 0 failing — this one-process run reproduces the pollution direction (bridge suites set 31337; the control-storage suite still passes)
  </acceptance_criteria>
  <done>Both bridge scaffolds declare the baseline through the helper and self-alias inside their own snapshot window; no duplicated probe remains.</done>
</task>

<task type="auto">
  <name>Task 2: Probe-fold plain scaffolds (3 files)</name>
  <files>test/unit/GNUSBridgeAttestorUpgrade.test.ts, test/unit/GNUSBridgeEnhanced.test.ts, test/unit/GNUSNFTFactoryEnhanced.test.ts</files>
  <read_first>
  - test/unit/GNUSBridgeAttestorUpgrade.test.ts (before() with probe :140-150-region, slot const :54, snapshot :148; NOTE the in-test setChainID at :244 is NOT a defect — leave it)
  - test/unit/GNUSBridgeEnhanced.test.ts (probe :49-59, slot const :19, snapshot :62)
  - test/unit/GNUSNFTFactoryEnhanced.test.ts (probe at :57-region, slot const :16, snapshot :61)
  - test/utils/diamond-baseline.ts
  </read_first>
  <action>
  Same transformation as Task 1 minus the re-alias concern, in all three files:

  1. Add `import { ensureDiamondTestBaseline } from '../utils/diamond-baseline';`
  2. Replace the probe block with `await ensureDiamondTestBaseline(geniusDiamond, diamondAddress);` positioned BEFORE the suite's `initialSnapshotId = ... evm_snapshot` statement (GNUSBridgeAttestorUpgrade snapshot :148; GNUSBridgeEnhanced snapshot :62; GNUSNFTFactoryEnhanced snapshot :61). If the existing probe block already sits before the snapshot, replace it in place.
  3. Delete the now-unused local `TREASURY_STORAGE_SLOT` constant (:54 / :19 / :16) after confirming no remaining references.
  4. GNUSBridgeAttestorUpgrade.test.ts ONLY: do NOT touch the `setChainID` call inside the `it(...)` at :238-244 — it is an in-test mutation reverted by afterEach, not scaffold pollution (17-RESEARCH.md pollution-source precision).
  </action>
  <verify>
    <automated>cd gnus-ai && for f in test/unit/GNUSBridgeAttestorUpgrade.test.ts test/unit/GNUSBridgeEnhanced.test.ts test/unit/GNUSNFTFactoryEnhanced.test.ts; do b=$(grep -n "ensureDiamondTestBaseline(" "$f" | head -1 | cut -d: -f1); s=$(grep -n "evm_snapshot" "$f" | head -1 | cut -d: -f1); echo "$f baseline=$b snap=$s"; [ -n "$b" ] && [ "$b" -lt "$s" ] && echo OK || echo FAIL; done</automated>
  </verify>
  <acceptance_criteria>
  - Each of the 3 files: `grep -c "GNUSTreasury_SetSeedSupply"` returns 0 and `grep -c "TREASURY_STORAGE_SLOT"` returns 0
  - Each of the 3 files: line(ensureDiamondTestBaseline() < line(first evm_snapshot)
  - GNUSBridgeAttestorUpgrade.test.ts still contains its in-test `setChainID` (the :244-region test is intact)
  </acceptance_criteria>
  <done>Three probe-carrying plain scaffolds folded into the helper with baseline-before-snapshot ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Plain inserts (7 files) + full-suite gate</name>
  <files>test/unit/DiamondInitFacet-limiter.test.ts, test/unit/ERC1155ProxyOperator.test.ts, test/unit/ERC20TransferBatch.test.ts, test/unit/GNUSContractAssets.test.ts, test/unit/GNUSWithdrawLimiter.test.ts, test/unit/GNUSWithdrawLimiterStorage.test.ts, test/unit/GeniusOwnershipFacet.test.ts</files>
  <read_first>
  - Each file being modified (its before() block — the region from the LocalDiamondDeployer block to the initialSnapshotId statement)
  - test/unit/GNUSControlStorage.test.ts (the reference shape post-17-01)
  - test/utils/diamond-baseline.ts
  </read_first>
  <action>
  Identical mechanical insertion in all 7 files (verified snapshot lines; they may drift by a few lines — locate by the `initialSnapshotId` statement, not by line number alone):

  1. Add `import { ensureDiamondTestBaseline } from '../utils/diamond-baseline';`
  2. Insert `await ensureDiamondTestBaseline(geniusDiamond, diamondAddress);` between the `loadDiamondContract` call and the `// Take initial snapshot` comment (DiamondInitFacet-limiter :97-region; ERC1155ProxyOperator :61-region; ERC20TransferBatch :52-region; GNUSContractAssets :69-region; GNUSWithdrawLimiter :97-region; GNUSWithdrawLimiterStorage :87-region; GeniusOwnershipFacet :45-region)
  3. Use whichever owner-satisfying instance the scaffold has in scope: the default-connected `geniusDiamond` where that is what the suite uses (all 7 do), with the suite's `diamondAddress` local. Do NOT restructure the surrounding scaffold (17-PATTERNS.md Shared Patterns: only insert between the loader block and the snapshot).
  4. GNUSWithdrawLimiterStorage.test.ts caution: it probes OTHER storage slots (limiter slots) for its own assertions — leave every non-treasury slot probe untouched; this task only inserts the baseline call.

  Then run the FULL Hardhat suite once and record the observed counts. If any suite fails mentioning chain/destination-chain guards (17-RESEARCH.md Pitfall 2 / assumption A3), that suite has a hidden inherited-chainID dependency: fix by adding its own `setChainID(localChainId)` self-alias AFTER the baseline call (same pattern as bridge suites), and record the deviation in the summary.
  </action>
  <verify>
    <automated>cd gnus-ai && for f in test/unit/DiamondInitFacet-limiter.test.ts test/unit/ERC1155ProxyOperator.test.ts test/unit/ERC20TransferBatch.test.ts test/unit/GNUSContractAssets.test.ts test/unit/GNUSWithdrawLimiter.test.ts test/unit/GNUSWithdrawLimiterStorage.test.ts test/unit/GeniusOwnershipFacet.test.ts; do b=$(grep -n "ensureDiamondTestBaseline(" "$f" | head -1 | cut -d: -f1); s=$(grep -n "evm_snapshot" "$f" | head -1 | cut -d: -f1); [ -n "$b" ] && [ "$b" -lt "$s" ] && echo "OK $f" || echo "FAIL $f"; done && yarn test 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
  - All 7 files report OK on the baseline-before-snapshot ordering check
  - `yarn test` full-suite mocha summary shows "0 failing" (passing count matches the 17-01 observation — no suite gained or lost tests)
  - Any Pitfall-2 deviation is fixed via self-alias and documented in the summary (expected: none — research A3 rates this moderate-risk only)
  </acceptance_criteria>
  <done>All 13 test/unit Tier-A scaffolds (12 here + GNUSControlStorage in 17-01) declare the shared baseline; full Hardhat suite 0-failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → in-process Hardhat network | Mechanical scaffold edits only; helper writes execute against the local test diamond via existing signer connections. No production code, no dependencies, no CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | bridge re-alias deletion risk | mitigate | Acceptance criteria assert `setChainID(localChainId)` still exists AFTER the baseline call — deleting it would silently weaken bridgeIn's destination-chain test coverage |
| T-17-05 | Denial of service | baseline reset breaking a hidden inherited-31337 dependency | mitigate | Full `yarn test` per plan + documented self-alias contingency (Task 3 action) — caught at the wave that causes it, not at phase end |
| T-17-SC | Tampering (supply chain) | package installs | accept | Zero installs; edits confined to test/unit/*.ts |
</threat_model>

<verification>
- Ordering gate across all 12 files: baseline call line < first evm_snapshot line
- Probe-fold gate: zero `GNUSTreasury_SetSeedSupply` and zero `TREASURY_STORAGE_SLOT` references remain in the 5 folded files
- Full `yarn test`: 0 failing
</verification>

<success_criteria>
- Every test/unit Tier-A scaffold is order-independent by construction (declares its own baseline pre-snapshot)
- Bridge behavior preserved (31337 alias inside own snapshot window)
- Full Hardhat suite remains 0-failing with unchanged test counts
</success_criteria>

<output>
Create `.planning/phases/17-test-suite-determinism/17-02-SUMMARY.md` when done. List any self-alias deviations and the observed full-suite counts.
</output>
