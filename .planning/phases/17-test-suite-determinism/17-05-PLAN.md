---
phase: 17-test-suite-determinism
plan: 05
type: execute
wave: 3
depends_on: [17-02, 17-03, 17-04]
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06]
must_haves:
  truths:
    - "5 consecutive `yarn test:all` runs (Hardhat + Foundry halves) each report 0 failing — no seed-luck, no order-luck"
    - "10 consecutive invariant-only wrapper runs of AccessControlInvariant are each green"
    - "STATE.md contains ONE canonical baseline ledger section recording the observed counts, the 3→5 skip change, and the per-run proof — collapsing the 665/666/'666+1 stale' fragmentation"
    - "PROJECT.md's Test gate line points at the STATE.md ledger instead of carrying its own figure"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "single canonical test-baseline ledger (D-06)"
      contains: "Test Baseline Ledger"
    - path: ".planning/PROJECT.md"
      provides: "re-pointed Test gate status line"
      contains: "STATE.md"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/STATE.md"
      via: "Test gate line references the ledger section"
      pattern: "STATE\\.md"
---

<objective>
Run the D-06 determinism proof (N=5 consecutive `yarn test:all` + N=10 invariant-only wrapper runs) against the fully-swept tree, then write the canonical baseline ledger into STATE.md and re-point PROJECT.md at it.

Purpose: Success criterion 4 — new baselines recorded with zero known-stale/failing entries, derived from observed runs (never from memory or historical figures — the stale-baseline failure mode RETROSPECTIVE.md:26 records). Ends the three-way fragmentation (STATE 665/2/1 vs PROJECT 666/2/0 vs ROADMAP "666/2/0 + 1 stale").
Output: Proof logs under /tmp, per-run results in the summary, ledger section in STATE.md, re-pointed PROJECT.md:11. No CI changes (D-07).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-suite-determinism/17-CONTEXT.md
@.planning/phases/17-test-suite-determinism/17-RESEARCH.md
@.planning/phases/17-test-suite-determinism/17-01-SUMMARY.md
@.planning/phases/17-test-suite-determinism/17-02-SUMMARY.md
@.planning/phases/17-test-suite-determinism/17-03-SUMMARY.md
@.planning/phases/17-test-suite-determinism/17-04-SUMMARY.md

<interfaces>
Verified commands and record targets:

- `yarn test:all` = `yarn test && yarn forge:test` (package.json:33) — the forge half needs the node
- Wrapper does NOT start the node (17-RESEARCH.md Pitfall 4): start `npx hardhat node` in the background first; poll eth_blockNumber on http://127.0.0.1:8545 until it answers
- Invariant-only command (wrapper accepts --match-contract, NOT --match-path — Pitfall 5):
  `npx hardhat diamonds-forge:test --diamond-name GeniusDiamond --network localhost --force --match-contract AccessControlInvariant`
- Ledger record targets (verified 17-PATTERNS.md):
  - .planning/STATE.md — insert the new canonical ledger section after the "## Phase Status" block; entry prose style follows the existing 07-04 gate line (:88-region: `command; counts; failure-set`)
  - .planning/STATE.md "## Next Actions" item 4 — the pre-existing test-suite cleanup entry (GNUSControlStorage root fix / Foundry Safe setUp reverts / AccessControlInvariant flake) this phase resolves
  - .planning/PROJECT.md:11 — current text "Test gate: 666 passing / 2 pending / 0 failing, green in CI (tests + tokenless security-audit)." (verify before editing; re-point, keep the CI clause — CI is unchanged per D-07)
  - .planning/ROADMAP.md:36-region phase-entry baselines are the HISTORICAL record of phase-start state — leave the figures untouched
  - Precedent for STATE-as-ledger: .github/workflows/security-audit.yml:8 already points at .planning/STATE.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-06 proof — N=5 full-suite runs + N=10 invariant-only runs</name>
  <files>(verification-only task — no source modifications; produces /tmp logs and recorded results)</files>
  <read_first>
  - 17-RESEARCH.md (Validation Architecture, Pitfall 1/4/5, Assumptions A1-A3)
  - The four prior plan SUMMARYs (17-01..17-04) — the observed counts each wave recorded
  </read_first>
  <action>
  Proof protocol (all runs against the final post-sweep tree; the node stays up for the whole protocol):

  1. Start `npx hardhat node` in the background; poll `eth_blockNumber` at http://127.0.0.1:8545 until it responds before any gate run.
  2. N=5 full runs: for run i in 1..5, execute `yarn test:all 2>&1 | tee /tmp/17-05-testall-$i.log` and capture wall time (date-stamp before/after). After each run, extract the mocha summary line (passing/pending/failing) and the forge summary (passed/failed/skipped).
  3. N=10 invariant runs: for run i in 1..10, execute `npx hardhat diamonds-forge:test --diamond-name GeniusDiamond --network localhost --force --match-contract AccessControlInvariant 2>&1 | tee /tmp/17-05-invariant-$i.log` with wall time. Expect every run: AccessControlInvariant suite ok, 0 failed.
  4. Kill the background node when done.

  Rules of record (Pitfall 1):
  - The pass count is DERIVED from run 1's output — record what prints (665 or 666 passing / 2 pending expected), never a remembered figure, and do NOT reconcile a count difference by editing tests.
  - If ANY run fails: STOP. Capture the failing suite, test name, and revert reason. A failure here is a determinism finding, not noise — record it in the summary as a routing event (project precedent: STATE 07-04 flake record) and do not paper over it with re-runs. Do not proceed to Task 2 with an unexplained failure.
  - Retain all 15 logs under /tmp and reference their paths in the summary.

  No CI workflow is created or modified — D-07 locks that out (CI cannot host the Foundry suite; a Hardhat-only guard would re-run the half that never flaked).
  </action>
  <verify>
    <automated>cd gnus-ai && for i in 1 2 3 4 5; do echo "run $i:"; grep -E "[0-9]+ (passing|pending|failing)" /tmp/17-05-testall-$i.log | tail -3; grep -cE "failed; 0 skipped|failed;" /tmp/17-05-testall-$i.log >/dev/null; grep -E "passed; [0-9]+ failed" /tmp/17-05-testall-$i.log | tail -1; done; grep -L "ok\." /dev/null; ls /tmp/17-05-*.log | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - 15 log files exist (/tmp/17-05-testall-1..5.log, /tmp/17-05-invariant-1..10.log)
  - Each of the 5 test:all logs shows mocha "0 failing" AND forge "0 failed" (skipped = 5 in the forge half)
  - Each of the 10 invariant logs shows the AccessControlInvariant suite result ok with 0 failed
  - The passing/pending/failed and passed/skipped counts are IDENTICAL across all 5 full runs (determinism), and match the counts recorded by 17-01..17-04 summaries
  - Per-run wall times captured for run 1 of each kind (A1: no recorded timings existed; the ledger starts recording them)
  - Zero CI workflow files in `git status` output
  </acceptance_criteria>
  <done>The determinism proof bar (D-06) is met: 5x full-suite + 10x invariant-only, all green, counts stable, evidence retained.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 ledger — STATE.md canonical section + PROJECT.md re-point</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
  - .planning/STATE.md (frontmatter, "## Phase Status" table, the 07-04 gate-line entry :88-region for prose style, "## Next Actions" item 4)
  - .planning/PROJECT.md (line 11 and surrounding status block)
  - .planning/ROADMAP.md (Phase 17 entry :33-45 — to confirm what stays historical)
  </read_first>
  <action>
  Four record edits (D-06):

  1. STATE.md — insert a new section `## Test Baseline Ledger (canonical)` immediately AFTER the "## Phase Status" table section. Content, in the existing `command; counts; failure-set` prose style:
     - Hardhat gate (`yarn test`): {observed from run 1} passing / {observed} pending / 0 failing — recorded 2026-08-31+actual-date, derived from N=5 consecutive runs (all identical)
     - Foundry gate (`yarn forge:test` via bridge node): {observed} passed / 0 failed / 5 skipped — skip count 3→5 is the DECLARED fork-skip for SafeSingleShotUpgrade + SafeDiamondCut per D-04/D-05 (not drift)
     - Per-run proof table: 5 test:all runs + 10 AccessControlInvariant-only wrapper runs, each with date, result, and the run-1 wall time for each kind
     - A single-source statement: this section is the canonical test-gate baseline; PROJECT.md and workflows point here (precedent: security-audit.yml:8). Historical fragmented figures (STATE 07-01/07-04 665/2/1, PROJECT 666/2/0) are superseded by this ledger.
  2. STATE.md — rewrite "## Next Actions" item 4 (the test-suite cleanup entry listing the GNUSControlStorage chainID pollution, the Foundry Safe setUp reverts, and the AccessControlInvariant flake) to record all three as RESOLVED by Phase 17 (TEST-04/05/06), pointing at the ledger section. Keep items 1-3 untouched.
  3. PROJECT.md:11 — replace the figure-carrying sentence with the observed baseline plus the pointer, keeping the CI clause: "Test gate: {observed} passing / {observed} pending / 0 failing (Hardhat) and {observed} passed / 0 failed / 5 skipped (Foundry via local bridge node); canonical baselines live in .planning/STATE.md § Test Baseline Ledger; green in CI (tests + tokenless security-audit)."

  4. ROADMAP.md — append ONE forward-pointer line to the end of the Phase 17 entry (after the `**Source:**` line at :45): `**Post-phase canonical baselines:** .planning/STATE.md § Test Baseline Ledger — all figures in this entry remain the historical phase-start record.` Nothing else in ROADMAP.md changes.

  Do NOT edit .planning/ROADMAP.md phase-entry baselines (:36-region) — they are the historical phase-start record; the single forward-pointer line above is the only ROADMAP addition (D-06 "ROADMAPs re-pointed"). Do NOT edit either workflow file (D-07). Update the STATE.md frontmatter `last_updated`/`stopped_at` to reflect this task per the file's existing convention.
  </action>
  <verify>
    <automated>cd gnus-ai && grep -n "## Test Baseline Ledger" .planning/STATE.md && grep -n "Test Baseline Ledger" .planning/PROJECT.md && grep -n "5 skipped" .planning/STATE.md .planning/PROJECT.md && grep -n "Post-phase canonical baselines" .planning/ROADMAP.md && git status --porcelain .github/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - `.planning/STATE.md` contains the heading `## Test Baseline Ledger (canonical)` with both gate lines showing "0 failing"/"0 failed", the "5 skipped" figure with its D-04/D-05 explanation, and the per-run proof results (5 full + 10 invariant)
  - Every count in the ledger matches the /tmp logs from Task 1 (no figure appears that the runs did not print — Pitfall 1)
  - `.planning/PROJECT.md` line-11-region no longer carries an un-sourced figure; it references `.planning/STATE.md` and retains the CI clause
  - STATE.md Next Actions item 4 records TEST-04/05/06 as resolved with a ledger pointer
  - `git status --porcelain .github/` is empty (no CI changes — D-07) and `.planning/ROADMAP.md` shows no baseline-figure edits
  </acceptance_criteria>
  <done>One canonical baseline ledger exists; the stale-figure fragmentation is collapsed; ROADMAP success criterion 4 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gates → localhost bridge node | Proof runs execute the existing test suites against a locally-started node. Documentation-only edits to .planning records. No production code, no dependencies, no CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Repudiation (record integrity) | baseline figures written from memory | mitigate | Acceptance criteria pin every ledger count to a /tmp log from Task 1; the proof bar (5+10 green runs) is the derivation — the exact RETROSPECTIVE.md:26 stale-baseline failure mode D-06 exists to end |
| T-17-13 | Tampering | proof failures re-run to green | mitigate | Task 1 rule of record: any failing run STOPS the protocol and is recorded as a routing event — silent re-run-to-green is forbidden |
| T-17-14 | Tampering | ledger masking known-stale entries | mitigate | Ledger explicitly encodes the 3→5 skip change with its D-04/D-05 declared-skip rationale (recorded baseline, not noise) and asserts zero known-stale/failing entries |
| T-17-SC | Tampering (supply chain) | package installs | accept | Zero installs; edits confined to .planning/*.md |
</threat_model>

<verification>
- 15 proof logs retained; 5 full runs + 10 invariant runs all green with identical counts
- STATE.md ledger present with observed figures only; PROJECT.md re-pointed; no CI/ROADMAP-figure changes
</verification>

<success_criteria>
- ROADMAP criterion 4: new baselines recorded with zero known-stale/failing entries
- D-06 satisfied (N-run proof + single ledger + re-points); D-07 satisfied (no CI guard created)
- Phase 17 exit evidence complete: criterion 1 (17-01/02/03 + 5x full-suite proof), criterion 2 (17-04 + 10x invariant proof), criterion 3 (17-04 skip arithmetic + recorded), criterion 4 (this plan)
</success_criteria>

<output>
Create `.planning/phases/17-test-suite-determinism/17-05-SUMMARY.md` when done. Include the per-run proof table (5 + 10 rows), the derived canonical counts, and the /tmp log paths.
</output>
