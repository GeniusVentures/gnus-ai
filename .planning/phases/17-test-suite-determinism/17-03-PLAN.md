---
phase: 17-test-suite-determinism
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - test/unit/GNUSBridge.test.ts
  - test/unit/GNUSTreasury.test.ts
  - test/unit/NFTFactory.test.ts
  - test/unit/GNUSLifecycleSettle.test.ts
  - test/unit/GNUSLifecycleUpgrade.test.ts
  - test/unit/GNUSLifecycleAICredits.test.ts
  - test/unit/GNUSLicensing.test.ts
  - test/unit/GNUSRedeemAdapter.test.ts
  - test/integration/withdraw-limiter-integration.test.ts
  - test/gas/withdraw-limiter-gas-comparison.test.ts
  - test/integration/erc1155-transfer-hook-limiter.test.ts
  - test/integration/erc20-transfer-batch-limiter.test.ts
  - test/deployment/GeniusDiamondDeployment.test.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "No duplicated probe-guard body or treasury slot constant remains anywhere under test/ outside test/utils/diamond-baseline.ts (the helper is the single copy)"
    - "The 4 non-unit Tier-A scaffolds (gas, 2x integration, deployment) call ensureDiamondTestBaseline() before their first snapshot"
    - "GNUSTreasury.test.ts one-shot test bodies (:504-:987) are byte-untouched — they ARE the specification for the one-shot behavior"
    - "Full `yarn test` reports 0 failing"
  artifacts:
    - path: "test/unit/GNUSTreasury.test.ts"
      provides: "scaffold-side seedProvenanceIfNeeded folded; test bodies preserved"
      contains: "ensureDiamondTestBaseline("
    - path: "test/deployment/GeniusDiamondDeployment.test.ts"
      provides: "multichain-nested scaffold with ownerDiamond baseline call at end of before()"
      contains: "ensureDiamondTestBaseline("
  key_links:
    - from: "test/integration/withdraw-limiter-integration.test.ts"
      to: "test/utils/diamond-baseline.ts"
      via: "probe block :114-122 replaced by helper call"
      pattern: "ensureDiamondTestBaseline\\("
    - from: "test/gas/withdraw-limiter-gas-comparison.test.ts"
      to: "test/utils/diamond-baseline.ts"
      via: "baseline call before :88 snapshot, probe :94-100 folded"
      pattern: "ensureDiamondTestBaseline\\("
---

<objective>
Complete the D-03 sweep: fold every remaining duplicated probe-guard block into the helper (the operator ruling's operative test — "tier-B suites carrying duplicated probe-guard blocks are deduped"; the tree grep verified 9 such suites, one more than research's named 8: test/integration/withdraw-limiter-integration.test.ts:122 also carries the block) and wire the 4 non-unit Tier-A scaffolds (gas, 2x integration, deployment).

Purpose: Finish the cross-suite baseline declaration so the full Hardhat suite is order-independent, and make the helper the only probe-guard copy in the repo.
Output: 13 files edited; zero duplicated probe sites remain; full suite 0-failing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-test-suite-determinism/17-CONTEXT.md
@.planning/phases/17-test-suite-determinism/17-PATTERNS.md
@.planning/phases/17-test-suite-determinism/17-01-SUMMARY.md

<interfaces>
<!-- From 17-01 (must exist before this plan runs) -->

From test/utils/diamond-baseline.ts:
```typescript
export async function ensureDiamondTestBaseline(
    geniusDiamond: GeniusDiamond,   // owner-connected or default signer0 instance
    diamondAddress: string,
): Promise<void>;  // probe-guarded seed + setChainID(0) + updateBridgeFee(0)
```

Import paths (17-PATTERNS.md import convention): `'../utils/diamond-baseline'` from test/unit/, test/gas/, test/integration/, test/deployment/ (same depth as existing `'../utils/bridge-fixtures'` imports, cf. test/gas/withdraw-limiter-gas-comparison.test.ts:31).

CRITICAL semantic difference vs the local helpers being replaced: the local fns (e.g. GNUSTreasury.test.ts:167-178 seedProvenanceIfNeeded) only probe-seed; ensureDiamondTestBaseline ALSO writes setChainID(0) + updateBridgeFee(0). Therefore the helper is NEVER called inside a test body or beforeEach — scaffold before() only (a mid-test call would clobber the test's own chainID/fee state).

Verified fold sites (grep-verified this session):
- Tier-B duplicates: GNUSBridge.test.ts:230-237 (ownerDiamond; slot const :56) · GNUSTreasury.test.ts:163-178 (local fn, param `diamond`/`address`; slot const :65) · NFTFactory.test.ts:121-128 (ownerDiamond; slot const :36 with `hre.ethers.` prefix) · GNUSLifecycleSettle.test.ts:240-249 (ownerDiamond; slot const :80) · GNUSLifecycleUpgrade.test.ts:165-173 (local fn, ownerDiamond; slot const :59; in-test call site :187) · GNUSLifecycleAICredits.test.ts:154-162 · GNUSLicensing.test.ts:179-region · GNUSRedeemAdapter.test.ts:154 (instance local named `dContract`) · test/integration/withdraw-limiter-integration.test.ts:122 (ownerDiamond; slot const :114)
- Non-unit Tier-A snapshots: gas/withdraw-limiter-gas-comparison.test.ts:88 (probe :94-100, slot const :94) · integration/erc1155-transfer-hook-limiter.test.ts:107 · integration/erc20-transfer-batch-limiter.test.ts:96 · deployment/GeniusDiamondDeployment.test.ts (before() ends :107; beforeEach snapshot :110; owner resolved from DeployerAddress :98, ownerDiamond connected :106)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the 9 duplicated probe-guard suites into the helper</name>
  <files>test/unit/GNUSBridge.test.ts, test/unit/GNUSTreasury.test.ts, test/unit/NFTFactory.test.ts, test/unit/GNUSLifecycleSettle.test.ts, test/unit/GNUSLifecycleUpgrade.test.ts, test/unit/GNUSLifecycleAICredits.test.ts, test/unit/GNUSLicensing.test.ts, test/unit/GNUSRedeemAdapter.test.ts, test/integration/withdraw-limiter-integration.test.ts</files>
  <read_first>
  - Each file being modified (its before()/beforeEach scaffold region and its probe block — line anchors in the interfaces block above; locate by content, lines may drift)
  - test/unit/GNUSTreasury.test.ts (scaffold helper :163-178 AND the one-shot test bodies :500-990 — to know what NOT to touch)
  - test/unit/GNUSLifecycleUpgrade.test.ts (local fn :165-173 and its in-test call site :187)
  - test/utils/diamond-baseline.ts
  - .planning/phases/17-test-suite-determinism/17-PATTERNS.md (Tier B folds section)
  </read_first>
  <action>
  Same transformation in all 9 files:

  1. Add the import (`'../utils/diamond-baseline'`).
  2. Place ONE scaffold-level call `await ensureDiamondTestBaseline(<instance>, <address>);`:
     - At the END of the suite's `before()` (these are beforeEach-isolation suites with no initialSnapshotId), after the diamond instance and address are in scope.
     - Pass the suite's owner-connected instance where one exists (GNUSBridge/GNUSLifecycleSettle/GNUSLifecycleUpgrade/NFTFactory/withdraw-limiter-integration use `ownerDiamond`; GNUSRedeemAdapter uses its `dContract` local; GNUSLicensing/GNUSLifecycleAICredits/GNUSTreasury use their default or owner instance per their existing probe call) plus the suite's `diamondAddress` local. The instance used MUST be the same signer the existing probe-guarded seed call used — that signer is proven to hold the required roles.
  3. DELETE the local probe implementation: the inline block or the local `seedProvenanceIfNeeded` function definition, and the local `TREASURY_STORAGE_SLOT` constant (delete each only after `grep -n` confirms no remaining references in that file).
  4. DELETE bare guard call sites of the removed local fn inside test bodies (e.g. GNUSLifecycleUpgrade.test.ts:187 `await seedProvenanceIfNeeded();`) — the scaffold-level baseline guarantees provenance is initialized before every snapshot-baked test, so the mid-test guard is dead code. DO NOT replace them with ensureDiamondTestBaseline() calls — the helper also resets chainID/fee and must never run mid-test.
  5. GNUSTreasury.test.ts HARD BOUNDARY: fold ONLY the scaffold-side local helper (:163-178) and its scaffold-level invocations. Every `GNUSTreasury_SetSeedSupply` call inside `it(...)` bodies (:504 through :987, including the `.catch(() => {})` variants) MUST remain byte-identical — those tests exercise the one-shot revert behavior itself (17-PATTERNS.md caution).

  Guard-free Tier-B suites (Erc20Batch, GNUSERC20, GNUSLifecycle, GNUSLifecyclePolicy, GNUSNFTFactoryAntiScalping, Phase5-circuit-breaker, TransferHelper, rpc/safe suites, etc.) are NOT touched — operator scope ruling.
  </action>
  <verify>
    <automated>cd gnus-ai && grep -rn "gnus.ai.treasury.storage" test/ --include='*.ts' | grep -v "utils/diamond-baseline.ts"; echo "slot-string hits above must be empty"; grep -rln "GNUSTreasury_SetSeedSupply(0n)" test/ --include='*.ts' | grep -v foundry | sort</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "gnus.ai.treasury.storage" test/ --include='*.ts' | grep -v "utils/diamond-baseline.ts"` returns NO hits (every duplicated slot constant folded)
  - `grep -rln "GNUSTreasury_SetSeedSupply(0n)" test/ --include='*.ts' | grep -v foundry` returns EXACTLY two files: test/unit/GNUSTreasury.test.ts and test/utils/diamond-baseline.ts
  - GNUSTreasury.test.ts: `git diff test/unit/GNUSTreasury.test.ts` shows changes confined to the scaffold region — no hunk touches lines in the :504-:987 test-body range
  - Each of the 9 files contains exactly the scaffold-level `ensureDiamondTestBaseline(` call inside its `before()` (grep line number < the suite's first `beforeEach`/snapshot site), and zero occurrences inside any `it(`/`beforeEach(` body
  - `grep -c "seedProvenanceIfNeeded" test/unit/GNUSLifecycleUpgrade.test.ts test/unit/GNUSTreasury.test.ts` returns 0 for both (local fns fully removed with their call sites)
  </acceptance_criteria>
  <done>All duplicated probe-guard bodies are collapsed into the shared helper; the helper is the repo's single probe-guard copy; one-shot test bodies preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Non-unit Tier-A scaffolds (gas, 2x integration, deployment) + full-suite gate</name>
  <files>test/gas/withdraw-limiter-gas-comparison.test.ts, test/integration/erc1155-transfer-hook-limiter.test.ts, test/integration/erc20-transfer-batch-limiter.test.ts, test/deployment/GeniusDiamondDeployment.test.ts</files>
  <read_first>
  - test/gas/withdraw-limiter-gas-comparison.test.ts (before() up to the :88 snapshot; probe block :94-100; slot const :94; existing utils import :31 for path depth)
  - test/integration/erc1155-transfer-hook-limiter.test.ts (before() up to :107)
  - test/integration/erc20-transfer-batch-limiter.test.ts (before() up to :96)
  - test/deployment/GeniusDiamondDeployment.test.ts (before() :76-107, owner resolution :98, ownerDiamond :106, beforeEach snapshot :110)
  - test/utils/diamond-baseline.ts
  </read_first>
  <action>
  These are multichain-nested scaffolds (they use `provider.send(...)`, not `hre.network.provider.send(...)` — 17-PATTERNS.md):

  1. gas/withdraw-limiter-gas-comparison.test.ts: add import `'../utils/diamond-baseline'`; insert `await ensureDiamondTestBaseline(geniusDiamond, diamondAddress);` BEFORE the `initialSnapshotId` snapshot (:88-region); DELETE the probe block (:94-100) and the slot constant (:94-region) once unreferenced. Caution: this file's probe sits AFTER its snapshot today — the fold MOVES the declaration before the snapshot (that relocation is the fix).
  2. integration/erc1155-transfer-hook-limiter.test.ts (:107-region) and integration/erc20-transfer-batch-limiter.test.ts (:96-region): add import; insert the baseline call between the diamond loading and the `initialSnapshotId` snapshot statement. No probe blocks exist in these two.
  3. deployment/GeniusDiamondDeployment.test.ts: add import; insert `await ensureDiamondTestBaseline(ownerDiamond, /* deployed diamond address in scope */);` as the LAST statement of `before()` (before() ends :107), i.e. BEFORE the `beforeEach` snapshot at :110. PASS `ownerDiamond` (connected at :106 from the DeployerAddress-resolved owner), NOT the default `geniusDiamond` instance — the template shape resolves a possibly non-signer0 owner (17-PATTERNS.md permission nuance).
  4. Run the FULL Hardhat suite; record observed counts. Same Pitfall-2 contingency as 17-02 Task 3 (self-alias any suite newly failing on chain guards; document it).
  </action>
  <verify>
    <automated>cd gnus-ai && for f in test/gas/withdraw-limiter-gas-comparison.test.ts test/integration/erc1155-transfer-hook-limiter.test.ts test/integration/erc20-transfer-batch-limiter.test.ts test/deployment/GeniusDiamondDeployment.test.ts; do b=$(grep -n "ensureDiamondTestBaseline(" "$f" | head -1 | cut -d: -f1); s=$(grep -n "evm_snapshot" "$f" | head -1 | cut -d: -f1); [ -n "$b" ] && [ "$b" -lt "$s" ] && echo "OK $f" || echo "FAIL $f"; done && yarn test 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
  - All 4 files report OK on baseline-before-first-snapshot ordering
  - test/deployment/GeniusDiamondDeployment.test.ts call uses `ownerDiamond` (grep shows `ensureDiamondTestBaseline(ownerDiamond`)
  - gas-comparison file: `grep -c "TREASURY_STORAGE_SLOT"` returns 0
  - `yarn test` mocha summary shows "0 failing" with the same passing count observed in 17-01/17-02
  </acceptance_criteria>
  <done>All 17 Tier-A snapshot scaffolds repo-wide declare the baseline; D-03 sweep complete; full Hardhat suite 0-failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → in-process Hardhat network | Scaffold edits and helper calls only; no production contract, no dependency, no CI change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering (test integrity) | GNUSTreasury.test.ts one-shot bodies | mitigate | Acceptance criterion pins `git diff` to the scaffold region — the :504-:987 bodies are the specification for the one-shot guard behavior the helper depends on |
| T-17-07 | Tampering | helper called mid-test would reset chainID/fee under test | mitigate | Acceptance criterion: zero occurrences of the call inside any `it(`/`beforeEach(` body across all 9 folded files |
| T-17-08 | Denial of service | deployment-suite owner resolution | mitigate | ownerDiamond (DeployerAddress-resolved signer) passed, not default instance — wrong signer would revert onlySuperAdminRole and fail the suite loudly |
| T-17-SC | Tampering (supply chain) | package installs | accept | Zero installs; edits confined to test/ TypeScript |
</threat_model>

<verification>
- Repo-wide fold gate: treasury slot string and `GNUSTreasury_SetSeedSupply(0n)` exist only in the helper + GNUSTreasury.test.ts test bodies
- Ordering gate: all 4 non-unit scaffolds baseline-before-snapshot
- Full `yarn test`: 0 failing
</verification>

<success_criteria>
- The D-03 sweep is complete over the operator-ruled blast radius (17 Tier-A + 9 probe-carrying Tier-B); guard-free Tier-B suites untouched
- The helper is provably the single probe-guard implementation in the repo
- Full Hardhat suite 0-failing with stable counts
</success_criteria>

<output>
Create `.planning/phases/17-test-suite-determinism/17-03-SUMMARY.md` when done. Record the observed full-suite counts and any self-alias deviations.
</output>
