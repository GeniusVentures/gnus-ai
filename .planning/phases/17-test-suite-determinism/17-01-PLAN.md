---
phase: 17-test-suite-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/utils/diamond-baseline.ts
  - test/utils/test-template.ts
  - test/unit/GNUSControlStorage.test.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "Running `yarn test` (full suite, one mocha process, alphabetical order) reports 0 failing — GNUSControlStorage 'should return initial protocol info' passes with NO test-side normalization"
    - "The GNUSControlStorage scaffold declares the protocol baseline (provenance initialized, chainID 0, bridgeFee 0) BEFORE its initialSnapshotId, so every snapshot revert restores the declared baseline"
    - "New suites copied from test-template.ts inherit the baseline call by default"
  artifacts:
    - path: "test/utils/diamond-baseline.ts"
      provides: "ensureDiamondTestBaseline() + TREASURY_STORAGE_SLOT — the single shared baseline declaration"
      exports: ["ensureDiamondTestBaseline", "TREASURY_STORAGE_SLOT"]
      min_lines: 35
    - path: "test/utils/test-template.ts"
      provides: "scaffold anchor with baseline call wired before evm_snapshot"
      contains: "ensureDiamondTestBaseline("
    - path: "test/unit/GNUSControlStorage.test.ts"
      provides: "TEST-04 victim with root fix and guard deleted"
      contains: "ensureDiamondTestBaseline("
  key_links:
    - from: "test/unit/GNUSControlStorage.test.ts"
      to: "test/utils/diamond-baseline.ts"
      via: "import { ensureDiamondTestBaseline } from '../utils/diamond-baseline' + call in before()"
      pattern: "ensureDiamondTestBaseline\\("
    - from: "test/utils/diamond-baseline.ts"
      to: "GNUSTreasury facet (one-shot seed)"
      via: "eth_getStorageAt(slot+1) probe guarding GNUSTreasury_SetSeedSupply(0n)"
      pattern: "eth_getStorageAt"
---

<objective>
Create the shared `ensureDiamondTestBaseline()` helper (D-03 core), wire it into the TEST-04 victim suite and the scaffold template, and delete the test-side guard that masked the cross-suite pollution.

Purpose: This is the root fix for TEST-04 — the shared LocalDiamondDeployer diamond caches mutations across ~28 suites in one `npx hardhat test` process; declaring the protocol baseline at every scaffold (before the snapshot) makes suite order irrelevant instead of masking inherited state inside a test.
Output: `test/utils/diamond-baseline.ts` (new), baseline call in `test/utils/test-template.ts` and `test/unit/GNUSControlStorage.test.ts`, guard at `GNUSControlStorage.test.ts:69-73` deleted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-suite-determinism/17-CONTEXT.md
@.planning/phases/17-test-suite-determinism/17-RESEARCH.md
@.planning/phases/17-test-suite-determinism/17-PATTERNS.md

<interfaces>
<!-- Verified on the working tree this session. Executor uses these directly — no exploration needed. -->

From test/unit/GNUSTreasury.test.ts:167-178 (signature precedent — parameterized local helper):
```typescript
async function seedProvenanceIfNeeded(
    diamond: GeniusDiamond = ownerDiamond,
    address: string = diamondAddress,
): Promise<void> {
    const initialized = await provider.send('eth_getStorageAt', [
        address,
        ethers.toBeHex(BigInt(TREASURY_STORAGE_SLOT) + 1n, 32),
    ]);
    if (BigInt(initialized) === 0n) {
        await diamond.GNUSTreasury_SetSeedSupply(0n);
    }
}
```

From test/unit/GNUSBridgeEnhanced.test.ts:49-59 (inline probe-body precedent; slot const at :19):
```typescript
const TREASURY_STORAGE_SLOT = ethers.keccak256(ethers.toUtf8Bytes('gnus.ai.treasury.storage'));
// probe: eth_getStorageAt(diamondAddress, toBeHex(BigInt(TREASURY_STORAGE_SLOT) + 1n, 32))
// if (BigInt(initialized) === 0n) → await geniusDiamond.GNUSTreasury_SetSeedSupply(0n);
```

Import conventions (verified):
- Type: `import { GeniusDiamond } from '../../diamond-typechain-types';` (test/unit/GNUSControlStorage.test.ts:6 — same depth from test/utils/)
- Utils module style: test/utils/network-utils.ts — JSDoc on the function, `export { ... };` block at file bottom (:27)
- Facet calls on the local shared diamond: `GNUSTreasury_SetSeedSupply(0n)`, `setChainID(0)`, `updateBridgeFee(0)` (updateBridgeFee(0) precedent: GNUSControlStorage.test.ts:402)

Contract semantics (verified against contracts/gnus-ai/ this session, cited in 17-RESEARCH.md):
- GNUSTreasury.sol:171-179 — SetSeedSupply is onlyRole(DEFAULT_ADMIN_ROLE), requires ownChainId != 0 (always set: deployInit GNUSTreasury_Initialize260()) and !provenanceInitialized → probe on slot+1 is REQUIRED (revert risk on cached diamond: "Already initialized")
- GNUSTreasuryStorage.sol:15-23 — slot = keccak256("gnus.ai.treasury.storage"); provenanceInitialized is slot +1
- GNUSControl.sol:176-178 — setChainID has NO require (onlySuperAdminRole); GNUSControl.sol:166-170 — updateBridgeFee requires newFee <= MAX_FEE(200), so 0 passes
- GeniusAccessControl.sol:73-76 — onlySuperAdminRole = LibDiamond contractOwner == msg.sender; signer0 (default-connected geniusDiamond) satisfies both roles on the local shared diamond
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/utils/diamond-baseline.ts (the D-03 shared helper)</name>
  <files>test/utils/diamond-baseline.ts</files>
  <read_first>
  - .planning/phases/17-test-suite-determinism/17-PATTERNS.md (Pattern Assignments → diamond-baseline.ts section: module conventions, probe body, signature precedent)
  - test/unit/GNUSTreasury.test.ts (lines 160-180 — parameterized seedProvenanceIfNeeded precedent)
  - test/unit/GNUSBridgeEnhanced.test.ts (lines 15-62 — inline probe body + slot const + snapshot ordering)
  - test/utils/network-utils.ts (whole file — module conventions: JSDoc, named export, no default export)
  - contracts/gnus-ai/GNUSTreasuryStorage.sol (slot layout: probe target is slot +1)
  </read_first>
  <action>
  Create a new module generalizing the probe-guard body duplicated across 13 suites (per D-03). Exact contents:

  - Import `{ ethers } from 'hardhat'` and `import { GeniusDiamond } from '../../diamond-typechain-types';`
  - Export a constant `TREASURY_STORAGE_SLOT = ethers.keccak256(ethers.toUtf8Bytes('gnus.ai.treasury.storage'));`
  - Export an async function with the exact signature `ensureDiamondTestBaseline(geniusDiamond: GeniusDiamond, diamondAddress: string): Promise<void>` whose body, in order:
    1. Probe: `const initialized = await ethers.provider.send('eth_getStorageAt', [diamondAddress, ethers.toBeHex(BigInt(TREASURY_STORAGE_SLOT) + 1n, 32)]);`
    2. `if (BigInt(initialized) === 0n) { await geniusDiamond.GNUSTreasury_SetSeedSupply(0n); }` — the ONLY one-shot call, always probe-guarded; NEVER call it unguarded (reverts "Already initialized" against the cached diamond)
    3. `await geniusDiamond.setChainID(0);`
    4. `await geniusDiamond.updateBridgeFee(0);`
  - JSDoc header on the function stating: it declares the cross-suite protocol baseline; it MUST be called in a suite's `before()` BEFORE `initialSnapshotId` is taken (so snapshot reverts restore the declared baseline, and bridge suites re-alias `setChainID(31337n)` AFTER it, inside their own snapshot window); the seed call is probe-guarded because it is one-shot on-chain; the caller must be the LibDiamond contractOwner + DEFAULT_ADMIN (signer0 satisfies both on the local shared diamond).
  - Module conventions copied from test/utils/network-utils.ts: JSDoc on the export, bottom `export { ensureDiamondTestBaseline, TREASURY_STORAGE_SLOT };` block, no default export. Tab indentation to match the test tree.

  Do NOT add a test file for this module — it is exercised by every suite that calls it; its correctness signal IS the full-suite gate (17-RESEARCH.md Wave 0).
  </action>
  <verify>
    <automated>cd gnus-ai && grep -c "export" test/utils/diamond-baseline.ts && grep -n "gnus.ai.treasury.storage" test/utils/diamond-baseline.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
  - test/utils/diamond-baseline.ts exists and contains `export async function ensureDiamondTestBaseline(` (or equivalent function declaration plus bottom export block naming it)
  - File contains `TREASURY_STORAGE_SLOT` defined via `ethers.keccak256(ethers.toUtf8Bytes('gnus.ai.treasury.storage'))`
  - The `GNUSTreasury_SetSeedSupply(0n)` call is lexically inside an `if` guarded by the `eth_getStorageAt` probe result
  - Body order is probe → conditional seed → setChainID(0) → updateBridgeFee(0)
  - `npx tsc --noEmit` introduces no new errors attributable to this file
  </acceptance_criteria>
  <done>Shared baseline helper exists as the single canonical probe-guard implementation; no behavior wired yet (Task 2 wires it).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the TEST-04 victim + scaffold template; delete the test-side guard</name>
  <files>test/unit/GNUSControlStorage.test.ts, test/utils/test-template.ts</files>
  <read_first>
  - test/unit/GNUSControlStorage.test.ts (whole before()/after() block :27-65, the guard :68-74, and the edge-case test :405-415)
  - test/utils/test-template.ts (before() :76-128 — note owner resolution :115-124 and snapshot :126-127)
  - .planning/phases/17-test-suite-determinism/17-PATTERNS.md (Tier A plain scaffolds + test-template insertion point sections)
  </read_first>
  <action>
  Three edits:

  1. test/unit/GNUSControlStorage.test.ts — ROOT FIX + GUARD DELETE (per D-03):
     - Add import: `import { ensureDiamondTestBaseline } from '../utils/diamond-baseline';`
     - In `before()`, insert `await ensureDiamondTestBaseline(geniusDiamond, diamondAddress);` between the `loadDiamondContract` call (ends :49) and the `// Take initial snapshot for test isolation` comment (:51). Both `geniusDiamond` and `diamondAddress` are already in scope (:42, :45).
     - DELETE the five lines at :69-73 inside `it('should return initial protocol info')`: the four comment lines ("Bridge suites alias the shared diamond's chainID..." through "...edge case below).") and the `await geniusDiamond.setChainID(0);` line. The test body must start at `const info = await geniusDiamond.protocolInfo();`.
     - DO NOT touch `it('should handle zero chain ID')` at :408-413 — its `setChainID(0)` is an edge-case test of the zero case, not a guard.

  2. test/utils/test-template.ts — anchor update so new suites inherit the baseline:
     - Add import: `import { ensureDiamondTestBaseline } from './diamond-baseline';`
     - In the template `before()`, insert `await ensureDiamondTestBaseline(ownerDiamond, deployedDiamondData.DiamondAddress!);` immediately before the `// Take initial snapshot` comment (:126). Use `ownerDiamond` (connected at :124), not a random signer — the helper's writes require the contractOwner.

  3. Run the full Hardhat suite. Record the observed passing/pending/failing counts verbatim (do NOT assume 665 vs 666 — 17-RESEARCH.md Pitfall 1; the observed figure becomes the Phase 17 baseline input).
  </action>
  <verify>
    <automated>cd gnus-ai && ! grep -n "normalize it before asserting" test/unit/GNUSControlStorage.test.ts && grep -n "ensureDiamondTestBaseline(" test/unit/GNUSControlStorage.test.ts test/utils/test-template.ts && yarn test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "normalize it before asserting" test/unit/GNUSControlStorage.test.ts` returns NO hits (guard deleted)
  - `grep -n "ensureDiamondTestBaseline(" test/unit/GNUSControlStorage.test.ts` returns exactly 1 hit whose line number is LOWER than the first `evm_snapshot` line (:52-region)
  - `grep -n "ensureDiamondTestBaseline(" test/utils/test-template.ts` returns exactly 1 hit whose line number is LOWER than the `provider.send('evm_snapshot', [])` line (:127-region)
  - `grep -n "should handle zero chain ID" -A 3 test/unit/GNUSControlStorage.test.ts` still shows its own `setChainID(0)` (edge-case test intact)
  - `yarn test` exits with mocha summary "0 failing"; the GNUSControlStorage suite block shows 0 failures — this is ROADMAP success criterion 1 (victim passes in the FULL suite with no test-side guard)
  - Observed passing/pending counts recorded in the plan summary (expected shape: ~665-666 passing / 2 pending / 0 failing)
  </acceptance_criteria>
  <done>TEST-04 root fix lands in the victim suite and the template; full Hardhat suite is 0-failing; observed counts captured for the Phase 17 ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → in-process Hardhat network | Helper writes flow from the test harness (signer0) to the local in-memory chain only. No production contract, no external network, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering (evidence) | GNUSControlStorage.test.ts:69-73 guard | mitigate | The deleted guard masked real cross-suite state pollution; D-03 replaces it with a scaffold-level baseline declaration so the test is a true specification again (root fix, exactly what TEST-04 demands) |
| T-17-02 | Elevation of privilege | diamond-baseline.ts owner-signer writes | accept | setChainID/updateBridgeFee/SetSeedSupply execute via the deployer signer against a LOCAL test-only diamond; the same calls already run in 13 suites today (probe-guard precedent); no production surface |
| T-17-03 | Denial of service | unguarded one-shot seed reverting "Already initialized" | mitigate | Probe on storage slot +1 (provenanceInitialized) gates the only one-shot call — same guard every existing duplicate site uses |
| T-17-SC | Tampering (supply chain) | package installs | accept | Zero npm/pip/cargo installs this phase (locked: no new dependencies); no [ASSUMED]/[SUS] packages to gate |
</threat_model>

<verification>
- `yarn test` full-suite run: 0 failing (TEST-04 criterion 1 proven at the victim level)
- Static gates: guard text gone, helper imported and called before the snapshot in both wired files
- 17-02/17-03 extend the same wiring to every remaining scaffold; 17-05 proves it N times
</verification>

<success_criteria>
- `test/utils/diamond-baseline.ts` exists with the exact export signature and probe-guarded body
- Full `yarn test` reports 0 failing with no test-side normalization anywhere in GNUSControlStorage.test.ts
- Template inheritance: any suite generated from test-template.ts now declares the baseline automatically
</success_criteria>

<output>
Create `.planning/phases/17-test-suite-determinism/17-01-SUMMARY.md` when done. Record the observed Hardhat pass/pending/fail counts verbatim — 17-05 consumes them for the ledger.
</output>
