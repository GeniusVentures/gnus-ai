---
phase: 17-test-suite-determinism
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/foundry/invariant/AccessControlInvariant.t.sol
  - test/foundry/unit/SafeSingleShotUpgrade.t.sol
  - test/foundry/unit/SafeDiamondCut.t.sol
autonomous: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "AccessControlInvariant asserts the fixed negative role expectation on `attacker` (outside the handler's fuzz grant surface), never on `user3` — deterministic across runs by construction"
    - "SafeSingleShotUpgrade and SafeDiamondCut report [SKIP ... setUp()] on the bridge-node fork instead of [FAIL ... setUp()] — the fork dependency is declared, not reverted on"
    - "The full Foundry gate (`yarn forge:test` via the bridge node) shows 0 failed and skipped = 5 (was 3), passed count unchanged (~215)"
  artifacts:
    - path: "test/foundry/invariant/AccessControlInvariant.t.sol"
      provides: "sound invariant subject (D-01)"
      contains: "_hasRole(UPGRADER_ROLE, attacker)"
    - path: "test/foundry/unit/SafeSingleShotUpgrade.t.sol"
      provides: "declared fork dependency (D-04)"
      contains: "vm.skip(SAFE_PROXY_FACTORY.code.length == 0"
    - path: "test/foundry/unit/SafeDiamondCut.t.sol"
      provides: "declared fork dependency (D-04)"
      contains: "vm.skip(SAFE_PROXY_FACTORY.code.length == 0"
  key_links:
    - from: "test/foundry/invariant/AccessControlInvariant.t.sol"
      to: "test/foundry/base/GeniusDiamondTestBase.sol"
      via: "attacker (declared :97 via makeAddr) used as the never-granted subject"
      pattern: "attacker\\)"
    - from: "test/foundry/unit/Safe*.t.sol setUp()"
      to: "canonical Sepolia Safe factory 0xC22834581EbC8527d974F8a1c97E1bEA4EF910BC"
      via: "code.length probe gating the createProxyWithNonce call"
      pattern: "SAFE_PROXY_FACTORY\\.code\\.length"
---

<objective>
Fix the two Foundry failure classes at the root: re-target the AccessControlInvariant assertion subject from `user3` to `attacker` (D-01, TEST-05) and declare the fork dependency in both Safe test setUps via `vm.skip` (D-04, TEST-06), then prove both through the wrapper gate.

Purpose: `user3` = `actors[3]` sits inside the handler's fuzz grant surface (roles[3] = UPGRADER_ROLE), so the fuzzer can legitimately violate the invariant — the flake recorded in STATE 07-04. The Safe setUps call the canonical Sepolia factory which has no code on the bridge-node fork — an undeclared environmental dependency, now declared as a skip per D-05.
Output: 3 Solidity test files edited (4 line-level changes total), gate arithmetic verified: 2 failed → 0 failed, skips 3 → 5.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-test-suite-determinism/17-CONTEXT.md
@.planning/phases/17-test-suite-determinism/17-RESEARCH.md
@.planning/phases/17-test-suite-determinism/17-PATTERNS.md

<interfaces>
<!-- Verified on the working tree this session (17-PATTERNS.md, re-read at planning time) -->

test/foundry/invariant/AccessControlInvariant.t.sol current text (:255-279, file is 280 lines):
```solidity
    /**
     * @notice Invariant: Revoking a role that wasn't granted has no effect
     * @dev View-only: Just checks that user3 doesn't have roles
     ...
     */
    function invariant_revokingUnownedRoleIsSafe() public view {
        // User3 shouldn't have UPGRADER_ROLE (never granted)
        assertFalse(_hasRole(UPGRADER_ROLE, user3), "User3 should not have UPGRADER_ROLE");

        console.log("[OK] Ungranted roles verified");
    }
```
The file's own never-granted convention (:165, :193): `assertFalse(_hasRole(<ROLE>, attacker), "Attacker should not have <role>");`
`user3` appears ONLY at :257 (stale doc) and :276 (assertion) in the whole file.

Handler grant surface (read-only context — do NOT edit test/foundry/handlers/GeniusDiamondHandler.sol):
- :84-88 actors = [address(this), user1, user2, user3]
- :540-547 handler_grantRole: role = roles[seed % 4] (roles[3] = UPGRADER_ROLE), target = actors[targetSeed % actors.length] → user3 is legitimately grantable UPGRADER_ROLE
- attacker is declared in test/foundry/base/GeniusDiamondTestBase.sol (:97, makeAddr("attacker")) and never pushed into actors

Safe test files (verified constants and setUp heads):
- SafeSingleShotUpgrade.t.sol: setUp at :62, first statement :63 (`safeOwner = makeAddr("safeOwner");`); SAFE_PROXY_FACTORY at :49 = 0xC22834581EbC8527d974F8a1c97E1bEA4EF910BC; reverting call ISafeProxyFactory(SAFE_PROXY_FACTORY).createProxyWithNonce(...) at :74
- SafeDiamondCut.t.sol: setUp at :49, first statement :50; SAFE_PROXY_FACTORY at :37 (same address); reverting call at :69
- `vm` already in scope in both via `import {Test} from "forge-std/Test.sol"` (:4) — vm.skip(bool,string) available from vendored lib/forge-std (Vm.sol:2540). ZERO import changes.
- Existing in-test skip precedent for reason-string style: SafeSingleShotUpgrade.t.sol:153 ("diamond already at v2.5; pre-upgrade baseline is stale")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 — re-target invariant_revokingUnownedRoleIsSafe from user3 to attacker</name>
  <files>test/foundry/invariant/AccessControlInvariant.t.sol</files>
  <read_first>
  - test/foundry/invariant/AccessControlInvariant.t.sol (whole file — 280 lines; the :165/:193 attacker convention, the :255-279 target block, and the adjacency of every other invariant)
  - test/foundry/handlers/GeniusDiamondHandler.sol (:84-88 actors, :535-557 handler_grantRole, :587-608 handler_revokeRole — read-only, the surface the subject must stay outside)
  </read_first>
  <action>
  Three line-level edits in test/foundry/invariant/AccessControlInvariant.t.sol (per D-01; comment wording is Claude's discretion per CONTEXT — keep the file's NatSpec block structure):

  1. :257 stale doc line `@dev View-only: Just checks that user3 doesn't have roles` → reference `attacker` (e.g. "@dev View-only: Just checks that attacker doesn't have roles").
  2. :275 comment `// User3 shouldn't have UPGRADER_ROLE (never granted)` → replace with a comment stating the soundness rule: attacker is outside the handler's actor set (GeniusDiamondHandler.actors) so no fuzz sequence can grant it any role, whereas user3 (actors[3]) was legitimately grantable via handler_grantRole (roles[3] = UPGRADER_ROLE).
  3. :276 assertion → `assertFalse(_hasRole(UPGRADER_ROLE, attacker), "Attacker should not have UPGRADER_ROLE");`

  Preserve file conventions: 4-space indent, `function invariant_*() public view`, the `console.log("[OK] Ungranted roles verified");` closer, and the NatSpec PROPERTY TESTED / WHY IT MUST HOLD / WHAT BREAKS IF VIOLATED block above the function.

  DO NOT touch any other invariant in the file (adjacency audit in 17-RESEARCH.md: every other subject — address(0), attacker, address(this), owner, double-query — is already sound). DO NOT edit foundry.toml and DO NOT add any `invariant.seed` or rely on `fuzz.seed` for invariant campaigns — D-02 verified both non-implementable on forge 1.7.1 (config key does not exist; fuzz.seed provably does not govern invariant campaigns).
  </action>
  <verify>
    <automated>cd gnus-ai && ! grep -n "UPGRADER_ROLE, user3" test/foundry/invariant/AccessControlInvariant.t.sol && [ "$(grep -c "user3" test/foundry/invariant/AccessControlInvariant.t.sol)" = "0" ] && grep -n "UPGRADER_ROLE, attacker" test/foundry/invariant/AccessControlInvariant.t.sol && forge build --skip test 2>/dev/null | tail -2; forge build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "user3" test/foundry/invariant/AccessControlInvariant.t.sol` returns 0 (both stale references gone)
  - `grep -n "_hasRole(UPGRADER_ROLE, attacker)"` returns exactly 1 hit at the :276-region
  - `grep -n "Attacker should not have UPGRADER_ROLE"` returns 1 hit (revert string updated with the subject)
  - `forge build` succeeds (compilation clean; the full wrapper build runs in Task 3)
  - `git diff test/foundry/handlers/GeniusDiamondHandler.sol` and `git diff foundry.toml` are empty
  </acceptance_criteria>
  <done>The last unsound invariant subject is outside the fuzz grant surface; the invariant is deterministic by construction (TEST-05 root fix).</done>
</task>

<task type="auto">
  <name>Task 2: D-04 — vm.skip fork gate as first statement of both Safe setUps</name>
  <files>test/foundry/unit/SafeSingleShotUpgrade.t.sol, test/foundry/unit/SafeDiamondCut.t.sol</files>
  <read_first>
  - test/foundry/unit/SafeSingleShotUpgrade.t.sol (:44-80 setUp region; :48-49 constants; :151-154 and :179-183 existing in-test skips for reason-string style)
  - test/foundry/unit/SafeDiamondCut.t.sol (:31-70 setUp region; :35-37 constants)
  </read_first>
  <action>
  In BOTH files, insert as the NEW FIRST STATEMENT of `setUp()` (SafeSingleShotUpgrade setUp at :62, currently first statement :63; SafeDiamondCut setUp at :49, currently first statement :50):

  `vm.skip(SAFE_PROXY_FACTORY.code.length == 0, "requires sepolia/anvil fork with canonical Safe deployments");`

  - Precede it with a 2-3 line comment declaring the fork dependency: the canonical Sepolia Safe v1.3.0 deployments exist only on a sepolia/anvil fork; the `yarn forge:test` bridge-node fork carries only the locally deployed diamond, so the `ISafeProxyFactory(SAFE_PROXY_FACTORY).createProxyWithNonce(...)` call below would revert (D-04).
  - Reason string style: lowercase, matching the file's own precedent (`"diamond already at v2.5; pre-upgrade baseline is stale"`).
  - NO import changes — `vm` is already in scope via the existing `import {Test} from "forge-std/Test.sol"` in both files (vendored forge-std Vm.sol:2540 provides vm.skip(bool,string) on forge 1.7.1).
  - Do NOT touch the existing in-test skips (:153/:182 in SafeSingleShotUpgrade) — they guard different conditions (v2.5 baseline staleness; ENCODED_CUT_PATH absence) and remain as-is.

  Expected gate arithmetic (verified empirically on forge 1.7.1, 17-RESEARCH.md): setUp-skip = exactly ONE skipped entry per contract → the two current setUp failures become 0 failed, skips 3 → 5, passed unchanged.
  </action>
  <verify>
    <automated>cd gnus-ai && for f in test/foundry/unit/SafeSingleShotUpgrade.t.sol test/foundry/unit/SafeDiamondCut.t.sol; do grep -n "vm.skip(SAFE_PROXY_FACTORY.code.length == 0" "$f" && grep -n "requires sepolia/anvil fork" "$f"; done && forge build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
  - Both files contain `vm.skip(SAFE_PROXY_FACTORY.code.length == 0, "requires sepolia/anvil fork with canonical Safe deployments");` exactly once each
  - In both files the vm.skip line is the first statement inside `setUp()` (its line number is the smallest statement line after the `function setUp()` line)
  - `forge build` succeeds with no new warnings for these files
  - `git diff` shows no import-line changes in either file
  </acceptance_criteria>
  <done>Both Safe setUps declare their fork dependency; on the bridge-node fork they will report [SKIP ... setUp()] instead of [FAIL ... setUp()] (TEST-06 root fix per D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Gate proof through the bridge node (skip arithmetic + invariant green)</name>
  <files>(verification-only task — no file modifications; run commands and record output)</files>
  <read_first>
  - 17-RESEARCH.md (Pitfall 4 — wrapper node protocol; Pitfall 5 — no --match-path on the wrapper; Validation Architecture section)
  </read_first>
  <action>
  Proof protocol (the wrapper does NOT start the node — Pitfall 4):

  1. Start the bridge node in the background and wait for it to listen: `npx hardhat node` as a background process; poll `curl -s -X POST -H 'Content-Type: application/json' --data '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}' http://127.0.0.1:8545` until it answers (do not proceed on connection refused).
  2. Full Foundry gate once: `yarn forge:test 2>&1 | tee /tmp/17-04-forge-full.log`. Expect: "Suite result: ok." for every contract, total ~215 passed / 0 failed / 5 skipped; SafeSingleShotUpgradeTest and SafeDiamondCutTest each show `[SKIP: requires sepolia/anvil fork with canonical Safe deployments] setUp()`. Record the exact counts observed (D-05: the 3→5 skip change must be written down, not silent).
  3. Invariant-only run once (TEST-05 direct signal): `npx hardhat diamonds-forge:test --diamond-name GeniusDiamond --network localhost --force --match-contract AccessControlInvariant 2>&1 | tee /tmp/17-04-invariant.log`. Expect: AccessControlInvariant suite ok, 0 failed (all invariants pass across runs=5 x depth=10 with fail_on_revert=false).
  4. Kill the background node.

  NEVER verify with bare `forge test` — without the wrapper-deployed localhost diamond it produces 26+ "Diamond has no code" setUp artifacts (invalid gate; 17-CONTEXT.md Established Patterns). If a run fails, capture the failing contract + revert reason in the summary; do not iterate on the node state (restart node + `--force` re-deploys).
  </action>
  <verify>
    <automated>cd gnus-ai && grep -E "passed;.*failed;.*skipped|Suite result" /tmp/17-04-forge-full.log | tail -20 && grep -c "SKIP: requires sepolia/anvil fork" /tmp/17-04-forge-full.log && grep -E "Suite result|passed" /tmp/17-04-invariant.log | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - /tmp/17-04-forge-full.log shows 0 failed for the whole run and exactly 2 occurrences of "SKIP: requires sepolia/anvil fork" (one per Safe contract)
  - Total skipped in the full gate = 5 (was 3) and passed count unchanged from the pre-phase 215 baseline (record the observed figure if it differs — do not reconcile by editing tests)
  - /tmp/17-04-invariant.log shows the AccessControlInvariant suite result ok with 0 failed
  - Observed counts (passed/skipped/failed, both runs) recorded in the plan summary for the 17-05 ledger (D-05)
  </acceptance_criteria>
  <done>TEST-05 and TEST-06 fixes proven through the required wrapper path; skip-count change recorded explicitly per D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forge test process → localhost bridge-node fork | Test contracts read diamond state at the wrapper-deployed localhost address and (Safe tests) attempt calls to canonical Sepolia Safe addresses. Test-only; no production contract is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Repudiation | invariant that cannot fail / flaky invariant | mitigate | D-01 keeps the assertion FALSIFIABLE for real grant bugs: attacker is outside actors[] so no fuzz sequence touches it — the invariant now detects genuine unintended UPGRADER grants instead of seed-luck |
| T-17-10 | Tampering (coverage masking) | vm.skip gates in Safe setUps | mitigate (declared, not silent) | The skip condition probes fork state (SAFE_PROXY_FACTORY.code.length == 0), not an assertion bypass: the skipped contracts' assertions target live-Sepolia state that the local gate structurally cannot provide. The skip count change 3→5 is recorded in the gate record per D-05 — no silent baseline drift. The tests' real value stays available via the documented manual fork/anvil flows (ENCODED_CUT_PATH) |
| T-17-11 | Information disclosure | none applicable | accept | No secrets, no PII; ENCODED_CUT_PATH env-or-default pattern unchanged |
| T-17-SC | Tampering (supply chain) | package installs | accept | Zero installs; 3 vendored Solidity test files edited, no remappings or foundry.toml changes |
</threat_model>

<verification>
- Static: zero `user3` references in the invariant file; vm.skip present as first setUp statement in both Safe files; no handler/foundry.toml changes
- Behavioral: full wrapper gate 0 failed / 5 skipped; invariant-only run green
</verification>

<success_criteria>
- AccessControlInvariant passes deterministically (subject outside the fuzz grant surface — ROADMAP criterion 2, "invariant aligned to handler" leg)
- Safe setUps green-with-declared-skip (ROADMAP criterion 3 per D-04/D-05)
- Gate arithmetic matches the empirically verified forge 1.7.1 semantics: 2 failed → 0, skips 3 → 5, passed unchanged
</success_criteria>

<output>
Create `.planning/phases/17-test-suite-determinism/17-04-SUMMARY.md` when done. Record the observed full-gate and invariant-only counts — 17-05 consumes them for the ledger.
</output>
