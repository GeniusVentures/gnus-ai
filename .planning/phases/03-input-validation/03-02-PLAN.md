---
phase: 03-input-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSBridge.sol
autonomous: true
requirements:
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "GNUSBridge.withdraw() rejects calls where exchangeRate is zero"
    - "GNUSBridge.withdraw() rejects calls where amount is less than exchangeRate"
    - "GNUSBridge.bridgeOut() rejects calls where destChainID equals the current chain ID"
  artifacts:
    - path: "contracts/gnus-ai/GNUSBridge.sol"
      provides: "withdraw() and bridgeOut() input validation"
      contains: "require(exchangeRate > 0,"
      contains: "require(amount >= exchangeRate,"
      contains: "require(destChainID != GNUSControlStorage.layout().chainID,"
  key_links:
    - from: "GNUSBridge.withdraw() div expression (line 156)"
      to: "exchangeRate validation guard (before division)"
      via: "require(exchangeRate > 0) and require(amount >= exchangeRate)"
      pattern: "require\\(exchangeRate > 0"
    - from: "GNUSBridge.bridgeOut() destChainID parameter"
      to: "GNUSControlStorage.layout().chainID"
      via: "chain ID inequality check"
      pattern: "require\\(destChainID != GNUSControlStorage\\.layout\\(\\)\\.chainID"
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@contracts/gnus-ai/GNUSBridge.sol
@contracts/gnus-ai/GNUSControlStorage.sol
@test/unit/GNUSBridgeEnhanced.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exchangeRate and amount validation to GNUSBridge.withdraw() (SEC-02)</name>
  <files>contracts/gnus-ai/GNUSBridge.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSBridge.sol (lines 149–165 for withdraw function)
    - contracts/gnus-ai/GNUSNFTFactoryStorage.sol (for NFT struct exchangeRate field location)
    - test/unit/GNUSBridgeEnhanced.test.ts (for existing withdraw test patterns)
    - .planning/codebase/CONVENTIONS.md (for require() error message style)
  </read_first>
  <action>
    In GNUSBridge.sol, inside the withdraw() function body (line 149), add two require() guards after the existing require checks (lines 151–153) and BEFORE the exchangeRate lookup and division at line 156.

    After line 153 (`require(balanceOf(sender, id) >= amount, "Insufficient tokens.");`), add a blank line, then add:

    // SEC-02: Validate exchangeRate to prevent division truncation losses
    uint256 exchangeRate = GNUSNFTFactoryStorage.layout().NFTs[id].exchangeRate;
    require(exchangeRate > 0, "Exchange rate must be greater than zero");
    require(amount >= exchangeRate, "Amount must be at least the exchange rate");

    Then replace line 156 (`uint256 convAmount = amount / GNUSNFTFactoryStorage.layout().NFTs[id].exchangeRate;`) with:

    uint256 convAmount = amount / exchangeRate;

    This hoists the NFT storage lookup so the exchangeRate is validated BEFORE the division. The existing `require(id != GNUS_TOKEN_ID, "Cannot withdraw GNUS tokens.")` on line 152 already ensures `exchangeRate` is read from a valid child NFT.

    Traceability: per SEC-02, per CONTEXT decision to validate `amount >= exchangeRate` and `exchangeRate > 0`.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - GNUSBridge.sol withdraw() function body contains `uint256 exchangeRate = GNUSNFTFactoryStorage.layout().NFTs[id].exchangeRate;` before the division line
    - GNUSBridge.sol withdraw() contains `require(exchangeRate > 0, "Exchange rate must be greater than zero");`
    - GNUSBridge.sol withdraw() contains `require(amount >= exchangeRate, "Amount must be at least the exchange rate");`
    - `uint256 convAmount = amount / exchangeRate;` uses the local variable, not double storage lookup
    - `npx hardhat compile` exits 0
    - Existing `test/unit/GNUSBridgeEnhanced.test.ts` suite passes (withdraw with valid params still works)
  </acceptance_criteria>
  <done>withdraw() validates exchangeRate > 0 and amount >= exchangeRate before performing the division. Hardhat compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add same-chain guard to GNUSBridge.bridgeOut() (SEC-03)</name>
  <files>contracts/gnus-ai/GNUSBridge.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSBridge.sol (lines 173–185 for bridgeOut function)
    - contracts/gnus-ai/GNUSControlStorage.sol (for chainID field location)
    - test/unit/GNUSBridgeEnhanced.test.ts (lines 408–455 for existing bridgeOut tests)
    - .planning/codebase/CONVENTIONS.md (for require() error message style)
  </read_first>
  <action>
    In GNUSBridge.sol, inside the bridgeOut() function body (line 173), add one require() guard after the existing require checks (lines 175–176) and BEFORE the _burn call at line 177. The guard verifies that the destination chain is different from the current chain.

    After line 176 (`require(balanceOf(sender, id) >= amount, "Insufficient tokens.");`), add a blank line, then add:

    // SEC-03: Prevent bridging to the same chain
    require(destChainID != GNUSControlStorage.layout().chainID, "Cannot bridge to same chain");

    The existing event emission at lines 178–184 already references `GNUSControlStorage.layout().chainID` as the source chain ID, so this guard is consistent with existing semantics: `destChainID` must differ from the current chain's ID.

    No other changes to the function body. The import of `GNUSControlStorage.sol` already exists at line 11 of the file.

    Traceability: per SEC-03, per CONTEXT decision to validate `destChainID != chainID`.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - GNUSBridge.sol bridgeOut() function body contains `require(destChainID != GNUSControlStorage.layout().chainID, "Cannot bridge to same chain");`
    - The guard is placed after existing require() checks and before the `_burn()` call at line 177
    - `npx hardhat compile` exits 0
    - Existing `test/unit/GNUSBridgeEnhanced.test.ts` suite passes (existing tests use destChainID=137 while current chain is different, no regressions)
  </acceptance_criteria>
  <done>bridgeOut() validates `destChainID != chainID` before burning tokens. Hardhat compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external→GNUSBridge.withdraw() | Any token holder can call withdraw; exchangeRate comes from NFT storage |
| external→GNUSBridge.bridgeOut() | Any token holder can call bridgeOut; destChainID is caller-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | GNUSBridge.withdraw() exchangeRate | mitigate | `require(exchangeRate > 0)` prevents division-by-zero; `require(amount >= exchangeRate)` prevents value-destroying partial withdrawals where truncation produces zero convAmount |
| T-03-05 | Tampering | GNUSBridge.bridgeOut() destChainID | mitigate | `require(destChainID != GNUSControlStorage.layout().chainID)` prevents self-bridging that could create fake bridge events on the same chain |
</threat_model>

<verification>
- `npx hardhat compile` exits 0
- `npx hardhat test --grep "GNUSBridge"` passes (no regressions from withdraw or bridgeOut guards)
- `grep -n 'require(exchangeRate > 0' contracts/gnus-ai/GNUSBridge.sol` returns a valid line
- `grep -n 'require(amount >= exchangeRate' contracts/gnus-ai/GNUSBridge.sol` returns a valid line
- `grep -n 'require(destChainID != GNUSControlStorage.layout().chainID' contracts/gnus-ai/GNUSBridge.sol` returns a valid line
</verification>

<success_criteria>
1. `GNUSBridge.withdraw()` validates `exchangeRate > 0` and `amount >= exchangeRate` before division — per SEC-02.
2. `GNUSBridge.bridgeOut()` validates `destChainID != chainID` before burning — per SEC-03.
3. Hardhat compilation succeeds. Existing test suites pass without regression.
</success_criteria>

<output>
Create `.planning/phases/03-input-validation/03-02-SUMMARY.md` when done
</output>
