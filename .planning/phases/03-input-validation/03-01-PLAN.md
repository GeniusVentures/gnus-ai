---
phase: 03-input-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/ERC20TransferBatch.sol
  - contracts/gnus-ai/GNUSControl.sol
autonomous: true
requirements:
  - SEC-01
  - SEC-04

must_haves:
  truths:
    - "mintBatch() rejects calls that send ETH"
    - "banTransferorBatch() rejects mismatched array lengths"
    - "allowTransferorBatch() rejects mismatched array lengths"
  artifacts:
    - path: "contracts/gnus-ai/ERC20TransferBatch.sol"
      provides: "mintBatch() with ETH rejection guard"
      contains: "require(msg.value == 0, \"ETH not accepted\")"
    - path: "contracts/gnus-ai/GNUSControl.sol"
      provides: "batch blacklist functions with array length validation"
      contains: "require(tokenIds.length == bannedAddresses.length"
  key_links:
    - from: "mintBatch() payable modifier"
      to: "require(msg.value == 0)"
      via: "ETH rejection guard"
      pattern: "require\\(msg\\.value == 0"
    - from: "banTransferorBatch/allowTransferorBatch loop body"
      to: "tokenIds.length == bannedAddresses.length"
      via: "length equality check"
      pattern: "require\\(tokenIds\\.length == bannedAddresses\\.length"
---

<objective>
Add two missing input validation guards: prevent ETH acceptance in `mintBatch()` and enforce array length parity in batch transferor ban/allow functions.

Purpose: Close a payable-gap attack surface (SEC-01) where `mintBatch()` is marked `payable` but neither uses nor refunds ETH, and prevent silent data misalignment (SEC-04) where mismatched `tokenIds`/`bannedAddresses` arrays in `banTransferorBatch()` and `allowTransferorBatch()` produce incorrect state mutations.

Output: `ERC20TransferBatch.sol` and `GNUSControl.sol` updated with `require()` guards.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@contracts/gnus-ai/ERC20TransferBatch.sol
@contracts/gnus-ai/GNUSControl.sol
@test/unit/ERC20TransferBatch.test.ts
@test/unit/GNUSControlStorage.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ETH rejection guard to mintBatch() (SEC-01)</name>
  <files>contracts/gnus-ai/ERC20TransferBatch.sol</files>
  <read_first>
    - contracts/gnus-ai/ERC20TransferBatch.sol (lines 38–49 for mintBatch function)
    - test/unit/ERC20TransferBatch.test.ts (lines 64–134 for existing mintBatch tests)
    - .planning/codebase/CONVENTIONS.md (for require() error message style)
  </read_first>
  <action>
    In ERC20TransferBatch.sol, inside the mintBatch() function body (line 42), add as the first statement after the function opening brace, before the operator assignment:

    require(msg.value == 0, "ETH not accepted");

    The function signature line (line 42: `function mintBatch(address[] memory destinations, uint256[] memory amounts) external payable {`) remains `external payable` — do NOT remove the `payable` modifier. The `require()` guard is the correct pattern per the success criteria: "is no longer payable OR includes require(msg.value == 0, 'ETH not accepted')". The `payable` modifier is needed for diamond delegatecall compatibility; the guard provides the security.

    No other changes to the function body.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - ERC20TransferBatch.sol line 42 reads `function mintBatch(address[] memory destinations, uint256[] memory amounts) external payable {`
    - The next line (43) reads `require(msg.value == 0, "ETH not accepted");`
    - `npx hardhat compile` exits 0
    - Existing test `test/unit/ERC20TransferBatch.test.ts` suite passes (no regressions — mintBatch with 0 ETH still works)
  </acceptance_criteria>
  <done>mintBatch() function body starts with `require(msg.value == 0, "ETH not accepted");` guard. Hardhat compilation passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add array length validation to banTransferorBatch and allowTransferorBatch (SEC-04)</name>
  <files>contracts/gnus-ai/GNUSControl.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSControl.sol (lines 81–110 for both batch functions)
    - test/unit/GNUSControlStorage.test.ts (lines 185–256 for existing batch blacklist tests)
    - .planning/codebase/CONVENTIONS.md (for require() error message style)
  </read_first>
  <action>
    In GNUSControl.sol, add a `require()` statement at the top of both `banTransferorBatch()` and `allowTransferorBatch()` function bodies, before the for-loop, to validate that `tokenIds.length == bannedAddresses.length`.

    For banTransferorBatch() (line 84, after the `external onlySuperAdminRole {` line, before the `for` loop at line 85):
    require(tokenIds.length == bannedAddresses.length, "Array length mismatch");

    For allowTransferorBatch() (line 102, after the `external onlySuperAdminRole {` line, before the `for` loop at line 103):
    require(tokenIds.length == bannedAddresses.length, "Array length mismatch");

    Use the same message string in both functions. Follow the existing pattern in the codebase: `require(tokenIds.length == bannedAddresses.length, "Array length mismatch");`

    No other changes to either function body.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - GNUSControl.sol line ~85 (inside banTransferorBatch, before for-loop) reads `require(tokenIds.length == bannedAddresses.length, "Array length mismatch");`
    - GNUSControl.sol line ~103 (inside allowTransferorBatch, before for-loop) reads `require(tokenIds.length == bannedAddresses.length, "Array length mismatch");`
    - `npx hardhat compile` exits 0
    - Existing test `test/unit/GNUSControlStorage.test.ts` suite passes (existing tests call both functions with matching arrays, no regressions)
  </acceptance_criteria>
  <done>Both banTransferorBatch() and allowTransferorBatch() validate `tokenIds.length == bannedAddresses.length` before iterating. Hardhat compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external→ERC20TransferBatch.mintBatch() | Any EOA or contract can call mintBatch; ETH may be attached |
| external→GNUSControl.banTransferorBatch() | Only super admin can call, but array args cross trust boundary |
| external→GNUSControl.allowTransferorBatch() | Only super admin can call, but array args cross trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ERC20TransferBatch.mintBatch() | mitigate | `require(msg.value == 0, "ETH not accepted")` rejects any attached ETH, preventing ETH locking |
| T-03-02 | Tampering | GNUSControl.banTransferorBatch() | mitigate | `require(tokenIds.length == bannedAddresses.length, "Array length mismatch")` prevents misaligned state writes |
| T-03-03 | Tampering | GNUSControl.allowTransferorBatch() | mitigate | Same length guard prevents incorrect unban operations |
</threat_model>

<verification>
- `npx hardhat compile` exits 0 (both plans)
- `npx hardhat test --grep "mintBatch"` passes (no regressions from SEC-01 guard)
- `npx hardhat test --grep "GNUSControl"` passes (no regressions from SEC-04 guard)
- `grep -n 'require(msg.value == 0)' contracts/gnus-ai/ERC20TransferBatch.sol` returns line 43
- `grep -c 'require(tokenIds.length == bannedAddresses.length' contracts/gnus-ai/GNUSControl.sol` returns 2
</verification>

<success_criteria>
1. `ERC20TransferBatch.mintBatch()` includes `require(msg.value == 0, "ETH not accepted")` — per SEC-01.
2. `GNUSControl.banTransferorBatch()` and `allowTransferorBatch()` include `require(tokenIds.length == bannedAddresses.length, "Array length mismatch")` — per SEC-04.
3. Hardhat compilation succeeds. Existing test suites pass without regression.
</success_criteria>

<output>
Create `.planning/phases/03-input-validation/03-01-SUMMARY.md` when done
</output>
