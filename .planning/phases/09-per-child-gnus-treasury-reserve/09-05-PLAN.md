---
phase: 09-per-child-gnus-treasury-reserve
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - test/unit/NFTFactory.test.ts
  - test/unit/GNUSNFTFactoryEnhanced.test.ts
  - test/unit/GNUSBridge.test.ts
  - test/unit/GNUSBridgeEnhanced.test.ts
  - test/unit/GNUSWithdrawLimiterStorage.test.ts
  - test/integration/withdraw-limiter-integration.test.ts
  - test/gas/withdraw-limiter-gas-comparison.test.ts
  - test/foundry/handlers/GeniusDiamondHandler.sol
  - test/foundry/invariant/ConservationInvariant.t.sol
  - scripts/devops/smart-trigger.ts
  - .planning/phases/09-per-child-gnus-treasury-reserve/09-05-SLITHER.json
  - .planning/phases/09-per-child-gnus-treasury-reserve/09-05-SUMMARY.md
autonomous: true
requirements: [TREASURY-01, TREASURY-02, TREASURY-03]
must_haves:
  truths:
    - "All pre-existing test files pass against the new conversion-native model"
    - "Every .withdraw( call site has been migrated to convert(id, GNUS_TOKEN_ID, amount, caller)"
    - "Mint-semantics flip is reflected: callers pass minions, assertions expect 1:1 movement"
    - "2nd-gen mint tests now assert depth-gate revert + convert-based issuance"
    - "Foundry handler exposes handler_convert and the invariant suite enforces I1, I2, I5; I3 and I6 are pinned by GNUSTreasury.test.ts unit suites"
    - "Full npx hardhat test + forge test are green"
    - "Slither has been run against GNUSBridge.sol, GNUSNFTFactory.sol, GNUSNFTFactoryStorage.sol, GNUSTreasury.sol, GNUSTreasuryStorage.sol; findings triaged in 09-05-SUMMARY.md; the slither-include gap for contracts/gnus-ai/ is explicitly flagged for Phase 7"
  artifacts:
    - path: "test/foundry/invariant/ConservationInvariant.t.sol"
      provides: "I1/I2/I5 invariant suite"
      min_lines: 80
    - path: "test/unit/NFTFactory.test.ts"
      provides: "Migrated minion-semantics assertions"
    - path: "test/unit/GNUSBridge.test.ts"
      provides: "Migrated withdraw→convert assertions"
  key_links:
    - from: "test/unit/GNUSBridge.test.ts"
      to: "GNUSTreasury.convert"
      via: "former withdraw cases rewritten against convert"
      pattern: "convert\\("
    - from: "test/foundry/invariant/ConservationInvariant.t.sol"
      to: "diamond state"
      via: "invariant_* functions reading totalSupply + globalSupply"
      pattern: "invariant_"
---

<objective>
Migrate every existing test/caller to the conversion-native model and land the Foundry invariant suite (I1–I6).

Purpose: Plans 09-01 through 09-04 land the new model + its new tests. This plan drags the pre-existing test suite forward. Research §E inventories every call site that breaks. The Foundry invariant suite is the fuzz-level proof that conservation (I1), convert-neutrality (I2), provenance consistency (I3), and the limiter charge matrix (I6) hold under arbitrary sequences.

Output:
- MOD 5 test files (`.withdraw(` migrations per research §I)
- MOD 2 test files (mint-semantics flips per research §E)
- MOD smart-trigger.ts:389 (mint calldata semantics — verify overload and update if needed)
- MOD Foundry handler (already has `handler_convert` stub from Plan 09-01 — flesh out bounding logic)
- NEW `test/foundry/invariant/ConservationInvariant.t.sol` (I1, I2, I5)
- Slither static-analysis run on the 5 changed contracts with findings triaged in the SUMMARY (security_and_upgrade #8 — REQUIRED, closes the CONTEXT mandate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-VALIDATION.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-04-SUMMARY.md

<interfaces>
<!-- Test call-site inventory from research §E and §I (VERIFIED by grep during research): -->
- `test/unit/GNUSBridge.test.ts` — 12 `.withdraw(` refs → rewrite as `convert(id, GNUS_TOKEN_ID, amount, caller)` (note arg-order flip: withdraw is `(amount, id)`, convert is `(fromId, toId, minionAmount, to)`)
- `test/unit/GNUSBridgeEnhanced.test.ts` — same migration
- `test/unit/GNUSWithdrawLimiterStorage.test.ts` — limiter tests through withdraw → re-home onto convert's GNUS-terminal leg
- `test/integration/withdraw-limiter-integration.test.ts` — same re-home
- `test/gas/withdraw-limiter-gas-comparison.test.ts` — same re-home; gas baselines shift (convert is 2 supply ops, no fee path)
- `test/unit/NFTFactory.test.ts` — mint call sites at lines 173, 297, 364, 407, 458; 2nd-gen assertions at lines 371-375 and 522-525 (per REQUIREMENTS.md:149) flip from "no burn" to "reverts with depth-gate; convert issues instead"
- `test/unit/GNUSNFTFactoryEnhanced.test.ts` — mint call sites at lines 339, 359, 376, 390, 411, 417, 431, 435, 474; burn-amount assertions flip from `amount*rate` to `amount`
- `scripts/devops/smart-trigger.ts:389` — references 'mint' selector; verify overload and update calldata semantics if it passes child units
- `test/foundry/handlers/GeniusDiamondHandler.sol` — `handler_convert` stub exists from Plan 09-01; flesh out bounding + add depth-gate revert expectation in `handler_mint1155`
- `test/unit/Phase5-circuit-breaker.test.ts` — uses 2-arg root mint; should survive unchanged (verify)
- ERC-20 facade tests — should survive wholesale (research §I: totalSupply facade unchanged)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate .withdraw( call sites to convert() — 5 test files</name>
  <files>test/unit/GNUSBridge.test.ts, test/unit/GNUSBridgeEnhanced.test.ts, test/unit/GNUSWithdrawLimiterStorage.test.ts, test/integration/withdraw-limiter-integration.test.ts, test/gas/withdraw-limiter-gas-comparison.test.ts</files>
  <behavior>
    Every previously-withdraw-driven test now drives the same flow via convert(id, GNUS_TOKEN_ID, amount, caller). Limiter bin observations, super-admin bypass events, and revert assertions are preserved semantically (with context strings updated to "GNUSTreasury.convert" where the bypass fires from convert).
  </behavior>
  <read_first>
    - test/unit/GNUSBridge.test.ts (12 .withdraw( refs — migrate each)
    - test/unit/GNUSBridgeEnhanced.test.ts
    - test/unit/GNUSWithdrawLimiterStorage.test.ts
    - test/integration/withdraw-limiter-integration.test.ts
    - test/gas/withdraw-limiter-gas-comparison.test.ts
    - test/unit/GNUSTreasury.test.ts (from Plan 09-04 — reuse its convert invocation idiom)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §I (Pitfall 8: do NOT touch GNUSContractAssets.test.ts — that file uses `withdrawToken`, a different contract)
  </read_first>
  <action>
    For each of the 5 files:

    1. Find every `.withdraw(` call site (`grep -n "\.withdraw(" <file>`).
    2. Rewrite each as `.convert(childId, GNUS_TOKEN_ID, amount, caller)` — mind the ARG-ORDER FLIP: withdraw was `(amount, id)`; convert is `(fromId, toId, minionAmount, to)`. A call `diamond.withdraw(toWei(5), childId)` becomes `diamond.convert(childId, GNUS_TOKEN_ID, toWei(5), signer.address)`.
    3. Update revert-message assertions:
       - "Cannot withdraw GNUS tokens." → no longer reachable (convert's `fromId == GNUS_TOKEN_ID && toId == GNUS_TOKEN_ID` is caught by the same-id guard "Cannot convert to same id")
       - "Insufficient tokens." → convert's `_burn` reverts with ERC1155's standard message (verify by reading the actual revert; do not guess)
       - "Token not created." → unchanged (convert also checks nftCreated)
       - "Exchange rate must be greater than zero" / "Amount must be at least the exchange rate" → GONE (no rate math in convert); delete those test cases
    4. Update event assertions:
       - `SuperAdminBypass` with context "GNUSBridge.withdraw" → context becomes "GNUSTreasury.convert"
    5. Update limiter assertions: previously charged `amount / exchangeRate` (convAmount); now charged `amount` directly (already minions). Update bin-delta assertions accordingly.
    6. Update the gas-comparison test's describe/comments to reference convert, and re-baseline if the file has hardcoded gas expectations.
    7. DO NOT touch `test/unit/GNUSContractAssets.test.ts` — it uses `withdrawToken`, a different contract (research Pitfall 8).
    8. **Per-file commit boundaries (REQUIRED — do not batch):** after migrating each file, run `npx hardhat test <file>` and ensure green, then commit that single file's change with message `test(09-05): migrate <basename> to convert()` before moving to the next file. This produces 5 atomic commits for Task 1 — one per file. If a file's migration reveals an unexpected dependency on a not-yet-migrated file, STOP, document the coupling in the test file as a comment, and surface to the user before proceeding — do NOT migrate multiple files in one commit to paper over cross-file coupling.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && for f in test/unit/GNUSBridge.test.ts test/unit/GNUSBridgeEnhanced.test.ts test/unit/GNUSWithdrawLimiterStorage.test.ts test/integration/withdraw-limiter-integration.test.ts test/gas/withdraw-limiter-gas-comparison.test.ts; do echo "=== $f ==="; grep -c "\.withdraw(" "$f" || true; npx hardhat test "$f" 2>&1 | tail -5; done</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: `grep -c "\.withdraw("` returns 0 (or only matches in comments describing the migration — if a comment retains `.withdraw(`, that's acceptable; verify no live call sites)
    - All 5 files pass `npx hardhat test <file>` individually
    - No file references the revert strings "Cannot withdraw GNUS tokens." or "Exchange rate must be greater than zero" or "Amount must be at least the exchange rate" in active assertions
    - `SuperAdminBypass` assertions reference "GNUSTreasury.convert" (not "GNUSBridge.withdraw") for the convert-driven paths
    - `test/unit/GNUSContractAssets.test.ts` is byte-identical to its pre-plan state (untouched)
  </acceptance_criteria>
  <done>5 test files migrated to convert; each green in isolation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mint-semantics flip — NFTFactory.test.ts + GNUSNFTFactoryEnhanced.test.ts + smart-trigger.ts</name>
  <files>test/unit/NFTFactory.test.ts, test/unit/GNUSNFTFactoryEnhanced.test.ts, scripts/devops/smart-trigger.ts</files>
  <behavior>
    Mint callers pass minions (D1). GNUS balance deltas are exactly `amount` (not `amount * rate`). Child balance assertions use `amount` (numerically equal — child balance IS minions). 2nd-gen mint tests flip from "no burn" to "reverts with depth gate; convert issues instead."
  </behavior>
  <read_first>
    - test/unit/NFTFactory.test.ts (mint sites at lines 173, 297, 364, 407, 458; 2nd-gen assertions at 371-375, 522-525)
    - test/unit/GNUSNFTFactoryEnhanced.test.ts (mint sites at lines 339, 359, 376, 390, 411, 417, 431, 435, 474; burn-revert paths at 376, 390)
    - scripts/devops/smart-trigger.ts (line 389 area — verify which mint overload is referenced)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §9 (assertion-flip example)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §E (caller inventory)
  </read_first>
  <action>
    1. **`test/unit/NFTFactory.test.ts`** — update mint call sites at lines 173, 297, 364, 407, 458 (per research §E):
       - The `mint(address,uint256,uint256,bytes)` calls keep the same shape but the AMOUNT is now minions. Tests that previously passed child units priced at `exchRate` (e.g., rate 2 → 5 units cost 10 GNUS) now pass minions directly and cost exactly `amount`.
       - Update GNUS balance-delta assertions from `units * rate` to `amount`.
       - Update child-balance assertions: previously `balance == units`; now `balance == amount` (same number, different unit).
       - Update the 2nd-gen mint tests at lines 371-375 and 522-525 (per REQUIREMENTS.md:149):
         - BEFORE: asserted "no burn on deeper mints" (2nd-gen mints were free)
         - AFTER: assert the deeper mint REVERTS with "Direct children only; use convert() for descendants", then issue the deeper token via `convert(parentId, childId, amount, to)` and assert exact conservation.
       - The canonical "burn correct amount" test at line 468 (PATTERNS.md §9): the GNUS burn assertion flips from `toWei(5) * exchangeRate` to `toWei(5)`.

    2. **`test/unit/GNUSNFTFactoryEnhanced.test.ts`** — update mint call sites at lines 339, 359, 376, 390, 411, 417, 431, 435, 474:
       - Same semantics flip as above.
       - The burn-revert tests at lines 376 and 390 (which assert "Not enough GNUS_TOKEN to burn"): the revert message is now "Not enough GNUS_TOKEN to convert" (changed in Plan 09-04 Task 1). Update the expected revert string.
       - Any assertion that the burn amount equals `units * rate` flips to `amount`.

    3. **`scripts/devops/smart-trigger.ts`** — inspect line 389 area:
       - Determine which `mint` overload is referenced. If it's the 2-arg root mint (signature `mint(address,uint256)`) — no change needed (semantics unchanged).
       - If it's the 4-arg factory mint (`mint(address,uint256,uint256,bytes)`) — the caller semantics flip to minions; update any hardcoded amount calculation accordingly.
       - If it references the 3-arg MINTER mint with a non-zero token id — that path is now blocked (D10); update to a two-step (mint id 0 → convert) or document why the path is no longer reachable.
       - This is a scripts file; if the line turns out to be inert (e.g., a comment or unused selector reference), document the finding and move on.

    4. After each file, run it and ensure green: `npx hardhat test <file>`.
    5. Verify the ERC-20 facade tests still pass untouched (research §I says they should): `npx hardhat test test/unit/GNUSERC20.test.ts` and `test/unit/Erc20Batch.test.ts` — if either fails, STOP and surface the failure (facade semantics were not supposed to change).
    6. Verify Phase5-circuit-breaker still passes: `npx hardhat test test/unit/Phase5-circuit-breaker.test.ts` — pause interplay with new hooks is a known unknown (research §I flags it for verification).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat test test/unit/NFTFactory.test.ts 2>&1 | tail -10 && npx hardhat test test/unit/GNUSNFTFactoryEnhanced.test.ts 2>&1 | tail -10 && npx hardhat test test/unit/GNUSERC20.test.ts 2>&1 | tail -5 && npx hardhat test test/unit/Phase5-circuit-breaker.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/NFTFactory.test.ts` passes
    - `test/unit/GNUSNFTFactoryEnhanced.test.ts` passes
    - `test/unit/GNUSERC20.test.ts` passes (facade regression check)
    - `test/unit/Erc20Batch.test.ts` passes
    - `test/unit/Phase5-circuit-breaker.test.ts` passes
    - No test in NFTFactory.test.ts or GNUSNFTFactoryEnhanced.test.ts asserts a burn amount of `units * rate` (verify by grep: no `* exchangeRate` or `* exchRate` in active assertion code)
    - 2nd-gen mint tests assert the depth-gate revert message
    - smart-trigger.ts:389 investigated; either updated with a comment explaining the new semantics, or documented as inert
  </acceptance_criteria>
  <done>Mint-semantics flip reflected across tests; facade regression green; smart-trigger dispositioned</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Foundry invariant suite — ConservationInvariant.t.sol (I1, I2, I5) + handler fleshing</name>
  <files>test/foundry/invariant/ConservationInvariant.t.sol, test/foundry/handlers/GeniusDiamondHandler.sol</files>
  <behavior>
    - I1: After any sequence of mint/convert/burn/bridgeOut actions, Σ_id totalSupply(id) changes only via root mint (_mintWithBridgeFee), admin burn, and bridgeOut.
    - I2: convert never changes Σ_id totalSupply(id) and never changes totalSupplyOfAll().
    - I3: Two-diamond bridge simulation — counters consistent across source bridgeOut + dest mint + admin sync.
    - I5: totalSupplyOfAll() ≤ GNUS_MAX_SUPPLY after every action.
    - I6: Limiter charge matrix — exactly once per GNUS-terminal convert; hook-only per GNUS→child; never token-to-token (verified by observing bin deltas across handler calls).
  </behavior>
  <read_first>
    - test/foundry/base/GeniusDiamondTestBase.sol (harness base)
    - test/foundry/invariant/EconomicInvariant.t.sol (analog — read the whole file)
    - test/foundry/invariant/NFTFactoryInvariant.t.sol (analog)
    - test/foundry/handlers/GeniusDiamondHandler.sol (current state — has handler_convert stub from Plan 09-01)
    - test/foundry/GeniusDiamond.forge.config.json (harness config)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §I (invariant specs)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §10, §11
  </read_first>
  <action>
    1. **Flesh out `handler_convert` in `test/foundry/handlers/GeniusDiamondHandler.sol`** (Plan 09-01 landed a compile-able stub; now wire it for real):
       - Bound `actorSeed` against the handler's existing actor set
       - Bound `fromIdSeed`/`toIdSeed` against the set of created token ids (track created ids in a ghost array if not already present — check the existing handler for an id-tracking pattern; if none, add `uint256[] public ghost_createdIds` and push to it from `handler_createNFT` or wherever token creation is fuzzed)
       - Bound `amount` to `[1, 1e30]` (or the file's existing magnitude convention)
       - Call `convert(fromId, toId, amount, to)` on the diamond; use `try/catch` — on success increment `ghost_convertCalls`; on revert, do nothing (revert paths are exercised by the unit tests, not the invariant)
       - In `handler_mint1155` (around line 465): if the fuzzed tokenId is at depth ≥2, expect the depth-gate revert. If the handler currently doesn't track id depth, add a ghost mapping `ghost_idDepth` or compute via `(id >> 128) != 0 && ghost_createdIds contains id`'s parent — pick whichever the existing handler pattern supports.

    2. **Create NEW `test/foundry/invariant/ConservationInvariant.t.sol`**:
       - Inherit `GeniusDiamondTestBase` (read the analogs for the exact pattern)
       - In `setUp`: deploy the diamond via the base harness, wire `targetContract(address(handler))`, register the handler's selectors per the existing invariant files
       - Initialize provenance with seed 0 via `GNUSTreasury_Initialize300(0)` from the super-admin
       - Implement these invariant functions (read live diamond state, do NOT maintain parallel ghost sums for supply — the diamond IS the source of truth):
         - `invariant_I1_conservation()` — Σ_id totalSupply(id) equals a tracked expected value updated only on root mint/burn/bridgeOut handler actions. The handler exposes `ghost_totalMintedByRoot`, `ghost_totalBurnedByAdmin`, `ghost_totalBridgedOut`; the invariant asserts Σ == seed + ghost_totalMintedByRoot - ghost_totalBurnedByAdmin - ghost_totalBridgedOut.
         - `invariant_I2_convertNeutral()` — totalSupplyOfAll() equals the value it had at the last non-convert handler action. Track via a `ghost_lastNonConvertGlobalSupply` set by every non-convert handler; assert `totalSupplyOfAll() == ghost_lastNonConvertGlobalSupply` after convert calls.
         - `invariant_I5_globalCap()` — `totalSupplyOfAll() <= GNUS_MAX_SUPPLY`.
         - `invariant_convertCallCountMonotonic()` — `ghost_convertCalls` only ever increases (sanity check on the handler).
       - I3 (two-diamond bridge) and I6 (limiter charge matrix) are covered by the unit tests in GNUSTreasury.test.ts — do NOT duplicate them in Foundry (unit-level is sufficient per VALIDATION.md).
       - I4 (free-GNUS identity) is single-chain and covered by unit tests — skip in Foundry.
       - File length target: 80-200 lines.

    3. Run: `cd test/foundry && forge test --match-contract ConservationInvariant -vvv` (or the appropriate match pattern per the file's name).
    4. If any invariant fails, do NOT weaken the invariant — fix the underlying contract or handler bug (project rule: fix root cause).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai/test/foundry && forge build 2>&1 | tail -5 && forge test --match-contract ConservationInvariant 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0
    - `forge test --match-contract ConservationInvariant` exits 0
    - File contains `invariant_I1_conservation`, `invariant_I2_convertNeutral`, `invariant_I5_globalCap` as literal function names
    - Handler file contains a working `handler_convert` (not a stub — the function actually calls `convert` via the diamond)
    - No `vm.warp`/`vm.roll`-based sleeping; no `std::this_thread::sleep_for` equivalents (Solidity: no artificial time manipulation unless the test is time-dependent by design)
    - Existing invariant files (EconomicInvariant, NFTFactoryInvariant, etc.) still pass: `forge test` (whole tree) exits 0
  </acceptance_criteria>
  <done>Foundry invariant suite green; handler_convert wired; full forge tree passes</done>
</task>

<task type="auto">
  <name>Task 4: Slither static analysis on the 5 changed contracts + findings triage</name>
  <files>.planning/phases/09-per-child-gnus-treasury-reserve/09-05-SUMMARY.md</files>
  <read_first>
    - contracts/gnus-ai/GNUSBridge.sol (post-09-03)
    - contracts/gnus-ai/GNUSNFTFactory.sol (post-09-04)
    - contracts/gnus-ai/GNUSNFTFactoryStorage.sol (post-09-01)
    - contracts/gnus-ai/GNUSTreasury.sol (post-09-02)
    - contracts/gnus-ai/GNUSTreasuryStorage.sol (post-09-01)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md security_and_upgrade #8
    - .planning/codebase/CONCERNS.md (slither currently excludes contracts/gnus-ai/ — the known gap)
    - hardhat.config.ts (slither/solc configuration, if any)
    - package.json (slither invocation scripts, if any)
  </read_first>
  <action>
    Run Slither against the 5 contracts changed by this phase and triage the findings into the SUMMARY. CONTEXT security_and_upgrade #8 mandates this; CONCERNS.md notes that `contracts/gnus-ai/` is currently EXCLUDED from the project's slither config — this task surfaces that gap explicitly for Phase 7.

    1. **Locate the existing slither configuration.** Check the repo root for `slither.config.json`, `slither.config.yaml`, or slither invocation in `package.json` scripts. Also check for a hardhat plugin (`hardhat-slither`). Read whatever you find.

    2. **Confirm the exclusion.** Inspect the config to verify that `contracts/gnus-ai/` is excluded (CONCERNS.md says it is). Record the exact filter/exclusion pattern in the SUMMARY under a "Slither Inclusion Gap" heading.

    3. **Run slither targeted at the 5 changed contracts.** Use a targeted invocation that does NOT require modifying the project's slither config (that modification is Phase 7's job — D-01 of Phase 7 owns the audit gate). From the `gnus-ai/` directory:
       ```
       slither --config-file <existing-config-if-any> \
         --filter-paths "node_modules|lib|test|script" \
         contracts/gnus-ai/GNUSBridge.sol \
         contracts/gnus-ai/GNUSNFTFactory.sol \
         contracts/gnus-ai/GNUSNFTFactoryStorage.sol \
         contracts/gnus-ai/GNUSTreasury.sol \
         contracts/gnus-ai/GNUSTreasuryStorage.sol \
         --json .planning/phases/09-per-child-gnus-treasury-reserve/09-05-SLITHER.json
       ```
       Adjust the exact flags to whatever the local slither version supports (`slither --version` first). If the existing config's `filter_paths` already excludes `contracts/gnus-ai/`, override it on the CLI for this run only — do NOT edit the committed config.

    4. **Triage findings.** For each finding Slither reports on the 5 contracts:
       - Classify: `false-positive`, `informational`, `low`, `medium`, `high`.
       - For `medium` and `high`: either fix in this plan (small, surgical) OR record as a follow-up task with a clear owner (Phase 7 audit, or a specific later phase). Do NOT silently ignore.
       - For `false-positive` / `informational`: record with a one-line justification.

    5. **Write the triage into the SUMMARY.** The SUMMARY MUST contain a `## Slither Triage` section with:
       - Slither version used.
       - The exact invocation command.
       - A table of findings: contract, detector, severity, disposition, one-line rationale.
       - A "Slither Inclusion Gap" subsection explicitly noting that `contracts/gnus-ai/` is excluded from the project's standing slither config and that this run was a one-off targeted invocation. Flag for Phase 7 to lift the exclusion as part of the audit gate.

    6. **Do NOT** modify `slither.config.json` (or equivalent) in this plan. Do NOT install slither if it isn't already available — if `slither --version` fails, STOP and surface to the user (environment setup is outside this plan's scope; the user may need to `pip install slither-analyzer` or use an existing venv).

    7. If slither fails to run to completion (e.g., compilation issues with the targeted file list), record the failure mode in the SUMMARY and surface to the user — do NOT skip silently.
  </action>
  <verify>
    <automated>slither --version && test -f .planning/phases/09-per-child-gnus-treasury-reserve/09-05-SLITHER.json && grep -c "Slither Triage" .planning/phases/09-per-child-gnus-treasury-reserve/09-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `slither --version` exits 0 (slither is installed in the environment)
    - Slither has been invoked against all 5 changed contracts (verify by inspecting the SUMMARY's recorded invocation command)
    - `.planning/phases/09-per-child-gnus-treasury-reserve/09-05-SLITHER.json` exists and is non-empty
    - The SUMMARY contains a `## Slither Triage` section with: version, invocation, findings table (contract × detector × severity × disposition), and a "Slither Inclusion Gap" subsection flagging the `contracts/gnus-ai/` exclusion for Phase 7
    - Every `medium` or `high` finding has an explicit disposition (fixed-in-plan with commit reference, or follow-up task with owner)
    - No change to the project's committed slither config (verify via `git diff --name-only` not including any slither config file)
  </acceptance_criteria>
  <done>Slither run on the 5 changed contracts; findings triaged in SUMMARY; inclusion gap flagged for Phase 7</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fuzz harness → diamond | Handler drives arbitrary sequences; invariants assert model properties hold |
| Migrated tests → diamond | Pre-existing assertions updated to match new semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-25 | Tampering | Test migration missing a call site (Pitfall 8) | mitigate | `grep -c "\.withdraw("` returns 0 for each of the 5 files; GNUSContractAssets.test.ts explicitly excluded (different contract) |
| T-09-26 | Tampering | Mint-semantics drift in 2nd-gen assertions | mitigate | 2nd-gen tests flip from "no burn" to "depth-gate revert + convert issuance" per REQUIREMENTS.md:149 / research §F |
| T-09-27 | Tampering | Invariant weakened to pass | mitigate | If an invariant fails, fix the contract or handler — never weaken the invariant (project rule: fix root cause) |
| T-09-28 | Denial of Service | Fuzz handler bounded to unrealistic ranges | mitigate | Bounded amounts use the existing handler's magnitude convention; id seeds draw from actually-created ids only |
| T-09-29 | Tampering | Facade regression undetected | mitigate | Explicit acceptance criterion: GNUSERC20.test.ts + Erc20Batch.test.ts + Phase5-circuit-breaker.test.ts must pass unchanged |
| T-09-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- All 5 .withdraw-migrated test files pass in isolation (one commit per file)
- NFTFactory + GNUSNFTFactoryEnhanced test files pass with flipped semantics
- ERC-20 facade + Phase5 circuit-breaker regression green
- ConservationInvariant.t.sol exists with I1/I2/I5 invariants
- Full forge tree passes
- smart-trigger.ts:389 dispositioned
- Slither triage present in SUMMARY; SLITHER.json artifact exists; contracts/gnus-ai/ inclusion gap flagged for Phase 7
</verification>

<success_criteria>
- `npx hardhat test` (full TS suite) green
- `forge test` (full foundry tree) green
- Zero remaining `.withdraw(` call sites outside of comments
- Zero remaining `units * rate` burn assertions in the two migrated factory test files
- I1/I2/I5 invariants actively fuzzed
- Slither findings on the 5 changed contracts triaged; security_and_upgrade #8 satisfied
</success_criteria>

<output>
Create `.planning/phases/09-per-child-gnus-treasury-reserve/09-05-SUMMARY.md` when done.
</output>
