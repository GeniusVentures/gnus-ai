---
phase: 09-per-child-gnus-treasury-reserve
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - contracts/gnus-ai/GNUSNFTFactory.sol
  - diamonds/GeniusDiamond/geniusdiamond.config.json
  - test/unit/GNUSTreasury.test.ts
autonomous: true
requirements: [TREASURY-01, TREASURY-02, TREASURY-03, TREASURY-04]
must_haves:
  truths:
    - "beforeMint deletes the amount * nft.exchangeRate multiplication and burns exactly `amount` id-0 minions"
    - "beforeMint reverts for any id where (id >> 128) != GNUS_TOKEN_ID with a depth-gate message"
    - "mint/mintBatch caller semantics flip: callers pass minions, not child units"
    - "Diamond config registers GNUSTreasury facet at priority 117 with deployInit/upgradeInit GNUSTreasury_Initialize300(uint256)"
    - "Diamond config bumps GNUSBridge to 3.0 (fromVersions [0.0, 2.4, 2.5]) and GNUSNFTFactory to 3.0 (fromVersions [0.0, 2.0, 2.3])"
    - "protocolVersion goes from 2.5 to 3.0"
    - "GNUSTreasury.test.ts has passing assertions for convert legs, reverts, display views, provenance, global cap, MINTER restriction, and counter-untouched properties"
    - "Loupe test confirms withdraw(uint256,uint256) selector is absent and stale calldata reverts"
    - "Legacy NFT records created BEFORE the struct-append upgrade decode correctly post-upgrade: parentId == 0, nonConvertible == false, all pre-existing fields byte-identical (security_and_upgrade #1)"
    - "Upgrade-path Initialize300 seed scenario is tested against a simulated pre-existing supply (research Pitfall 4): seeding with the chain's current global figure yields correct totalSupplyOfAll, seeding with 0 against non-zero on-chain supply is detected as a misconfiguration"
  artifacts:
    - path: "contracts/gnus-ai/GNUSNFTFactory.sol"
      provides: "beforeMint rewrite (minion-for-minion + depth gate)"
      contains: "Direct children only"
    - path: "diamonds/GeniusDiamond/geniusdiamond.config.json"
      provides: "GNUSTreasury registration + version bumps + protocolVersion 3.0"
      contains: "GNUSTreasury"
    - path: "test/unit/GNUSTreasury.test.ts"
      provides: "Full passing test suite for the conversion-native model"
      min_lines: 200
  key_links:
    - from: "contracts/gnus-ai/GNUSNFTFactory.sol"
      to: "_burn(sender, GNUS_TOKEN_ID, amount)"
      via: "beforeMint 1:1 burn of caller's id-0 minions"
      pattern: "_burn\\(sender,\\s*GNUS_TOKEN_ID,\\s*amount\\)"
    - from: "diamonds/GeniusDiamond/geniusdiamond.config.json"
      to: "contracts/gnus-ai/GNUSTreasury.sol"
      via: "facet registration with deployInit/upgradeInit"
      pattern: "GNUSTreasury_Initialize300"
---

<objective>
Land the depth-gate + 1:1 minion issuance rewrite in `beforeMint`, register the new facet in the diamond config (versions 3.0), and write the full passing unit test suite.

Purpose: This is the plan that makes the new model real at the source level (D6 depth gate, D1 issuance semantics) and at the deployment level (diamond cut for 3.0). The unit tests written here pin the charge matrix (I6), the revert matrix, the display views (D2), provenance semantics (D8, I3, I5), and the conservation hole closure (D10).

Output:
- MOD `contracts/gnus-ai/GNUSNFTFactory.sol` — `beforeMint` rewritten
- MOD `diamonds/GeniusDiamond/geniusdiamond.config.json` — GNUSTreasury registered, versions bumped
- MOD `test/unit/GNUSTreasury.test.ts` — all 13 suites from Plan 09-01 stub now have real, passing assertions

**Why this is Wave 3:** The depth-gate revert message references `convert()`, which only exists after Plan 09-02. The diamond cut requires the new facet (09-02) AND the edited GNUSBridge (09-03). Tests assert the new behavior end-to-end and therefore need all three prior plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-VALIDATION.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-01-SUMMARY.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-02-SUMMARY.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-03-SUMMARY.md

<interfaces>
<!-- Current GNUSNFTFactory.sol beforeMint (lines 83-94) — to be rewritten: -->
```solidity
function beforeMint(address to, uint256 id, NFT storage nft, uint256 amount) internal {
    address sender = _msgSender();
    require(id != GNUS_TOKEN_ID, "Shouldn't mint GNUS tokens tokens, only deposit and withdraw");
    require(to != address(0), "ERC1155: mint to the zero address");
    require(nft.nftCreated, "Cannot mint NFT that doesn't exist");
    require((sender == nft.creator) || hasRole(DEFAULT_ADMIN_ROLE, sender), "Creator or Admin can only mint NFT");
    if ((id >> 128) == GNUS_TOKEN_ID) {
        uint256 convAmount = amount * nft.exchangeRate;   // ← DIES (D1)
        require(balanceOf(sender, GNUS_TOKEN_ID) >= convAmount, "Not enough GNUS_TOKEN to burn");
        _burn(sender, GNUS_TOKEN_ID, convAmount);
    }
}
```

<!-- From Plan 09-02 (already landed): -->
GNUSTreasury.convert(fromId, toId, minionAmount, to) — the depth-≥2 issuance path

<!-- Diamond config current state (lines 90-98 for GNUSBridge, 24-39 for GNUSNFTFactory, line 2 for protocolVersion): -->
- `protocolVersion: 2.5`
- GNUSBridge: versions `0.0` and `2.5` (fromVersions [0.0, 2.4])
- GNUSNFTFactory: versions `0.0` and `2.3` (fromVersions [0.0, 2.0])
- No GNUSTreasury entry yet — Plan 09-01 created the storage lib, Plan 09-02 created the facet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: beforeMint rewrite — 1:1 minion burn + depth gate</name>
  <files>contracts/gnus-ai/GNUSNFTFactory.sol</files>
  <behavior>
    - mint(to, directChildId, X, data) by creator/admin: burns exactly X of sender's id-0 balance; child supply increases by exactly X; tree-wide supply unchanged
    - mint(to, grandchildId, X, data) (depth ≥2): reverts with "Direct children only; use convert() for descendants"
    - mint(to, id, X, data) where balanceOf(sender, 0) < X: reverts with "Not enough GNUS_TOKEN to convert"
    - mintBatch: same rules applied per id (loop already calls beforeMint per id — no change to mintBatch body)
    - The `exchangeRate` field is NOT read inside beforeMint
  </behavior>
  <read_first>
    - contracts/gnus-ai/GNUSNFTFactory.sol (current beforeMint at lines 83-94 — see interfaces block)
    - contracts/gnus-ai/GNUSConstants.sol (GNUS_TOKEN_ID)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §5 (copy-ready beforeMint rewrite)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §F (depth-gate rationale — Option 3, user-locked per D6)
  </read_first>
  <action>
    Replace `beforeMint` in `contracts/gnus-ai/GNUSNFTFactory.sol` (currently lines 83-94) with the D1 + D6 rewrite. The new body:

    1. Keep the existing 4 requires unchanged (id != GNUS_TOKEN_ID, to != address(0), nft.nftCreated, creator-or-admin).
    2. ADD a new require as the 5th check (D6 depth gate): `require((id >> 128) == GNUS_TOKEN_ID, "Direct children only; use convert() for descendants");`
    3. DELETE the `if ((id >> 128) == GNUS_TOKEN_ID) { ... }` conditional wrapper entirely — the depth gate makes it always-true.
    4. DELETE the line `uint256 convAmount = amount * nft.exchangeRate;` (D1 — rate math dies in state transitions).
    5. Change the balance check to use `amount` directly: `require(balanceOf(sender, GNUS_TOKEN_ID) >= amount, "Not enough GNUS_TOKEN to convert");` (note the message change from "to burn" to "to convert" per the user's terminology lock in CONTEXT specifics).
    6. Change the burn to use `amount` directly: `_burn(sender, GNUS_TOKEN_ID, amount);` — 1:1 minion move.
    7. Update the docstring: amount is now minions, not child units; the caller pays exactly `amount` id-0 minions and the child supply increases by exactly `amount`.
    8. Do NOT touch `mint`, `mintBatch`, `createNFTs`, or any other function — this task is scoped to `beforeMint` only. (createNFTs was already updated in Plan 09-01.)
    9. Do NOT add any reference to `exchangeRate` inside `beforeMint` — the field is now display-only (D2). Verify with grep after edit.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -10 && grep -c "Direct children only" contracts/gnus-ai/GNUSNFTFactory.sol && grep -c "convAmount" contracts/gnus-ai/GNUSNFTFactory.sol; grep -A 20 "function beforeMint" contracts/gnus-ai/GNUSNFTFactory.sol | grep -c "exchangeRate"</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0
    - The `beforeMint` function body (extract with `grep -A 20 "function beforeMint"`) contains ZERO references to `exchangeRate` and ZERO references to `convAmount`
    - The function contains the string `"Direct children only; use convert() for descendants"`
    - The function contains `_burn(sender, GNUS_TOKEN_ID, amount);` (amount, not convAmount)
    - The function contains `require(balanceOf(sender, GNUS_TOKEN_ID) >= amount, ...)` (amount, not convAmount)
    - The function contains `require((id >> 128) == GNUS_TOKEN_ID, ...)` as an unconditional require (not wrapped in an `if`)
    - No `if ((id >> 128) == GNUS_TOKEN_ID)` conditional remains in the function
    - GNUSNFTFactory deployed bytecode remains under 24,576 bytes (research §H: net-negative byte impact from removing the multiplication + conditional)
  </acceptance_criteria>
  <done>beforeMint rewritten: 1:1 minion burn, depth gate, no rate math; compile green</done>
</task>

<task type="auto">
  <name>Task 2: Diamond config — register GNUSTreasury, bump versions to 3.0</name>
  <files>diamonds/GeniusDiamond/geniusdiamond.config.json</files>
  <read_first>
    - diamonds/GeniusDiamond/geniusdiamond.config.json (current full file — note priority ordering and fromVersions pattern)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §7 (copy-ready JSON edits)
  </read_first>
  <action>
    Edit `diamonds/GeniusDiamond/geniusdiamond.config.json`:

    1. Change top-level `"protocolVersion": 2.5` to `"protocolVersion": 3.0` (line 2).

    2. Add a NEW facet entry for `GNUSTreasury`, positioned in the JSON between `GNUSBridge` (priority 115) and `GNUSWithdrawLimiter` (priority 120) to match its priority value:
       ```json
       "GNUSTreasury": {
         "priority": 117,
         "versions": {
           "3.0": {
             "deployInit": "GNUSTreasury_Initialize300(uint256)",
             "upgradeInit": "GNUSTreasury_Initialize300(uint256)"
           }
         }
       },
       ```
       - The `deployInit`/`upgradeInit` signature must include the parameter list `(uint256)` so the diamonds tooling encodes the correct calldata. The actual seed VALUE is a per-chain deployment decision (deploy-script wiring is Claude's discretion per CONTEXT; the runbook entry is Task 3 suite 15 sub-case C).

    3. Bump `GNUSBridge` versions: add `"3.0": { "fromVersions": [0.0, 2.4, 2.5] }` alongside the existing `0.0` and `2.5` entries. Do NOT remove existing version entries.

    4. Bump `GNUSNFTFactory` versions: add `"3.0": { "fromVersions": [0.0, 2.0, 2.3] }` alongside the existing `0.0` and `2.3` entries. Do NOT remove existing entries.

    5. Do NOT touch `DiamondInitFacet`, `GNUSControl`, `ERC1155ProxyOperator`, `ERC20TransferBatch`, `GNUSNFTCollectionName`, `GNUSContractAssets`, `GNUSWithdrawLimiter`, or the `protocolInitFacet` / `protocolExcludeFuncSelectors` fields.

    6. Verify the file remains valid JSON: `node -e "JSON.parse(require('fs').readFileSync('diamonds/GeniusDiamond/geniusdiamond.config.json', 'utf8'))"` (run from the gnus-ai directory).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && node -e "const c = JSON.parse(require('fs').readFileSync('diamonds/GeniusDiamond/geniusdiamond.config.json', 'utf8')); if (c.protocolVersion !== 3.0) throw new Error('protocolVersion wrong'); if (!c.facets.GNUSTreasury) throw new Error('GNUSTreasury missing'); if (c.facets.GNUSTreasury.priority !== 117) throw new Error('priority wrong'); if (!c.facets.GNUSBridge.versions['3.0']) throw new Error('GNUSBridge 3.0 missing'); if (!c.facets.GNUSNFTFactory.versions['3.0']) throw new Error('GNUSNFTFactory 3.0 missing'); console.log('config OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The JSON parse succeeds
    - `protocolVersion` equals `3.0` (number, not string)
    - `facets.GNUSTreasury` exists with `priority: 117` and `versions["3.0"].deployInit == "GNUSTreasury_Initialize300(uint256)"`
    - `facets.GNUSBridge.versions["3.0"].fromVersions` equals `[0.0, 2.4, 2.5]` (note: JSON numbers, may need to match however existing entries format them — verify by inspection of the `2.5` entry's format and mirror it)
    - `facets.GNUSNFTFactory.versions["3.0"].fromVersions` equals `[0.0, 2.0, 2.3]` (same format-mirroring caveat)
    - All pre-existing facet entries unchanged
    - `npx hardhat compile` still exits 0 (config change alone doesn't break compile)
  </acceptance_criteria>
  <done>Diamond config registers GNUSTreasury at priority 117, versions bumped, protocolVersion 3.0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GNUSTreasury.test.ts — full passing suite (convert legs, reverts, display, provenance, cap, loupe)</name>
  <files>test/unit/GNUSTreasury.test.ts</files>
  <behavior>
    All 13 suites from the Plan 09-01 stub now have real, passing assertions, PLUS two revision-added suites: "legacy decode" and "upgrade init seed". See 09-VALIDATION.md "Per-Task Verification Map" for the canonical list of the 13; the two new suites are defined inline in this task.
  </behavior>
  <read_first>
    - test/unit/GNUSTreasury.test.ts (current stub from Plan 09-01)
    - test/unit/GNUSBridge.test.ts (assertion idiom — `.to.be.revertedWith`, balance-change assertions, event assertions)
    - test/unit/GNUSWithdrawLimiterStorage.test.ts (limiter assertion idiom — how tests observe checkAndRecordWithdraw effects)
    - contracts/gnus-ai/GNUSTreasury.sol (the facet under test — from Plan 09-02)
    - contracts/gnus-ai/GNUSBridge.sol (post-Plan-09-03 state)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-VALIDATION.md (Per-Task Verification Map — the authoritative test list)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md (testing section — Required Test Categories)
  </read_first>
  <action>
    Replace the `it.skip` stubs in `test/unit/GNUSTreasury.test.ts` with real passing tests. Implement all 13 stub suites PLUS two additional suites added by this revision: `"legacy decode"` (security_and_upgrade #1) and `"upgrade init seed"` (research Pitfall 4) — 15 suites total. Key requirements per suite (grep targets from 09-VALIDATION.md):

    1. **"convert to GNUS"** (TREASURY-03, I6):
       - Setup: super-admin initializes provenance with seed 0; MINTER_ROLE mints 1000 GNUS to alice; alice creates child with rate 2e18; alice mints 100 minions of child (100 id-0 burned, 100 child minted).
       - Assert: alice.convert(childId, 0, 30, alice) → alice's child balance 70, id-0 balance up by exactly 30 (mod limiter fee — limiter is a counter, not a tax), tree supply unchanged.
       - Assert: limiter bin for alice records exactly 30 (read GNUSWithdrawLimiterStorage bin state via the existing accessor used in GNUSWithdrawLimiterStorage.test.ts).
       - Assert: super-admin convert → SuperAdminBypass event with context "GNUSTreasury.convert", limiter unchanged.
       - Assert: `Converted` event emitted with (childId, 0, 30, alice).

    2. **"GNUS to child"** (TREASURY-03):
       - Assert: convert(0, childId, X, to) charges the limiter exactly once via the hook (no explicit charge in convert). Verify by checking limiter bin delta == X.
       - Assert: per-id maxSupply hook fires on the mint leg (research §C).

    3. **"child to child"** (TREASURY-03):
       - Assert: convert(childA, childB, X, to) → exact reallocation, limiter bin unchanged (zero charge).

    4. **"deep"** (TREASURY-03):
       - Setup: create depth-1 child A; create depth-2 child B under A (via createNFTs with parentID = A's id — token creation is NOT depth-gated per research §F caveat); alice mints A; alice convert(A, B, X, alice); then convert(B, 0, X, alice) in one hop.
       - Assert: B→GNUS convert succeeds, no tree-walking, rate never applied, exact minion amounts.

    5. **"reverts"**:
       - same-id: `convert(id, id, X, to)` reverts with "Cannot convert to same id"
       - zero amount: `convert(a, b, 0, to)` reverts with "Amount must be greater than zero"
       - uncreated toId: reverts with "Token not created."
       - insufficient balance: reverts (ERC1155 _burn balance check)
       - nonConvertible source (D5 enforcement #4 — REQUIRED, no skip allowed): create a token via createNFTs, then flip its `nonConvertible` field to `true` via `ethers.provider.send("hardhat_setStorageAt", ...)` writing the struct field directly (compute the slot: NFTs mapping slot = keccak256(abi.encode(tokenId, GNUSNFTFactoryStorage_Layout_NFTs_slot)); the NFT struct's `nonConvertible` field is at the appended offset after `parentId` — consult `npx hardhat storage-layout` output or the artifacts' storageLayout for the exact byte offset). Do NOT add a setter to production code for tests. Do NOT skip this sub-case — D5 enforcement #4 must be proven.
       - nonConvertible destination: same hardhat_setStorageAt approach; assert convert reverts with "Token is non-convertible" when toId is nonConvertible (and toId != GNUS_TOKEN_ID)

    6. **"selector removed"** (TREASURY-03, T-09-18):
       - Use the diamond loupe: `await diamond.facetFunctionSelectors(bridgeAddress)` (or equivalent idiom from existing tests) — assert the selector for `withdraw(uint256,uint256)` (compute via `ethers.utils.id("withdraw(uint256,uint256)").slice(0, 10)`) is NOT present in any facet.
       - Send stale calldata directly to the diamond via `signer.sendTransaction({ to: diamond.address, data: withdrawCalldata })` — assert it reverts.

    7. **"provenance"** (TREASURY-04):
       - Pre-init: `totalSupplyOfAll()` reverts with "Global supply not initialized"
       - Init: super-admin calls `GNUSTreasury_Initialize300(0)` → succeeds, emits GlobalSupplyInitialized(0, superAdmin)
       - Re-init: reverts with "Already initialized"
       - Non-super-admin init: reverts with "Only SuperAdmin allowed"
       - syncGlobalSupply by non-admin: reverts (AccessControl)
       - syncGlobalSupply by admin: emits GlobalSupplySynced(old, new, admin), updates value

    8. **"cross chain"** (TREASURY-04, I3):
       - Use the two-diamond fixture from the Plan 09-01 stub (diamondName 'GeniusDiamond' and 'GeniusDiamondChainB').
       - Initialize both with seed 0.
       - Mint 1000 on chain A → A.totalSupplyOfAll() == 1000; B still 0.
       - Simulate bridge: A.bridgeOut(200, 0, chainB_chainId, ...) → A's counter stays 1000 (B1: bridgeOut doesn't touch counter).
       - On B: MINTER_ROLE mint(bob, 0, 200) → B.totalSupplyOfAll() == 200.
       - Admin syncs B: `syncGlobalSupply(1000)` → B reports 1000.
       - Assert: A and B both report 1000 after sync (eventual consistency).

    9. **"global cap"** (TREASURY-04, I5):
       - Initialize with seed 0. Attempt to mint GNUS_MAX_SUPPLY + 1 → reverts with "Global max supply exceeded".
       - Mint exactly GNUS_MAX_SUPPLY → succeeds.
       - Convert child→GNUS after cap reached → succeeds (convert is NOT cap-checked, research §C item 4).
       - Bridge-fee drift test (Pitfall 3): set bridgeFee = 200 (20%) via GNUSControl; MINTER mint(1000) → globalSupply increments by 800 (post-fee), NOT 1000.

    10. **"display"** (TREASURY-05):
        - Child with rate 2e18; alice holds 100 minions → unitsOf(childId, alice) == 50 (floor: 100 * 1e18 / 2e18)
        - Dust: 101 minions at rate 2e18 → unitsOf == 50 (floor)
        - unitsOf(0, alice) reverts with "GNUS has no child units"
        - totalUnitsOf(0) reverts
        - Manually craft a token with rate == 0 (if createNFTs rejects it, use a storage-manipulation helper or document the impossibility; the require(rate > 0) check in the view should never fire in practice given the createNFTs guard)

    11. **"counter untouched"** (Pitfall 2):
        - Initialize with seed 0; mint 1000 id-0; assert globalSupply == 1000.
        - Alice mints 100 child (factory mint — burns 100 id-0, mints 100 child). Assert globalSupply STILL == 1000.
        - Alice convert(child, 0, 30, alice). Assert globalSupply STILL == 1000.
        - Alice convert(0, child, 20, alice). Assert globalSupply STILL == 1000.

    12. **"minter restriction"** (D10):
        - MINTER_ROLE mint(user, 0, amount) → succeeds
        - MINTER_ROLE mint(user, childId, amount) → reverts with "MINTER_ROLE mints GNUS only"

    13. **"minion cap"** (research §C):
        - Create child with maxSupply = 1000 minions. Mint exactly 1000 → succeeds. Mint 1 more → reverts "Max Supply for NFT would be exceeded".
        - Convert exactly-to-cap into the child → succeeds. Convert cap+1 → reverts.

    14. **"legacy decode"** (security_and_upgrade #1 — REQUIRED, closes the CONTEXT-mandated upgrade test):
        - Purpose: prove that NFT records written BEFORE the struct-append (Phase 9's `parentId` + `nonConvertible` fields) decode with zero defaults and unchanged behavior post-upgrade.
        - Approach: simulate the pre-upgrade state. The cleanest path is to deploy a fresh diamond, create 2–3 child tokens via `createNFTs` (these writes go through the new struct shape — that's fine; the assertion target is the READ path against records whose appended fields would be zero anyway), then directly overwrite the appended storage slots to zero via `hardhat_setStorageAt` to simulate "this record predates the upgrade." An equally valid approach: use a Foundry or hardhat fixture that deploys a pre-upgrade bytecode of GNUSNFTFactoryStorage (from git history if practical) — but `hardhat_setStorageAt` on the appended slots is the simpler deterministic route.
        - Assertions for each simulated-legacy token:
          - `parentId` reads as `0` (= GNUS_TOKEN_ID — correct for existing direct children, D7).
          - `nonConvertible` reads as `false` (= convertible, the intended opt-out default, D5).
          - All PRE-EXISTING fields (name, symbol, uri, exchangeRate, maxSupply, creator, childCurIndex, nftCreated) read back exactly as written.
          - The token can be converted as both source and destination (proves the zero-default decode doesn't break the convert path).
        - Reference: 09-CONTEXT.md security_and_upgrade #1; 09-RESEARCH.md Pitfall 7.

    15. **"upgrade init seed"** (research Pitfall 4 — REQUIRED):
        - Purpose: prove the deployer-facing contract for `GNUSTreasury_Initialize300(seed)` when the chain already has a non-zero supply. All other tests seed fresh diamonds at 0; this suite documents and tests the upgrade-path scenario.
        - Sub-case A (correct seed): deploy diamond, MINTER_ROLE mint 5000 GNUS to alice (post-fee path may apply — set bridgeFee = 0 first to make the math deterministic). Now `totalSupply(0) == 5000`. Call `GNUSTreasury_Initialize300(5000)` as super-admin. Assert `totalSupplyOfAll() == 5000`. Subsequent root mint of 1000 → `totalSupplyOfAll() == 6000` (proves the counter tracks from the seeded base).
        - Sub-case B (misconfiguration detection): same setup, but seed with `0`. Assert `totalSupplyOfAll() == 0` while `totalSupply(0) == 5000` — the DIVERGENCE between the two views is the misconfiguration signal. Document in a test comment: "Runbook MUST read `totalSupply(0)` (plus any outstanding bridge-out amounts on other chains) at upgrade time and pass that figure as the seed. Seeding 0 against non-zero on-chain supply silently breaks the global cap (D9)."
        - Sub-case C (runbook assertion): emit a console.log from the test with the exact runbook sentence, so the test output itself carries the deployer-facing instruction: `RUNBOOK: upgradeInit seed must equal the chain's current global figure (sum of totalSupply(0) across all chains).`

    Implementation notes:
    - Use the existing fixture pattern from GNUSBridge.test.ts verbatim.
    - Use the overload-resolution idiom `diamond['mint(address,uint256,uint256,bytes)'](...)` for factory mints and `diamond['mint(address,uint256)'](...)` for root mints.
    - For MINTER_ROLE grants in tests, follow the idiom already used in GNUSBridge.test.ts.
    - For the limiter bin reads, find the existing accessor in GNUSWithdrawLimiterStorage.test.ts and reuse it.
    - All tests use the snapshot/revert isolation pattern (already in the fixture from Plan 09-01).
    - Do NOT skip writing any suite, and do NOT skip any sub-case within a suite. The `nonConvertible` sub-cases are mandatory: use `ethers.provider.send("hardhat_setStorageAt", ...)` to write the struct field directly — this is a test-only technique and is the REQUIRED mechanism. If the slot computation is non-trivial, use the storage layout emitted by `npx hardhat compile` (artifacts/.../*.json `storageLayout` field) or add a one-line `hardhat-storage-layout` plugin invocation — do not fall back to skipping.
    - The full file should be 400-700 lines when complete.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat test test/unit/GNUSTreasury.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test test/unit/GNUSTreasury.test.ts` exits 0
    - All 13 stub suites from Plan 09-01 are present with real assertions, PLUS the two revision-added suites `"legacy decode"` and `"upgrade init seed"` (15 suites total). No `it.skip` remains for any of the 15.
    - Test count: at least 30 passing assertions across the 15 suites
    - The "selector removed" suite asserts withdraw selector absence via the loupe AND stale-calldata revert
    - The "global cap" suite includes a non-zero-bridgeFee case asserting globalSupply increments by the post-fee amount
    - The "counter untouched" suite asserts globalSupply is unchanged across factory mint and both convert directions
    - No test uses `setTimeout`, `setInterval`, or arbitrary `await new Promise(r => setTimeout(r, ...))` — snapshot/revert isolation only (project rule: no sleep-style hacks in tests)
  </acceptance_criteria>
  <done>All 15 suites pass (30+ assertions); conversion-native model is behaviorally pinned at the unit level</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → factory mint | Caller-controlled `amount` (now minions) and `id` (depth-gated) |
| Deployer → diamond cut | New facet registration + selector removal in one cut |
| Test harness → diamond | Test fixture manipulates roles and storage for assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-20 | Tampering | Depth-gate bypass via crafted id | mitigate | `(id >> 128) == GNUS_TOKEN_ID` is exact for direct children by construction (createNFTs builds ids as `(parentID << 128) \| childCurIndex`); collision guard (Plan 09-01) prevents id reuse |
| T-09-21 | Tampering | Rate-math survivor in beforeMint (Pitfall 1) | mitigate | `convAmount = amount * nft.exchangeRate` deleted; final grep verifies no `exchangeRate` reference in beforeMint body |
| T-09-22 | Elevation of Privilege | Unbacked child mint | mitigate | 1:1 burn of sender's id-0 balance is mandatory; `_burn` reverts on insufficient balance; no mint-from-nothing path |
| T-09-23 | Tampering | Diamond cut misconfiguration | mitigate | Config change verified by JSON parse + assertion script; deploy-time loupe test (Task 3 "selector removed" suite) confirms withdraw absence + new selectors resolve |
| T-09-24 | Repudiation | Provenance seed misconfiguration at deploy | accept | Deploy-time seed value is a runbook concern (Plan 09-05 documents); the revert-when-uninitialized guard (D8) makes "forgot to seed" fail loudly |
| T-09-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- `npx hardhat compile` green
- `npx hardhat test test/unit/GNUSTreasury.test.ts` green (all 15 suites — 13 stub + legacy decode + upgrade init seed)
- `node -e "JSON.parse(...)"` on the config succeeds with all assertions
- Loupe test confirms withdraw selector absent and GNUSTreasury selectors present
- No exchangeRate references remain in beforeMint
- GNUSNFTFactory + GNUSTreasury + GNUSBridge all under EIP-170
</verification>

<success_criteria>
- beforeMint: 1:1 minion burn, depth gate, no rate math
- Diamond config registers GNUSTreasury at priority 117, versions 3.0 across the three modified facets, protocolVersion 3.0
- GNUSTreasury.test.ts passes 30+ assertions across all 15 suites (13 stub + legacy decode + upgrade init seed)
- Loupe verifies withdraw selector absent + new selectors present
- I6 charge matrix pinned: explicit-once for GNUS-terminal convert, hook-only for GNUS→child, zero for child→child
</success_criteria>

<output>
Create `.planning/phases/09-per-child-gnus-treasury-reserve/09-04-SUMMARY.md` when done.
</output>
