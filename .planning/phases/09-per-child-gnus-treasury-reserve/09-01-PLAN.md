---
phase: 09-per-child-gnus-treasury-reserve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSTreasuryStorage.sol
  - contracts/gnus-ai/GNUSNFTFactoryStorage.sol
  - contracts/gnus-ai/GNUSNFTFactory.sol
  - test/unit/GNUSTreasury.test.ts
  - test/foundry/handlers/GeniusDiamondHandler.sol
autonomous: true
requirements: [TREASURY-01, TREASURY-02]
must_haves:
  truths:
    - "GNUSTreasuryStorage library exists with globalSupply + provenanceInitialized fields and a keccak256 storage slot"
    - "NFT struct has parentId and nonConvertible appended at the end (existing field order unchanged)"
    - "createNFTs rejects a re-created token id (collision guard) and records parentId + nonConvertible=false"
    - "GNUSTreasury.test.ts exists as a compile-ready stub with the two-diamond fixture scaffolded"
    - "Foundry handler exposes a handler_convert action wired into targetContract"
  artifacts:
    - path: "contracts/gnus-ai/GNUSTreasuryStorage.sol"
      provides: "Treasury Layout (globalSupply, provenanceInitialized)"
      contains: "GNUS_TREASURY_STORAGE_POSITION"
    - path: "contracts/gnus-ai/GNUSNFTFactoryStorage.sol"
      provides: "NFT struct with parentId + nonConvertible appended"
      contains: "uint256 parentId"
    - path: "contracts/gnus-ai/GNUSNFTFactory.sol"
      provides: "createNFTs collision guard + parentId recording"
      contains: "Token ID collision"
    - path: "test/unit/GNUSTreasury.test.ts"
      provides: "Wave-0 test stub scaffold + two-diamond fixture"
      contains: "GNUSTreasury"
    - path: "test/foundry/handlers/GeniusDiamondHandler.sol"
      provides: "handler_convert action + ghost_convertCalls"
      contains: "handler_convert"
  key_links:
    - from: "contracts/gnus-ai/GNUSNFTFactory.sol"
      to: "GNUSNFTFactoryStorage.Layout.NFTs"
      via: "createNFTs writes parentId + nonConvertible fields on the new struct"
      pattern: "parentId:\\s*parentID"
---

<objective>
Land the storage foundation + Wave-0 test scaffolding for the conversion-native model.

Purpose: Everything else in this phase reads or writes the new storage shapes — the `NFT` struct's `parentId`/`nonConvertible` fields (D5, D7) and the new `GNUSTreasuryStorage.Layout` (`globalSupply`, `provenanceInitialized`, D8). Wave 0 also lands the test scaffolds (per 09-VALIDATION.md "Wave 0 Requirements") so that later waves can write assertions against an existing harness instead of creating files mid-flow.

Output:
- NEW `contracts/gnus-ai/GNUSTreasuryStorage.sol` — keccak-slotted Layout library
- MOD `contracts/gnus-ai/GNUSNFTFactoryStorage.sol` — append `parentId`, `nonConvertible` to `NFT`
- MOD `contracts/gnus-ai/GNUSNFTFactory.sol` — `createNFTs` collision guard + new fields in the struct literal
- NEW `test/unit/GNUSTreasury.test.ts` — skeleton (describe + fixtures + empty `it` blocks)
- MOD `test/foundry/handlers/GeniusDiamondHandler.sol` — `handler_convert` action + ghost var
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-VALIDATION.md

<interfaces>
<!-- Existing storage library pattern (copy verbatim, change slot name): -->
From contracts/gnus-ai/GNUSNFTFactoryStorage.sol (current shape, lines 10-42):
```solidity
struct NFT {
    string name;
    string symbol;
    string uri;
    uint256 exchangeRate;
    uint256 maxSupply;
    address creator;
    uint128 childCurIndex;
    bool nftCreated;
}

library GNUSNFTFactoryStorage {
    struct Layout { mapping(uint256 => NFT) NFTs; }
    bytes32 constant GNUS_NFT_FACTORY_STORAGE_POSITION = keccak256("gnus.ai.nft.factory.storage");
    function layout() internal pure returns (Layout storage l) {
        bytes32 slot = GNUS_NFT_FACTORY_STORAGE_POSITION;
        assembly { l.slot := slot }
    }
}
```

From contracts/gnus-ai/GNUSNFTFactory.sol — createNFTs current struct write (lines 168-178):
```solidity
uint256 newTokenID = (parentID << 128) | nft.childCurIndex++;
GNUSNFTFactoryStorage.layout().NFTs[newTokenID] = NFT({
    name: names[i],
    symbol: symbols[i],
    exchangeRate: exchRates[i],
    maxSupply: max_supplies[i],
    uri: newuris[i],
    creator: sender,
    childCurIndex: 0,
    nftCreated: true
});
```

From test/unit/GNUSBridge.test.ts (fixture pattern source — analog for GNUSTreasury.test.ts):
- diamondName: 'GeniusDiamond'
- LocalDiamondDeployer.getInstance(hre, config)
- snapshot/revert per-test (lines 107-115)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GNUSTreasuryStorage library + append parentId/nonConvertible to NFT struct</name>
  <files>contracts/gnus-ai/GNUSTreasuryStorage.sol, contracts/gnus-ai/GNUSNFTFactoryStorage.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSNFTFactoryStorage.sol (current struct + library pattern — copy this verbatim)
    - contracts/gnus-ai/GNUSWithdrawLimiterStorage.sol (reference for library-with-helpers shape)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md (sections 2 and 4 — copy-ready skeletons)
  </read_first>
  <action>
    1. Create NEW file `contracts/gnus-ai/GNUSTreasuryStorage.sol`. Contents (D8):
       - SPDX MIT, pragma `^0.8.19`
       - `library GNUSTreasuryStorage` with `struct Layout { uint256 globalSupply; bool provenanceInitialized; }` — field order per D8 / research §B (globalSupply first, bool second; do NOT reorder)
       - `bytes32 constant GNUS_TREASURY_STORAGE_POSITION = keccak256("gnus.ai.treasury.storage");`
       - `function layout() internal pure returns (Layout storage l)` using inline-assembly slot assignment, matching GNUSNFTFactoryStorage.sol:36-41 verbatim
       - Add a top-of-file NatSpec comment `@custom:security-contact support@gnus.ai`
       - Do NOT add RATE_SCALE here — per research §D, RATE_SCALE lives on the GNUSTreasury facet (Plan 09-02) as `uint256 internal constant RATE_SCALE = 1e18;`
    2. Modify `contracts/gnus-ai/GNUSNFTFactoryStorage.sol`:
       - APPEND two fields at the END of `struct NFT` (after `bool nftCreated;`), preserving existing field order:
         - `uint256 parentId;` — D7; zero-default decodes as `GNUS_TOKEN_ID`, correct for existing direct children
         - `bool nonConvertible;` — D5; zero-default `false` = convertible (opt-out)
       - Update the misleading comment on `exchangeRate` from "Exchange rate for withdrawing to GNUS" to "Display-only fixed-point rate: minions per 1 child unit, 1e18 scale (D2)" — research §E requires this NatSpec fix
       - Update the comment on `maxSupply` to "Maximum supply of NFTs (minion cap per research §C)" — semantics change only
       - Do NOT touch the `Layout` struct, the storage slot constant, or `layout()`
    3. Do NOT reorder existing NFT fields. Phase 13 will append after Phase 9's fields (D7 coordination).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -20 && grep -c "uint256 parentId" contracts/gnus-ai/GNUSNFTFactoryStorage.sol && grep -c "bool nonConvertible" contracts/gnus-ai/GNUSNFTFactoryStorage.sol && grep -c "gnus.ai.treasury.storage" contracts/gnus-ai/GNUSTreasuryStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0 with no errors (warnings acceptable)
    - `contracts/gnus-ai/GNUSTreasuryStorage.sol` exists and contains `GNUS_TREASURY_STORAGE_POSITION = keccak256("gnus.ai.treasury.storage")`
    - `contracts/gnus-ai/GNUSNFTFactoryStorage.sol` contains `uint256 parentId;` AND `bool nonConvertible;` AFTER `bool nftCreated;` in the struct body (order: name, symbol, uri, exchangeRate, maxSupply, creator, childCurIndex, nftCreated, parentId, nonConvertible)
    - The `exchangeRate` field's inline comment no longer says "withdrawing to GNUS" — it says "Display-only" or equivalent
    - No existing field was reordered, renamed, or retyped
  </acceptance_criteria>
  <done>Both storage files compile; new fields exist at the end of NFT; the storage library is importable by downstream facets</done>
</task>

<task type="auto">
  <name>Task 2: createNFTs collision guard + parentId/nonConvertible recording</name>
  <files>contracts/gnus-ai/GNUSNFTFactory.sol</files>
  <read_first>
    - contracts/gnus-ai/GNUSNFTFactory.sol (current createNFTs at lines 152-181)
    - contracts/gnus-ai/GNUSNFTFactoryStorage.sol (the new NFT struct shape from Task 1)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md (section 5 — copy-ready edit)
  </read_first>
  <action>
    Modify `createNFTs` in `contracts/gnus-ai/GNUSNFTFactory.sol` (currently lines 152-181) per D7:
    1. Immediately AFTER the line `uint256 newTokenID = (parentID << 128) | nft.childCurIndex++;` and BEFORE the `NFTs[newTokenID] = NFT({...})` write, insert:
       `require(!GNUSNFTFactoryStorage.layout().NFTs[newTokenID].nftCreated, "Token ID collision");`
    2. Inside the `NFT({...})` struct literal (currently lines 169-178), append two fields after `nftCreated: true`:
       - `parentId: parentID,`
       - `nonConvertible: false` — default convertible per D5; Phase 13 flips it at creation time for burn-only tokens
    3. Do NOT modify the `require(exchRates[i] > 0, ...)` guard at line 166 — display rate must still be > 0 (research §E: zero rate would divide-by-zero in `unitsOf`/`totalUnitsOf`)
    4. Do NOT change the `beforeMint` function in this plan — that rewrite is Plan 09-04's job (it depends on GNUSTreasury landing first so the revert message can point to `convert()`)
    5. Do NOT add the `nonConvertible` immutability enforcement yet — Phase 13 owns that setter; this plan only lands the field + zero default
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -10 && grep -c "Token ID collision" contracts/gnus-ai/GNUSNFTFactory.sol && grep -c "parentId: parentID" contracts/gnus-ai/GNUSNFTFactory.sol && grep -c "nonConvertible: false" contracts/gnus-ai/GNUSNFTFactory.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0
    - `createNFTs` body contains the string `"Token ID collision"` inside a `require`
    - The `NFT({...})` literal in `createNFTs` contains `parentId: parentID` and `nonConvertible: false`
    - The collision-guard `require` is positioned BEFORE the struct-write statement (verifiable by line order in the file)
    - `beforeMint` is byte-identical to its pre-plan state (this plan does not touch it)
  </acceptance_criteria>
  <done>createNFTs writes the new struct fields; collision guard in place; compile green</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test scaffolds — GNUSTreasury.test.ts stub + Foundry handler_convert</name>
  <files>test/unit/GNUSTreasury.test.ts, test/foundry/handlers/GeniusDiamondHandler.sol</files>
  <read_first>
    - test/unit/GNUSBridge.test.ts (fixture + snapshot pattern, lines 21-115 — copy this shape)
    - test/foundry/handlers/GeniusDiamondHandler.sol (existing handler skeleton — note the existing ghost vars and `handler_transfer` shape at line 81)
    - test/foundry/base/GeniusDiamondTestBase.sol (harness base class)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md (sections 8 and 10)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    1. Create NEW file `test/unit/GNUSTreasury.test.ts`:
       - Copy the fixture shape from `test/unit/GNUSBridge.test.ts` lines 21-115 verbatim (imports, `diamondName = 'GeniusDiamond'`, multichain providers, LocalDiamondDeployer setup, snapshot/revert per-test)
       - Rename the top-level describe to `'GNUS Treasury Tests'`
       - Add `describe` placeholders (empty `it.skip` blocks OK — they will be filled by Plans 09-04 and 09-05) for these suites, exactly matching 09-VALIDATION.md's Per-Task Verification Map grep targets:
         - `"convert to GNUS"` (TREASURY-03, I6)
         - `"GNUS to child"` (TREASURY-03, hook-charged leg)
         - `"child to child"` (TREASURY-03, no limiter)
         - `"deep"` (TREASURY-03, grandchild → GNUS single hop)
         - `"reverts"` (same-id, zero amount, uncreated id, insufficient balance, nonConvertible source/destination)
         - `"selector removed"` (withdraw selector absence + stale-calldata revert)
         - `"provenance"` (TREASURY-04: init seed, re-init revert, syncGlobalSupply event + role gate, totalSupplyOfAll reverts pre-seed)
         - `"cross chain"` (TREASURY-04, I3: two-diamond fixture)
         - `"global cap"` (TREASURY-04, I5: root mint/bridge-in cap; convert-to-GNUS never cap-checked)
         - `"display"` (TREASURY-05: unitsOf/totalUnitsOf floor; id-0 revert; rate=0 revert)
         - `"counter untouched"` (Pitfall 2: convert + factory mint do NOT touch globalSupply)
         - `"minter restriction"` (D10: MINTER_ROLE mint id-0 succeeds, non-zero id reverts)
         - `"minion cap"` (per-id maxSupply as minion cap — exactly-cap OK, cap+1 reverts)
       - For the `"cross chain"` suite, scaffold a two-diamond fixture: instantiate `LocalDiamondDeployer.getInstance(hre, config)` twice with two distinct `diamondName` values (`'GeniusDiamond'` and `'GeniusDiamondChainB'`). Leave the bodies as `it.skip('TODO: I3 cross-chain provenance', ...)` — Plan 09-05 fills them in. PATTERNS.md "No Analog Found" table confirms this is a NEW pattern; copy the single-diamond fixture and parameterize.
       - The file MUST compile under hardhat (imports resolve, no unused-variable errors at the top level). `it.skip` bodies may be empty arrows.
    2. Modify `test/foundry/handlers/GeniusDiamondHandler.sol`:
       - Add a ghost var `uint256 public ghost_convertCalls;` near the existing ghost vars (lines 14-21)
       - Add a new external function `handler_convert(uint256 actorSeed, uint256 fromIdSeed, uint256 toIdSeed, uint256 amount) public` modeled on `handler_transfer` at line 81:
         - Bound `amount` to a sane non-zero range (`bound(amount, 1, 1e30)` or the file's existing bound idiom)
         - Use the file's existing actor-selection idiom for `msg.sender`
         - Call the diamond's `convert(fromId, toId, amount, to)` (selector may not exist yet — wrap in a `try/catch` like other handlers do, or use low-level call so this file compiles before Plan 09-02 lands)
         - On success: `ghost_convertCalls++`
       - Do NOT add invariant contracts in this task — Plan 09-05 owns `test/foundry/invariant/ConservationInvariant.t.sol`
       - Do NOT delete or rename any existing handler — additive only
    3. Both files MUST compile: `npx hardhat compile` for the .ts side (typechain will regenerate), and `forge build` for the .sol side.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -10 && cd test/foundry && forge build 2>&1 | tail -10 && grep -c "handler_convert" handlers/GeniusDiamondHandler.sol && grep -c "ghost_convertCalls" handlers/GeniusDiamondHandler.sol && grep -c "GNUSTreasury" ../unit/GNUSTreasury.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0
    - `forge build` (inside `test/foundry/`) exits 0
    - `test/unit/GNUSTreasury.test.ts` exists, contains all 13 describe-suite names listed above as literal strings
    - The `"cross chain"` suite instantiates two distinct diamond fixtures (two different `diamondName` literals)
    - `test/foundry/handlers/GeniusDiamondHandler.sol` contains `handler_convert` and `ghost_convertCalls`
    - No existing handler function is renamed or removed (verify with `git diff --stat` against the file showing only additions)
  </acceptance_criteria>
  <done>Test scaffolds compile; handler exposes convert action; later waves can write assertions directly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External caller → diamond | All entry points are user-controlled calldata |
| Facet → diamond storage | Struct append must not corrupt pre-upgrade NFT records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | NFT struct append | mitigate | Append-only at struct END; existing fields never reordered; legacy-decode upgrade test deferred to Plan 09-04 (Pitfall 7) |
| T-09-02 | Tampering | createNFTs (id collision) | mitigate | New `require(!NFTs[newTokenID].nftCreated, "Token ID collision")` guard (D7) prevents id reuse |
| T-09-03 | Tampering | GNUSTreasuryStorage slot | mitigate | keccak256("gnus.ai.treasury.storage") slot — distinct from all existing slots (verified by inspection of GNUSNFTFactoryStorage, GNUSControlStorage, GNUSWithdrawLimiterStorage) |
| T-09-SC | Tampering | npm installs | accept | No new dependencies introduced in this plan |
</threat_model>

<verification>
- `npx hardhat compile` green
- `forge build` green in test/foundry
- New struct fields present at the end of NFT
- New storage library imports cleanly (no unused-import warnings breaking compile)
- Existing tests (NFTFactory.test.ts, GNUSNFTFactoryEnhanced.test.ts) still compile — semantic changes to assertions land in Plans 09-04/09-05, not here
</verification>

<success_criteria>
- GNUSTreasuryStorage library exists with the exact slot name `gnus.ai.treasury.storage`
- NFT struct has `parentId` and `nonConvertible` appended in that order after `nftCreated`
- createNFTs has the collision guard and writes both new fields
- GNUSTreasury.test.ts compiles with all 13 suite names present
- GeniusDiamondHandler.sol has `handler_convert` and `ghost_convertCalls`
- `git diff` for GNUSNFTFactory.sol shows ONLY the createNFTs edit (no beforeMint change)
</success_criteria>

<output>
Create `.planning/phases/09-per-child-gnus-treasury-reserve/09-01-SUMMARY.md` when done.
</output>
