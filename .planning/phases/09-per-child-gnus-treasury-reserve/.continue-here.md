---
context: phase
phase: 09-per-child-gnus-treasury-reserve
task: post-review fixes + provenance redesign
status: paused
last_updated: 2026-08-11T02:14:41.730Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not fold prior-version init deltas into `diamondInitialize260` — attempted twice this session (cross-call reverts on `onlySuperAdminRole` msg.sender check; inlined version accumulated missing pieces one test-run at a time: CREATOR_ROLE → MINTER_ROLE → GNUS NFT creation). User ordered revert. The 2.6 config + `diamondInitialize250()` as protocol init eliminates the need entirely.
- [ ] CONSTRAINT: Do not proceed on unanswered design questions — user stopped the session twice for implementing on assumptions ("why the fuck did you proceed?"). When the user is mid-design, wait for the full answer set.
- [ ] CONSTRAINT: Cut-time initializers cannot take parameters — `BaseDeploymentStrategy.getInitCalldata()` calls `iface.encodeFunctionData(initFn)` with zero args. Any initializer needing a value (e.g. cross-chain seed) must be a post-deploy call.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| incremental fold-in | Adding init deltas to diamondInitialize260 one test failure at a time without the full inventory | blocking | Reverted. Protocol init = diamondInitialize250() only; version ordering fixed via config rename instead |
| implement-before-answers | Writing code while design questions are still open | blocking | Present complete question list; wait for all answers before editing |
| version-number substitution | Substituting 300 for 260 because "3.0 appeared in config" | blocking | Version numbers come from the user/roadmap, never inferred from working-tree state |
| no-op intermediate writes | `protocolVersion = 230` immediately overwritten by `= 260` in same function | advisory | Dead writes signal the design is wrong — stop and re-examine |

<current_state>
Phase 9 review fixes are DONE (WR-01, WR-02 committed; REVIEW.md marked fixes-applied).

The deploy-fixture "Constructor was already initialized >= 2.30" failure is FIXED — root cause was the erroneous 3.0 config bump (commits 919ee4c/f249ade in GeniusDiamond submodule), which made DiamondInitFacet run `diamondInitialize260()` (setting protocolVersion=260) inside the diamond cut, BEFORE the post-cut `GNUSControl_Initialize230()` guard `require(protocolVersion < 230)`. Fixed by renaming all 3.0/"3" config keys to 2.6, renaming `GNUSTreasury_Initialize300` → `GNUSTreasury_Initialize260` (contract + config signatures + 7 test files), and setting DiamondInitFacet 2.6 init to `diamondInitialize250()` (roles only, no version write).

Also removed `deployInitSignature`/`upgradeInitSignature` from GNUSTreasury 2.6 config (cut-time inits can't take params).

All work is committed and pushed on `wip/phase-9-pause` branches in all three repos.

PAUSED MID-DESIGN: user is redesigning the provenance model to per-chain supply tracking. Design direction dictated, 5 semantics questions still open (below).
</current_state>

<completed_work>

- WR-01 fix — `de7fe76` (contracts/gnus-ai submodule): explicit underflow require in GNUSBridge.burn
- WR-02 fix — `000e611` (gnus-ai): stale withdraw refs → convert in commented test stubs
- REVIEW.md committed `status: fixes-applied` — `fcc6313` (gnus-ai)
- Deploy-fixture root cause diagnosed and fixed (3.0→2.6 rename):
  - contracts submodule `8520166`: GNUSTreasury_Initialize300→260 in GNUSTreasury.sol
  - diamonds submodule `27e90cb`: config keys 3.0/"3"→2.6, protocolVersion 2.6, signatures renamed, treasury auto-seed signature removed
  - gnus-ai parent WIP: 7 test files renamed Initialize300→260
- Verified: GNUSBridge suite 9/9, GNUSTreasury suite 31/31, GNUSWithdrawLimiterStorage suite 9/9
</completed_work>

<remaining_work>

1. **Provenance redesign implementation (BLOCKED — see design questions)**
2. Re-run full suite at final config state (last full run: 398 pass / 25 fail / 2 pending, but that was BEFORE removing the treasury auto-seed signature; the 25 failures were 'Already initialized' collisions from auto-seed + explicit test seeding — may be resolved now)
3. Bump submodule pointers: gnus-ai parent → contracts+GeniusDiamond; root TokenContracts → gnus-ai
4. `/gsd:verify-work 9`
5. Draft PRs per repo + `/gsd-inbox`
</remaining_work>

<decisions_made>

- **2.6 not 3.0** — user: "no config should have 3.0 in the config". 3.0 was an AI error in the original Phase 9 execution.
- **Initialize300 → Initialize260 rename** — version-embedded naming convention (commit 2691cea); the function was born misnamed, nothing to cherry-pick.
- **No cut-time seeding** — tooling encodes init calldata with zero args; cross-chain total is fetched off-chain pre-deploy and seeded via post-deploy call.
- **Per-chain supply model** — user design: `mapping(uint256 chainId => uint256) chainSupply`; own chain keyed by `block.chainid`; `setSisterChainSupply(chainId, newSupply)` computes delta and adjusts `globalSupply`; called at onboarding (as part of the multisig facet-cut batch) and after any out-of-band mint on a sister chain.
- **No EVMRelay** — user explicitly dropped it: "not going to happen often enough to warrant an EVM monitoring system".
- **`GNUSTreasury_Initialize260()` stays** — no-arg, cut-safe; records own `block.chainid` at seed time; sister chainIds default to 0 in the mapping.
</decisions_made>

<blockers>

**5 OPEN DESIGN QUESTIONS (user was answering when pausing — get ALL answers before writing code):**

1. Who flips `provenanceInitialized`? User said move it into `GNUSTreasury_SetSeedSupply`. Consequence: `totalSupplyOfAll()` reverts between deploy and seed. Confirm acceptable, or does Initialize260 flip it?
2. `SetSeedSupply` semantics on chain N: is the seed this chain's OWN starting supply (0 for fresh chain), with `globalSupply` = seed + Σ sisters as registered? Or the global total?
3. `setSisterChainSupply`: single (chainId, newSupply) per call, or batch arrays? (User's latest message said singular "UINT 256 chainIDs, uint256 newSupply" — ambiguous; earlier updateSupply draft was arrays.)
4. Role on new setters: `onlySuperAdminRole` (consistent with initializers) or `DEFAULT_ADMIN_ROLE` (consistent with syncGlobalSupply)?
5. Does `syncGlobalSupply` (D8 honesty valve) survive the per-chain model, or is it superseded/deleted?

Also implied but unconfirmed: do mint/burn paths (`GNUSBridge._mintWithBridgeFee`/`burn`) now maintain `chainSupply[block.chainid]` alongside `globalSupply`? (Assumed yes for local-chain truthfulness.)
</blockers>

## Required Reading (in order)

1. This file + `.planning/HANDOFF.json` — current state
2. `contracts/gnus-ai/GNUSTreasury.sol` (in contracts submodule, branch wip/phase-9-pause) — current provenance surface (lines 139–174)
3. `contracts/gnus-ai/GNUSTreasuryStorage.sol` — storage layout to extend (comment says "Do NOT reorder - Phase 13 will append after these fields" — append chainSupply mapping after)
4. `diamonds/GeniusDiamond/geniusdiamond.config.json` — final 2.6 config state
5. `.planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md` — D8 provenance decision being revised

## Infrastructure State

- Three repos, all on `wip/phase-9-pause` branches, all pushed:
  - gnus-ai: `git@github.com:GeniusVentures/gnus-ai.git`
  - contracts/gnus-ai submodule: `git@github.com:GeniusVentures/gnus-ai-contracts.git` (HEAD 8520166)
  - diamonds/GeniusDiamond submodule: `git@github.com:GeniusVentures/GeniusDiamond.git` (HEAD 27e90cb)
- Root TokenContracts repo: still on `develop`, has unpushed commits (a0c6ed1 etc.) + pending gnus-ai gitlink bump
- All three develop branches have unpushed commits — the wip branches contain that history (cut from develop)
- Hardhat compile + test runs clean from `gnus-ai/` dir (run `cd gnus-ai && npx hardhat test`)

<context>
User was mid-design on per-chain provenance when pausing to switch devices. The design intent is clear (per-chain mapping, delta-based sister updates, multisig-batch-friendly setters) but 5 semantics questions were still open. Do NOT implement until answered. When implementing: one pass — storage layout, GNUSTreasury functions, GNUSBridge mint/burn maintenance, config, then test updates.
</context>

<next_action>
Start with: present the 5 design questions from the blockers section to the user, get all answers, then implement the provenance redesign in one pass.
</next_action>
