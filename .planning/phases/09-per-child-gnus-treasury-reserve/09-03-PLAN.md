---
phase: 09-per-child-gnus-treasury-reserve
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - contracts/gnus-ai/GNUSBridge.sol
autonomous: true
requirements: [TREASURY-01, TREASURY-02]
must_haves:
  truths:
    - "GNUSBridge.withdraw(uint256,uint256) function is deleted"
    - "MINTER_ROLE 3-arg mint reverts for any tokenID != GNUS_TOKEN_ID"
    - "_mintWithBridgeFee increments GNUSTreasuryStorage.globalSupply by the post-fee amount when tokenID == GNUS_TOKEN_ID"
    - "_mintWithBridgeFee enforces globalSupply + mintedAmount <= GNUS_MAX_SUPPLY on id-0 mints"
    - "burn() decrements globalSupply by amount"
    - "bridgeOut's CR-03 limiter charge uses amount directly (no / exchangeRate division)"
    - "bridgeOut does NOT touch globalSupply (B1 model — destination chain's bridge-in mint is the + side)"
    - "No exchangeRate math remains in any state-transition path"
  artifacts:
    - path: "contracts/gnus-ai/GNUSBridge.sol"
      provides: "Edited facet: withdraw removed, MINTER restricted, provenance hooks, simplified bridgeOut"
      contains: "GNUSTreasuryStorage"
  key_links:
    - from: "contracts/gnus-ai/GNUSBridge.sol"
      to: "GNUSTreasuryStorage.Layout.globalSupply"
      via: "_mintWithBridgeFee increments (post-fee) and burn decrements"
      pattern: "globalSupply"
    - from: "contracts/gnus-ai/GNUSBridge.sol"
      to: "GNUSWithdrawLimiterStorage.checkAndRecordWithdraw"
      via: "bridgeOut charge uses amount directly"
      pattern: "checkAndRecordWithdraw\\(sender,\\s*amount\\)"
---

<objective>
Rewire GNUSBridge: delete `withdraw()`, restrict MINTER_ROLE mint to id 0, hook the global-supply counter into `_mintWithBridgeFee` and `burn`, add the global cap check, and simplify `bridgeOut`'s limiter math.

Purpose: This plan implements D4 (withdraw removal), D8 (provenance counter hooks on entry points — never on `_mint`/`_burn` primitives, research §A/Pitfall 2), D9 (global cap at `_mintWithBridgeFee`), D10 (MINTER restriction), and the bridgeOut CR-03 rate-math cleanup (Pitfall 1 — the easy-to-miss survivor).

Output: MOD `contracts/gnus-ai/GNUSBridge.sol`. Net byte impact: negative (withdraw deletion ~-700 B, hooks ~+300 B, per research §H) — comfortably under EIP-170.

**Cross-plan dependency note:** This plan writes to `GNUSTreasuryStorage.layout().globalSupply` (created in Plan 09-01). It does NOT depend on the GNUSTreasury facet itself (Plan 09-02) — only on the storage library. Therefore it can run in Wave 2 in parallel with 09-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-CONTEXT.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md
@.planning/phases/09-per-child-gnus-treasury-reserve/09-01-SUMMARY.md

<interfaces>
<!-- Current GNUSBridge.sol state — verified line numbers (PATTERNS.md "Key line-number anchors"): -->
- `_mintWithBridgeFee(address user, uint256 tokenID, uint256 amount)`: lines 77-89
- `mint(address user, uint256 amount)`: lines 97-99 (2-arg, root mint — UNCHANGED in this plan)
- `mint(address user, uint256 tokenID, uint256 amount)`: lines 108-110 (3-arg, MINTER_ROLE — RESTRICT to id 0)
- `burn(address user, uint256 amount)`: lines 118-121 (MINTER_ROLE — ADD counter decrement)
- `withdraw(uint256 amount, uint256 id)`: lines 162-191 (DELETE entirely, including the WR-07 comment block at lines 175-180 — that comment is transplanted to GNUSTreasury.convert in Plan 09-02)
- `bridgeOut(...)`: lines 203-242 (CR-03 block at lines 221-230 — REPLACE rate math with direct-amount charge)
- `totalSupply() external view override returns (uint256)`: lines 250-252 (UNCHANGED — D8 keeps ERC-20 facade semantics)

<!-- From Plan 09-01: -->
From contracts/gnus-ai/GNUSTreasuryStorage.sol:
```solidity
library GNUSTreasuryStorage {
    struct Layout {
        uint256 globalSupply;
        bool provenanceInitialized;
    }
    function layout() internal pure returns (Layout storage l);
}
```

From contracts/gnus-ai/GNUSConstants.sol:
- `uint256 constant GNUS_TOKEN_ID = 0;`
- `uint256 constant GNUS_MAX_SUPPLY = 50_000_000 * 1e18;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete withdraw() + restrict MINTER_ROLE 3-arg mint to id 0</name>
  <files>contracts/gnus-ai/GNUSBridge.sol</files>
  <behavior>
    - Calling withdraw(amount, id) on the diamond reverts (selector not registered after the diamondCut in Plan 09-04 — but at the source level, the function is gone)
    - mint(user, 0, amount) by MINTER_ROLE succeeds (root/bridge-in path)
    - mint(user, childId, amount) by MINTER_ROLE reverts with "MINTER_ROLE mints GNUS only"
    - 2-arg mint(user, amount) still works (root mint unchanged)
  </behavior>
  <read_first>
    - contracts/gnus-ai/GNUSBridge.sol (current state — see line anchors in interfaces block)
    - contracts/gnus-ai/GNUSConstants.sol (GNUS_TOKEN_ID)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §6 (copy-ready edit)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §A (MINTER mint warning + D10)
  </read_first>
  <action>
    Two surgical edits to `contracts/gnus-ai/GNUSBridge.sol`:

    1. **DELETE the `withdraw(uint256 amount, uint256 id)` function entirely** (D4) — currently lines 162-191, including its docstring at lines 157-161 and the WR-07 comment block at lines 175-180. The WR-07 charge logic is transplanted to `GNUSTreasury.convert` in Plan 09-02; do not preserve it here. After deletion, `bridgeOut` (currently lines 203-242) becomes the next function.

    2. **Restrict the 3-arg MINTER mint to id 0** (D10, Pitfall 5 — conservation hole). Modify `mint(address user, uint256 tokenID, uint256 amount)` (currently lines 108-110):
       - Insert at the top of the function body: `require(tokenID == GNUS_TOKEN_ID, "MINTER_ROLE mints GNUS only");`
       - The `_mintWithBridgeFee(user, tokenID, amount);` call on the next line is unchanged.
       - Update the docstring to note: bridge-in of a child id is effected as mint id 0 → convert via GNUSTreasury (research §A "Warning" / Open Question 2 resolution — restrict per D10).

    3. Do NOT touch the 2-arg `mint(address user, uint256 amount)` (lines 97-99) — root mint path is unchanged.

    4. Do NOT touch `_mintWithBridgeFee`, `burn`, or `bridgeOut` in this task — those are Task 2.

    5. Verify the deletion by byte-count: research §H estimates ~-700 B from this change, easily absorbed by EIP-170 headroom.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -10 && grep -c "function withdraw" contracts/gnus-ai/GNUSBridge.sol; grep -c "MINTER_ROLE mints GNUS only" contracts/gnus-ai/GNUSBridge.sol; grep -c "convAmount" contracts/gnus-ai/GNUSBridge.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0
    - `grep -c "function withdraw" contracts/gnus-ai/GNUSBridge.sol` returns 0
    - `grep -c "convAmount" contracts/gnus-ai/GNUSBridge.sol` returns 1 (bridgeOut's convAmount survives Task 1 — it dies in Task 2)
    - `grep -c "MINTER_ROLE mints GNUS only" contracts/gnus-ai/GNUSBridge.sol` returns 1
    - The 3-arg `mint` function body contains `require(tokenID == GNUS_TOKEN_ID, ...)` as its FIRST statement
    - The 2-arg `mint` and `burn` functions are byte-identical to their pre-plan state in this task (Task 2 modifies burn)
  </acceptance_criteria>
  <done>withdraw removed; MINTER_ROLE 3-arg mint gated to id 0; compile green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provenance hooks + global cap in _mintWithBridgeFee and burn + bridgeOut rate-math cleanup</name>
  <files>contracts/gnus-ai/GNUSBridge.sol</files>
  <behavior>
    - Root mint with bridgeFee=0: globalSupply increments by exactly `amount`; requires globalSupply + amount <= GNUS_MAX_SUPPLY
    - Root mint with bridgeFee=200 (20%): globalSupply increments by `amount * 800 / 1000` (post-fee, Pitfall 3)
    - Bridge-in (3-arg mint id 0 by MINTER_ROLE): same as root mint (increments globalSupply by post-fee amount)
    - burn(user, amount) by MINTER_ROLE: globalSupply decrements by `amount`
    - bridgeOut(childId, ...): limiter charged with `amount` directly (no `/exchangeRate` division); globalSupply UNCHANGED (B1)
    - bridgeOut(GNUS_TOKEN_ID, ...): hook charges limiter automatically; explicit block skipped (id == 0 branch)
    - Convert operations do NOT touch globalSupply (Pitfall 2 — verified by Plan 09-04 unit tests)
  </behavior>
  <read_first>
    - contracts/gnus-ai/GNUSBridge.sol (current state after Task 1 — line numbers shifted by ~-30 after withdraw deletion)
    - contracts/gnus-ai/GNUSTreasuryStorage.sol (Layout fields from Plan 09-01)
    - contracts/gnus-ai/GNUSControlStorage.sol (bridgeFee field — used in _mintWithBridgeFee)
    - contracts/gnus-ai/GNUSConstants.sol (GNUS_MAX_SUPPLY, GNUS_TOKEN_ID)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-PATTERNS.md §6 (copy-ready edits for _mintWithBridgeFee + burn + bridgeOut)
    - .planning/phases/09-per-child-gnus-treasury-reserve/09-RESEARCH.md §A/§B/§C + Pitfalls 1, 2, 3 (rationales for each hook placement)
  </read_first>
  <action>
    Three surgical edits to `contracts/gnus-ai/GNUSBridge.sol`:

    1. **Add `GNUSTreasuryStorage` import + `using` declaration.**
       - Add `import "./GNUSTreasuryStorage.sol";` to the import block.
       - Add `using GNUSTreasuryStorage for GNUSTreasuryStorage.Layout;` alongside the existing `using` declarations at the top of the contract (currently lines 19-21).

    2. **Hook `_mintWithBridgeFee`** (D8 + D9 + Pitfall 3). The function currently reads:
       - Line 77: signature
       - Lines 78-86: fee adjustment (with WR-04 guard)
       - Line 87: `_mint(user, tokenID, amount, "");`
       - Line 88: `emit Transfer(...)`
       Insert AFTER the fee-adjustment `if` block (after current line 86's closing brace) and BEFORE `_mint(...)`:
       ```
       if (tokenID == GNUS_TOKEN_ID) {
           GNUSTreasuryStorage.Layout storage t = GNUSTreasuryStorage.layout();
           require(t.globalSupply + amount <= GNUS_MAX_SUPPLY, "Global max supply exceeded");
           t.globalSupply += amount;
       }
       ```
       - CRITICAL (Pitfall 3): the `amount` referenced here is the POST-FEE-ADJUSTED value (the local variable was reassigned inside the fee block). Do NOT capture a pre-fee amount into a separate variable — just use `amount` directly at this position.
       - CRITICAL (research §C item 4): the cap check fires ONLY when `tokenID == GNUS_TOKEN_ID`. After Task 1's D10 restriction, the 3-arg MINTER mint can only mint id 0 anyway, so this guard is defense-in-depth.
       - CRITICAL (research §C item 4 + Anti-Patterns): do NOT add a global-cap check anywhere else. In particular, do NOT add it to `_mint` or to `_beforeTokenTransfer` — convert's GNUS-terminal mint leg would then be cap-checked, which is wrong (convert conserves).

    3. **Hook `burn`** (D8). Modify `burn(address user, uint256 amount)` (currently lines 118-121):
       - After the `_burn(user, GNUS_TOKEN_ID, amount);` line and before `emit Transfer(...)`, insert:
         `GNUSTreasuryStorage.layout().globalSupply -= amount;`
       - The function otherwise stays identical (still `onlyRole(MINTER_ROLE)`).

    4. **Simplify `bridgeOut` CR-03 charge** (Pitfall 1 — drop the rate division). Replace the block currently at lines 221-230:
       - BEFORE: `if (id != GNUS_TOKEN_ID) { uint256 exchangeRate = ...NFTs[id].exchangeRate; require(exchangeRate > 0, ...); uint256 convAmount = amount / exchangeRate; if (owner != sender) { checkAndRecordWithdraw(sender, convAmount); } else { emit SuperAdminBypass(sender, convAmount, "GNUSBridge.bridgeOut"); } }`
       - AFTER: `if (id != GNUS_TOKEN_ID) { if (LibDiamond.diamondStorage().contractOwner != sender) { GNUSWithdrawLimiterStorage.checkAndRecordWithdraw(sender, amount); } else { emit GNUSWithdrawLimiterStorage.SuperAdminBypass(sender, amount, "GNUSBridge.bridgeOut"); } }`
       - The `amount` is already minions under D1/D2 — no division needed.
       - Keep the surrounding `_burn(sender, id, amount);` and `emit BridgeOutInitiated(...)` lines unchanged.
       - Per D4: do NOT add a globalSupply hook here — bridgeOut burns on this chain, the destination's bridge-in mint is the + side (B1 model).
       - Verify against the actual code: the current `bridgeOut` signature is `bridgeOut(uint256 amount, uint256 id, uint256 destChainID, bytes32 sgnsDestination, bool destinationYOdd)` — `amount` is the first arg. If the signature or surrounding logic differs from what PATTERNS.md §6 describes, follow the actual code, not the pattern doc.

    5. Final sweep — confirm no `exchangeRate` reads remain in state-transition paths:
       - `grep -n "exchangeRate" contracts/gnus-ai/GNUSBridge.sol` — should return 0 hits (the only remaining `exchangeRate` reads in the codebase are in `getNFTInfo` and the display views on GNUSTreasury).
       - `grep -n "convAmount" contracts/gnus-ai/GNUSBridge.sol` — should return 0 hits.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && npx hardhat compile 2>&1 | tail -10 && grep -c "GNUSTreasuryStorage" contracts/gnus-ai/GNUSBridge.sol && grep -c "Global max supply exceeded" contracts/gnus-ai/GNUSBridge.sol && grep -c "globalSupply" contracts/gnus-ai/GNUSBridge.sol && grep -cv "exchangeRate" contracts/gnus-ai/GNUSBridge.sol && grep -c "checkAndRecordWithdraw(sender, amount)" contracts/gnus-ai/GNUSBridge.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0
    - `grep -c "exchangeRate" contracts/gnus-ai/GNUSBridge.sol` returns 0
    - `grep -c "convAmount" contracts/gnus-ai/GNUSBridge.sol` returns 0
    - `_mintWithBridgeFee` body contains, in order: fee adjustment → `if (tokenID == GNUS_TOKEN_ID) { ... require(globalSupply + amount <= GNUS_MAX_SUPPLY ...); globalSupply += amount; }` → `_mint(...)` → `emit Transfer(...)`
    - The cap-check `require` and the counter increment reference `amount` (post-fee local variable), NOT a separately captured pre-fee variable
    - `burn` body contains `_burn(user, GNUS_TOKEN_ID, amount);` then `GNUSTreasuryStorage.layout().globalSupply -= amount;` then `emit Transfer(...)`
    - `bridgeOut` body contains `checkAndRecordWithdraw(sender, amount)` (no `convAmount`, no division)
    - `bridgeOut` does NOT contain any reference to `globalSupply` (B1 model)
    - Deployed bytecode for GNUSBridge remains under 24,576 bytes (research §H estimates net negative byte impact)
    - `totalSupply()` (ERC-20 facade) is unchanged
  </acceptance_criteria>
  <done>Provenance hooks in place on entry points only; global cap enforced on root mint/bridge-in; bridgeOut limiter math simplified; no rate math remains in state transitions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MINTER_ROLE → mint/burn | Privileged entry points that mutate the global counter |
| Caller → bridgeOut | Cross-chain burn event; limiter charge must be correct |
| Relayer → mint(user, id, amount) | Bridge-in path (research A1: relayer calls MINTER 3-arg mint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Elevation of Privilege | 3-arg MINTER mint (conservation hole, Pitfall 5) | mitigate | `require(tokenID == GNUS_TOKEN_ID, ...)` — D10; child-id bridge-in becomes mint(0)→convert two-step |
| T-09-14 | Tampering | globalSupply counter double-touch (Pitfall 2) | mitigate | Hooks placed ONLY on `_mintWithBridgeFee` and `burn` — never on `_mint`/`_burn` primitives; convert/factory mint are supply-neutral and untouched |
| T-09-15 | Tampering | Bridge-fee drift in counter (Pitfall 3) | mitigate | Counter incremented by POST-fee `amount` (after line 86's adjustment), not the requested amount; unit test in Plan 09-04 pins this |
| T-09-16 | Elevation of Privilege | Global cap bypass | mitigate | Cap check on `_mintWithBridgeFee` against `globalSupply` (D9); convert's GNUS-terminal mint leg is NOT cap-checked (conservation) — research §C item 4 |
| T-09-17 | Tampering | bridgeOut rate-math survivor (Pitfall 1) | mitigate | Division `amount / exchangeRate` deleted; charge uses `amount` directly; final grep sweep verifies zero `exchangeRate` references in the file |
| T-09-18 | Tampering | Selector squatting after withdraw removal | mitigate | Plan 09-04 includes a loupe test asserting `withdraw(uint256,uint256)` selector absence + stale-calldata revert |
| T-09-19 | Denial of Service | Limiter double-charge via bridgeOut hook + explicit | mitigate | bridgeOut explicit block fires only when `id != GNUS_TOKEN_ID`; id-0 bridgeOut is hook-charged on its `_burn` (research §G) — no overlap |
| T-09-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- `npx hardhat compile` green
- `grep "exchangeRate" contracts/gnus-ai/GNUSBridge.sol` returns 0 hits
- `grep "convAmount" contracts/gnus-ai/GNUSBridge.sol` returns 0 hits
- `_mintWithBridgeFee` increments counter post-fee and enforces cap on id-0 mints only
- `burn` decrements counter
- `bridgeOut` charges limiter with `amount` directly, no counter touch
- No hooks on `_mint`/`_burn` primitives
</verification>

<success_criteria>
- withdraw() deleted
- MINTER 3-arg mint restricted to id 0
- globalSupply tracked on entry points only
- Global cap enforced on root mint/bridge-in (post-fee amount)
- bridgeOut limiter charge uses minions directly
- No exchangeRate math in state-transition paths anywhere in the file
</success_criteria>

<output>
Create `.planning/phases/09-per-child-gnus-treasury-reserve/09-03-SUMMARY.md` when done.
</output>
