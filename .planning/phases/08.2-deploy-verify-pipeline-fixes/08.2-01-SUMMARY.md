---
phase: 08.2
plan: 01
status: complete
completed: 2026-07-07
one_liner: Fixed Safe upgrade pipeline bugs, deployed GNUSBridge v2.5 bridgeOut to Sepolia, added ESLint rules, migrated deployment file format
key_files:
  created:
    - scripts/safe/safeUpgrade.ts (orchestrator — propose → wait → confirm → verify → etherscan)
  modified:
    - scripts/deploy/rpc/common.ts (fix addSafeOptions dead code)
    - scripts/setup/RPCDiamondDeployer.ts (chainId auto-detect before validation)
    - scripts/safe/confirmDeployment.ts (majority selector overlap match + unrecognised facet recording)
    - scripts/safe/checkSafeExecuted.ts (nonce stamp + retry on transient errors)
    - scripts/safe/proposeSafeTransaction.ts (key normalization)
    - eslint.config.mjs (C++-aligned linting rules)
    - package.json (yarn safe:upgrade, safe:check, safe:confirm)
    - geniusdiamond.config.json (GNUSBridge v2.5 entry)
  deleted:
    - deployments/sepolia.json (migrated to geniusdiamond-sepolia-11155111.json)
    - deployments/base.json, bsc.json, mainnet.json, etc. (old format)
    - deployments/*-31337.json (Hardhat fork snapshots)
---
