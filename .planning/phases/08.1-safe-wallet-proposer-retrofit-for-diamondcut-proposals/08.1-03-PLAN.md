---
phase: 08.1-safe-wallet-proposer-retrofit-for-diamondcut-proposals
plan: 03
type: tdd
wave: 3
depends_on: [08.1-01, 08.1-02]
files_modified:
  - test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts
  - test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts
  - test/unit/safe/proposeSafeTransaction.test.ts
  - docs/safe-proposal-smoke-test.md
autonomous: true
requirements: [SWP-10, SWP-11]

must_haves:
  truths:
    - "yarn test runs the new RPCDiamondDeployerSafePropose and SafeProposerRPCDeploymentStrategy suites with zero failures"
    - "Unit tests mock proposeSafeTransaction and the Safe SDK — no real Safe Transaction Service network call is made from any unit test"
    - "Tests assert: createConfigFromEnv reads SAFE_PROPOSE and SAFE_ADDRESS; validateConfig rejects/accepts the safePropose branches; RPCDiamondDeployer picks SafeProposerRPCDeploymentStrategy when safePropose=true and RPCDeploymentStrategy otherwise"
    - "Tests assert: SafeProposerRPCDeploymentStrategy calls proposeSafeTransaction with the diamond address as 'to' and encoded diamondCut calldata as 'data', and does NOT call super.performDiamondCutTasks"
    - "Tests assert: writeSafeProposalArtifact is invoked with target=diamondAddress and the safeTxHash returned by proposeSafeTransaction"
    - "A documented manual Sepolia smoke-test command exists in docs/safe-proposal-smoke-test.md"
  artifacts:
    - path: "test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts"
      provides: "Unit tests for config extension, env loading, validation branches, and strategy selection"
      min_lines: 120
    - path: "test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts"
      provides: "Unit tests for the performDiamondCutTasks override using a sinon stub on proposeSafeTransaction"
      min_lines: 100
    - path: "test/unit/safe/proposeSafeTransaction.test.ts"
      provides: "Unit tests for the proposeSafeTransaction helper input/output contract using mocked SafeApiKit"
      min_lines: 60
    - path: "docs/safe-proposal-smoke-test.md"
      provides: "Manual Sepolia smoke-test procedure and acceptance criteria"
  key_links:
    - from: "test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts"
      to: "scripts/safe/proposeSafeTransaction.ts"
      via: "sinon.stub(proposeSafeTransactionModule, 'proposeSafeTransaction').resolves({...})"
      pattern: "sinon\\.stub.*proposeSafeTransaction"
    - from: "test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts"
      to: "scripts/setup/RPCDiamondDeployer.ts validateConfig"
      via: "expect(() => (RPCDiamondDeployer as any).validateConfig({...})).to.throw('SAFE_ADDRESS is required when SAFE_PROPOSE=true')"
      pattern: "validateConfig.*to\\.throw"
---

<objective>
Lock in the Safe proposal retrofit behavior with unit tests (mocked — no Safe Transaction Service calls) and document the manual Sepolia smoke-test procedure. Every public surface added in Plans 01 and 02 gets a failing-test-first coverage.

Purpose: TDD verification — these tests must exist and pass before the phase ships, proving default-behavior-preserved, safe-mode-wired, mainnet-guard, and artifact-write behaviors all hold.
Output: Three new test files and one smoke-test doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/Safe Wallet Proposer Setup.md
@.planning/phases/08.1-safe-wallet-proposer-retrofit-for-diamondcut-proposals/08.1-01-SUMMARY.md
@.planning/phases/08.1-safe-wallet-proposer-retrofit-for-diamondcut-proposals/08.1-02-SUMMARY.md

<interfaces>
<!-- Existing test patterns the executor must replicate. Extracted from test/unit/rpc/RPCDiamondDeployer.test.ts. -->

Test conventions (from test/unit/rpc/RPCDiamondDeployer.test.ts):
```typescript
import { expect } from 'chai';
import { RPCDiamondDeployer, RPCDiamondDeployerConfig, DeploymentStatus } from '../../../scripts/setup/RPCDiamondDeployer';
import { ethers } from 'hardhat';
import sinon from 'sinon';

describe('RPCDiamondDeployer', function () {
    this.timeout(60000);
    let sandbox: sinon.SinonSandbox;
    before(async function () { sandbox = sinon.createSandbox(); /* set process.env */ });
    afterEach(function () { sandbox.restore(); });
    after(async function () {
        /* delete process.env keys */
        (RPCDiamondDeployer as any).instances.clear();  // CRITICAL — clear singletons between tests
    });

    it('should validate required configuration fields', function () {
        expect(() => (RPCDiamondDeployer as any).validateConfig(validConfig)).to.not.throw();
    });
});
```

Key fixtures already in use:
- Test private key: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80` (Hardhat default account #0)
- Test chain ID: `31337` (Hardhat local)
- Clear singletons via `(RPCDiamondDeployer as any).instances.clear()` in `beforeEach` or `after`

Public surfaces to test:
- `RPCDiamondDeployer.createConfigFromEnv(overrides?)` — returns config with safePropose/safeAddress/etc. fields
- `(RPCDiamondDeployer as any).validateConfig(config)` — static private, accessed via cast; pushes errors for invalid safe configs and mainnet-without-safe
- `RPCDiamondDeployer.getInstance(config)` — after the constructor branches, `deployer.getStrategy()` returns either `RPCDeploymentStrategy` or `SafeProposerRPCDeploymentStrategy`
- `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks(diamond)` — overridden; calls proposeSafeTransaction and writeSafeProposalArtifact; does NOT send a tx
- `proposeSafeTransaction(input)` — helper that builds and submits a Safe proposal; unit tests must mock SafeApiKit.proposeTransaction and Safe.init

Mocking pattern for ESM helper imports (sinon):
```typescript
import * as proposeSafeTransactionModule from '../../../scripts/safe/proposeSafeTransaction';
const stub = sandbox.stub(proposeSafeTransactionModule, 'proposeSafeTransaction').resolves({
    safeAddress: '0x...', safeTxHash: '0x' + 'ab'.repeat(32), proposerAddress: '0x...',
    to: '0xdiamond', value: '0', data: '0xcalldata', operation: 0
});
```
NOTE: sinon stubbing of named exports requires the module to be loaded as a namespace (`import * as ...`). If the project's TS module config prevents stubbing ESM exports, fall back to dependency-injection: refactor proposeSafeTransaction to accept an injectable apiKit/protocolKit factory, OR use `sinon.stub` against the SafeApiKit prototype (`SafeApiKit.prototype.proposeTransaction`). Prefer the prototype stub for proposeSafeTransaction tests (cleaner) and the module-namespace stub for the strategy test (where the strategy imports the helper directly).

Diamond fixture for SafeProposerRPCDeploymentStrategy test:
- Construct a minimal Diamond stub with `getDeployedDiamondData()` returning `{ DiamondAddress: '0xdiamond...' }`, `getDeployConfig()` returning a minimal object, and the inherited protected methods (`getInitCalldata`, `getFacetCuts`, `validateNoOrphanedSelectors`) stubbed via sinon on the strategy instance prototype.
- OR construct the strategy and stub its inherited methods via `(strategy as any).getInitCalldata = sandbox.stub().resolves(['0xinitcalldata', '0x0000000000000000000000000000000000000000'])` etc.

Spec test list (Safe Wallet Proposer Setup.md lines 929-938) — minimum coverage required:
1. createConfigFromEnv reads SAFE_PROPOSE and SAFE_ADDRESS
2. validateConfig fails when SAFE_PROPOSE=true and SAFE_ADDRESS is missing
3. validateConfig accepts SAFE_PROPOSE=true with a valid Safe address
4. RPCDiamondDeployer uses normal RPCDeploymentStrategy when SAFE_PROPOSE=false
5. RPCDiamondDeployer uses SafeProposerRPCDeploymentStrategy when SAFE_PROPOSE=true
6. SafeProposerRPCDeploymentStrategy passes normal contract deployments through to super
7. SafeProposerRPCDeploymentStrategy proposes, rather than sends, Diamond cut txs
8. Safe proposal artifact contains target, calldata, value, operation, safeTxHash, proposer, chainId

Plus mainnet guard: validateConfig fails when networkName='mainnet' and safePropose is not true.

Sepolia smoke-test command (spec lines 944-958):
```bash
SAFE_PROPOSE=true SAFE_ADDRESS=0xSafe... PRIVATE_KEY=0xProposer... \
npx ts-node scripts/deploy/rpc/upgrade-rpc.ts upgrade GeniusDiamond sepolia --dry-run
```
Then real proposal (same command without --dry-run).
</interfaces>
</context>

<feature>
  <name>Safe proposal retrofit test coverage</name>
  <files>test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts, test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts, test/unit/safe/proposeSafeTransaction.test.ts, docs/safe-proposal-smoke-test.md</files>
  <behavior>
    - createConfigFromEnv reads SAFE_PROPOSE=true and SAFE_ADDRESS=0xABC from process.env and the returned config reflects them
    - validateConfig throws 'SAFE_ADDRESS is required when SAFE_PROPOSE=true' when safePropose=true and safeAddress is undefined
    - validateConfig throws 'SAFE_ADDRESS must be a valid address' when safePropose=true and safeAddress='not-an-address'
    - validateConfig throws 'A proposer private key is required when SAFE_PROPOSE=true' when safePropose=true and both safeProposerPrivateKey and privateKey are missing
    - validateConfig throws 'Safe proposer private key must be 64 hex characters with 0x prefix' when safePropose=true and proposerKey='0xdeadbeef'
    - validateConfig does NOT throw when safePropose=true, safeAddress is valid, and a valid proposer key is present
    - validateConfig throws 'Mainnet privileged Diamond cut/admin upgrades require SAFE_PROPOSE=true — refusing to deploy directly' when networkName='mainnet' and safePropose is falsy
    - validateConfig does NOT throw for mainnet when safePropose=true
    - RPCDiamondDeployer.getStrategy() returns an instance of RPCDeploymentStrategy when safePropose is unset/false
    - RPCDiamondDeployer.getStrategy() returns an instance of SafeProposerRPCDeploymentStrategy when safePropose=true with valid config
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks, with proposeSafeTransaction and writeSafeProposalArtifact stubbed, calls proposeSafeTransaction exactly once with to=diamondAddress and a data string starting with '0x1f931c1c' (diamondCut selector)
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks calls writeSafeProposalArtifact exactly once with target=diamondAddress and safeTxHash matching the stubbed proposeSafeTransaction result
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks does NOT call super.performDiamondCutTasks (verified by stubbing super and asserting call count 0, or by asserting no ethers Contract sendTransaction occurs)
    - proposeSafeTransaction, with SafeApiKit and protocolKit mocked, returns a ProposeSafeTransactionResult containing the stubbed safeTxHash, proposerAddress, to, value='0', data, operation=0
  </behavior>
  <implementation>
    Write tests FIRST (red), then run them. They will fail until Plans 01+02 are in place (which they are, since this is Wave 3). After green, add the smoke-test doc. Use sinon sandboxes and clear RPCDiamondDeployer singletons between tests. Do NOT hit any real network — stub SafeApiKit.prototype.proposeTransaction for the proposeSafeTransaction test, and stub the proposeSafeTransaction module export for the strategy test.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write config + validation + strategy-selection unit tests</name>
  <files>test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts</files>
  <read_first>
    - test/unit/rpc/RPCDiamondDeployer.test.ts (FULL file — replicate the before/after process.env setup, the singleton-clear pattern, the validateConfig-via-cast pattern)
    - scripts/setup/RPCDiamondDeployer.ts (Plan 02 final state — RPCDiamondDeployerConfig interface, validateConfig branches, createConfigFromEnv, constructor strategy branch)
    - scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts (Plan 01 output — class export name)
    - .planning/Safe Wallet Proposer Setup.md (lines 929-938 — required test cases 1-5)
  </read_first>
  <behavior>
    - createConfigFromEnv reads SAFE_PROPOSE=true and SAFE_ADDRESS=0xABC from process.env
    - validateConfig throws 'SAFE_ADDRESS is required when SAFE_PROPOSE=true' (safePropose=true, no safeAddress)
    - validateConfig throws 'SAFE_ADDRESS must be a valid address' (safePropose=true, safeAddress='not-an-address')
    - validateConfig throws 'A proposer private key is required when SAFE_PROPOSE=true' (safePropose=true, no proposer key at all)
    - validateConfig throws 'Safe proposer private key must be 64 hex characters with 0x prefix' (safePropose=true, proposerKey='0xdeadbeef')
    - validateConfig does not throw for safePropose=true with valid safeAddress and valid proposerKey
    - validateConfig throws 'Mainnet privileged Diamond cut/admin upgrades require SAFE_PROPOSE=true' (networkName='mainnet', safePropose falsy)
    - validateConfig does not throw for networkName='mainnet' + safePropose=true
    - getStrategy() returns RPCDeploymentStrategy instance when safePropose is false
    - getStrategy() returns SafeProposerRPCDeploymentStrategy instance when safePropose is true
  </behavior>
  <action>
    Create `test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts`. Replicate the test harness from `RPCDiamondDeployer.test.ts`: `describe('RPCDiamondDeployer Safe Proposal Mode', function() { this.timeout(60000); let sandbox; before(() => { sandbox = sinon.createSandbox(); }); afterEach(() => { sandbox.restore(); (RPCDiamondDeployer as any).instances.clear(); delete process.env.SAFE_PROPOSE; delete process.env.SAFE_ADDRESS; /* etc */ }); })`.

    Use the same test private key (`0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`) and chainId 31337. Use a valid checksummed address for the safeAddress fixture (e.g. `0x1234567890123456789012345678901234567890` — but verify it passes `ethers.isAddress`; use `ethers.getAddress('0x1234567890123456789012345678901234567890')` if needed).

    For the strategy-selection tests: build two configs (one with safePropose=false, one with safePropose=true + valid safeAddress + valid proposer key), call `await RPCDiamondDeployer.getInstance(config)`, then `deployer.getStrategy()` and assert `instanceof`:
    - `import { RPCDeploymentStrategy } from '@diamondslab/diamonds'`
    - `import { SafeProposerRPCDeploymentStrategy } from '../../../scripts/setup/strategies/SafeProposerRPCDeploymentStrategy'`
    - For the safePropose=true case, also stub `SafeProposerRPCDeploymentStrategy.prototype.validateConnection` with sinon to avoid a real RPC call during getInstance. If getInstance itself triggers network I/O before the strategy is returned, stub the provider's `getNetwork` or use the existing `config.provider` injection point with a stubbed JsonRpcProvider.

    For the createConfigFromEnv test: set `process.env.SAFE_PROPOSE = 'true'` and `process.env.SAFE_ADDRESS = '0x1234...'` (valid address), call `RPCDiamondDeployer.createConfigFromEnv()`, and assert `config.safePropose === true` and `config.safeAddress === '0x1234...'` and `config.safeOrigin === 'gnus-ai-rpc-upgrade'`.

    Group tests with nested `describe()` blocks: 'createConfigFromEnv', 'validateConfig Safe branches', 'validateConfig mainnet guard', 'Strategy selection'.
  </action>
  <verify>
    <automated>cd gnus-ai &amp;&amp; npx hardhat test test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts --grep "Safe Proposal Mode" 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `gnus-ai/test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts` exists with at least 10 `it()` test cases covering the behavior list above
    - All tests pass via `npx hardhat test test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts`
    - No test makes a real network call (validateConnection or RPC provider is stubbed where needed)
    - Strategy-selection tests assert both `instanceof RPCDeploymentStrategy` and `instanceof SafeProposerRPCDeploymentStrategy`
    - The mainnet-guard test asserts the exact error string 'Mainnet privileged Diamond cut/admin upgrades require SAFE_PROPOSE=true — refusing to deploy directly'
  </acceptance_criteria>
  <done>All config/validation/strategy-selection tests pass; no real network calls; singleton state cleared between tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write SafeProposerRPCDeploymentStrategy + proposeSafeTransaction unit tests</name>
  <files>test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts, test/unit/safe/proposeSafeTransaction.test.ts</files>
  <read_first>
    - test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts (Task 1 — replicate the harness)
    - scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts (performDiamondCutTasks override — what to assert)
    - scripts/safe/proposeSafeTransaction.ts (the function to mock)
    - scripts/safe/writeSafeProposalArtifact.ts (called inside the override — verify call args)
    - node_modules/@safe-global/api-kit/dist/src/index.d.ts (SafeApiKit class shape for prototype stubbing)
    - .planning/Safe Wallet Proposer Setup.md (lines 929-938 — test cases 6-8)
  </read_first>
  <behavior>
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks calls proposeSafeTransaction exactly once
    - The proposeSafeTransaction call's `to` arg equals the diamond address from getDeployedDiamondData()
    - The proposeSafeTransaction call's `data` arg starts with '0x1f931c1c' (the diamondCut selector)
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks calls writeSafeProposalArtifact exactly once
    - The writeSafeProposalArtifact call's `target` arg equals the diamond address
    - The writeSafeProposalArtifact call's `safeTxHash` arg equals the safeTxHash returned by the stubbed proposeSafeTransaction
    - SafeProposerRPCDeploymentStrategy.performDiamondCutTasks does NOT throw and does NOT call any ethers Contract send method (no real tx)
    - proposeSafeTransaction with mocked SafeApiKit returns a result containing safeTxHash, proposerAddress, to, value='0', data, operation=0
    - proposeSafeTransaction invokes SafeApiKit.proposeTransaction exactly once with senderAddress matching the wallet address derived from proposerPrivateKey
  </behavior>
  <action>
    Create `test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts`:
    - Build a minimal Diamond stub: `const diamond = { getDeployedDiamondData: () => ({ DiamondAddress: '0xdiamond0000000000000000000000000000000000' }), getDeployConfig: () => ({}), getInitCalldata: () => undefined, getFacetCuts: () => undefined } as any;` — but since these methods are on the strategy (inherited), stub them on the strategy instance AFTER construction: `const strategy = new SafeProposerRPCDeploymentStrategy({...}); sinon.stub(strategy as any, 'getInitCalldata').resolves(['0xinitcalldata', '0x0000000000000000000000000000000000000000']); sinon.stub(strategy as any, 'getFacetCuts').resolves([{ facetAddress: '0xfacet00000000000000000000000000000000000', action: 0, functionSelectors: ['0x12345678'], name: 'TestFacet' }]); sinon.stub(strategy as any, 'validateNoOrphanedSelectors').resolves();`
    - Also stub the IDiamondCut artifact loading: the strategy uses `getContractArtifact` or a local ABI. Either inject the artifact via the diamond stub or stub the helper. The key assertion is on the encoded calldata's first 10 chars: `'0x1f931c1c'`.
    - Stub the proposeSafeTransaction module: `import * as safeHelper from '../../../scripts/safe/proposeSafeTransaction'; const proposeStub = sandbox.stub(safeHelper, 'proposeSafeTransaction').resolves({ safeAddress: '0x...', safeTxHash: '0x' + 'ab'.repeat(32), proposerAddress: '0x...', to: '0xdiamond...', value: '0', data: '0x...', operation: 0 });`
    - Stub the writeSafeProposalArtifact module similarly: `import * as artifactWriter from '../../../scripts/safe/writeSafeProposalArtifact'; const writeStub = sandbox.stub(artifactWriter, 'writeSafeProposalArtifact').resolves('/tmp/fake.json');`
    - Call `await (strategy as any).performDiamondCutTasks(diamond)`.
    - Assert `proposeStub.calledOnce`, `proposeStub.firstCall.args[0].to === '0xdiamond0000000000000000000000000000000000'`, `proposeStub.firstCall.args[0].data.startsWith('0x1f931c1c')`, `writeStub.calledOnce`, `writeStub.firstCall.args[0].target === '0xdiamond0000000000000000000000000000000000'`, `writeStub.firstCall.args[0].safeTxHash === proposeStub.firstCall.returnValue.safeTxHash`.

    Create `test/unit/safe/proposeSafeTransaction.test.ts`:
    - Stub `SafeApiKit.prototype.proposeTransaction` and `Safe.prototype.createTransaction`/`getTransactionHash`/`signHash` via sinon (or use proxyquire/rewire if available — check package.json devDeps; sinon is already a dep).
    - Provide a stubbed `Safe.init` that returns an object with the four chained methods.
    - Call `proposeSafeTransaction({ chainId: 11155111n, rpcUrl: 'http://localhost:8545', safeAddress: '0x...', proposerPrivateKey: '0xac09...', to: '0x...', data: '0x1f931c1c...' })`.
    - Assert the returned result contains the stubbed safeTxHash, proposerAddress derived from the private key, to, value='0', data, operation=0.
    - Assert `SafeApiKit.prototype.proposeTransaction.calledOnce` and the call's `senderAddress` matches `new ethers.Wallet('0xac09...', ...).address`.

    If stubbing the Safe SDK classes proves difficult due to how they're constructed inside the helper, refactor `proposeSafeTransaction` to accept an optional `apiKitFactory?` and `protocolKitFactory?` (defaults to the real constructors) — this is a minimal, justified DI refactor for testability, NOT a scope expansion. Document this in a code comment. If the DI refactor is not needed (sinon prototype stub works), do NOT add it.
  </action>
  <verify>
    <automated>cd gnus-ai &amp;&amp; npx hardhat test test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts test/unit/safe/proposeSafeTransaction.test.ts 2&gt;&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `gnus-ai/test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts` exists with at least 6 `it()` cases
    - `gnus-ai/test/unit/safe/proposeSafeTransaction.test.ts` exists with at least 3 `it()` cases
    - All tests pass via `npx hardhat test` for both files
    - No test makes a real Safe Transaction Service call (verified by sinon stubs on SafeApiKit.prototype.proposeTransaction)
    - The strategy test asserts `proposeStub.firstCall.args[0].data.startsWith('0x1f931c1c')` (diamondCut selector present in proposed calldata)
    - The strategy test asserts `writeStub.firstCall.args[0].target === <diamond address>`
    - No test calls `super.performDiamondCutTasks` (the strategy override is the only code path under test)
  </acceptance_criteria>
  <done>Strategy and helper unit tests pass with fully mocked Safe SDK; calldata selector and target address are asserted; artifact write is asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Sepolia smoke-test documentation</name>
  <files>docs/safe-proposal-smoke-test.md</files>
  <read_first>
    - .planning/Safe Wallet Proposer Setup.md (lines 218-247 — desired user commands; lines 942-969 — smoke-test acceptance criteria)
    - scripts/deploy/rpc/upgrade-rpc.ts (the script the smoke test invokes)
    - .env.example (the Safe section added in Plan 01 — for the env-var quick mode)
  </read_first>
  <action>
    Create `docs/safe-proposal-smoke-test.md` documenting the manual Sepolia smoke-test procedure. Structure:

    1. **Prerequisites** — Safe wallet deployed on Sepolia; deployer EOA added as a Proposer on the Safe (Safe UI -> Settings -> Proposers); Sepolia RPC URL + funded deployer EOA; GeniusDiamond already deployed on Sepolia (address from `diamonds/GeniusDiamond/deployments/`).

    2. **Dry-run command** (verifies config loads, no tx sent): `SAFE_PROPOSE=true SAFE_ADDRESS=0xYourSafe... PRIVATE_KEY=0xProposerKey... RPC_URL=https://sepolia... NETWORK_NAME=sepolia CHAIN_ID=11155111 npx ts-node scripts/deploy/rpc/upgrade-rpc.ts upgrade GeniusDiamond sepolia --dry-run`. Expected: analysis prints, no proposal created.

    3. **Real proposal command**: same without `--dry-run`. Expected: new facets (if any) deploy directly; the diamondCut is NOT sent directly; a Safe proposal appears in the Safe UI (app.safe.global); a local artifact is written under `diamonds/GeniusDiamond/safe-proposals/sepolia-11155111-<timestamp>-<safeTxHash>.json`; console prints the success block with Diamond, Network, Chain ID, Safe, Proposer, Target, Value, Operation, SafeTxHash.

    4. **CLI flag form** (alternative): `npx ts-node scripts/deploy/rpc/upgrade-rpc.ts upgrade GeniusDiamond sepolia --safe-propose --safe-address 0xYourSafe...`.

    5. **Acceptance checklist** (from spec lines 962-969):
       - [ ] New facets deployed directly (transaction hashes visible)
       - [ ] Diamond cut NOT executed directly by the EOA (no diamondCut tx from deployer address on Sepolia)
       - [ ] Safe proposal appears in the Safe UI
       - [ ] Proposal target equals the GeniusDiamond address
       - [ ] Proposal calldata starts with `0x1f931c1c` (diamondCut selector)
       - [ ] Local artifact file written with safeTxHash matching the Safe UI
       - [ ] Safe owners can review/sign/execute the proposal in the UI

    6. **Rollback note** — if the proposal is rejected or the Safe tx fails, the GeniusDiamond on Sepolia is unchanged (no direct cut was performed). Re-run the command to create a new proposal after fixing the underlying issue.

    7. **Mainnet note** — direct privileged upgrades on mainnet are blocked unless `SAFE_PROPOSE=true`. Sepolia direct upgrades trigger a warning but are allowed.
  </action>
  <verify>
    <automated>cd gnus-ai &amp;&amp; test -f docs/safe-proposal-smoke-test.md &amp;&amp; grep -c "SAFE_PROPOSE=true" docs/safe-proposal-smoke-test.md | awk '$1 &gt;= 2 {found=1} END {exit !found}' &amp;&amp; grep -c "0x1f931c1c" docs/safe-proposal-smoke-test.md | grep -qx '1' &amp;&amp; echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `gnus-ai/docs/safe-proposal-smoke-test.md` exists
    - Document contains the dry-run command AND the real-proposal command (both using `SAFE_PROPOSE=true`)
    - Document contains the 7-item acceptance checklist from spec lines 962-969
    - Document references the `0x1f931c1c` diamondCut selector as the calldata verification value
    - Document notes the mainnet guard behavior
  </acceptance_criteria>
  <done>Sepolia smoke-test procedure documented with prerequisites, dry-run command, real-proposal command, acceptance checklist, and rollback notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit test process -> Safe Transaction Service | Tests must NOT cross this boundary; all SDK calls stubbed |
| Test environment -> process.env | Tests mutate process.env; must clean up in afterEach/after to avoid polluting other suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-11 | Denial of Service | Unit tests making real Safe API calls | mitigate | All SafeApiKit interactions stubbed via sinon; proposeSafeTransaction module export stubbed in strategy tests; no network calls in any test |
| T-08.1-12 | Tampering | Singleton state leaking between tests | mitigate | `(RPCDiamondDeployer as any).instances.clear()` in afterEach; process.env keys deleted in afterEach |
| T-08.1-13 | Repudiation | Smoke-test execution on Sepolia | mitigate | docs/safe-proposal-smoke-test.md records the acceptance checklist including Safe UI verification; the local artifact provides a durable audit trail |
</threat_model>

<verification>
- `npx hardhat test test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts test/unit/safe/proposeSafeTransaction.test.ts` — all pass.
- `npx hardhat test` (full suite) — no regressions in existing RPCDiamondDeployer tests.
- No test file imports `fetch`, `http`, or instantiates a real `JsonRpcProvider` against an external URL.
- `docs/safe-proposal-smoke-test.md` exists with the dry-run, real-proposal, and acceptance-checklist sections.
</verification>

<success_criteria>
- All 8 spec-required test cases (Safe Wallet Proposer Setup.md lines 929-938) covered and passing.
- Mainnet guard test covered and passing.
- Strategy intercept test proves proposeSafeTransaction is called with diamond address as `to` and diamondCut-calldata as `data`, and that writeSafeProposalArtifact is called with matching target and safeTxHash.
- No real Safe Transaction Service call from any unit test.
- Smoke-test doc ready for a human to execute on Sepolia.
</success_criteria>

<output>
Create `.planning/phases/08.1-safe-wallet-proposer-retrofit-for-diamondcut-proposals/08.1-03-SUMMARY.md` when done
</output>
