---
phase: 08.1-safe-wallet-proposer-retrofit-for-diamondcut-proposals
verified: 2026-06-30T02:15:00Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: N/A
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Sepolia smoke test — run real Safe proposal against a live Sepolia Safe + GeniusDiamond"
    expected: "diamondCut is proposed (not executed directly), Safe proposal appears in app.safe.global queue with target=diamondAddress and calldata starting 0x1f931c1c, local artifact file written under diamonds/GeniusDiamond/safe-proposals/"
    why_human: "Requires a funded Sepolia deployer EOA, a configured Safe wallet with the deployer added as a Proposer, and a live Safe Transaction Service — none of which can be exercised from a unit test or grep. This is the only validation path for the TS-level Safe SDK proposal submission per CONTEXT.md D-02."
  - test: "Verify Safe Transaction Service accepts the proposal and records senderAddress for non-repudiation (T-08.1-07)"
    expected: "The proposal persists in the Safe Transaction Service queue with the proposer EOA's address attributed; Safe signers can review/sign/execute from the UI"
    why_human: "Depends on the Safe Transaction Service being available for the target chainId and the proposer EOA being pre-registered as a Proposer in the Safe UI (cannot be auto-verified)"
---

# Phase 08.1: Safe Wallet Proposer retrofit for diamondCut proposals — Verification Report

**Phase Goal:** Retrofit the existing RPC Diamond deployment pipeline so diamondCut/admin transactions are proposed to a Safe wallet instead of executed directly by EOA. Keep existing facet deployment unchanged — only intercept the final privileged transaction.
**Verified:** 2026-06-30T02:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                        | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `yarn install` resolves `@safe-global/api-kit`, `@safe-global/protocol-kit`, `@safe-global/types-kit` without errors                                                                                         | ✓ VERIFIED | `package.json:110-112` pins all three packages with exact versions (no `^`/`~`): api-kit@5.0.1, protocol-kit@8.0.1, types-kit@4.0.1. `node -e "require('@safe-global/...')"` confirmed by tests importing them successfully.                                                                                                                                                                                                                                                                                                          |
| 2   | `proposeSafeTransaction()` returns a `ProposeSafeTransactionResult` with all 7 fields                                                                                                                        | ✓ VERIFIED | `scripts/safe/proposeSafeTransaction.ts:134-142` returns the full object literal. `test/unit/safe/proposeSafeTransaction.test.ts` asserts all fields (6 tests pass).                                                                                                                                                                                                                                                                                                                                                                  |
| 3   | `writeSafeProposalArtifact()` writes JSON under `diamonds/<name>/safe-proposals/` with all 14 keys                                                                                                           | ✓ VERIFIED | `scripts/safe/writeSafeProposalArtifact.ts:108-119` computes the spec path and writes JSON. Artifact object (lines 91-105) contains all 14 required keys including `calldataSelector` computed via `slice(0, 10)`.                                                                                                                                                                                                                                                                                                                    |
| 4   | `.env.example` contains the Safe proposal section with all 6 vars                                                                                                                                            | ✓ VERIFIED | `.env.example:39-45` contains `# Safe proposal mode for Diamond upgrades` header followed by exactly one occurrence each of SAFE_PROPOSE, SAFE_ADDRESS, SAFE_PROPOSER_PRIVATE_KEY, SAFE_TX_SERVICE_URL, SAFE_API_KEY, SAFE_ORIGIN. Original `# RPC Deployment Configuration` (line 24), `PRIVATE_KEY=` (line 7), `DIAMOND_NAME=GeniusDiamond` (line 25) all intact.                                                                                                                                                                   |
| 5   | Importing `proposeSafeTransaction` performs no network I/O at module load                                                                                                                                    | ✓ VERIFIED | All SDK calls (`Safe.init`, `apiKit.proposeTransaction`, `protocolKit.createTransaction`) are inside the `proposeSafeTransaction` function body (lines 75-131); module top-level only contains imports and the function declaration.                                                                                                                                                                                                                                                                                                  |
| 6   | When `config.safePropose` is true with valid `safeAddress`, `RPCDiamondDeployer` constructs `SafeProposerRPCDeploymentStrategy`; otherwise constructs `RPCDeploymentStrategy` (default behavior unchanged)   | ✓ VERIFIED | `scripts/setup/RPCDiamondDeployer.ts:212-239` — `if (config.safePropose)` branch constructs `SafeProposerRPCDeploymentStrategy`; else branch constructs `RPCDeploymentStrategy` with identical args to pre-phase code. `test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts:318-364` asserts both branches via `instanceof` (4 strategy-selection tests pass).                                                                                                                                                                        |
| 7   | `validateConfig` rejects: missing SAFE_ADDRESS, invalid SAFE_ADDRESS, missing proposer key, mainnet-without-safe                                                                                             | ✓ VERIFIED | `scripts/setup/RPCDiamondDeployer.ts:569-594` — all four error conditions pushed (SAFE_ADDRESS required, must be valid address, proposer key required, mainnet guard). Network name normalized via `trim().toLowerCase()` for case-insensitive guard. `test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts` covers all branches (tests pass).                                                                                                                                                                                         |
| 8   | `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks` proposes diamondCut to Safe instead of sending, writes artifact, skips deployed-data finalization; delegates all other lifecycle methods to super | ✓ VERIFIED | `scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts:132-275` overrides ONLY `performDiamondCutTasks`; calls `proposeSafeTransaction` with `to: diamondAddress` and `data: calldata` encoded via `encodeFunctionData('diamondCut', ...)`; calls `writeSafeProposalArtifact` with `target: diamondAddress`; explicitly does NOT call `postDiamondCutDeployedDataUpdate` or `markDeploymentComplete` (lines 264-274 comment block). `test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts` asserts all 8 behaviors (pass). |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                                                        | Expected                                                   | Status     | Details                                                                                                                                                                                                                                  |
| --------------------------------------------------------------- | ---------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/safe/safeProposalTypes.ts`                             | Exports 3 shared interfaces                                | ✓ VERIFIED | 97 lines; exports `ProposeSafeTransactionInput`, `ProposeSafeTransactionResult`, `SafeProposalArtifact` with all spec fields. Imported by helper, writer, and strategy.                                                                  |
| `scripts/safe/proposeSafeTransaction.ts`                        | Exports `proposeSafeTransaction` using Safe SDK            | ✓ VERIFIED | 143 lines; imports SafeApiKit, Safe, OperationType, MetaTransactionData; calls `Safe.init`, `createTransaction`, `signTransaction`, `getTransactionHash`, `signHash`, `apiKit.proposeTransaction`. Exceeds spec (adds input validation). |
| `scripts/safe/writeSafeProposalArtifact.ts`                     | Exports `writeSafeProposalArtifact`                        | ✓ VERIFIED | 122 lines; computes selector via `slice(0,10)`, uses `mkdirSync({recursive:true})` + `writeFileSync`, returns path.                                                                                                                      |
| `scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts` | Subclass overriding performDiamondCutTasks                 | ✓ VERIFIED | 276 lines; `extends RPCDeploymentStrategy`; constructor calls super with spec defaults (1.2/3/2000); override proposes + writes + skips finalization.                                                                                    |
| `scripts/setup/RPCDiamondDeployer.ts`                           | Extended config, strategy branch, validation, env loading  | ✓ VERIFIED | 6 safe fields added to interface; constructor branches; `createConfigFromEnv` reads SAFE\_\* (lines 521-526); `validateConfig` Safe block (569-583) + mainnet guard (586-594); sepolia warning at line 647.                              |
| `scripts/deploy/rpc/common.ts`                                  | Extended BaseRPCOptions + addSafeOptions + config passthru | ✓ VERIFIED | 6 fields in BaseRPCOptions (29-34); `addSafeOptions` exported (146) with 6 `.option()` calls; composed inside `addCommonOptions` (137); `createRPCConfig` passes safe fields in both hardhat (366-371) and legacy (405-410) paths.       |
| `test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts`           | ≥10 tests covering config/validation/strategy selection    | ✓ VERIFIED | 15101 bytes, 20 tests across 4 describe blocks; all pass.                                                                                                                                                                                |
| `test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts`       | ≥6 tests asserting intercept behavior                      | ✓ VERIFIED | 13039 bytes, 8 tests; asserts `0x1f931c1c` selector, `to=diamondAddress`, artifact target, no direct tx.                                                                                                                                 |
| `test/unit/safe/proposeSafeTransaction.test.ts`                 | ≥3 tests with mocked Safe SDK                              | ✓ VERIFIED | 12900 bytes, 6 tests; SafeApiKit.prototype.proposeTransaction stubbed; no real network call.                                                                                                                                             |
| `docs/safe-proposal-smoke-test.md`                              | Manual Sepolia runbook with dry-run + real + checklist     | ✓ VERIFIED | 164 lines; prerequisites, dry-run command, real proposal command, CLI flag form, 7-item acceptance checklist, rollback/retry, mainnet note.                                                                                              |

### Key Link Verification

| From                                                       | To                                                   | Via                                                                                                                   | Status | Details                                                                                                                   |
| ---------------------------------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------- |
| `proposeSafeTransaction.ts`                                | `@safe-global/api-kit SafeApiKit.proposeTransaction` | `apiKit.proposeTransaction({safeAddress, safeTransactionData, safeTxHash, senderAddress, senderSignature, origin})`   | WIRED  | `proposeSafeTransaction.ts:124-131` — call present with all required args; senderSignature via `senderSignature.data`.    |
| `proposeSafeTransaction.ts`                                | `@safe-global/protocol-kit Safe.init`                | `Safe.init({provider, signer, safeAddress})` then createTransaction + signTransaction + getTransactionHash + signHash | WIRED  | `proposeSafeTransaction.ts:86-117` — full chain present.                                                                  |
| `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks` | `proposeSafeTransaction`                             | `proposeSafeTransaction({chainId, rpcUrl, safeAddress, ..., to: diamondAddress, data: calldata})`                     | WIRED  | `SafeProposerRPCDeploymentStrategy.ts:207-219` — call with `to: diamondAddress` and `data: calldata` encoded at line 200. |
| `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks` | `writeSafeProposalArtifact`                          | `writeSafeProposalArtifact({diamondName, ..., target: diamondAddress, safeTxHash: result.safeTxHash})`                | WIRED  | `SafeProposerRPCDeploymentStrategy.ts:222-234` — call with `target: diamondAddress` and `safeTxHash: result.safeTxHash`.  |
| `RPCDiamondDeployer.ts` constructor                        | `SafeProposerRPCDeploymentStrategy`                  | `new SafeProposerRPCDeploymentStrategy({...})` when `config.safePropose === true`                                     | WIRED  | `RPCDiamondDeployer.ts:213-229` — branch constructs strategy with full config.                                            |
| `common.ts createRPCConfig`                                | `RPCDiamondDeployer createConfigFromHardhat`         | passes 6 safe fields in overrides object                                                                              | WIRED  | `common.ts:366-371` (hardhat path) and `405-410` (legacy path).                                                           |

### Data-Flow Trace (Level 4)

| Artifact                                                   | Data Variable      | Source                                                                                                         | Produces Real Data                                      | Status    |
| ---------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------- |
| `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks` | `calldata`         | `diamondContract.interface.encodeFunctionData('diamondCut', [facetSelectorCutMap, initAddress, initCalldata])` | Yes (real facet cuts from `this.getFacetCuts(diamond)`) | ✓ FLOWING |
| `SafeProposerRPCDeploymentStrategy.performDiamondCutTasks` | `diamondAddress`   | `diamond.getDeployedDiamondData().DiamondAddress`                                                              | Yes (real deployed diamond data)                        | ✓ FLOWING |
| `proposeSafeTransaction` result                            | `safeTxHash`       | `protocolKit.getTransactionHash(signedSafeTransaction)`                                                        | Yes (real Safe SDK hash)                                | ✓ FLOWING |
| `writeSafeProposalArtifact` artifact                       | `calldataSelector` | `params.calldata.slice(0, 10)`                                                                                 | Yes (derived from real calldata, expected `0x1f931c1c`) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior                                                    | Command                                                                                                                                                                      | Result                   | Status |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------ |
| All 3 new test suites pass with zero real network calls     | `npx hardhat test test/unit/rpc/RPCDiamondDeployerSafePropose.test.ts test/unit/rpc/SafeProposerRPCDeploymentStrategy.test.ts test/unit/safe/proposeSafeTransaction.test.ts` | 34 passing (2s)          | ✓ PASS |
| No regression in pre-existing RPCDiamondDeployer unit tests | `npx hardhat test test/unit/rpc/RPCDiamondDeployer.test.ts` (combined with new tests)                                                                                        | 61 passing (3s)          | ✓ PASS |
| Safe SDK packages installed and require-able                | (implicit via test imports)                                                                                                                                                  | all tests import and run | ✓ PASS |
| deploy-rpc.ts and upgrade-rpc.ts untouched by this phase    | `git log --since="2026-06-29" -- scripts/deploy/rpc/{upgrade,deploy}-rpc.ts`                                                                                                 | empty (no commits)       | ✓ PASS |
| addSafeOptions composed into addCommonOptions               | `grep addSafeOptions scripts/deploy/rpc/common.ts`                                                                                                                           | composition at line 137  | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` probes declared in PLAN or conventional locations. This is a TypeScript deployment-infrastructure phase, not a migration/tooling phase.

### Requirements Coverage

| Requirement | Source Plan | Description                                                                          | Status        | Evidence                                                                                                                                                                                                           |
| ----------- | ----------- | ------------------------------------------------------------------------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SWP-01      | (08.1-01)   | Install Safe SDK deps as exact-pinned devDependencies                                | ✓ SATISFIED   | `package.json:110-112` — three packages pinned exactly.                                                                                                                                                            |
| SWP-02      | 08.1-02     | Extend config/CLI/environment validation for Safe proposal mode                      | ✓ SATISFIED   | `RPCDiamondDeployer.ts:111-121` (config fields), `569-594` (validation); `common.ts:29-34` (BaseRPCOptions). NOTE: REQUIREMENTS.md traceability still marks this "Pending" — stale, should be updated to Complete. |
| SWP-03      | 08.1-02     | Wire CLI options and env loading                                                     | ✓ SATISFIED   | `common.ts:146-179` (addSafeOptions with 6 flags); `RPCDiamondDeployer.ts:521-526` (createConfigFromEnv reads SAFE\_\*). NOTE: REQUIREMENTS.md traceability still marks "Pending" — stale.                         |
| SWP-04      | (08.1-01)   | Implement `proposeSafeTransaction()` helper                                          | ✓ SATISFIED   | `scripts/safe/proposeSafeTransaction.ts` — full implementation.                                                                                                                                                    |
| SWP-05      | (08.1-01)   | Implement `writeSafeProposalArtifact()`                                              | ✓ SATISFIED   | `scripts/safe/writeSafeProposalArtifact.ts` — full implementation.                                                                                                                                                 |
| SWP-06      | 08.1-02     | Implement `SafeProposerRPCDeploymentStrategy`                                        | ✓ SATISFIED   | `scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts` — subclass overrides performDiamondCutTasks. NOTE: REQUIREMENTS.md traceability still marks "Pending" — stale.                                     |
| SWP-07      | 08.1-02     | Wire strategy selection in RPCDiamondDeployer                                        | ✓ SATISFIED   | `RPCDiamondDeployer.ts:212-239` — strategy branch. NOTE: REQUIREMENTS.md traceability still marks "Pending" — stale.                                                                                               |
| SWP-08      | (08.1-01)   | Extend `.env.example` with Safe proposal section                                     | ✓ SATISFIED   | `.env.example:39-45`.                                                                                                                                                                                              |
| SWP-09      | 08.1-02     | Add mainnet guard — block direct privileged upgrades on mainnet without SAFE_PROPOSE | ✓ SATISFIED   | `RPCDiamondDeployer.ts:586-594` — mainnet guard in validateConfig with case-insensitive network normalization; sepolia warning at line 647. NOTE: REQUIREMENTS.md traceability still marks "Pending" — stale.      |
| SWP-10      | 08.1-03     | Unit tests covering config validation, env loading, strategy selection, intercept    | ✓ SATISFIED   | 3 test files, 34 tests, all pass.                                                                                                                                                                                  |
| SWP-11      | 08.1-03     | Sepolia smoke test — manual verification that Safe proposal appears in Safe UI       | ? NEEDS HUMAN | `docs/safe-proposal-smoke-test.md` exists with full runbook; execution requires live Sepolia + Safe + funded EOA.                                                                                                  |

**Orphaned Requirements Check:** All SWP-01 through SWP-11 are mapped to this phase in REQUIREMENTS.md traceability table and all are accounted for in the plans. No orphaned requirements.

**Traceability Drift:** REQUIREMENTS.md traceability table (lines 107-117) marks SWP-02, SWP-03, SWP-06, SWP-07, SWP-09 as "Pending" while the corresponding checkbox list (lines 50-59) marks them `[ ]` unchecked. Codebase evidence shows all five are implemented and verified. This is a documentation stale-state issue — the traceability table should be updated to "Complete" when the phase ships, but it does NOT indicate a goal-achievement gap.

### Anti-Patterns Found

| File                                                            | Line | Pattern                                                                  | Severity | Impact                                                                                                                                                                                               |
| --------------------------------------------------------------- | ---- | ------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/setup/strategies/SafeProposerRPCDeploymentStrategy.ts` | 155  | "Hardhat not available" (substring of "not yet implemented" scan)        | ℹ️ Info  | False positive — this is a comment describing the IDiamondCut artifact fallback behavior, not a stub marker. The fallback is fully implemented (uses `kIDiamondCutAbi` constant at lines 51-72).     |
| `test/deployment/GeniusDiamondDeployment.test.ts`               | —    | Pre-existing `MODULE_NOT_FOUND` in typechain-types/factories/diamond-abi | ℹ️ Info  | Pre-existing failure unrelated to this phase (typechain not regenerated; needs `npx hardhat typechain`). Does not affect any phase 08.1 artifact. Last touched in commit `8cd78cf` (predates phase). |

No `TBD`, `FIXME`, `XXX`, or unreferenced `TODO` debt markers in any file created or modified by this phase. No stub patterns (`return null`, `return {}`, `return []`, `=> {}`) in helper/strategy/writer implementations.

### Human Verification Required

### 1. Sepolia End-to-End Smoke Test

**Test:** Execute the real proposal command from `docs/safe-proposal-smoke-test.md` Section 3 against a live Sepolia Safe wallet that owns a deployed GeniusDiamond. Prerequisites: funded deployer EOA added as Proposer in Safe UI, Safe Transaction Service available for chainId 11155111.
**Expected:**

- New facets (if any) deploy directly via RPC
- The `diamondCut` transaction is NOT sent directly by the EOA (no diamondCut tx from deployer address on Sepolia explorer)
- A Safe proposal appears in the Safe UI (app.safe.global) Transactions queue
- Proposal target equals GeniusDiamond address
- Proposal calldata starts with `0x1f931c1c` (diamondCut selector)
- Local artifact written under `diamonds/GeniusDiamond/safe-proposals/sepolia-11155111-<timestamp>-<safeTxHash>.json`
- Safe owners can review/sign/execute from the UI
  **Why human:** Per CONTEXT.md D-02, the TS-level Safe SDK proposal submission is validated via the Sepolia smoke test, NOT unit tests. Requires live external services (Safe Transaction Service, Sepolia RPC, funded EOA) and Safe UI interaction that cannot be exercised programmatically.

### 2. Safe Transaction Service Non-Repudiation Check (T-08.1-07)

**Test:** After the proposal is submitted, verify in the Safe Transaction Service that the proposal is persisted with the proposer EOA's address as `senderAddress`.
**Expected:** The proposal is non-repudiable — the Safe Transaction Service records the proposer identity permanently alongside `safeTxHash`.
**Why human:** Requires the proposer EOA to be pre-registered as a Proposer in the Safe UI before submission will be accepted; this configuration step cannot be auto-verified.

### Gaps Summary

No goal-blocking gaps found. All 8 observable truths are verified. All 10 required artifacts exist, are substantive (Level 2), are wired (Level 3), and have real data flowing through them (Level 4). All 34 unit tests pass; no regression in the pre-existing RPCDiamondDeployer test suite. No debt markers or stubs in phase code.

The only outstanding items are the two human verification steps required to validate the TS-level Safe SDK integration against live external services — this is by design per CONTEXT.md decision D-02, which explicitly defers Safe Transaction Service integration validation to the Sepolia smoke test rather than unit tests.

**REQUIREMENTS.md traceability note:** SWP-02, SWP-03, SWP-06, SWP-07, SWP-09 are implemented and verified but their traceability entries still read "Pending". This is a stale-documentation issue, not a goal-achievement gap. The orchestrator may want to update the traceability table when committing the phase close.

---

_Verified: 2026-06-30T02:15:00Z_
_Verifier: Claude (gsd-verifier)_
