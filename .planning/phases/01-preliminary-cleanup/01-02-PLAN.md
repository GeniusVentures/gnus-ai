---
phase: 01-preliminary-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hardhat.config.ts
autonomous: true
requirements:
  - DEBT-06

must_haves:
  truths:
    - "hardhat.config.ts contains no commented-out network configuration blocks"
    - "All currently active networks (sepolia, polygon, mainnet, bsc, base, polygon_amoy, base_sepolia, bsc_testnet) remain present and functional"
    - "The file remains valid TypeScript and the Hardhat config is structurally sound"
  artifacts:
    - path: "hardhat.config.ts"
      provides: "Hardhat network configuration"
      contains: "sepolia:"
      contains: "polygon:"
      contains: "mainnet:"
      contains: "bsc:"
      contains: "base:"
      contains: "polygon_amoy:"
      contains: "base_sepolia:"
      contains: "bsc_testnet:"
      lacks: "arbitrum_sepolia"
      lacks: "arbitrum:"
      lacks: "// local:"
  key_links:
    - from: "hardhat.config.ts networks object"
      to: "yarn hardhat compile"
      via: "TypeScript compilation"
      pattern: "networks:"
---

<objective>
Remove all commented-out network configuration blocks from `hardhat.config.ts` (DEBT-06). The file has accumulated stale commented-out duplicates of active networks and deprecated network configs (arbitrum, local). Keep only the active network entries.

Purpose: Eliminate configuration clutter that creates confusion about which networks are actually configured. Active networks remain untouched.

Output: A clean `hardhat.config.ts` with only active network configurations.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@hardhat.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove commented-out network blocks from hardhat.config.ts</name>
  <files>hardhat.config.ts</files>
  <read_first>
    - hardhat.config.ts (lines 237-324 — the networks object section with commented-out blocks)
  </read_first>
  <action>
    **DEBT-06 — Remove all commented-out network configuration blocks from hardhat.config.ts:**

    READ the file first to see exact current state. Then remove the following entirely:

    **Block 1 (lines 237-241):** Commented-out duplicate of the active `polygon` network. Remove:
    ```
    // polygon: {
    //   url: polygonUrl,
    //   chainId: 137,
    //   accounts: process.env.PRIVATE_KEY !== undefined ? [process.env.PRIVATE_KEY] : [],
    // },
    ```
    Note: the active `polygon` block at lines 248-252 MUST remain.

    **Block 2 (lines 282-324):** All commented-out network blocks after the active `bsc_testnet` entry. Remove the following in their entirety:
    - `// arbitrum_sepolia: { ... }` (lines 282-286)
    - `// arbitrum: { ... }` (lines 287-291)
    - `// base_sepolia: { ... }` (lines 292-296) — active base_sepolia at line 272 remains
    - `// bsc_testnet: { ... }` (lines 297-301) — active bsc_testnet at line 277 remains
    - `// polygon_amoy: { ... }` (lines 302-306) — active polygon_amoy at line 267 remains
    - `// local: { ... }` (lines 307-310)
    - `// mainnet: { ... }` (lines 311-314) — active mainnet at line 253 remains
    - `// bsc: { ... }` (lines 315-319) — active bsc at line 257 remains
    - `// base: { ... }` (lines 320-324) — active base at line 262 remains

    **CRITICAL — do NOT remove:**
    - Any active (non-commented) network entries
    - The closing `},` of the networks object at line 325 (it belongs to `gasReporter` context)
    - Any lines outside the networks section
    - The block comment at lines 16-29 (that's documentation for env vars, not a network block)

    **After removal, verify structural integrity:**
    1. The `networks:` object opens and closes correctly (matching braces)
    2. No trailing commas before closing braces that would cause TypeScript errors
    3. Active networks remain in their current order: hardhat, localhost, hardhat-multichain config, sepolia, polygon, mainnet, bsc, base, polygon_amoy, base_sepolia, bsc_testnet
    4. Run `npx tsc --noEmit hardhat.config.ts` or `yarn hardhat compile` to verify the config is valid
  </action>
  <verify>
    <automated>grep -c "//.*arbitrum\|//.*local:" hardhat.config.ts && echo "FAIL: commented networks remain" || echo "PASS: no commented-out networks"</automated>
    <automated>grep -c "^[^/]*sepolia:" hardhat.config.ts && echo "PASS: sepolia present" || echo "FAIL: active sepolia missing"</automated>
    <automated>yarn hardhat compile</automated>
  </verify>
  <done>
    1. `grep -c "//.*arbitrum\|//.*local:\|//.*polygon:\|//.*mainnet:\|//.*bsc:\|//.*base:" hardhat.config.ts` returns 0 for commented-out network entries (may match non-network comments — verify visually or with targeted grep)
    2. `grep -c "^\s*sepolia:\|^\s*polygon:\|^\s*mainnet:\|^\s*bsc:\|^\s*base:\|^\s*polygon_amoy:\|^\s*base_sepolia:\|^\s*bsc_testnet:" hardhat.config.ts` returns 8 (all 8 active networks present)
    3. `yarn hardhat compile` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript config → Hardhat runtime | hardhat.config.ts is source of truth for network configuration. Removing commented-out blocks must not break the active config structure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | hardhat.config.ts | mitigate | Accidental removal of active network entry or closing brace would break Hardhat compilation. Mitigated by: (1) targeted grep verification that all 8 active network keys remain; (2) `yarn hardhat compile` gate verifies the config is valid TypeScript and Hardhat can parse it. |
| T-01-05 | Information Disclosure | hardhat.config.ts | accept | Commented-out configs contain no secrets (they reference env vars like `process.env.PRIVATE_KEY` via variable names, not literal values). Removing them has no security impact. |
</threat_model>

<verification>
1. `grep -c "^\s*//.*\(arbitrum\|local:\|polygon:\|mainnet:\|bsc:\|base:\)" hardhat.config.ts` — MUST return 0 (no commented-out network blocks with those names)
2. `grep -c "^\s*sepolia:\|^\s*polygon:\|^\s*mainnet:" hardhat.config.ts` — MUST return 3 (active main networks present)
3. `yarn hardhat compile` — MUST exit 0
</verification>

<success_criteria>
- hardhat.config.ts: zero commented-out network configuration blocks
- All 8 active networks (sepolia, polygon, mainnet, bsc, base, polygon_amoy, base_sepolia, bsc_testnet) remain present with their full configuration intact
- Hardhat compilation succeeds with zero errors
</success_criteria>

<output>
Create `.planning/phases/01-preliminary-cleanup/01-02-SUMMARY.md` when done
</output>
