---
phase: 18-scanner-triage-upgrades
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - package.json
  - .github/workflows/security-audit.yml
  - .planning/STATE.md
autonomous: false
requirements:
  - SEC-10

must_haves:
  truths:
    - "yarn semgrep:scan exits 0 with zero findings across 3 consecutive runs on the expanded 119-file surface"
    - "The CI semgrep step is a hard gate: continue-on-error removed, semgrep==1.174.0 pinned, pipefail + tee + artifact upload retained"
    - "Every one of the 37 surfaced findings has a recorded disposition in STATE (D-11/D-12/D-13 + the 13 fixed at source via D-06)"
    - "The 07-03 semgrep promotion follow-up (STATE:138) is closed as executed, and the security-check honesty note reflects the post-promotion gate states"
    - "Full `yarn test:all` at the 666/2/0 + 215/0/5 ledger baseline after all Phase 18 scanner changes (phase-exit gate)"
  artifacts:
    - path: "package.json"
      provides: "D-13-ruled unsafe-math-operation exclusion alongside D-11/D-12"
      contains: "--exclude-rule unsafe-math-operation"
    - path: ".github/workflows/security-audit.yml"
      provides: "Promoted semgrep hard gate on the pinned version"
      contains: "semgrep==1.174.0"
    - path: ".planning/STATE.md"
      provides: "Scanner Baseline Ledger — semgrep leg + 50-row disposition table (37 owner-ruled + 13 fixed at source)"
  key_links:
    - from: ".github/workflows/security-audit.yml"
      to: "package.json semgrep:scan"
      via: "hard-gate invocation through the script"
      pattern: "yarn semgrep:scan"
    - from: ".github/workflows/security-audit.yml"
      to: ".planning/STATE.md"
      via: "header baseline pointer to the Scanner Baseline Ledger"
      pattern: "STATE.md"
---

<objective>
Execute the SEC-10 gate promotion under the D-13 owner ruling: run the blocking checkpoint on the 4 unsafe-math-operation sites, apply the ruled exclusion, prove semgrep zero-findings stability (N runs), promote the CI semgrep step from continue-on-error advisory to a zero-findings hard gate on the pinned 1.174.0, and record the complete semgrep-leg baseline + the 50-row disposition table (37 owner-ruled + 13 fixed at source) in STATE.

Purpose: this is the phase's completion step — the gate cannot go green past the D-13 checkpoint, and Phase 18 ends only when the upgraded gates exit 0 with every finding dispositioned (D-04).
Output: promoted workflow, D-13-ruled script exclusion, Scanner Baseline Ledger semgrep leg + disposition records, closed 07-03 follow-up, phase-exit proof logs under /tmp/18-03-*.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md
@.planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md
@.planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md

# Prior plan outputs this plan consumes:
@.planning/phases/18-scanner-triage-upgrades/18-01-SUMMARY.md
@.planning/phases/18-scanner-triage-upgrades/18-02-SUMMARY.md

# Files being modified (read before editing):
@package.json
@.github/workflows/security-audit.yml
@.planning/STATE.md

<interfaces>
<!-- Current state / handoff state of the edit targets. -->

.github/workflows/security-audit.yml semgrep step (lines ~105-128) — the step being promoted:
- "- name: Semgrep static analysis (advisory baseline)" with `continue-on-error: true` (line ~114 — the ONLY continue-on-error in the file)
- run block: `set -o pipefail` (CR-01 comment — KEEP), `pip install semgrep` (unpinned — floats to 1.175.0 today; D-07 closes it), `yarn semgrep:scan | tee semgrep-scan.log`
- Followed by the "Upload semgrep scan output" step (if: always()) — KEEP untouched
- The ADVISORY BOOTSTRAP comment above the step names the promotion condition — REWRITE it as the executed promotion record
- Header (lines ~8-14): the semgrep half of the baseline sentence ("13 semgrep INFO-lint findings in one file") is superseded by this plan; the slither half was already updated by 18-01

package.json semgrep:scan (post-18-02 state): `semgrep scan --config .semgrep.yml --exclude-rule weak-encryption --exclude-rule diamond-selector-clash --exclude-rule hard-coded-secret --no-git-ignore` + 15 `--exclude` flags + `--error`. This plan adds exactly one `--exclude-rule unsafe-math-operation`.

STATE.md anchors (append-only — supersede via new ledger rows, never edit history):
- "## Scanner Baseline Ledger (canonical)" — created by 18-01 (slither leg); this plan appends the semgrep leg under it
- 07-03 rows to close/supersede by reference: semgrep 13-finding baseline row, promotion follow-up ("Promote the CI semgrep step ... once ... confirmed stable across runs"), security-check honesty note
- 07-03 disposition-table format (sub-command | result | class | evidence) — the shape for the 50-row record

The D-13 4-site table (from 18-RESEARCH Pitfall 2, full finding list in /tmp/18-research/probeE-final-sim.json):
| # | Site | Rule | Class |
|---|------|------|-------|
| 1 | contracts/gnus-ai/GNUSBridge.sol:110 | unsafe-math-operation | $A * $B / $C in Solidity >=0.8 checked arithmetic |
| 2 | contracts/gnus-ai/GNUSBridgeAttestor.sol:393 | unsafe-math-operation | same idiom |
| 3 | contracts/gnus-ai/GNUSTreasury.sol:126 | unsafe-math-operation | same idiom |
| 4 | contracts/gnus-ai/GNUSTreasury.sol:137 | unsafe-math-operation | same idiom |
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-13 owner checkpoint — unsafe-math-operation (4 sites) permanent vs temp exclusion + routing</name>
  <read_first>
  - .planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md (D-04, D-13)
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Pitfall 2 — the disposition set; Open Question 1)
  - .planning/STATE.md ("Phase 7 Decisions Logged (07-03)" — D-09 routing-event precedent + disposition-table format)
  - package.json (current semgrep:scan flags)
  </read_first>
  <files>/tmp/18-03-precheckpoint.log</files>
  <action>
  Automate first, then pause. Run `yarn semgrep:scan` captured to /tmp/18-03-precheckpoint.log (redirect form, exit code recorded). Verify the finding set is EXACTLY the 4 unsafe-math-operation sites in the table below (exit 1 is the expected honest red). If the set differs (count ≠ 4, different rules, or new sites): re-baseline BEFORE pausing per D-04 — scripts/TS-side extras are fixed in-phase first; contract-side extras are appended to the checkpoint table. Make NO repo edits (the exclusion is applied only in Task 2 after the ruling), then present the 4-site table with both options to the owner and PAUSE for the ruling. Promotion (Task 3) cannot proceed without it (D-13).
  </action>
  <verify>
    <automated>yarn semgrep:scan > /tmp/18-03-precheckpoint.log 2>&1; echo "exit=$?" | grep -q "^exit=1$" && grep -c "unsafe-math-operation" /tmp/18-03-precheckpoint.log</automated>
  </verify>
  <what-built>
  Plans 18-01 and 18-02 landed: slither gate is bare-triage green on 0.11.6; the semgrep rule executes (D-09 proven 2→0); the surface reaches the submodule with vendored noise excluded; D-11 (diamond-selector-clash) and D-12 (hard-coded-secret) exclusions are applied; the 13 typescript-any-usage findings are typed away at source.

  BEFORE pausing, verify the live pre-checkpoint state and capture it to /tmp/18-03-precheckpoint.log: run `yarn semgrep:scan` — the finding set MUST be exactly the 4 unsafe-math-operation sites below (exit 1). If the set differs (count ≠ 4, different rules, or new sites): re-baseline first per D-04 — scripts/TS-side extras get fixed in-phase before the checkpoint; contract-side extras get added to the table below — then pause.
  </what-built>
  <decision>
  Disposition of the 4 `unsafe-math-operation` findings — the last unresolved class of the 37-item owner-ruled set. Per D-04 no contract-source fix is possible in Phase 18 (deployed-facet fixes require diamond-cut upgrade discipline and belong to an owning phase), and per D-10 no nosemgrep annotation is allowed. The ruling decides the exclusion's permanence and whether a precision review is routed:

  | # | Site | Detail |
  |---|------|--------|
  | 1 | GNUSBridge.sol:110 | `$A * $B / $C` idiom — checked arithmetic in Solidity >=0.8 |
  | 2 | GNUSBridgeAttestor.sol:393 | same idiom |
  | 3 | GNUSTreasury.sol:126 | same idiom |
  | 4 | GNUSTreasury.sol:137 | same idiom |

  Research context: likely false-positive class (0.8 checked arithmetic means the multiply cannot overflow silently; the real risk is mul-div precision/rounding, which this rule does not measure), but the sites are contract-side so the owner rules.
  </decision>
  <context>
  The semgrep gate promotion (Task 3) cannot go green without this ruling — exclusion is the only in-phase lever for contract-side findings (D-04/D-10/D-13). The choice determines whether the exclusion carries a lift condition + routing event (like D-12) or is permanent with rationale (like D-11).
  </context>
  <options>
    <option id="option-a">
      <name>Permanent exclusion + STATE rationale (D-11 pattern)</name>
      <pros>Matches the rule's actual coverage gap — it flags the checked-arithmetic idiom itself, not precision; no synthetic follow-up debt; zero maintenance</pros>
      <cons>Permanently blinds this rule to the idiom repo-wide, including in future facets where precision ordering might matter</cons>
    </option>
    <option id="option-b">
      <name>Temporary exclusion + mul-div precision routing event (D-12 pattern)</name>
      <pros>Keeps the finding visible as a tracked routing event to an owning phase that reviews the 4 sites' precision under diamond-cut upgrade discipline; exclusion carries a recorded lift condition</pros>
      <cons>Leaves a standing exclusion + open routing item; the owning phase inherits 4 low-suspicion sites</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b (or rule on individual sites, e.g. "option-a for all", "b for Treasury only")</resume-signal>
  <acceptance_criteria>
  - /tmp/18-03-precheckpoint.log exists and shows exactly the 4 unsafe-math-operation findings at GNUSBridge.sol:110, GNUSBridgeAttestor.sol:393, GNUSTreasury.sol:126, GNUSTreasury.sol:137 (or the re-baselined superset table per D-04)
  - The 4-site table was presented to the owner with both options before any exclusion was applied
  - The owner's ruling is recorded verbatim in the task summary (option-a / option-b / per-site ruling)
  - No repo file was modified before the ruling (package.json untouched at checkpoint time)
  </acceptance_criteria>
  <done>Pre-checkpoint evidence captured (exactly the 4 unsafe-math-operation findings, or the re-baselined superset per D-04); the 4-site table presented with both options; the owner's D-13 ruling recorded verbatim; zero repo files modified before the ruling.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the D-13 ruling + prove semgrep zero-findings across N runs</name>
  <files>package.json</files>
  <read_first>
  - package.json (semgrep:scan line — post-18-02 flag string)
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Pattern 2/3 — stability proof shape; Pitfall 6 — exclusion integrity re-check)
  - .planning/STATE.md ("Test Baseline Ledger (canonical)" per-run proof-table format)
  </read_first>
  <action>
  Add exactly one flag to the `semgrep:scan` script in package.json per the Task-1 ruling: `--exclude-rule unsafe-math-operation`. The spelling is identical for a permanent or temporary exclusion — the difference lives only in the STATE record (Task 3). If the owner ruled per-site, apply the minimal script-level form that satisfies the ruling and record the per-site detail in Task 3 (there is no per-site exclude mechanism at script level — a per-site ruling that cannot be expressed as a rule exclusion becomes a routing event instead; surface this if it arises).

  Then the stability proof (Phase-17 N-run harness pattern): run `yarn semgrep:scan` three consecutive times, each captured separately to /tmp/18-03-semgrep-1.log, /tmp/18-03-semgrep-2.log, /tmp/18-03-semgrep-3.log with exit codes recorded (capture form `> /tmp/18-03-semgrep-N.log 2>&1` — the "Findings: 0 (0 blocking)" footer the verify chain asserts arrives on stderr; verified 2026-08-31) — every run must exit 0 and report zero findings. Build the per-run table (run #, date, result, wall time) for the Task-3 STATE entry.

  Post-edit integrity re-checks on the final run's log (Pitfall 6 + Pitfall 1 + D-14): zero weak-encryption / diamond-selector-clash / hard-coded-secret findings (all exclusions still hold after the script edit); zero paths under lib/forge-std, typechain-types/, node_modules/, or test/; the rule-parse-error count is zero; `--config .semgrep.yml` still the relative reference.
  </action>
  <verify>
    <!-- [ -s ] guard inside both loops: on a MISSING log the grep errors (rc 2) instead of matching, so `grep ... && exit 1` never fires and the loop would pass vacuously — the guard fails the chain honestly (verified 2026-08-31). "Findings: 0 (0 blocking)" asserts each run's zero-findings outcome via semgrep's Scan Summary footer (footer lands on stderr; captures use 2>&1 — confirmed 2026-08-31); the (^| )test/ alternation mirrors 18-02 Task 2 (D-14). -->
    <automated>grep -q -- "--exclude-rule unsafe-math-operation" package.json && for i in 1 2 3; do [ -s /tmp/18-03-semgrep-$i.log ] || exit 1; grep -q "Findings: 0 (0 blocking)" /tmp/18-03-semgrep-$i.log || exit 1; grep -qi "Rule parse error" /tmp/18-03-semgrep-$i.log && exit 1; done; for i in 1 2 3; do [ -s /tmp/18-03-semgrep-$i.log ] || exit 1; grep -qE "lib/forge-std|typechain-types/|node_modules/|(^| )test/" /tmp/18-03-semgrep-$i.log && exit 1; done; echo "stability-logs-clean"</automated>
  </verify>
  <acceptance_criteria>
  - package.json semgrep:scan contains `--exclude-rule unsafe-math-operation` and the diff shows exactly one added flag
  - Three consecutive `yarn semgrep:scan` runs each exit 0 with zero findings (logs /tmp/18-03-semgrep-{1,2,3}.log; exit codes captured in the per-run table)
  - Final-run log contains zero "Rule parse error" lines and zero findings/findings-paths from weak-encryption, diamond-selector-clash, hard-coded-secret, lib/forge-std, typechain-types/, node_modules/, or test/
  - `--config .semgrep.yml` relative reference unchanged
  </acceptance_criteria>
  <done>Semgrep gate green and stable (3× exit 0, zero findings) with all exclusions intact.</done>
</task>

<task type="auto">
  <name>Task 3: Promote the CI semgrep gate to hard gate + write the semgrep-leg ledger and phase-exit proof</name>
  <files>.github/workflows/security-audit.yml, .planning/STATE.md</files>
  <read_first>
  - .github/workflows/security-audit.yml (full — header lines ~8-15, semgrep step ~105-128, upload step, and the WR-01/snyk zero-finding comment as the wording model)
  - .planning/STATE.md (Scanner Baseline Ledger created by 18-01; 07-03 rows 131/136-139 being closed; 07-03 disposition-table format)
  - .planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md (workflow section — promotion step shape, pin-comment convention, WR-01 wording)
  - .planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md (D-07, D-11, D-12, D-13, D-14)
  - .planning/phases/18-scanner-triage-upgrades/18-02-SUMMARY.md (surface figures, D-09 proof results, devcontainer mirror commits)
  </read_first>
  <action>
  Promote the workflow semgrep step (D-07 + the 07-03 promotion condition, now satisfied):
  - DELETE the `continue-on-error: true` line (the file's only one).
  - Replace the unpinned `pip install semgrep` with `pip install semgrep==1.174.0`, with a pin comment in the existing parity convention: exact parity with the local brew 1.174.0 that produced the committed zero baseline; note the pin is load-bearing (unpinned resolves to 1.175.0 today and exit semantics were proven version-sensitive on 1.174.0).
  - Rename the step "Semgrep static analysis (advisory baseline)" to "Semgrep static analysis (hard gate)".
  - REWRITE the ADVISORY BOOTSTRAP comment into the executed promotion record, modeled on the WR-01 closure wording: promotion executed per the 07-03 condition (baseline confirmed stable across runs — 3 consecutive zero-finding runs); red now means a NEW finding, nothing recorded; all four rule exclusions (weak-encryption 07-02, diamond-selector-clash D-11, hard-coded-secret D-12, unsafe-math-operation D-13) are owner-ruled with rationale + lift conditions in the Scanner Baseline Ledger.
  - KEEP unchanged: the CR-01 `set -o pipefail` comment and line, the `yarn semgrep:scan | tee semgrep-scan.log` line, and the entire "Upload semgrep scan output" step.
  - Header: update the semgrep half of the baseline sentence (lines ~10-13) — the 13-finding advisory baseline is superseded by the zero-finding hard gate + the exclusion ledger under the Scanner Baseline Ledger.

  STATE.md — append the semgrep leg to `## Scanner Baseline Ledger (canonical)`:
  - Scanner baseline entry: semgrep 1.174.0 pin (CI + brew parity), surface definition (`--no-git-ignore` + the 15 excludes; recorded figure ~119 files / 42 .sol = 34 submodule + 8 mocks; test/ excluded by the bundled default ignore per D-14), D-09 executes-proof results (positive control exactly 2 at GNUSContractAssets.sol:48 + TransferHelper.sol:52; shipped rule 0), the 3-run stability table with /tmp/18-03-* paths, and the devcontainer mirror-fix commits + gitlink bump from 18-02.
  - The 50-row disposition table (37 owner-ruled + 13 fixed at source) in the 07-03 format: 31 diamond-selector-clash → D-11 permanent exclusion, rationale (structural FP generator for diamond facets; the rule's `bytes4 public constant $SELECTOR = $VALUE;` escape hatch matches no facet idiom; real selector-clash coverage lives in diamond-cut tooling); 2 hard-coded-secret → D-12 temporary exclusion + routing event upstream to diamondslab/diamonds-devcontainer (commented-out code at .devcontainer/scripts/setup/VaultSecretManager.ts:34,91), lift condition = upstream cleans the file; 4 unsafe-math-operation → the Task-1 D-13 ruling verbatim (permanent rationale or temp + routing event + lift condition); 13 typescript-any-usage → fixed at source in-phase (D-06).
  - Close the 07-03 promotion follow-up: add the executed-marker row (date, plan 18-03, condition satisfied via the 3-run proof) referencing the original follow-up line — append-only, do not edit the 07-03 rows.
  - Update the security-check honesty note for the post-promotion chain state: semgrep and slither legs are now zero-finding hard gates; the chain's remaining red legs are the pre-existing routing events (snyk/osv/git-secrets per 07-03), unchanged by Phase 18.
  - Post-edit sanity: re-run both gates locally (`yarn semgrep:scan`, `yarn slither:scan` — both exit 0) and YAML-parse the workflow.

  Phase-exit gate (before declaring the phase done): with the Foundry bridge node up (`npx hardhat node` on 127.0.0.1:8545 first — bare `forge test` without the wrapper node is NOT a valid gate), run `yarn test:all` captured to /tmp/18-03-testall.log — expect the ledger baseline 666/2/0 Hardhat + 215/0/5 Foundry (220 total, 37 suites). Any deviation is a stop-and-investigate, not a re-baseline.
  </action>
  <verify>
    <!-- Workflow YAML parse uses /usr/local/bin/python3 — the default python3 on this machine (brew 3.14.7 and /usr/bin/python3 alike) lacks PyYAML; the interpreter path is load-bearing. -->
    <automated>grep -q "semgrep==1.174.0" .github/workflows/security-audit.yml && [ "$(grep -v '^\s*#' .github/workflows/security-audit.yml | grep -c 'continue-on-error')" -eq 0 ] && grep -q "set -o pipefail" .github/workflows/security-audit.yml && /usr/local/bin/python3 -c "import yaml; yaml.safe_load(open('.github/workflows/security-audit.yml'))" && yarn semgrep:scan > /tmp/18-03-final-semgrep.log 2>&1; echo "exit=$?" | grep -q "exit=0" && yarn slither:scan > /tmp/18-03-final-slither.log 2>&1; echo "exit=$?" | grep -q "exit=0" && grep -q "666 passing" /tmp/18-03-testall.log</automated>
  </verify>
  <acceptance_criteria>
  - .github/workflows/security-audit.yml contains `pip install semgrep==1.174.0`; `grep -v '^\s*#' <workflow> | grep -c "continue-on-error"` returns 0; step renamed to the hard-gate name; the pipefail line and `| tee semgrep-scan.log` and the upload step are present and unchanged
  - /usr/local/bin/python3 yaml.safe_load parses the workflow (the default python3 lacks PyYAML — the interpreter path is load-bearing)
  - Header baseline sentence no longer references "13 semgrep INFO-lint findings" as the tolerated baseline
  - STATE.md Scanner Baseline Ledger contains: the semgrep-leg entry (pin, surface, D-09 proof, 3-run table), the 50-row disposition table (37 owner-ruled + 13 fixed at source) with D-11/D-12/D-13 records and lift conditions where temporary, the executed-marker for the 07-03 promotion follow-up, and the updated security-check honesty note
  - Local gates both exit 0 post-edit (/tmp/18-03-final-{semgrep,slither}.log)
  - /tmp/18-03-testall.log shows 666 passing / 2 pending / 0 failing Hardhat and 215 passed / 0 failed / 5 skipped Foundry
  - Historical 07-03 rows are byte-untouched (append-only ledger)
  </acceptance_criteria>
  <done>SEC-10 complete: CI semgrep step is a zero-findings hard gate on the pinned version, every surfaced finding dispositioned, baselines single-sourced in STATE, phase-exit gates green at the ledger baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner ruling → script exclusion + STATE record | the D-13 decision crosses from human authority into committed gate config (recorded rationale is the audit trail) |
| CI runner → pinned semgrep + promoted gate | install under exact pin; gate exit now blocks CI (was advisory) |
| workflow header → STATE ledger | committed baseline pointer keeps suppression state reviewable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | D-13 exclusion | mitigate | Blocking owner checkpoint BEFORE any exclusion (Task 1, gate="blocking"); ruling recorded verbatim; permanent-vs-temporary distinction + lift condition live in STATE |
| T-18-11 | Tampering | CI semgrep pin | mitigate | D-07: exact pin `semgrep==1.174.0` (unpinned floats to 1.175.0 today; exit semantics proven version-sensitive) |
| T-18-12 | DoS | promoted gate that can never red | mitigate | CR-01 pipefail comment + line retained; `--error` retained (findings exit 1, rule parse errors exit 2); tee + artifact upload retained so CI output stays diffable |
| T-18-13 | Repudiation | baseline drift unrecorded | mitigate | STATE Scanner Baseline Ledger is the single committed baseline home (surface + exclusions + stability proof); workflow header points at it; append-only supersession of the 07-03 rows |
| T-18-14 | Tampering | suppression creep (4th rule exclusion) | mitigate | Every exclusion now carries an owner ruling + STATE rationale/lift condition; the disposition table enumerates all 50 findings (37 owner-ruled + 13 fixed at source) so nothing is suppressed silently; no nosemgrep in contract source (D-10) |
| T-18-SC | Tampering | CI pip installs | mitigate | Package Legitimacy Audit in 18-RESEARCH.md: semgrep Approved (PyPI + local official binary + Homebrew cross-verified); no [ASSUMED]/[SUS]/[SLOP] — no blocking checkpoint required |
</threat_model>

<verification>
- Workflow: semgrep==1.174.0 pinned; zero non-comment continue-on-error; pipefail/tee/upload intact; YAML parses (via /usr/local/bin/python3 — the only local interpreter with PyYAML); slither leg from 18-01 untouched by this plan
- Gates: `yarn semgrep:scan` exit 0 (3 consecutive + final post-edit); `yarn slither:scan` exit 0 (18-01 wiring intact)
- STATE: semgrep leg + 50-row disposition table (37 owner-ruled + 13 fixed at source) + closed 07-03 follow-up + updated honesty note; 07-03 history untouched
- Phase-exit: `yarn test:all` at 666/2/0 + 215/0/5 via the bridge node
</verification>

<success_criteria>
- SEC-10 delivered in full: fixed pattern parses AND executes (proven in 18-02), CI step promoted to hard gate on a stable recorded baseline with zero findings
- Phase 18 end-state per D-04: both upgraded gates exit 0 with every finding dispositioned; no contract-source changes, no test-suite changes, no new dependencies
- CI observation on next push is expected green for both promoted steps (workflow-run verification belongs to the ship/verify flow, not this plan)
</success_criteria>

<output>
Create `.planning/phases/18-scanner-triage-upgrades/18-03-SUMMARY.md` when done
</output>
