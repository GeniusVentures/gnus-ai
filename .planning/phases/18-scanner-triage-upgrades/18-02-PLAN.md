---
phase: 18-scanner-triage-upgrades
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .semgrep.yml
  - .devcontainer/config/.semgrep.yml
  - package.json
  - scripts/utils/GNUSLifecyclePolicyLinking.ts
autonomous: true
requirements:
  - SEC-10

must_haves:
  truths:
    - "The unsafe-external-call rule parses (zero 'Rule parse error' in output) and demonstrably matches — positive control with suppressions dropped yields EXACTLY the 2 known checked .call{} sites"
    - "yarn semgrep:scan reaches the contracts/gnus-ai submodule (real .sol surface scanned) with zero vendored/generated noise (no lib/forge-std, typechain-types/, node_modules/, or test/ paths)"
    - "scripts/utils/GNUSLifecyclePolicyLinking.ts contains zero ': any' annotations, keeps zero top-level runtime imports, and still loads during Hardhat config load (full Hardhat suite at the 666/2/0 ledger baseline)"
    - "After this plan the ONLY remaining yarn semgrep:scan findings are the 4 unsafe-math-operation sites awaiting the D-13 owner ruling in plan 18-03"
  artifacts:
    - path: ".semgrep.yml"
      provides: "Fixed unsafe-external-call rule (generic mode, executes-proven)"
      contains: "languages: [generic]"
    - path: ".devcontainer/config/.semgrep.yml"
      provides: "Mirror of the fixed rule (rule-scoped only)"
      contains: "languages: [generic]"
    - path: "package.json"
      provides: "Expanded scan surface + owner-ruled exclusions"
      contains: "--no-git-ignore"
    - path: "scripts/utils/GNUSLifecyclePolicyLinking.ts"
      provides: "Zero-any typed linking harness"
  key_links:
    - from: "package.json semgrep:scan"
      to: ".semgrep.yml"
      via: "relative --config reference (rule-ID prefixing breaks --exclude-rule if changed — Pitfall 6)"
      pattern: "--config .semgrep.yml"
    - from: "hardhat.config.ts"
      to: "scripts/utils/GNUSLifecyclePolicyLinking.ts"
      via: "import during config load (type-only-import constraint)"
      pattern: "GNUSLifecyclePolicyLinking"
---

<objective>
Execute the SEC-10 scanner-side groundwork (D-06, D-08, D-09, D-10, D-11, D-12, D-14): replace the broken unsafe-external-call rule with the verified generic-mode rule in both .semgrep.yml and its .devcontainer mirror, expand the scan surface to the contracts/gnus-ai submodule with the compensating exclusion set, apply the two locked owner-ruled rule exclusions, prove the rule executes via the D-09 positive control, and type away the 13 `typescript-any-usage` sites under the type-only-import constraint.

Purpose: the rule as committed today never runs (silent parse failure) and the scan as scripted today can never reach the real call sites — this plan makes the scanner honest before plan 18-03 promotes the CI gate.
Output: fixed rule in two files (nested-submodule commit + gitlink bump for the mirror), expanded-and-calibrated `yarn semgrep:scan`, zero-`any` linking harness, D-09 proof logs under /tmp/18-02-*.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md
@.planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md
@.planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md

# Files being modified (read before editing):
@.semgrep.yml
@.devcontainer/config/.semgrep.yml
@package.json
@scripts/utils/GNUSLifecyclePolicyLinking.ts

<interfaces>
<!-- Current state of the edit targets (verified 2026-08-31). -->

package.json scripts (line 44 is the D-10/D-11/D-12 edit target):
- "semgrep:scan": "semgrep scan --config .semgrep.yml --exclude-rule weak-encryption --error"
- The `--config .semgrep.yml` relative reference is LOAD-BEARING (Pitfall 6: loading from a different path prefixes rule IDs and defeats --exclude-rule) — keep it exactly.

.semgrep.yml: the broken rule occupies lines 10-16 (two plain-scalar patterns `".call{"` and `require(success`; languages: [solidity]). The verified replacement lives in 18-RESEARCH.md "Code Examples" → "The verified fixed rule" — copy it verbatim; block scalars (`pattern: |`) are load-bearing because `$K: $V` breaks plain-scalar YAML.

.devcontainer/config/.semgrep.yml: identical broken rule at lines 11-17. The two files DIVERGE beyond this rule (devcontainer copy lacks paths: exclude blocks on 3 rules, has an extra pattern-not-inside on diamond-selector-clash, trailing whitespace, no final newline) — mirror-fix scope is the unsafe-external-call rule ONLY (D-08). This file is inside the .devcontainer NESTED SUBMODULE: commit inside .devcontainer first, then stage the gitlink in gnus-ai (repo pattern per STATE 07-01). Run on the main tree, not a worktree.

scripts/utils/GNUSLifecyclePolicyLinking.ts — the 13 `: any` sites (verified at exact line numbers):
- 77: async function chainKey(signer?: any, hre?: any): Promise<string>  [2 sites]
- 94: function runtimeHre(): any
- 104: export async function deployAndLinkLifecyclePolicy(hre?: any): Promise<string>
- 129: export async function deployAndLinkLifecyclePolicyWithSigner(signer: any): Promise<string>
- 152: function patchGetContractFactory(hre: any, lazyDeploy: boolean): void
- 158: const ethersRef: any = hre.ethers;
- 160: ethersRef.getContractFactory = async (nameOrAbi: any, opts?: any) => {...}  [2 sites]
- 162: let artifact: any;
- 170: (byFile: any) => Object.keys(byFile ?? {}).includes(LIBRARY_NAME),
- 220: export async function installLifecyclePolicyLinker(libraryAddress: string, hre?: any): Promise<void>
- 235: export function installLazyLifecyclePolicyLinker(hre: any): void

Binding constraint (file header lines 42-45, CONFIG-LOAD SAFETY): the module must NOT `import ... from 'hardhat'` at top level — hardhat.config.ts imports it during config loading and the main entry throws LIB_IMPORTED_FROM_THE_CONFIG. The file currently has ZERO top-level imports; that must remain effectively true at runtime. The runtimeHre() lazy-require body + both eslint-disable comments (lines 93-97) stay untouched.

Consumers: hardhat.config.ts:14 (import) + :28-29 (extendEnvironment wiring); ~15 test suites call setupLifecyclePolicyLinking() in before hooks. tsconfig.json is strict: true and includes ./scripts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the broken rule in .semgrep.yml + mirror it in .devcontainer/config/.semgrep.yml (D-08)</name>
  <files>.semgrep.yml, .devcontainer/config/.semgrep.yml</files>
  <read_first>
  - .semgrep.yml (full — the broken rule at lines 10-16, plus the paths: conventions on the 3 diamond rules and the block-scalar precedent on expensive-operation-in-loop)
  - .devcontainer/config/.semgrep.yml (full — the identical broken rule at lines 11-17 and the documented divergences)
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Code Examples → "The verified fixed rule" — copy-ready, proven end-to-end on this tree; Anti-Patterns → block-scalar requirement + do-not-full-sync warning)
  - .planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md (".devcontainer/config/.semgrep.yml" section — nested-submodule commit mechanics)
  </read_first>
  <action>
  Replace the entire unsafe-external-call rule block in .semgrep.yml (current lines 10-16) with the RESEARCH-verified rule VERBATIM from 18-RESEARCH.md "Code Examples" → "The verified fixed rule": `patterns:` containing a `pattern-either` of the two block-scalar destructure shapes, the four `pattern-not-inside` suppression blocks, the unchanged message, `severity: WARNING`, `languages: [generic]` (NOT solidity — native-Solidity patterns do not parse on 1.174.0), and `paths: include: ["**/*.sol"]`. Do not re-derive or reformat it — every pattern line uses block scalars (`pattern: |`) because `$K: $V` breaks plain-scalar YAML parsing.

  Mirror the identical rule body into .devcontainer/config/.semgrep.yml lines 11-17 (the same seven-line broken block). Scope discipline (D-08): replace ONLY the unsafe-external-call rule — do not sync any other rule, do not touch the trailing-whitespace/final-newline divergences, do not touch the extra diamond-selector-clash pattern-not-inside that exists only in the devcontainer copy.

  Nested-submodule mechanics for the mirror: commit inside .devcontainer first (message: "fix(security): mirror unsafe-external-call generic-mode rule from gnus-ai"), then `git add .devcontainer` in gnus-ai to stage the gitlink bump alongside this plan's other changes. Work on the main tree (worktrees lose nested-submodule commits). Then verify both replaced blocks are text-identical and both files still parse as YAML.
  </action>
  <verify>
    <automated>grep -A2 "id: unsafe-external-call" .semgrep.yml | grep -q "languages: \[generic\]" && grep -c "pattern-not-inside" .semgrep.yml && python3 -c "import yaml,sys; [yaml.safe_load(open(f)) for f in ['.semgrep.yml','.devcontainer/config/.semgrep.yml']]" && ! grep -q "require(success" .semgrep.yml && ! grep -q "require(success" .devcontainer/config/.semgrep.yml && git -C .devcontainer status --porcelain | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
  - The unsafe-external-call rule in .semgrep.yml contains `languages: [generic]`, a `pattern-either`, exactly four `pattern-not-inside` entries, and `include: ["**/*.sol"]` under paths
  - Zero occurrences of `require(success` as pattern text in either file
  - Every pattern line in the replaced rule uses block-scalar form (`pattern: |` or `pattern-not-inside: |`)
  - The two replaced rule bodies are text-identical (diff the extracted blocks)
  - Both files parse via python3 yaml.safe_load
  - .devcontainer has its own commit containing only the rule-block change (`git -C .devcontainer status` clean afterward); the gnus-ai gitlink for .devcontainer is staged/committed with this plan
  - git diff of .devcontainer/config/.semgrep.yml shows changes confined to the unsafe-external-call rule block
  </acceptance_criteria>
  <done>Fixed rule live in both configs; nested-submodule commit landed and gitlink bumped; YAML valid.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the scan surface + D-11/D-12 exclusions + D-09 executes-proof (D-09, D-10, D-11, D-12, D-14)</name>
  <files>package.json</files>
  <read_first>
  - package.json (scripts block — line 44 semgrep:scan, line 54 security-check)
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Pattern 2 executes-proof procedure; Pattern 3 exclusion list + verified 119-file/42-.sol end-state; Pitfall 1 flood; Pitfall 2 the 37-item set; Pitfall 6 rule-ID prefixing)
  - .planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md (D-09, D-10, D-11, D-12, D-14)
  </read_first>
  <action>
  Edit the `semgrep:scan` script in package.json (line 44) in ONE change — keep `--config .semgrep.yml` exactly as-is (Pitfall 6) and keep `--error`:
  - Add `--no-git-ignore` (D-10 — reaches the contracts/gnus-ai submodule; alone it floods 81→341 files / 13→168 findings)
  - Add the 15 verified `--exclude` flags from RESEARCH Pattern 3: `--exclude lib/** --exclude typechain-types/** --exclude typechain/** --exclude out/** --exclude cache/** --exclude cache_forge/** --exclude artifacts/** --exclude broadcast/** --exclude diamond-abi/** --exclude diamond-typechain-types/** --exclude dist/** --exclude logs/** --exclude tmp/** --exclude notes/** --exclude .yarn/**` (they must ship in the SAME change as --no-git-ignore — Pitfall 1)
  - Add `--exclude-rule diamond-selector-clash` (permanent, D-11) and `--exclude-rule hard-coded-secret` (temporary, D-12) alongside the existing `--exclude-rule weak-encryption`

  Then run the three proofs (all output captured under /tmp/18-02-*, redirect form):
  - Surface check: `yarn semgrep:scan` → the scan reaches contracts/gnus-ai (expected recorded end-state: ~119 files, 42 .sol = 34 submodule + 8 mocks) and NO findings or scanned paths under lib/forge-std, typechain-types/, node_modules/, or test/ (Pitfall 1 warning signs + D-14: test/ stays excluded by semgrep's bundled default ignore).
  - D-09 positive control: build /tmp/18-02-positive-rule.yml = the fixed rule from .semgrep.yml with the four pattern-not-inside blocks DELETED (rebuild it; the research-session template /tmp/18-research/probe-rule-positive.yml may be gone). Run semgrep directly mirroring the script's flags except --config and --error: `semgrep scan --config /tmp/18-02-positive-rule.yml --exclude-rule weak-encryption --exclude-rule diamond-selector-clash --exclude-rule hard-coded-secret --no-git-ignore` plus the same 15 `--exclude` flags, captured to /tmp/18-02-positive-control.log. Expect EXACTLY 2 findings: contracts/gnus-ai/GNUSContractAssets.sol:48 and contracts/gnus-ai/libraries/TransferHelper.sol:52 — no other count passes.
  - Shipped-rule proof: `yarn semgrep:scan` (through the script, with --error) → zero "Rule parse error" lines in output; unsafe-external-call contributes 0 findings; the total remaining finding set at this task's end is EXACTLY 17: the 13 typescript-any-usage findings in scripts/utils/GNUSLifecyclePolicyLinking.ts (typed away in Task 3) + the 4 unsafe-math-operation findings (GNUSBridge.sol:110, GNUSBridgeAttestor.sol:393, GNUSTreasury.sol:126, GNUSTreasury.sol:137 — awaiting the D-13 ruling in plan 18-03). Exit 1 at this stage is the expected honest red.
  - Also confirm the exclusions hold post-edit (Pitfall 6): zero weak-encryption, diamond-selector-clash, or hard-coded-secret findings in the output.

  If the finding set differs from the expected 17 (beyond line-number drift in the same rules/classes), STOP and disposition the extras per D-04 before proceeding: scripts/TS-side findings get fixed in Task 3; contract-side findings get added to the D-13 checkpoint table in plan 18-03 (never fixed here, never nosemgrep-annotated). Record every probe's output path in the task summary. STATE rationale records for D-11/D-12 land in plan 18-03's ledger entry (single STATE write for the semgrep leg).
  </action>
  <verify>
    <automated>grep -q -- "--no-git-ignore" package.json && [ "$(grep -o -- '--exclude ' package.json | wc -l)" -ge 15 ] && grep -q -- "--exclude-rule diamond-selector-clash" package.json && grep -q -- "--exclude-rule hard-coded-secret" package.json && ! grep -qi "Rule parse error" /tmp/18-02-shipped-rule.log && ! grep -qE "lib/forge-std|typechain-types/|node_modules/|(^| )test/" /tmp/18-02-shipped-rule.log</automated>
  </verify>
  <acceptance_criteria>
  - package.json semgrep:scan contains `--config .semgrep.yml` (unchanged), `--error`, `--no-git-ignore`, all 15 `--exclude` flags, and `--exclude-rule` for weak-encryption, diamond-selector-clash, and hard-coded-secret
  - Positive-control run (/tmp/18-02-positive-control.log) reports exactly 2 findings at contracts/gnus-ai/GNUSContractAssets.sol:48 and contracts/gnus-ai/libraries/TransferHelper.sol:52
  - Shipped-rule run (/tmp/18-02-shipped-rule.log) contains zero "Rule parse error" lines and zero findings outside the expected 17 (13 typescript-any-usage in GNUSLifecyclePolicyLinking.ts + 4 unsafe-math-operation at the named sites)
  - Zero weak-encryption / diamond-selector-clash / hard-coded-secret findings (exclusions intact after the script edit)
  - Zero scanned paths under lib/forge-std, typechain-types/, node_modules/, or test/ (surface calibrated; D-14 holds)
  - The 42-.sol surface figure (34 submodule + 8 mocks) recorded in the task summary
  </acceptance_criteria>
  <done>Scanner surface includes the submodule without vendored noise; rule proven to execute (2→0); gate red only on the known 13+4 findings awaiting Tasks 3 and the D-13 checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Type the 13 `any` sites in GNUSLifecyclePolicyLinking.ts (D-06) + regression proof</name>
  <files>scripts/utils/GNUSLifecyclePolicyLinking.ts</files>
  <read_first>
  - scripts/utils/GNUSLifecyclePolicyLinking.ts (full 248-line file — the CONFIG-LOAD SAFETY header, runtimeHre() lazy-require convention, the interception body at lines 160-210, and all 13 sites)
  - hardhat.config.ts (consumer — import at line 14, extendEnvironment at lines 28-29)
  - tsconfig.json (strict: true; includes ./scripts)
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Pitfall 5 — the type-only-import constraint and its warning signs)
  - .planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md (D-06 + Claude's-Discretion on annotation choice)
  </read_first>
  <action>
  Replace all 13 `: any` annotations (lines 77×2, 94, 104, 129, 152, 158, 160×2, 162, 170, 220, 235) with concrete types using `unknown` + narrowing, small explicit local interfaces (e.g., a minimal HRE-shaped parameter type carrying only the members actually read), and/or `Parameters<>` / `typeof import('hardhat')` extraction — the concrete annotations are owner-delegated discretion; the constraints are not:
  - NO new top-level VALUE import. The file currently has zero top-level imports and must stay effectively import-free at runtime — it is imported BY hardhat.config.ts during config load, and a value import of 'hardhat' throws LIB_IMPORTED_FROM_THE_CONFIG (Pitfall 5). Only type-erased forms are allowed if any import is added (`import type ...`).
  - Keep the runtimeHre() `require('hardhat')` lazy-require body and both its eslint-disable comments byte-untouched (its `: any` return annotation may be retyped, e.g. via `ReturnType`/`typeof import(...)` extraction, as long as the require line and comments are unchanged).
  - The overloaded getContractFactory interception at lines 160-162 (nameOrAbi/opts/artifact): use `unknown` + narrowing or `Parameters<>` extraction; the `original(nameOrAbi, opts)` passthrough call and all runtime behavior stay identical — this is a typing-only change, zero behavioral edits.
  - Blanket `--error` stays in the scan script (D-06 rejected severity-scoping — do not add --severity flags).

  Regression proof: `npx tsc --noEmit` (strict, ./scripts included) exits 0; `yarn test` (Hardhat full suite — the module loads from hardhat.config.ts in every process) at the STATE ledger baseline 666 passing / 2 pending / 0 failing; `yarn semgrep:scan` now reports EXACTLY the 4 unsafe-math-operation findings (typescript-any-usage at 0) with exit 1 — the pre-D-13 expected state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && [ "$(grep -c ': any' scripts/utils/GNUSLifecyclePolicyLinking.ts)" -eq 0 ] && [ "$(grep -cE '^import (?!type)' -P scripts/utils/GNUSLifecyclePolicyLinking.ts)" -eq 0 ] && yarn test > /tmp/18-02-hardhat-regression.log 2>&1; echo "exit=$?" | grep -q "exit=0" && grep -q "666 passing" /tmp/18-02-hardhat-regression.log && yarn semgrep:scan > /tmp/18-02-post-typing.log 2>&1; grep -c "typescript-any-usage" /tmp/18-02-post-typing.log</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c ": any" scripts/utils/GNUSLifecyclePolicyLinking.ts` returns 0
  - `grep -c "^import " scripts/utils/GNUSLifecyclePolicyLinking.ts` matches only `import type` lines (or returns nothing) — zero top-level value imports
  - The runtimeHre() require line and both eslint-disable comments are byte-identical to the pre-edit file
  - `npx tsc --noEmit` exits 0
  - `yarn test` exits 0 at 666 passing / 2 pending / 0 failing (ledger baseline; log /tmp/18-02-hardhat-regression.log)
  - /tmp/18-02-post-typing.log shows 0 typescript-any-usage findings and exactly the 4 unsafe-math-operation findings remain (exit 1)
  - No runtime behavioral change: git diff on the file shows annotation-only edits (plus any type-only import)
  </acceptance_criteria>
  <done>Zero `any` in the linking harness under strict mode; Hardhat config load and full suite green; semgrep surface reduced to the 4 D-13-pending findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner config (.semgrep.yml, package.json flags) → gate exit code | committed rule/flag text determines what the gate can ever detect |
| nested .devcontainer submodule → gnus-ai gitlink | mirror fix crosses a submodule boundary (pointer bump reviewed in gnus-ai PR) |
| hardhat config load → linking harness module | TS edits load inside every hardhat process (type-only-import constraint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Repudiation | unsafe-external-call rule | mitigate | D-09 executes-proof: positive control MUST yield exactly the 2 known sites before the shipped-rule 0-finding run counts; a parsing-but-never-matching rule still exits 0, which is the silent-disablement failure being fixed |
| T-18-06 | Tampering | package.json --exclude-rule additions | mitigate | Only owner-ruled exclusions added (D-11 permanent, D-12 temporary — both locked in 18-CONTEXT); rationale + lift condition recorded in STATE by plan 18-03; no nosemgrep annotations in contract source (D-10) |
| T-18-07 | Tampering | scan surface | mitigate | The 15-flag --exclude set ships in the SAME change as --no-git-ignore (Pitfall 1); acceptance greps forbid lib/forge-std, typechain-types, node_modules, test/ paths; D-14 keeps test/ out |
| T-18-08 | Tampering | rule-ID prefixing defeats exclusions | mitigate | Pitfall 6: `--config .semgrep.yml` relative reference kept byte-identical; post-edit proof requires zero weak-encryption/diamond-selector-clash/hard-coded-secret findings |
| T-18-09 | DoS | linking-harness typing | mitigate | Pitfall 5: type-only-import constraint enforced (zero top-level value imports acceptance grep) + npx tsc + full Hardhat suite at ledger baseline |
| T-18-SC | Tampering | package installs | accept | No new installs in this plan — semgrep 1.174.0 already local (brew), audit-table Approved in 18-RESEARCH.md |
</threat_model>

<verification>
- Rule: `.semgrep.yml` + `.devcontainer/config/.semgrep.yml` carry the identical generic-mode rule; both parse; no `require(success` pattern remains
- Surface: `yarn semgrep:scan` reaches contracts/gnus-ai with zero vendored/generated/test paths; exclusions intact (zero weak-encryption/diamond-selector-clash/hard-coded-secret findings)
- D-09: positive control = exactly 2 findings; shipped rule = 0 findings, 0 parse errors
- Typing: zero `: any`; zero top-level value imports; tsc green; `yarn test` 666/2/0; remaining semgrep findings = exactly the 4 unsafe-math-operation sites
</verification>

<success_criteria>
- SEC-10 groundwork complete: rule parses AND executes (proven), surface includes the submodule without noise, 13 any-typings fixed at source, D-11/D-12 exclusions applied with their STATE records deferred to 18-03 by design
- Phase boundary intact: no contract-source changes, no test-suite changes, no nosemgrep annotations, no new dependencies
</success_criteria>

<output>
Create `.planning/phases/18-scanner-triage-upgrades/18-02-SUMMARY.md` when done
</output>
