---
phase: 18-scanner-triage-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slither.config.json
  - .slither.db.json
  - .github/workflows/security-audit.yml
  - .planning/STATE.md
autonomous: true
requirements:
  - SEC-09

must_haves:
  truths:
    - "Bare `yarn slither:scan` exits 0 with zero untriaged findings on slither 0.11.6, on two consecutive identical runs"
    - "The 3 Phase-9 FPs remain recoverable on demand via `yarn slither:scan --show-ignored-findings` (3 visible, exit 255 — advisory only)"
    - "The CI slither gate step runs bare `yarn slither:scan` on the pinned 0.11.6 with no `--fail-none` spelling anywhere in the workflow"
    - "slither.config.json gains exactly one key (triage_database) — zero detector excludes, severity keys byte-identical (D-05)"
  artifacts:
    - path: ".slither.db.json"
      provides: "Committed tool-generated triage database for the 3 Phase-9 FPs"
      contains: "\"id\""
    - path: "slither.config.json"
      provides: "Triage-db wiring consumed by every slither run"
      contains: "triage_database"
    - path: ".github/workflows/security-audit.yml"
      provides: "Pinned 0.11.6 bare-triage slither gate"
      contains: "slither-analyzer==0.11.6"
    - path: ".planning/STATE.md"
      provides: "Scanner Baseline Ledger — slither leg"
  key_links:
    - from: "slither.config.json"
      to: ".slither.db.json"
      via: "triage_database key honored on every run"
      pattern: "\"triage_database\": \".slither.db.json\""
    - from: ".github/workflows/security-audit.yml"
      to: "package.json slither:scan"
      via: "bare gate invocation"
      pattern: "run: yarn slither:scan$"
---

<objective>
Execute the complete SEC-09 slither triage leg (D-01, D-02, D-03, D-05): upgrade slither to 0.11.6 locally, re-prove exit-code mechanics on the pinned version BEFORE any gate flip, mark the 3 Phase-9 FPs into a committed triage database, wire it via `slither.config.json`, flip the CI gate from the `--fail-none` workaround to a bare PEDANTIC-default run, and record the new slither baseline in STATE.

Purpose: replace the 0.11.5 `--fail-none` exit-code workaround with real triage — green becomes a property of the tool's own accounting (zero untriaged findings), not a flag saying don't-count.
Output: committed `.slither.db.json`, updated `slither.config.json` / `.github/workflows/security-audit.yml`, Scanner Baseline Ledger entry in STATE.md, probe logs under /tmp/18-01-*.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md
@.planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md
@.planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md

# Files being modified (read before editing):
@slither.config.json
@.github/workflows/security-audit.yml

<interfaces>
<!-- Current state of the edit targets (verified 2026-08-31). -->

slither.config.json (flat keys, NO comments — prose lives in workflow header + STATE):
- Severity keys (MUST stay byte-identical per D-05): exclude_informational/exclude_optimization/exclude_low = true, exclude_medium/exclude_high = false
- filter_paths: "node_modules|artifacts|cache|diamond-abi|diamond-typechain-types|typechain-types|coverage|flat|test/|test-assets|scripts|docs|contracts/test|contracts/mocks|test/foundry/fuzz"
- New key to add (top-level, exact spelling — it is in slither's defaults_flag_in_config; unknown keys are silently logged-and-skipped): "triage_database": ".slither.db.json"

.github/workflows/security-audit.yml:
- Pin step (lines ~77-80): "- name: Install slither 0.11.5" / "run: pip install slither-analyzer==0.11.5" + parity comment
- Gate step (lines ~93-103): "- name: Slither static analysis" / "run: yarn slither:scan --fail-none" + 0.11.5-empirics comment block (REWRITE, do not append)
- Header baseline sentence (lines ~10-13): names "exactly 3 slither false positives, 13 semgrep INFO-lint findings in one file" — update the SLITHER half only (semgrep half belongs to plan 18-03)

package.json (NOT modified by this plan — D-01/PATTERNS verified):
- "slither:scan": "slither . --config-file slither.config.json" — ALREADY bare; `--fail-none` lives only in the workflow step

The committed 07-03 baseline identities (STATE.md "Phase 7 Decisions Logged (07-03)", row `yarn slither:scan`):
- weak-prng @ GNUSWithdrawLimiterStorage.calculateCurrentBin (sol#114-138, expr #137)
- erc721-interface @ GNUSBridge.approve (sol#406-410)
- erc721-interface @ GNUSBridge.transferFrom (sol#506-516)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade slither to 0.11.6 and run the exit-code probes (D-02 — probe-then-flip, BEFORE any gate change)</name>
  <files>.github/workflows/security-audit.yml</files>
  <read_first>
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Pattern 1 "Verified 0.11.6 probe sequence" + Code Examples "slither 0.11.6 exit-code core")
  - .planning/STATE.md ("Phase 7 Decisions Logged (07-03)" — the 3-FP identities and the 0.11.5 exit-code empirics being re-proved)
  - .github/workflows/security-audit.yml (current pin + gate steps)
  - slither.config.json
  - package.json (scripts block)
  </read_first>
  <action>
  Run `brew upgrade slither-analyzer` and verify `slither --version` prints 0.11.6 (D-01 local leg). Then execute the two exit-code probes with output captured by redirect (never piped through tee — it masks exit codes); use the form `yarn slither:scan > /tmp/18-01-slither06-bare.log 2>&1; echo "exit=$?"` (yarn propagates slither's 255 — proven in 07-03).

  Probe A (bare run, PEDANTIC default): expect exit 255 and EXACTLY 3 findings with the 07-03 identities — weak-prng @ GNUSWithdrawLimiterStorage.calculateCurrentBin, erc721-interface @ GNUSBridge.approve, erc721-interface @ GNUSBridge.transferFrom. If the count is NOT exactly 3 (e.g., a new HIGH finding from the 0.11.6-only `msg-value-in-nonpayable` detector — RESEARCH A2): do NOT proceed to triage marking; record the extra finding in the task summary and disposition per D-04 — triage-with-evidence if demonstrably FP, otherwise STOP the plan and surface it as a routing event (contract-source fixes are forbidden in Phase 18).

  Probe B (mutual exclusion): run `yarn slither:scan --fail-high --fail-none` captured to /tmp/18-01-slither06-mutual.log — expect an argparse mutually-exclusive-group error (proves the exclusive `fail_on` group on 0.11.6, contradicting any remembered 0.11.5 empirics).

  Make NO edits in this task — this task is probes only (probe-then-flip per D-02).
  </action>
  <verify>
    <automated>slither --version | grep -q "0.11.6" && grep -c "weak-prng" /tmp/18-01-slither06-bare.log && grep -ci "unrecognized arguments\|not allowed with" /tmp/18-01-slither06-mutual.log</automated>
  </verify>
  <acceptance_criteria>
  - `slither --version` output contains 0.11.6
  - /tmp/18-01-slither06-bare.log shows exactly 3 findings matching the 07-03 detector+site identities and the captured exit code is 255
  - /tmp/18-01-slither06-mutual.log shows an argparse mutual-exclusion error
  - Zero edits made to any repo file in this task
  - Any deviation from exactly-3 findings is recorded as a D-04 disposition in the task summary (not silently triaged)
  </acceptance_criteria>
  <done>0.11.6 installed; both exit-code probes captured with expected results; 3-FP identity confirmed on 0.11.6 (or D-04 disposition recorded); gate spellings untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the 3 FPs into the committed triage database + config wiring + stability and re-surface proofs (D-01)</name>
  <files>slither.config.json, .slither.db.json</files>
  <read_first>
  - slither.config.json
  - .planning/phases/18-scanner-triage-upgrades/18-RESEARCH.md (Code Examples "slither triage commands"; Pitfall 3 — db MUST be generated on 0.11.6; Pitfall notes on id-hash drift)
  - .planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md (".slither.db.json" section — tool-generated, never hand-authored; .gitignore verified not to match it)
  </read_first>
  <action>
  Add the single key "triage_database": ".slither.db.json" to slither.config.json (top-level, exact spelling — load-bearing; unknown keys are silently logged-and-skipped). Change NOTHING else in the file (D-05: zero detector excludes; severity keys and filter_paths stay byte-identical).

  One-time interactive marking on 0.11.6 (generate the db — Pitfall 3): run `printf 'All\nAll\nAll\nAll\nAll\n' | slither . --config-file slither.config.json --triage-mode --triage-database .slither.db.json > /tmp/18-01-triage-mode.log 2>&1` — answer `All` at every per-detector prompt (2 expected: weak-prng, erc721-interface; extra piped lines are ignored, and if a prompt hits EOF it defaults to "not hide", which the stability run below will catch — re-run marking if so). The tool writes .slither.db.json as a JSON array of result dicts each carrying the sha3-256 finding id. NEVER hand-edit the db.

  Then the three post-marking probes (redirect form, captured under /tmp/18-01-):
  - Stability run 1: `yarn slither:scan` → expect "0 result(s)" and exit 0
  - Stability run 2 (identical invocation, immediately after): expect byte-identical "0 result(s)" and exit 0 — this is D-02's two-consecutive-runs stability bar
  - Re-surface probe: `yarn slither:scan --show-ignored-findings` → expect the 3 FPs visible again AND exit 255 — advisory-only proof; `--show-ignored-findings` must NEVER appear in any gate script (it re-adds triaged findings to the pedantic count)

  V14 db-safety check (RESEARCH Security Domain — no secrets, no Solidity source beyond snippet references in any new committed artifact): run `grep -c 'pragma solidity' .slither.db.json` — it MUST return 0 before the db is committed (the db carries finding metadata — ids, descriptions, snippet references — only).

  Commit .slither.db.json at repo root (verified: no .gitignore rule matches it — confirm with `git check-ignore .slither.db.json` returning nothing; make NO .gitignore change).
  </action>
  <verify>
    <automated>grep -q '"triage_database": ".slither.db.json"' slither.config.json && python3 -c "import json; db=json.load(open('.slither.db.json')); assert isinstance(db,list) and len(db)>=3 and all('id' in e for e in db)" && ! grep -q 'pragma solidity' .slither.db.json && yarn slither:scan > /tmp/18-01-stability-2.log 2>&1; echo "exit=$?" | grep -q "exit=0" && grep -q "0 result" /tmp/18-01-stability-2.log</automated>
  </verify>
  <acceptance_criteria>
  - slither.config.json contains the literal "triage_database": ".slither.db.json" and git diff on it shows exactly one added line
  - .slither.db.json exists, parses as a JSON array with at least 3 entries each carrying an "id" field
  - The V14 db-safety grep (`grep -c 'pragma solidity' .slither.db.json`) returns 0 — no Solidity source beyond snippet references committed
  - Two consecutive bare `yarn slither:scan` runs (logs /tmp/18-01-stability-1.log, /tmp/18-01-stability-2.log) each report 0 results and exit 0
  - /tmp/18-01-resurface.log shows the 3 triaged FPs visible and exit 255
  - `git check-ignore .slither.db.json` produces no output; .gitignore is unmodified
  </acceptance_criteria>
  <done>Triage db committed (V14 db-safety-checked) and wired via config; bare gate green twice consecutively; FPs re-surfaced on demand.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the slither CI gate to bare 0.11.6 + record the Scanner Baseline Ledger (D-01 flip, D-02 satisfied)</name>
  <files>.github/workflows/security-audit.yml, .planning/STATE.md</files>
  <read_first>
  - .github/workflows/security-audit.yml (full file — header lines ~8-15, pin step ~77-80, gate step ~93-103)
  - .planning/STATE.md (the "## Test Baseline Ledger (canonical)" section for ledger format; "Phase 7 Decisions Logged (07-03)" rows being superseded)
  - .planning/phases/18-scanner-triage-upgrades/18-PATTERNS.md (".github/workflows/security-audit.yml" section — pin-comment convention, WR-01 zero-finding-gate wording model)
  - .planning/phases/18-scanner-triage-upgrades/18-CONTEXT.md (D-01, D-03, D-05)
  </read_first>
  <action>
  Flip the workflow now that all probes passed (Tasks 1-2):
  - Pin step: change `pip install slither-analyzer==0.11.5` to `pip install slither-analyzer==0.11.6`; rename step "Install slither 0.11.5" to "Install slither 0.11.6"; update its parity comment to state exact parity with the local brew 0.11.6 that generated the committed triage db.
  - Gate step: change the run line from `yarn slither:scan --fail-none` to bare `yarn slither:scan`; REWRITE (not append to) the 0.11.5-empirics comment block to the new semantics: PEDANTIC default fails iff untriaged findings remain; triage db `.slither.db.json` (wired via slither.config.json) suppresses the 3 committed FPs; red now means a NEW untriaged finding, nothing recorded; triage-id drift after edits near the FP sites is a CORRECT red — re-run --triage-mode and re-commit the db, never widen suppression (D-03). Reference the probe evidence (/tmp/18-01-* logs, probe-then-flip per D-02). Use the WR-01 closure comment (workflow header) as the wording model.
  - Header: update the slither half of the baseline sentence (lines ~10-13) — the "exactly 3 slither false positives" reading is superseded by the committed triage db under the Scanner Baseline Ledger. Leave the semgrep half ("13 semgrep INFO-lint findings") untouched — plan 18-03 owns it.
  - Do NOT touch the semgrep advisory step or any other step in this plan.

  STATE.md: add a new sibling section `## Scanner Baseline Ledger (canonical)` immediately after the "## Test Baseline Ledger (canonical)" section (this is the single-source home both scanner legs write to; plan 18-03 appends the semgrep leg under it). Record under a slither subsection: the 0.11.6 pin (CI + brew parity), the .slither.db.json path + the 3 triaged identities (detector + site), a per-run proof table in the Phase-17 ledger format (all 8 probes from Tasks 1-2 with exit codes and /tmp/18-01-* log paths, captured date), the D-03 drift policy (correct red → re-triage, never widen), the D-05 zero-detector-excludes stance, and a supersession statement over the 07-03 0.11.5 exit-code mechanics + `--fail-none` spelling (append-only: do NOT edit the historical 07-03 rows; mirror how 17-05 superseded 07-01/07-04 figures). Note the accepted crytic-compile range float (>=0.4.2,<0.5.0 — Pitfall 4) as a known accepted float unless the probes showed instability.

  Post-edit sanity: re-run `yarn slither:scan` (must still exit 0 — the workflow edit must not affect the local gate) and YAML-parse the workflow.
  </action>
  <verify>
    <!-- Workflow YAML parse uses /usr/local/bin/python3 — the default python3 on this machine (brew 3.14.7 and /usr/bin/python3 alike) lacks PyYAML; the interpreter path is load-bearing. -->
    <automated>grep -q "slither-analyzer==0.11.6" .github/workflows/security-audit.yml && ! grep -q "fail-none" .github/workflows/security-audit.yml && /usr/local/bin/python3 -c "import yaml; yaml.safe_load(open('.github/workflows/security-audit.yml'))" && yarn slither:scan > /tmp/18-01-postflip.log 2>&1; echo "exit=$?" | grep -q "exit=0" && grep -q "Scanner Baseline Ledger" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
  - .github/workflows/security-audit.yml contains `pip install slither-analyzer==0.11.6` and zero occurrences of "fail-none" anywhere in the file (comment included — the workaround spelling is retired)
  - The gate step's run line is exactly `run: yarn slither:scan`
  - The semgrep advisory step is byte-untouched (its `continue-on-error: true` still present — plan 18-03 removes it)
  - /usr/local/bin/python3 yaml.safe_load parses the workflow (the default python3 lacks PyYAML — the interpreter path is load-bearing)
  - STATE.md contains a "## Scanner Baseline Ledger (canonical)" section naming .slither.db.json, the 3 triaged identities, the probe table with /tmp/18-01-* paths, and the supersession statement over the 07-03 slither spelling
  - Post-edit `yarn slither:scan` still exits 0
  </acceptance_criteria>
  <done>SEC-09 complete: pinned 0.11.6 bare-triage gate in CI, committed db wired, STATE ledger records the new baseline and the drift policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local slither 0.11.6 → committed .slither.db.json | tool-generated suppression state crosses into the repo (PR-reviewable) |
| CI runner → pinned tool versions | install step pulls from PyPI under an exact pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | .slither.db.json | mitigate | Tool-generated only (slither --triage-mode); never hand-authored/edited; V14 db-safety grep (no Solidity source beyond snippet references) before commit; reviewer check: db entries must match live `--show-ignored-findings` output; db diffs get PR scrutiny like baseline tables |
| T-18-02 | Tampering | slither.config.json | mitigate | D-05: the file gains exactly one key (triage_database); zero detector excludes — acceptance criterion enforces a one-line diff |
| T-18-03 | Tampering | CI pin step | mitigate | Exact pin `slither-analyzer==0.11.6` (was ==0.11.5); crytic-compile range float recorded as accepted in STATE (Pitfall 4) unless probes show instability |
| T-18-04 | Repudiation | gate exit semantics | mitigate | D-02 probe-then-flip: exit codes re-proven on 0.11.6 (bare 255-with-findings, mutual-exclusion error, triaged-exits-0 ×2) BEFORE the flip; evidence in /tmp/18-01-* + STATE ledger |
| T-18-SC | Tampering | brew/pip tool installs | mitigate | Package Legitimacy Audit present in 18-RESEARCH.md: slither-analyzer Approved via PyPI + GitHub tag-source + Homebrew cross-verification; no [ASSUMED]/[SUS]/[SLOP] verdicts — no blocking checkpoint required |
</threat_model>

<verification>
- `slither --version` → 0.11.6; `yarn slither:scan` → 0 results, exit 0 (run twice)
- `yarn slither:scan --show-ignored-findings` → 3 FPs visible, exit 255 (advisory)
- Workflow: `grep -c "0.11.6" .github/workflows/security-audit.yml` ≥ 1; zero "fail-none" occurrences; YAML parses (via /usr/local/bin/python3 — the only local interpreter with PyYAML); semgrep step untouched
- STATE.md carries the Scanner Baseline Ledger (slither leg) with probe table and supersession statement
</verification>

<success_criteria>
- SEC-09 delivered in full: triage-capable 0.11.6 line, 3 Phase-9 FPs triaged in a committed db honored via config, bare gate exits 0, triage visible on demand
- Every probe preceded the flip (D-02); zero detector excludes (D-05); db generated on 0.11.6 (Pitfall 3)
- No production contract changes, no test-suite changes, no new dependencies (phase boundary intact)
</success_criteria>

<output>
Create `.planning/phases/18-scanner-triage-upgrades/18-01-SUMMARY.md` when done
</output>
