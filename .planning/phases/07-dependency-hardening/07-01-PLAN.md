---
phase: 07-dependency-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - .devcontainer/config/package.json
  - hardhat.config.ts
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "package.json contracts-starter descriptor ends with #commit=bf67b736ad5fa3366551f599e204784856fb3069 and yarn install --immutable exits 0"
    - "yarn.lock contracts-starter resolution and checksum lines are byte-identical to pre-pin (zero drift per D-02)"
    - "yarn npm audit --severity moderate exits 0 with zero advisories (both deprecation advisories fixed in-phase per owner ruling 2026-08-27)"
    - "Hardhat suite reports 661 passing / 2 pending / 1 failing (GNUSControlStorage chainID only) and Foundry reports 215 passed / 2 failed / 3 skipped (Phase 08.1 setUp reverts only)"
    - "npx hardhat help test-multichain exits 0 under the renamed @diamondslab/hardhat-multichain plugin (task registration intact; the full forked-network run is dispositioned out of scope in Task 3)"
    - "D-03: pin uses Yarn 4 native Git syntax (#commit=<sha> keyword form matching the lockfile's existing normalization — never a bare #<sha>)"
    - "D-04: pin scope is the npm/test-harness dependency only — no vendoring, no gitmodule conversion, no source copying into the repo; contracts reach contracts-starter via Hardhat compile-time imports"
    - "D-05: the contracts/gnus-ai git submodule is out of scope (already commit-pinned by the submodule mechanism; submodule bumps continue per-phase as before)"
  artifacts:
    - path: "package.json"
      provides: "Pinned contracts-starter descriptor; @diamondslab/hardhat-multichain 1.1.0; supported eslint; semgrep stub removed"
      contains: "#commit=bf67b736ad5fa3366551f599e204784856fb3069"
    - path: "yarn.lock"
      provides: "Tool-regenerated lockfile (never hand-edited)"
      contains: "contracts-starter@https://github.com/mudgen/diamond-2-hardhat.git#commit=bf67b736"
    - path: ".devcontainer/config/package.json"
      provides: "Second floating copy pinned identically (Pitfall 8 consistency move)"
      contains: "#commit=bf67b736ad5fa3366551f599e204784856fb3069"
    - path: "hardhat.config.ts"
      provides: "Renamed plugin import"
      contains: "import '@diamondslab/hardhat-multichain'"
  key_links:
    - from: "package.json"
      to: "yarn.lock"
      via: "yarn install descriptor re-key"
      pattern: "#commit=bf67b736ad5fa3366551f599e204784856fb3069"
    - from: "hardhat.config.ts"
      to: "node_modules/@diamondslab/hardhat-multichain"
      via: "plugin import resolution"
      pattern: "import '@diamondslab/hardhat-multichain'"
    - from: "package.json \"test-multichain\" script (line 8)"
      to: "@diamondslab/hardhat-multichain task registration"
      via: "npx hardhat help test-multichain smoke probe (Task 3)"
      pattern: "test-multichain"
---

<objective>
DEP-01 zero-drift pin plus the owner-ruled in-phase advisory fixes, proven behavior-neutral by the full test matrix at the known-stale baselines.

Purpose: This is the phase's only production-manifest change — deterministic builds (DEP-01) and an audit gate that can exit 0 without waivers (owner ruling 2026-08-27: the 2 moderate deprecation advisories are FIXED, not waived).
Output: Pinned manifests (root + devcontainer), regenerated lockfile, renamed multichain plugin, supported eslint line, dead semgrep stub removed, zero-drift and baseline proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-dependency-hardening/07-RESEARCH.md
@.planning/phases/07-dependency-hardening/07-PATTERNS.md

<interfaces>
<!-- Verified current state (planning session 2026-08-27). Executor: re-read the files, do not trust line numbers blindly. -->

package.json line 135 (BEFORE):
    "contracts-starter": "https://github.com/mudgen/diamond-2-hardhat.git",
package.json line 135 (AFTER, per D-03):
    "contracts-starter": "https://github.com/mudgen/diamond-2-hardhat.git#commit=bf67b736ad5fa3366551f599e204784856fb3069",
package.json line 149 (BEFORE):  "hardhat-multichain": "1.0.6",   → REMOVED; new key "@diamondslab/hardhat-multichain": "1.1.0" (only published version of the renamed package)
package.json line 137 (BEFORE):  "eslint": "9.39.2",             → "9.39.5" (max version <10, verified via yarn npm info eslint@'<10.0.0')
package.json line 154 (BEFORE):  "semgrep": "1.0.0",             → REMOVED (inert npm stub: no bin, empty package, zero imports repo-wide)

yarn.lock lines 4883-4887 (current entry — resolution + checksum must remain byte-identical after the pin):
"contracts-starter@https://github.com/mudgen/diamond-2-hardhat.git":
  version: 1.0.0
  resolution: "contracts-starter@https://github.com/mudgen/diamond-2-hardhat.git#commit=bf67b736ad5fa3366551f599e204784856fb3069"
  checksum: 10c0/bb02edc42733588af6ad2344d2f9d4da62892d2b95edd9674c2bf6718854ab97fc39553ab84181897b8b5474130414f4db72da6ee01b1366c0d3b4685ecc4f9e

yarn.lock line 1458 (workspace deps listing, BEFORE):
    contracts-starter: "https://github.com/mudgen/diamond-2-hardhat.git"

hardhat.config.ts line 10 (BEFORE):  import 'hardhat-multichain';
hardhat.config.ts line 10 (AFTER):   import '@diamondslab/hardhat-multichain';

.devcontainer/config/package.json line 111 (BEFORE):  "contracts-starter": "https://github.com/mudgen/diamond-2-hardhat.git",

Advisory baseline (captured 2026-08-27 via yarn npm audit --severity moderate --json; both are deprecation-class, no GHSA CVEs):
  ID "eslint (deprecation)" — "This version is no longer supported" — Vulnerable 9.39.2 (direct dep)
  ID "hardhat-multichain (deprecation)" — "Renamed: this package is now @diamondslab/hardhat-multichain" — Vulnerable 1.0.6 (direct dep)

Multichain task (verified in node_modules/hardhat-multichain/dist/index.js line 64):
  The plugin itself registers hardhat task "test-multichain" (description "Launches multiple forked Hardhat networks");
  package.json line 8 script "test-multichain": "npx hardhat test-multichain" invokes it. hardhat.config.ts lines 86-97
  branch on process.argv containing BOTH 'test-multichain' AND '--chains' — a help probe (no --chains) takes the
  localhost:8555 string default, so config load stays network-free.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin contracts-starter to bf67b736 (root + devcontainer) and prove lockfile determinism</name>
  <files>package.json, .devcontainer/config/package.json, yarn.lock</files>
  <read_first>package.json (devDependencies block), .devcontainer/config/package.json (lines ~105-135), yarn.lock (lines ~1450-1465 and ~4875-4890), 07-PATTERNS.md sections "package.json" and "yarn.lock", 07-RESEARCH.md Pattern 1 + Pitfall 1</read_first>
  <action>
Edit exactly one line in each manifest (per D-02/D-03 — explicit "#commit=" keyword form, never a bare "#<sha>"):
1. package.json line ~135: append "#commit=bf67b736ad5fa3366551f599e204784856fb3069" to the contracts-starter URL (exact AFTER form in the interfaces block).
2. .devcontainer/config/package.json line ~111: the identical one-line pin (Pitfall 8 consistency move — this file is NOT the CI/local build path and has no lockfile; touch nothing else in it).
3. Run "yarn install" to let Yarn re-key the lockfile (NEVER hand-edit yarn.lock — checksumBehavior: throw will catch surgery later).
4. Verify the zero-drift proof BEFORE any Task 2 manifest edit: "git diff yarn.lock" must show exactly the two descriptor lines changed (workspace listing ~1458 + entry key ~4883, each gaining "#commit=bf67b736..."), with the resolution: and checksum: 10c0/bb02edc4... lines byte-identical. If the checksum changes, STOP — resolved content drifted, contradicting D-02.
5. Run "yarn install --immutable" — must exit 0.
Do not chase any upstream HEAD (D-02: upstream is verified to still point at bf67b736 anyway). This task's diff verification and its git commit MUST land before Task 2's manifest edits so the pin hunk stays isolatable.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && grep -c '#commit=bf67b736ad5fa3366551f599e204784856fb3069' package.json && grep -c '#commit=bf67b736ad5fa3366551f599e204784856fb3069' yarn.lock && grep -c '#commit=bf67b736ad5fa3366551f599e204784856fb3069' .devcontainer/config/package.json && git diff --unified=0 yarn.lock | grep '^[+-][^+-]' | grep -cv 'contracts-starter' ; git diff yarn.lock | grep -c '^[-+]checksum: 10c0/bb02edc4' ; yarn install --immutable && echo IMMUTABLE-OK</automated>
    Expected outputs in order: 1 (package.json), 2 (yarn.lock descriptor lines), 1 (devcontainer), 0 (no non-contracts-starter lockfile changes), 0 (checksum untouched), IMMUTABLE-OK.
  </verify>
  <acceptance_criteria>
    - grep '#commit=bf67b736ad5fa3366551f599e204784856fb3069' package.json → exactly 1; yarn.lock → exactly 2; .devcontainer/config/package.json → exactly 1
    - yarn install --immutable exits 0
    - Every changed yarn.lock line mentions contracts-starter (diff is scoped to the pin)
    - The checksum line for contracts-starter is absent from the diff (zero drift)
    - No other dependency version changed in this task
  </acceptance_criteria>
  <done>contracts-starter pinned in both manifests; lockfile regenerated by Yarn with a 2-descriptor-line diff and byte-identical resolution+checksum; immutable install green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix both deprecation advisories in-phase (multichain rename + eslint support bump) and remove the dead semgrep stub; audit must exit 0</name>
  <files>package.json, hardhat.config.ts, yarn.lock</files>
  <read_first>package.json (devDependencies + scripts "audit", "test-multichain", "lint"), hardhat.config.ts (line 10 import and the "/* hardhat-multichain config */" block near line 213), 07-RESEARCH.md Pattern 2 + Pitfall 5, 07-PATTERNS.md "Advisory-fix surface", 07-CONTEXT.md D-08/D-09</read_first>
  <action>
Owner ruling 2026-08-27 overrides the research waiver recommendation: both advisories are fixed here. Minimal fix per advisory, exact pins only (no ranges — .yarnrc.yml defaultSemverRangePrefix: "" policy).
1. Re-capture the advisory IDs first: "yarn npm audit --severity moderate --json" — expect exactly the two planning-time IDs ("eslint (deprecation)", "hardhat-multichain (deprecation)"). If the advisory set differs from planning time, record the delta in the SUMMARY before proceeding.
2. hardhat-multichain rename: remove the "hardhat-multichain": "1.0.6" line; add "@diamondslab/hardhat-multichain": "1.1.0" in alphabetical position (after @commitlint/config-conventional, before @geniusventures/diamonds). 1.1.0 is the only version the renamed package ever published (verified via yarn npm info: repository github.com/DiamondsLab/hardhat-multichain.git — legitimate rename successor, not a squatter). Edit hardhat.config.ts line 10 to "import '@diamondslab/hardhat-multichain';". Then "grep -rn 'hardhat-multichain'" excluding node_modules/yarn.lock/.devcontainer: the only acceptable remaining hits are the config comment near line 213 and doc text. The .devcontainer line 127 fork URL (GeniusVentures git fork, different source form, not the build path) is deliberately left untouched — record that decision.
3. eslint minimal bump: change "9.39.2" to "9.39.5" (max <10, verified). Run "yarn install", then "yarn npm audit --severity moderate" with NO pipe (Pitfall 5 — pipes mask exit codes) and capture EXIT. If 9.39.5 still trips the deprecation, the next candidate is "10.9.1" (latest) — but BEFORE attempting any major-version jump, STOP and ask the owner for explicit approval (AskUserQuestion-style blocking stop): the plugin-compat proof below is necessary but not sufficient, and a major-version jump is never taken on executor discretion. Only on the owner's go-ahead, attempt it if the plugin matrix survives: yarn install resolves clean (no peer conflicts) AND "npx eslint --version" reports 10.9.1 AND a "npx eslint <two representative .ts files> --fix-dry-run" run behaves identically to a pre-bump capture of the same files. If eslint 10 breaks plugins/config, STOP and report per the owner deviation protocol — do not force it.
4. Remove the inert npm stub "semgrep": "1.0.0" from devDependencies. Precondition gate: "grep -rn "from 'semgrep'\|require('semgrep')" --include='*.ts' --include='*.js' ." excluding node_modules returns 0 hits (verified at planning: only nosemgrep comment annotations and dashboard string literals reference the name). If the gate unexpectedly fails, leave the stub and record why.
5. Run "yarn install"; the advisory-fix lockfile hunks (new @diamondslab entry, eslint entry re-key, semgrep entry removed) are separate hunks from Task 1's committed pin hunk.
6. Prove the gate: "yarn npm audit --severity moderate" must EXIT 0.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && grep -c '"@diamondslab/hardhat-multichain": "1.1.0"' package.json && grep -c "import '@diamondslab/hardhat-multichain'" hardhat.config.ts && grep -c "import 'hardhat-multichain'" hardhat.config.ts ; grep -c '"semgrep"' package.json ; grep -c '"hardhat-multichain"' package.json ; yarn npm audit --severity moderate > /tmp/audit-exit-proof.txt 2>&1; echo "AUDIT_EXIT=$?"</automated>
    Expected outputs in order: 1, 1, 0, 0, 0, AUDIT_EXIT=0.
  </verify>
  <acceptance_criteria>
    - "@diamondslab/hardhat-multichain": "1.1.0" present exactly once; old "hardhat-multichain" key absent from package.json
    - hardhat.config.ts imports '@diamondslab/hardhat-multichain'; the old import string returns 0 grep hits
    - eslint pinned at 9.39.5 (or 10.9.1 only via owner-approved escalation with the plugin-compat proof; otherwise STOPPED+reported)
    - "semgrep" key absent from package.json with the zero-imports precondition gate recorded
    - yarn npm audit --severity moderate exits 0 (no pipes in the proof command)
    - Lockfile changed only via yarn install; advisory hunks separate from the Task 1 pin hunk
  </acceptance_criteria>
  <done>Both deprecation advisories eliminated by minimal exact-pinned fixes; audit sub-gate green (exit 0); dead stub gone; no behavioral tooling change beyond the rename/bump (anything beyond = STOP+report, per owner deviation protocol; any major-version jump additionally requires the explicit owner checkpoint).</done>
</task>

<task type="auto">
  <name>Task 3: Full-matrix behavioral no-op proof at the known-stale baselines</name>
  <files>none (verification-only gate — no file modifications)</files>
  <read_first>07-RESEARCH.md Pitfall 6, .planning/STATE.md "Next Actions" item 2 (authoritative baselines), 07-VALIDATION.md "Known baselines"</read_first>
  <action>
Re-establish and prove the baselines exactly (STATE.md is authoritative; counts were verified 2026-08-27 at Phase 15 exit):
1. "yarn test" — expect 661 passing / 2 pending / 1 failing, where the single failure is GNUSControlStorage.test.ts "should return initial protocol info" (chainID 31337 vs 0 cross-suite pollution; passes in isolation). NEVER fix this here — root fix belongs to a future Phase-9-style sweep.
2. "yarn forge:test" — expect 215 passed / 2 failed / 3 skipped, where the 2 failures are the Phase 08.1 SafeDiamondCut + SafeSingleShotUpgrade setUp reverts (unchanged since Phase 9).
3. Multichain task smoke (rename key-link): run "npx hardhat help test-multichain" — must exit 0 and print the task's description ("Launches multiple forked Hardhat networks"), proving the renamed @diamondslab/hardhat-multichain plugin still registers the task that package.json's "test-multichain" script (line 8) invokes, and that hardhat.config.ts loads the renamed plugin cleanly (the help probe takes the config's no---chains branch, so no network is touched). If hardhat reports the task as not found, that is a D-09 routing event: STOP, report, do not patch. Disposition (why the FULL "yarn test-multichain" run is excluded): the task launches multiple forked Hardhat networks — an external-RPC, long-running harness that no phase gate consumes; registration + config-load smoke is the planned coverage, recorded in the SUMMARY.
4. Tolerance rule: the known-stale failures must be identical IN KIND (same test names, nothing else new). Any additional failure — especially anything touching hardhat plugin loading or the eslint/multichain toolchain — is a D-09 routing event: STOP, report in the SUMMARY, do not patch.
5. Do not run slither in this task (Pitfall 4: its forge clean/build side effects would force a recompile ahead of 07-03).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && yarn test 2>&1 | grep -E 'passing|pending|failing' | head -4 && yarn forge:test 2>&1 | tail -15 && npx hardhat help test-multichain 2>&1 | grep -c 'Launches multiple forked Hardhat networks'</automated>
    Hardhat summary must read 661 passing / 2 pending / 1 failing; Foundry must read 215 passed / 2 failed / 3 skipped with only SafeDiamondCut/SafeSingleShotUpgrade setUp reverts; the multichain smoke must print 1 (task registered under the renamed plugin — 0 or a hardhat task-not-found error means the rename broke the task).
  </verify>
  <acceptance_criteria>
    - Hardhat suite: 661 passing, 2 pending, 1 failing; the failing test is GNUSControlStorage.test.ts "should return initial protocol info" and nothing else
    - Foundry suite: 215 passed, 2 failed, 3 skipped; failures are the Phase 08.1 Safe-proposer setUp reverts and nothing else
    - npx hardhat help test-multichain exits 0 with the task description printed (renamed plugin still registers the task package.json's script invokes); the full forked-network run is explicitly out of scope with the disposition recorded in the SUMMARY
    - Any deviation from these exact counts/kinds is recorded as a routing event, not silently tolerated or fixed
  </acceptance_criteria>
  <done>Pin + advisory fixes proven behavior-neutral against both suites at the documented baselines, including the renamed multichain plugin's task registration; DEP-01 verified end-to-end (descriptor, lockfile, immutability, zero drift, no-op).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/GitHub registry → package.json | External code enters the tree via the manifest and lockfile (the exact surface DEP-01 hardens) |
| yarn resolution → node_modules | Lockfile regeneration is an implicit code-introduction step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | contracts-starter pin | mitigate | Exact-SHA grep gate + "#commit=" keyword form; a typo'd hash would fail the grep count or change the lockfile checksum, which the diff gate catches as drift |
| T-07-02 | Tampering | yarn.lock regeneration | mitigate | Diff scoped to contracts-starter lines only; checksum byte-identity assertion; --immutable proof; advisory hunks verified separately from the pin hunk |
| T-07-03 | Tampering/Spoofing | @diamondslab/hardhat-multichain rename | mitigate | Rename legitimacy verified at planning (repository field → github.com/DiamondsLab/hardhat-multichain.git, official migration notice); exact pin 1.1.0; full-matrix re-run plus the test-multichain task-registration smoke prove plugin behavior |
| T-07-04 | Elevation of Privilege | eslint major-version escalation | mitigate | 9.39.5 patch first; 10.9.1 only through the explicit owner checkpoint (AskUserQuestion-style stop) plus the documented plugin-compat proof; anything beyond a bump = STOP per owner deviation protocol |
| T-07-05 | Tampering | semgrep stub removal | mitigate | Zero-imports grep precondition gate before removal; stub provides no bin/code (verified empty package) |
| T-07-SC | Tampering | package-graph changes | mitigate | No new packages enter: pin re-keys an existing git dep; @diamondslab is a verified rename successor; eslint bumps within the existing dep — slopcheck surface is n/a for all three |
</threat_model>

<verification>
Task 1 grep/diff/immutable gates; Task 2 audit-exit-0 proof; Task 3 baseline matrix + multichain task smoke. All deterministic exits, no pipes masking status codes.
</verification>

<success_criteria>
- DEP-01 complete: pinned descriptor (both manifests), deterministic lockfile, zero-drift proof, immutable install green
- yarn npm audit --severity moderate exits 0
- Test matrix identical in kind to the STATE.md baselines
- Renamed plugin's test-multichain task registration proven intact (help smoke); full forked-network run dispositioned out of scope
</success_criteria>

<output>
Create `.planning/phases/07-dependency-hardening/07-01-SUMMARY.md` when done
</output>
