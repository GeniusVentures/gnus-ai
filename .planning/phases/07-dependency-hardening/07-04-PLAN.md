---
phase: 07-dependency-hardening
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [DEP-01] # closes DEP-01's checkbox and the remediation-arc audit per D-06/D-07
must_haves:
  truths:
    - "All 13 unchecked remediation-arc requirement boxes are flipped to [x], each citing phase/plan/commit evidence gathered by a source-level probe run in this task"
    - "D-07: REQUIREMENTS.md checkbox reconciliation is in-scope audit output (code-complete but unchecked requirements like DEBT-01/SEC-03 get evidence-cited flips), not scope creep"
    - "BRIDGE-17 remains [ ] (Pending by design — SuperGenius#363 gate); no SWP/PROXY checkbox was flipped (D-06 boundary)"
    - "ROADMAP Phase 7 success criterion 2 references the remediation-arc set instead of the stale hardcoded 22"
    - "PROJECT.md Active list is purged down to BRIDGE-17 only, with one Validated bullet for the closed remediation arc"
    - "Phase-exit gate re-run: immutable install 0, audit 0, Hardhat 661/2/1, Foundry 215/2/3, slither --fail-high 0"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled checkboxes + traceability rows"
      contains: "[x] **DEP-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Rewritten Phase 7 success criterion (D-06) + D-01 sequencing note"
      contains: "remediation"
    - path: ".planning/PROJECT.md"
      provides: "Active purge + Validated bullet + advisory-fix Key Decision row"
      contains: "BRIDGE-17"
    - path: ".planning/STATE.md"
      provides: "Phase 7 close-out: status row, decisions log, next actions"
      contains: "### Phase 7 Decisions Logged (07-04)"
  key_links:
    - from: ".planning/REQUIREMENTS.md checkbox"
      to: "source-level probe evidence"
      via: "probe-then-flip rule (no flip without a passing probe)"
      pattern: "\\[x\\] \\*\\*(DEBT|SEC|PERF|QUAL|DEP)"
---

<objective>
Close the remediation arc in the planning artifacts with evidence-first reconciliation, rewrite the stale ROADMAP criterion (D-06), and run the phase-exit gate.

Purpose: REQUIREMENTS/ROADMAP/PROJECT have drifted from reality across 16 completed phases; this phase exists to eliminate exactly that staleness (D-07) without over-closing anything (Pitfall 7). The audit is only honest if every checkbox flip cites a probe run in THIS task, not memory.
Output: Reconciled docs, closed phase records, green phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-dependency-hardening/07-PATTERNS.md

<interfaces>
<!-- Reconciliation targets (verified current state 2026-08-27). -->

REQUIREMENTS.md — the 13 unchecked remediation-arc boxes to flip (lines ~10-45):
  DEBT-01, DEBT-04, DEBT-05, DEBT-06, SEC-01, SEC-02, SEC-03, SEC-04, SEC-08, PERF-01, PERF-02, QUAL-01, DEP-01
Already checked (no flip needed): DEBT-02, DEBT-03, SEC-05, SEC-06, SEC-07, TEST-01, TEST-02, TEST-03
NEVER flip (D-06/Pitfall 7 boundary): SWP-02/03/06/07/09, PROXY-01/02/03, BRIDGE-17 (Pending by design — SuperGenius#363 OPEN; gate record docs/Secure-BridgeIn-Exporter-ABI.md §5)
Traceability table rows (~lines 121-141): sync Status column to current checkbox state for ALL arcs (mechanical bookkeeping — zero checkbox changes outside the remediation 13)

ROADMAP.md Phase 7 section (lines ~163-173): criterion 2 currently reads
  "Full test suite passes (`yarn test` and `yarn forge:test`). All 22 requirements are verified complete."
PROJECT.md: Active bullets lines ~35-55 (purge all remediation items); line ~56 BRIDGE-17 stays; Key Decisions table lines ~97-104; footer line ~126
STATE.md: Phase Status table row 7 (line ~38); Current focus (line ~26); Next Actions (lines ~86-89)

Known-stale baselines (STATE.md authoritative — never fix, never tolerate anything else):
  Hardhat 661 passing / 2 pending / 1 failing — GNUSControlStorage.test.ts "should return initial protocol info" only
  Foundry 215 passed / 2 failed / 3 skipped — Phase 08.1 SafeDiamondCut + SafeSingleShotUpgrade setUp reverts only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-exit gate — re-run the deterministic hard gates</name>
  <files>none (verification-only gate — no file modifications)</files>
  <read_first>07-VALIDATION.md (phase gate row + known baselines), .planning/STATE.md Next Actions, 07-RESEARCH.md Pitfall 4/6</read_first>
  <action>
Deterministic subset of the full gate (the drift-prone external-intelligence scanners — snyk/socket/osv/semgrep — were executed and dispositioned in 07-03; re-running them here adds noise, not assurance — record that scoping decision):
1. "yarn install --immutable" → exit 0.
2. "yarn npm audit --severity moderate" → exit 0 (no pipe).
3. "yarn test" → 661/2/1 with only the GNUSControlStorage known-stale failure.
4. "yarn forge:test" → 215/2/3 with only the two Phase 08.1 setUp reverts.
5. "yarn slither:scan --fail-high" → exit 0 (run AFTER the test suites — Pitfall 4: the scan wipes/rebuilds forge artifacts).
Any deviation from these exact outcomes is a routing event: record it in the SUMMARY and surface — do not fix here (D-09).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && yarn install --immutable > /dev/null 2>&1 && echo IMMUTABLE-OK && yarn npm audit --severity moderate > /dev/null 2>&1 && echo AUDIT-OK && yarn test 2>&1 | grep -E 'passing|pending|failing' | head -4 && yarn forge:test 2>&1 | tail -15 && yarn slither:scan --fail-high > /tmp/07-04-slither-failhigh.log 2>&1; echo "SLITHER_FAILHIGH_EXIT=$?"</automated>
    IMMUTABLE-OK, AUDIT-OK, the Hardhat summary reads 661 passing / 2 pending / 1 failing, the Foundry suite reads 215 passed / 2 failed / 3 skipped (only the Phase 08.1 setUp reverts), SLITHER_FAILHIGH_EXIT=0 — all five gates of the acceptance criteria run in this command, in the action's step order (slither LAST per Pitfall 4: its forge clean/build side effects must follow the test suites).
  </verify>
  <acceptance_criteria>
    - yarn install --immutable exits 0; yarn npm audit --severity moderate exits 0
    - Hardhat 661/2/1 and Foundry 215/2/3, failures identical in kind to the documented known-stale sets (same test names, nothing new)
    - yarn slither:scan --fail-high exits 0
    - Zero code changes made in this task
  </acceptance_criteria>
  <done>Phase-exit gate green at every deterministic hard gate — immutable install, audit, Hardhat, Foundry, and slither --fail-high all executed and proven in the one automated command; any drift surfaced as a routing event rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md — probe-then-flip the 13 remediation boxes and sync the traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (full v1 section + Traceability), 07-RESEARCH.md Pattern 3, 07-PATTERNS.md REQUIREMENTS section (commit 3f7261e shape), git log evidence for phases 1-6 completions</read_first>
  <action>
PROBE-THEN-FLIP RULE: run each probe below BEFORE flipping its box; a failed probe means the requirement is NOT complete — leave it unchecked and record a routing note in the SUMMARY (that is the audit working, not failing). Flip shape per PATTERNS (commit 3f7261e): "- [ ]" → "- [x]" with description text unchanged, and the Traceability row flips Pending → Complete in the same commit.
Probes (evidence must be pasted into the commit/decision log, not asserted from memory):
- DEBT-01 (Phase 2, 02-02): "ls contracts/gnus-ai/GeniusAI.sol contracts/gnus-ai/GeniusAIStorage.sol" → both absent; "grep -c GeniusAI diamonds/GeniusDiamond/geniusdiamond.config.json" → 0.
- DEBT-04 (Phase 2, 02-01): read DiamondInitialize250() in DiamondInitFacet.sol — each role is set by exactly one of _setupRole/_grantRole, never both.
- DEBT-05 (Phase 2, 02-01): "grep -c 'modifier onlySuperAdminRole' contracts/gnus-ai/DiamondInitFacet.sol" → 0 (no local modifier definition; inherited usage remains).
- DEBT-06 (Phase 1, 01-02): hardhat.config.ts contains no commented-out network configuration blocks.
- SEC-01 (Phase 3, 03-01): mintBatch in ERC20TransferBatch.sol is either not payable or guards msg.value == 0.
- SEC-02 (Phase 3, 03-02 + superseded by Phase 9): GNUSBridge.sol withdraw() was DELETED by Phase 9's conversion-native rework — the truncation-loss surface no longer exists; flip citing Phase 3 validation AND Phase 9 removal (grep "function withdraw" contracts/gnus-ai/GNUSBridge.sol → 0 confirms).
- SEC-03 (Phase 3, 03-02): bridgeOut's destChainID != chainID (same-chain) guard present in GNUSBridge.sol.
- SEC-04 (Phase 3, 03-01): banTransferorBatch/allowTransferorBatch in GNUSControl.sol require tokenIds.length == bannedAddresses.length.
- SEC-08 (Phase 5): a diamond-level emergency pause mechanism exists and mutative paths check it (grep the pause integration across facets; name the facet/file in the evidence).
- PERF-01 (Phase 5): _beforeTokenTransfer in GNUSERC1155MaxSupply.sol aggregates in a single loop.
- PERF-02 (Phase 5): setDefaultBinCount in GNUSWithdrawLimiterStorage.sol enforces a maximum cap.
- QUAL-01 (Phase 2, 02-01): supportsInterface override present in DiamondInitFacet.sol.
- DEP-01 (Phase 7, 07-01): the pin grep — '#commit=bf67b736ad5fa3366551f599e204784856fb3069' in package.json → 1, and immutable install green (Task 1 evidence).
Traceability table: sync the Status column for ALL rows to the current checkbox state (already-checked SWP/LIC/BRIDGE rows flip Pending → Complete as pure bookkeeping — evidence is the checkbox itself; already-pending rows stay). Footer "_Last updated: ..._" bumped with a reconciliation note matching the existing convention.
BOUNDARY: no SWP/PROXY/BRIDGE-17 checkbox changes (Pitfall 7). BRIDGE-17 stays [ ] BY DESIGN.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && grep -c '\- \[x\] \*\*DEP-01\*\*' .planning/REQUIREMENTS.md && grep -c '\- \[ \] \*\*BRIDGE-17\*\*' .planning/REQUIREMENTS.md && grep -cE '\- \[ \] \*\*(DEBT|SEC|PERF|TEST|QUAL)' .planning/REQUIREMENTS.md ; grep -c '| DEP-01      | Phase 7    | Complete |' .planning/REQUIREMENTS.md</automated>
    Expected outputs in order: 1 (DEP-01 checked), 1 (BRIDGE-17 still unchecked), 0 (zero unchecked remediation-arc boxes remain), 1 (traceability row Complete).
  </verify>
  <acceptance_criteria>
    - All 13 remediation-arc boxes are [x], each backed by a probe output recorded in the decision log (probe evidence, not memory)
    - Zero unchecked boxes remain in the DEBT/SEC/PERF/TEST/QUAL/DEP sections; BRIDGE-17 remains [ ]
    - Traceability table Status column matches checkbox state for every row (all arcs, mechanical sync only)
    - Any probe that failed left its box unchecked with a routing note — no forced flips
    - Footer updated with the reconciliation note
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects verified reality across the remediation arc with cited evidence; the audit boundary (SWP/PROXY/BRIDGE-17) is intact.</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP criterion rewrite (D-06), PROJECT.md purge, STATE.md close-out</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md (Phase 7 section lines ~163-173 + header line 4 + Phase Summary row for 7), .planning/PROJECT.md (Active/Validated/Key Decisions/footer), .planning/STATE.md (Phase Status table + Current focus + Next Actions + 07-03 decisions section), 07-PATTERNS.md ROADMAP/PROJECT/STATE sections (analogs 1/2 and commit 263944a shape), 07-CONTEXT.md D-01/D-06</read_first>
  <action>
1. ROADMAP.md Phase 7 section: rewrite success criterion 2 from "All 22 requirements are verified complete." to reference the remediation-arc set, e.g. "Remediation-arc requirements verified complete in REQUIREMENTS.md (DEBT-01..06, SEC-01..08, PERF-01..02, TEST-01..03, QUAL-01, DEP-01 — 21 items); BRIDGE-17 remains Pending by design (SuperGenius#363 gate)." Add criterion 3 (numbered in place per PATTERNS): full security gate executed with written dispositions and the CI audit workflow at .github/workflows/security-audit.yml running the tokenless gate with secret-conditional snyk/socket steps. Add the D-01 sequencing note as plain text under the goal (per 07-RESEARCH OQ4 — gsd-sdk parses no dependency fields, so a plain note like "Executed last per D-01; Phases 9-15 complete 2026-08-27" is the correct form; no functional marker exists). Mark the four plan checkboxes [x] and set the Plans line to "4/4 plans complete". Bump the header "**Updated:**" field.
2. PROJECT.md: append ONE "✓" bullet to Validated summarizing the closed remediation arc — name the requirement set, the validating phases (1-7), and the key artifacts (package.json pin, .github/workflows/security-audit.yml, STATE 07-03 disposition record). Purge every Active bullet for remediation items (lines ~35-55) while KEEPING the BRIDGE-17 bullet verbatim. Add one Key Decisions row: "In-phase deprecation-advisory fixes (hardhat-multichain → @diamondslab/hardhat-multichain 1.1.0; eslint supported-line bump; semgrep stub removal)" with rationale "Owner ruling 2026-08-27 — the audit gate must exit 0 without waivers" and outcome "✓ Implemented". Bump the footer "_Last updated: ..._" to reference Phase 7 completion.
3. STATE.md: set the Phase Status row 7 to the completed shape ("✓ | 4/4 | 100%" — cf. rows for phases 1-6); append "### Phase 7 Decisions Logged (07-04)" with bullets for: the D-06 criterion rewrite wording, the reconciliation evidence rule (probe-then-flip), the remediation-arc closure (21/21), the advisory-fix decision (already logged in 07-01/07-03 sections — cross-reference, do not duplicate), and the phase-exit gate results from Task 1. Update "Current focus" (remediation arc complete; remaining tracked item: BRIDGE-17 gate) and rewrite "Next Actions" to: BRIDGE-17 tracking (SuperGenius#363 — production bridgeIn activation gate, record at docs/Secure-BridgeIn-Exporter-ABI.md §5) and milestone close-out as the next GSD step. Do NOT touch the frontmatter progress block (GSD-managed tooling).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && grep -c 'All 22 requirements' .planning/ROADMAP.md ; grep -ci 'remediation' .planning/ROADMAP.md && grep -c '✓' .planning/PROJECT.md && grep -c 'BRIDGE-17' .planning/PROJECT.md && grep -c 'Phase 7 Decisions Logged (07-04)' .planning/STATE.md && grep -cE '^\| 7     \| Dependency Hardening' .planning/STATE.md</automated>
    Expected outputs in order: 0 (stale "22" wording gone), ≥2 (criterion references the remediation set), ≥1 increased from baseline (new Validated bullet), ≥1 (BRIDGE-17 still present in Active), 1 (new decisions section), 1 (Phase 7 status row present).
  </verify>
  <acceptance_criteria>
    - ROADMAP criterion 2 contains no "All 22" wording and names the 21-item remediation set with the BRIDGE-17 carve-out; criterion 3 covers the audit gate + CI workflow; D-01 note present as plain text; plans list shows 4/4
    - PROJECT.md: one new ✓ Validated bullet; Active contains only BRIDGE-17; advisory-fix Key Decision row added; footer bumped
    - STATE.md: Phase 7 row completed; 07-04 decisions section appended; Next Actions/Current focus updated; frontmatter untouched
  </acceptance_criteria>
  <done>Planning artifacts tell the truth: remediation arc closed with evidence, stale figures rewritten, BRIDGE-17 correctly framed as the sole deliberate remainder, phase records complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → downstream consumers | Traceability claims drive routing/verification decisions for every future reader |
| phase-exit gate → phase sign-off | The gate output is the evidence the phase close rests on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Tampering | checkbox reconciliation | mitigate | Probe-then-flip rule: every flip requires a source-level probe output captured in this task; failed probe = no flip + routing note (marking from memory is the documented anti-pattern) |
| T-07-16 | Tampering/Repudiation | over-closing the audit | mitigate | Hard boundary (Pitfall 7): SWP/PROXY untouched, BRIDGE-17 stays Pending by design; traceability sync is mechanical only (Status ← checkbox state, no re-audit) |
| T-07-17 | Tampering | phase-exit gate | mitigate | Deterministic hard gates re-run in Task 1 with exact-count assertions across all five gates (immutable, audit, Hardhat, Foundry, slither) in the automated command itself; deviations are routing events, never silently tolerated or patched |
| T-07-18 | Repudiation | STATE.md close-out | mitigate | Decisions log cites probe evidence and cross-references 07-01/07-03 records instead of duplicating claims; frontmatter progress left to GSD tooling |

<verification>
Task 1 hard-gate exits (all five gates in the one automated command, slither last per Pitfall 4); Task 2 checkbox/traceability greps (1/1/0/1); Task 3 doc greps (0/≥2/≥1/≥1/1/1).
</verification>

<success_criteria>
- Remediation arc: 21/21 checked with cited probe evidence; BRIDGE-17 the sole deliberate remainder
- ROADMAP/PROJECT/STATE synchronized to verified reality
- Phase gate green within the documented tolerances
</success_criteria>

<output>
Create `.planning/phases/07-dependency-hardening/07-04-SUMMARY.md` when done
</output>
