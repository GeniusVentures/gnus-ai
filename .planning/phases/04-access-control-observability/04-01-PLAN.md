# Phase 4 Plan 01: Access Control & Observability

## Objective

Harden admin-only code paths with proper modifiers, add event emissions for super-admin bypass paths, and enable Slither static analysis on all production contracts.

## Tasks

### T1: Add `onlySuperAdminRole` to `diamondInitialize250()`

**File:** `contracts/gnus-ai/DiamondInitFacet.sol:39`

`diamondInitialize250()` is the only `_Initialize*` function without the modifier. The other three (`GNUSControl_Initialize230`, `GNUSNFTFactory_Initialize`, `GNUSNFTFactory_Initialize230`) already have it.

**Change:** Add `onlySuperAdminRole` modifier to `diamondInitialize250()`.

```solidity
function diamondInitialize250() public onlySuperAdminRole {
```

### T2: Create shared `SuperAdminBypass` event and emit at all bypass paths

**New:** `SuperAdminBypass` event in `contracts/gnus-ai/GNUSWithdrawLimiterStorage.sol` (where the limiter logic lives) or a shared interface.

**Event shape:**
```solidity
event SuperAdminBypass(address indexed caller, uint256 amount, string context);
```

**Emit at 3 bypass paths:**

| File | Line | Context |
|---|---|---|
| `GNUSBridge.sol` | 169 | `"GNUSBridge.withdraw"` |
| `GNUSERC1155MaxSupply.sol` | 57 | `"GNUSERC1155MaxSupply._beforeTokenTransfer"` |
| `ERC20TransferBatch.sol` | 156 | `"ERC20TransferBatch.batchTransfer"` |

Pattern: `else { emit SuperAdminBypass(operator, amount, "context"); }` — emit when the bypass IS taken (contract owner is caller).

### T3: Enable Slither on production contracts, fix all findings

**File:** `slither.config.json:13`

Remove `contracts/gnus-ai/` from `filter_paths`.

**Then:** Run `yarn slither:scan`, fix every finding. Likely findings include missing access control checks (already addressed by T1) and reentrancy/reentrancy-eth patterns on the bypass paths.

## Verification

- T1: `diamondInitialize250()` has `onlySuperAdminRole` modifier, compiles, all existing tests pass
- T2: Each bypass path emits `SuperAdminBypass` when owner calls; test that non-owner does NOT emit; test event fields
- T3: `yarn slither:scan` runs on `contracts/gnus-ai/` with 0 findings, or all findings addressed with fixes (no suppressions)
