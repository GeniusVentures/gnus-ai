---
phase: 14-private-network-ai-licensing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSNFTFactoryStorage.sol
  - test/unit/GNUSLifecycleUpgrade.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LIC-02, LIC-07]

must_haves:
  truths:
    - "Pre-Phase-14 token IDs decode the appended NFT struct fields with zero defaults (networkScope=PublicOnly, privateNetworkId=0, publicSettlementEnabled=false) and remain behaviorally unchanged"
    - "REQUIREMENTS LIC-04 no longer mentions USDC or Banxa contract rails"
    - "ROADMAP Phase 14 SC4 reflects the GNUS-burn rail and SC7 is annotated CLOSED by D-29"
  artifacts:
    - path: "contracts/gnus-ai/GNUSNFTFactoryStorage.sol"
      provides: "D-03/D-25 struct appends at slots +11/+12/+13"
      contains: "companyAdmin"
    - path: "test/unit/GNUSLifecycleUpgrade.test.ts"
      provides: "slot probe + zero-default decode test for +11/+12/+13"
  key_links:
    - from: "test/unit/GNUSLifecycleUpgrade.test.ts"
      to: "GNUSNFTFactoryStorage.layout().NFTs"
      via: "eth_getStorageAt probe"
      pattern: "nftSlot\\(.*1[123]\\)"
---

<objective>
Storage foundation + docs amendments for Phase 14: append the D-03 network-scope fields (plus D-25 `companyAdmin`) to the `NFT` struct with slot-annotated decode-compat proof, and amend the stale LIC-04 / ROADMAP SC4/SC7 wording per D-26/D-29.

Purpose: Every downstream facet depends on these struct fields; the docs amendments close LIC-07 (zero contract work) and remove the dead USDC/Banxa rail wording before executors read it as spec.
Output: Extended `GNUSNFTFactoryStorage.sol`, extended upgrade slot-probe suite, amended REQUIREMENTS.md and ROADMAP.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-private-network-ai-licensing/14-CONTEXT.md
@.planning/phases/14-private-network-ai-licensing/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs amendments — LIC-04 rewording (D-26) + ROADMAP SC4/SC7 closure (D-29)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/REQUIREMENTS.md (LIC-04 entry), .planning/ROADMAP.md (§Phase 14 goal + success criteria 4 and 7), .planning/phases/14-private-network-ai-licensing/14-CONTEXT.md (D-26, D-29)</read_first>
  <action>
Wording-only edits, no code.
1. REQUIREMENTS.md LIC-04: replace "USDC / GNUS-minions / Banxa-confirmed payment" and "All three rails" wording with: payment router facet accepting GNUS-minions payment only (paid GNUS BURNED per D-10), producing License NFT renewal + AI Credit minting/top-up; off-chain operator fiat path (~$20 → GV buys GNUS → CREATOR_ROLE mint) per D-26. Append "(amended 2026-08-25, D-26 — no USDC/Banxa contract code)". Also annotate LIC-07 as RESOLVED by D-07/D-08 (SG spend → GV wallet → Phase 10 bridgeIn → ops burn; no new mechanism). Also annotate LIC-06 (per D-28): its "Phase 9 mintBackedChild" reference is dead terminology — Phase 9 pivoted to the conversion-native model, so the requirement text must read that hybrid redeemability is provided by the existing Phase 13 REDEEM_TO_PARENT settlement plus Phase 9 `GNUSTreasury.convert()` collateralization; `mintBackedChild` does not exist. Append "(amended 2026-08-25, D-28 — Phase 9 convert() model; mintBackedChild does not exist)".
2. ROADMAP.md §Phase 14 goal line: change "payment router for USDC/GNUS/Banxa rails" to "GNUS-burn payment router + operator fiat path". SC4: change "USDC / GNUS-minions / Banxa-confirmed rails all produce equivalent final state" to "GNUS-minions rail (paid GNUS burned) produces license created/renewed + credits minted/extended + activation event; fiat path is off-chain operator minting". SC7: prefix "RESOLVED (D-07/D-29 2026-08-25): SG spend → GV wallet → existing Phase 10 bridgeIn → ops burn; no new on-chain settlement mechanism." Do not touch any other phase's entries.
  </action>
  <verify>
    <automated>grep -c "Banxa" .planning/REQUIREMENTS.md | grep -q "^0$" && grep -q "RESOLVED (D-07/D-29" .planning/ROADMAP.md && grep -q "amended 2026-08-25, D-26" .planning/REQUIREMENTS.md && grep -q "amended 2026-08-25, D-28" .planning/REQUIREMENTS.md && grep -q "GNUSTreasury.convert" .planning/REQUIREMENTS.md && echo DOCS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Banxa" .planning/REQUIREMENTS.md` returns 0
    - ROADMAP §Phase 14 contains the string "RESOLVED (D-07/D-29" at SC7 and "GNUS-burn payment router" in the goal
    - LIC-07 entry in REQUIREMENTS.md contains "RESOLVED by D-07/D-08"
    - LIC-06 entry in REQUIREMENTS.md contains "amended 2026-08-25, D-28" and references GNUSTreasury.convert(), with no remaining `mintBackedChild` mention
  </acceptance_criteria>
  <done>LIC-04/SC4 wording amended per D-26; SC7 and LIC-07 annotated resolved; no other phases' text changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03/D-25 NFT struct append + slot-probe upgrade test (LIC-02)</name>
  <files>contracts/gnus-ai/GNUSNFTFactoryStorage.sol, test/unit/GNUSLifecycleUpgrade.test.ts</files>
  <read_first>contracts/gnus-ai/GNUSNFTFactoryStorage.sol (whole file — Phase 13 append block lines ~22-33 is the discipline), test/unit/GNUSLifecycleUpgrade.test.ts (lines 56-84 slot helpers, 252-345 probe, 347+ legacy-behavior test), contracts/gnus-ai/GNUSLifecycleTypes.sol (enum-doc style)</read_first>
  <behavior>
    - Test: writing companyAdmin=X, privateNetworkId=N, networkScope=Hybrid(2), publicSettlementEnabled=true on a fresh token reads back exactly those values via getNFTInfo/storage probe at slots +11 (address, full slot), +12 (uint256, full slot), +13 (packed uint8 + bool).
    - Test: a token created and persisted WITHOUT setting the new fields reads networkScope=0 (PublicOnly), privateNetworkId=0, publicSettlementEnabled=false, companyAdmin=address(0) — zero-default decode.
    - Test: legacy-token behavioral-unchanged pattern (existing line ~347 test) still passes unmodified against the appended struct.
  </behavior>
  <action>
Append after `credentialVerifier` (slot +10) under a new banner comment `// Phase 14 appends below - do not reorder, do not insert above this line` with slot annotations mirroring the Phase 13 comment block: `address companyAdmin` (slot +11, D-25 — on-chain readable config field, operator-set at creation), `uint256 privateNetworkId` (slot +12, full slot — uint256 cannot share), `uint8 networkScope` and `bool publicSettlementEnabled` packed adjacent into slot +13 (1B + 1B). Doxygen comments `///< D-03/PD-3 — ...; 0 = PublicOnly (zero-default)` per PATTERNS. Do NOT reorder or insert above the Phase 13 block. Then extend GNUSLifecycleUpgrade.test.ts per the behavior block, writing probe assertions from the declared field order first and correcting from compiled bytecode if they disagree (documented lesson from plan 13-01, lines 77-80 of the test file). No protocolVersion or config changes in this task.
  </action>
  <verify>
    <automated>cd contracts/gnus-ai 2>/dev/null; npx hardhat test test/unit/GNUSLifecycleUpgrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "companyAdmin" contracts/gnus-ai/GNUSNFTFactoryStorage.sol` and `grep -q "privateNetworkId" contracts/gnus-ai/GNUSNFTFactoryStorage.sol`
    - GNUSLifecycleUpgrade.test.ts contains probe assertions for offsets +11, +12, +13
    - Suite passes with zero-default decode test green; no existing test in the file modified except additive
  </acceptance_criteria>
  <done>Struct appended append-only at +11/+12/+13; zero-default decode and round-trip probe tests green; Phase 13 tests in the file untouched and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployer→diamond | struct append must not change decode of existing stored tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | NFT struct append | mitigate | slot-annotated append-only fields below Phase 13 block + eth_getStorageAt probe test proving zero-default decode |
| T-14-01-02 | Repudiation | docs amendments | accept | wording-only planning docs, tracked in git |
</threat_model>

<verification>
- `npx hardhat test test/unit/GNUSLifecycleUpgrade.test.ts` green including new +11/+12/+13 probes
- Docs greps from Task 1 pass
</verification>

<success_criteria>
- LIC-02 proven by probe test; LIC-07 closed via annotation; D-26/D-29 docs amendments landed.
</success_criteria>

<output>
Create `.planning/phases/14-private-network-ai-licensing/14-01-SUMMARY.md` when done
</output>
