---
phase: 14-private-network-ai-licensing
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - contracts/gnus-ai/GNUSLicensingPurchase.sol
  - test/unit/GNUSLicensing.test.ts
  - diamonds/GeniusDiamond/geniusdiamond.config.json
autonomous: true
requirements: [LIC-01, LIC-03, LIC-04, LIC-05, LIC-06]

must_haves:
  truths:
    - "A permissionless buyer pays priceInMinions GNUS which is BURNED (GNUS totalSupply decreases by exactly priceInMinions) and credits are minted into the chosen device wallet (D-27 permissionless surface; D-19 device-wallet destination)"
    - "License NFTs are created as children of the product root only by CREATOR_ROLE/ADMIN; company credits are grandchildren; individual credits stay direct product-root children (D-02 hierarchy on the D-01 public EVM canonical billing layer; the company token is namespace-only per D-20 — no governance machinery)"
    - "LicenseActivated(companyAdmin, licenseId, privateNetworkId, expiresAt) fires on license creation and every renewal with the extended validUntil"
    - "Purchase of an inactive SKU or with insufficient allowance reverts with nothing minted and nothing burned"
    - "Hybrid-scope token with exchangeRate>0 + REDEEM_TO_PARENT is redeemable; burn-only SOULBOUND credits are not (D-05 hybrid redeemability via the D-28 convert() model; D-06 Phase 13 mechanisms referenced, never redefined)"
  artifacts:
    - path: "contracts/gnus-ai/GNUSLicensingPurchase.sol"
      provides: "purchase/renewal rail facet"
      contains: "purchase"
    - path: "test/unit/GNUSLicensing.test.ts"
      provides: "LIC-01/03/04/05/06 unit suite"
      min_lines: 200
  key_links:
    - from: "contracts/gnus-ai/GNUSLicensingPurchase.sol"
      to: "contracts/gnus-ai/GNUSLicensingStorage.sol"
      via: "SKU lookup + licenseSku mapping"
      pattern: "GNUSLicensingStorage\\.layout"
    - from: "contracts/gnus-ai/GNUSLicensingPurchase.sol"
      to: "GNUS ERC-20 transferFrom + ERC-1155 id-0 burn"
      via: "payment pull + burn (D-10)"
      pattern: "safeTransferFrom"
    - from: "contracts/gnus-ai/GNUSLicensingPurchase.sol"
      to: "createNFTWithLifecycle / _checkMintPolicy"
      via: "SKU-driven creation routes through existing creation path"
      pattern: "createNFTWithLifecycle"
---

<objective>
Build the GNUS-burn purchase/renewal rail facet (LIC-04 per amended D-26/D-10) and the full licensing unit suite proving the hierarchy (LIC-01), SKU gating (LIC-03), activation events (LIC-05), and hybrid redeemability config (LIC-06).

Purpose: This is the only permissionless money-touching surface of the phase — payment burn accounting and mint routing through existing policy hooks are the core invariants.
Output: GNUSLicensingPurchase.sol, GNUSLicensing.test.ts, config entry for the purchase facet.

Non-goal per D-07: no new settlement mechanism — private-spend settlement reuses existing Phase 10 rails; the purchase facet adds no settlement path (D-08 flag is informational only).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-private-network-ai-licensing/14-CONTEXT.md
@.planning/phases/14-private-network-ai-licensing/14-PATTERNS.md
@.planning/phases/14-private-network-ai-licensing/14-02-SUMMARY.md

<interfaces>
From plan 14-02 (GNUSLicensingTypes/Storage):
- struct SKU { uint256 priceInMinions; uint256 creditAmount; uint64 duration; bool createsLicense; bool renewsLicense; bool active; }
- event LicenseActivated(address indexed companyAdmin, uint256 indexed licenseId, uint256 privateNetworkId, uint64 expiresAt)
- GNUSLicensingStorage.layout().skus / .licenseSku

From plan 14-01 (GNUSNFTFactoryStorage NFT struct appends):
- nft.companyAdmin (slot +11), nft.privateNetworkId (+12), nft.networkScope + nft.publicSettlementEnabled (+13)

Existing code (verified line refs from RESEARCH.md):
- GNUSLifecycle.createNFTWithLifecycle (GNUSLifecycle.sol:335-365) — grandchild auth at 351-353, PerHolder+SOULBOUND allowed at 362-365
- GNUSLifecycleMint._checkMintPolicy (149-167, "Sale ended" gate at 155), _applyPerHolderRenewal (219-250)
- GNUSBridge burn internals with globalSupply/chainSupply decrements (GNUSBridge.sol:178-186)
- GNUSLifecycle.sol:205-207 — REDEEM_TO_PARENT ⇒ !nonConvertible gate (LIC-06 is config + tests, no new code)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GNUSLicensingPurchase facet — permissionless purchase, operator creation, renewal, burn payment</name>
  <files>contracts/gnus-ai/GNUSLicensingPurchase.sol</files>
  <read_first>contracts/gnus-ai/GNUSLifecycleMint.sol (whole file — mint facet pattern, _checkMintPolicy 149-167, _applyPerHolderRenewal 219-250), contracts/gnus-ai/GNUSLifecycle.sol (createNFTWithLifecycle 335-365, _requireCreatorOrAdmin 86-92, local _CREATOR_ROLE 33-36), contracts/gnus-ai/GNUSBridge.sol (burn path 178-186), contracts/gnus-ai/GNUSLicensingTypes.sol, contracts/gnus-ai/GNUSLicensingStorage.sol, contracts/gnus-ai/GNUSNFTFactoryStorage.sol, .planning/phases/14-private-network-ai-licensing/14-PATTERNS.md</read_first>
  <action>
Create the sibling action facet per PATTERNS (analogue GNUSLifecycleMint; facet-split header, local _CREATOR_ROLE, never calls GNUSLicensing). Surface:
1. `purchaseCredits(uint256 skuId, uint256 licenseId, address deviceWallet)` — permissionless (D-27): loads SKU from GNUSLicensingStorage, requires `sku.active` ("SKU inactive") and `!sku.createsLicense && !sku.renewsLicense`; payment leg: ERC-20 `safeTransferFrom(msg.sender, address(this)/diamond, sku.priceInMinions)` then BURN that GNUS through the existing ERC-1155 id-0 burn machinery so totalSupply decreases (globalSupply/chainSupply decrements per GNUSBridge.sol:178-186 pattern) — do NOT use GNUSTreasury.convert() (supply-neutral, Pitfall 5); mint leg: mint `sku.creditAmount` of the license's credit token into `deviceWallet` routing through the existing mint path so `_checkMintPolicy` gates (perWalletMintCap, "Sale ended", credential ordering) and `_applyPerHolderRenewal` extends the holder clock (Pitfall 6, top-up gets renewal free).
2. `createLicense(...)` — CREATOR_ROLE/ADMIN-gated (D-12): creates the License NFT as child of the product root via `createNFTWithLifecycle` (PerTokenId validUntil from sku.duration, D-12; networkScope/privateNetworkId/companyAdmin/publicSettlementEnabled set from calldata onto the new NFT struct fields), stores licenseSku[licenseId]=skuId, emits `LicenseActivated(companyAdmin, licenseId, privateNetworkId, expiresAt)` with the D-14 field order.
3. `renewLicense(uint256 skuId, uint256 licenseId)` — permissionless (D-27): requires active renewsLicense SKU; same burn payment leg; extends PerTokenId `validUntil` to `max(current, block.timestamp) + sku.duration` internally against storage (do NOT widen the role-gated setValidUntil external setter); re-emits LicenseActivated (LIC-05).
Never write GNUSNFTFactoryStorage lifecycle fields or GNUSLifecycleStorage directly except where the analog mint path does (Pitfall 3). CEI ordering throughout. All numeric literals as named constants. Compile check must show ≤ 24,576 bytes — if over, split further per D-16.
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LicenseActivated" contracts/gnus-ai/GNUSLicensingPurchase.sol`
    - `grep -q "safeTransferFrom" contracts/gnus-ai/GNUSLicensingPurchase.sol` and `grep -c "convert(" contracts/gnus-ai/GNUSLicensingPurchase.sol` returns 0
    - `grep -q "SKU inactive" contracts/gnus-ai/GNUSLicensingPurchase.sol`
    - Compiles clean; bytecode ≤ 24,576 bytes
  </acceptance_criteria>
  <done>Purchase/renewal/create surface implemented against plan 14-02 interfaces with D-10 burn payment and policy-hook-routed mints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GNUSLicensing unit suite + config entry (LIC-01/03/04/05/06)</name>
  <files>test/unit/GNUSLicensing.test.ts, diamonds/GeniusDiamond/geniusdiamond.config.json</files>
  <read_first>test/unit/GNUSLifecycleAICredits.test.ts (whole file — actor model deployer/creator/holder, fixtures, deployment helpers), test/unit/GNUSBridgePolicy.test.ts (D7 test block for policy expectations), contracts/gnus-ai/GNUSLicensingPurchase.sol, contracts/gnus-ai/GNUSLicensing.sol, diamonds/GeniusDiamond/geniusdiamond.config.json</read_first>
  <behavior>
    - LIC-03: creator configures SKU, non-privileged configureSKU reverts "Only creator or admin"; getSKU round-trips all seven fields; setSKUActive(false) then purchase reverts "SKU inactive".
    - LIC-04: purchaseCredits with adequate allowance: GNUS totalSupply delta == priceInMinions exactly (D-10); credit balance of deviceWallet == creditAmount; insufficient-allowance purchase reverts with no mint and totalSupply unchanged.
    - LIC-01: product root → license NFT (creator-only; non-creator createLicense reverts) → company credits as license children; individual credits as direct product-root children (Phase 13 shape); grandchild creation auth follows GNUSLifecycle.sol:351-353.
    - LIC-05: LicenseActivated emitted on createLicense with (companyAdmin, licenseId, privateNetworkId, expiresAt==validUntil); renewLicense extends validUntil to max(current,block.timestamp)+duration and re-emits LicenseActivated with the new expiry.
    - LIC-06: Hybrid-scope token (networkScope=Hybrid, exchangeRate>0, REDEEM_TO_PARENT) redeems via the existing Phase 13 path; burn-only SOULBOUND credit redeem/settle attempt yields zero GNUS (AI Credits invariant, Phase 13 SC7).
  </behavior>
  <action>
Write the suite per the behavior block, following GNUSLifecycleAICredits.test.ts structure (same deploy helpers, roles fixture). Then add the `"GNUSLicensingPurchase"` config entry per the 14-02 Task 2 pattern (next priority, versions["2.6"], fromVersions [0.0, 2.4, 2.5], never 2.7) and rerun the ABI regeneration pipeline. Do not modify known-stale baselines elsewhere.
  </action>
  <verify>
    <automated>npx hardhat test test/unit/GNUSLicensing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suite passes with tests covering all five behavior bullets
    - totalSupply-delta assertion uses exact equality with priceInMinions
    - `grep -A4 '"GNUSLicensingPurchase"' diamonds/GeniusDiamond/geniusdiamond.config.json | grep -q '2.6'`
    - Full `npx hardhat test` stays at known baseline (≥571 passing + new, 2 pending, only the known-stale GNUSControlStorage chainID failure)
  </acceptance_criteria>
  <done>All LIC-01/03/04/05/06 behaviors green; purchase facet registered at 2.6; full suite at known baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer→purchase facet | untrusted msg.sender + caller-chosen deviceWallet and licenseId cross here |
| diamond→GNUS ERC-20 | payment pull via user allowance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Spoofing/Repudiation | purchaseCredits | mitigate | payment pulled via safeTransferFrom from msg.sender; mint destination paid-for; SOULBOUND policy prevents transfer of minted credits |
| T-14-03-02 | Tampering | renewal validUntil extension | mitigate | extension only to max(current,block.timestamp)+duration via active renewsLicense SKU; LicenseActivated re-emitted for audit |
| T-14-03-03 | DoS | whale/maxSupply exhaustion | accept | bounded by per-token maxSupply + perWalletMintCap via _checkMintPolicy; attacker burns own GNUS (research D-11 table) |
| T-14-03-04 | Elevation of Privilege | createLicense | mitigate | CREATOR_ROLE/ADMIN gate per D-12; permissionless surface limited to credit top-up + renewal (D-27) |
| T-14-03-05 | Information | payment burn | accept | deflationary by design (D-10), no custody to drain |
</threat_model>

<verification>
- `npx hardhat test test/unit/GNUSLicensing.test.ts` green; full `npx hardhat test` at known baseline; config greps pass.
</verification>

<success_criteria>
Permissionless GNUS-burn rail + operator license creation + renewal with LicenseActivated events, all proven by unit suite; hierarchy per D-02 verified.
</success_criteria>

<output>
Create `.planning/phases/14-private-network-ai-licensing/14-03-SUMMARY.md` when done
</output>
