---
phase: 14-private-network-ai-licensing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSLicensingTypes.sol
  - contracts/gnus-ai/GNUSLicensingStorage.sol
  - contracts/gnus-ai/GNUSLicensing.sol
  - diamonds/GeniusDiamond/geniusdiamond.config.json
autonomous: true
requirements: [LIC-03]

must_haves:
  truths:
    - "A holder of CREATOR_ROLE or DEFAULT_ADMIN_ROLE can configure a SKU and a non-privileged caller reverts"
    - "An inactive SKU is stored and queryable but flagged inactive"
    - "The LicenseActivated event signature exists exactly as (companyAdmin, licenseId, privateNetworkId, expiresAt)"
    - "GNUSLicensing facet compiles under 24,576 bytes and registers at versions[\"2.6\"]"
  artifacts:
    - path: "contracts/gnus-ai/GNUSLicensingTypes.sol"
      provides: "NetworkScope enum, SKU struct, LicenseActivated event"
      contains: "enum NetworkScope"
    - path: "contracts/gnus-ai/GNUSLicensingStorage.sol"
      provides: "diamond storage Layout { skus, licenseSku }"
      contains: "GNUS_LICENSING_STORAGE_POSITION"
    - path: "contracts/gnus-ai/GNUSLicensing.sol"
      provides: "SKU CRUD + views facet"
      contains: "configureSKU"
  key_links:
    - from: "contracts/gnus-ai/GNUSLicensing.sol"
      to: "contracts/gnus-ai/GNUSLicensingStorage.sol"
      via: "layout() diamond-storage access"
      pattern: "GNUSLicensingStorage\\.layout"
    - from: "diamonds/GeniusDiamond/geniusdiamond.config.json"
      to: "GNUSLicensing facet"
      via: "versions[\"2.6\"] registration"
      pattern: "\"GNUSLicensing\""
---

<objective>
Create the licensing types, diamond-storage library, and SKU registry facet (LIC-03, D-04/D-12), and register the facet in the diamond config under protocolVersion 2.6 (D-15).

Purpose: Establishes the SKU registry and the `LicenseActivated` event contract that the purchase facet (plan 14-03) consumes. Interface-first: this plan defines the contracts, 14-03 implements against them.
Output: Three new contract files + one config entry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-private-network-ai-licensing/14-CONTEXT.md
@.planning/phases/14-private-network-ai-licensing/14-PATTERNS.md

<interfaces>
<!-- Contracts created by this plan; plan 14-03 consumes them. -->

GNUSLicensingTypes.sol:
- enum NetworkScope { PublicOnly, PrivateOnly, Hybrid }
- struct SKU { uint256 priceInMinions; uint256 creditAmount; uint64 duration; bool createsLicense; bool renewsLicense; bool active; }
- event LicenseActivated(address indexed companyAdmin, uint256 indexed licenseId, uint256 privateNetworkId, uint64 expiresAt)  // field order is the SG contract (D-14/A3) — do not reorder

GNUSLicensingStorage.sol:
- struct Layout { mapping(uint256 => SKU) skus; mapping(uint256 => uint256) licenseSku; }
- bytes32 constant GNUS_LICENSING_STORAGE_POSITION = keccak256("gnus.ai.licensing.storage")

GNUSLicensing.sol:
- configureSKU(uint256 skuId, SKU calldata sku) external   // CREATOR_ROLE or DEFAULT_ADMIN_ROLE
- setSKUActive(uint256 skuId, bool active) external         // same gate
- getSKU(uint256 skuId) external view returns (SKU memory)
- event SKUConfigured(uint256 indexed skuId, SKU sku, address indexed operator)
- event SKUActiveToggled(uint256 indexed skuId, bool active, address indexed operator)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + storage library + registry facet (LIC-03, D-04)</name>
  <files>contracts/gnus-ai/GNUSLicensingTypes.sol, contracts/gnus-ai/GNUSLicensingStorage.sol, contracts/gnus-ai/GNUSLicensing.sol</files>
  <read_first>contracts/gnus-ai/GNUSLifecycleTypes.sol (whole file — types/header style), contracts/gnus-ai/GNUSLifecycleStorage.sol (whole file — storage lib pattern), contracts/gnus-ai/GNUSLifecycle.sol (header lines 1-36 facet-split paragraph, lines 33-36 local _CREATOR_ROLE, lines 45-48 supportsInterface, lines 50-79 event style, lines 86-92 _requireCreatorOrAdmin), .planning/phases/14-private-network-ai-licensing/14-PATTERNS.md</read_first>
  <action>
Create the three files exactly per the `<interfaces>` block and PATTERNS.md assignments:
1. GNUSLicensingTypes.sol — pure types file, no imports, `pragma solidity ^0.8.19`, SPDX MIT, `@custom:security-contact support@gnus.ai`; enum ordinals documented with zero-default semantics (PublicOnly = 0). SKU struct has exactly the seven D-04 fields — NO `priceUsd`, no USD anything. `LicenseActivated` field order is fixed: companyAdmin, licenseId, privateNetworkId, expiresAt (D-14).
2. GNUSLicensingStorage.sol — copy GNUSLifecycleStorage structure wholesale: Layout {skus, licenseSku}, `GNUS_LICENSING_STORAGE_POSITION = keccak256("gnus.ai.licensing.storage")`, assembly `layout()` accessor; pure library, no imports; field-order-load-bearing comment.
3. GNUSLicensing.sol — registry facet: header imports GeniusAccessControl, GNUSConstants, GNUSNFTFactoryStorage, the two new files; facet-split Doxygen paragraph ("configuration + read paths only; purchase lives on the sibling facet; facets NEVER call each other — state shared only via diamond storage"); local `_CREATOR_ROLE = keccak256("CREATOR_ROLE")` constant (avoids circular import); `configureSKU(uint256 skuId, SKU calldata sku)` gated `hasRole(_CREATOR_ROLE, msg.sender) || hasRole(DEFAULT_ADMIN_ROLE, msg.sender)` reverting "Only creator or admin"; validation: `sku.priceInMinions > 0`, `sku.duration > 0`, and `!(sku.createsLicense && sku.renewsLicense)` (a SKU is one or the other, named constant error strings, no magic strings); `setSKUActive` same gate; `getSKU` view; events `SKUConfigured(uint256 indexed skuId, SKU sku, address indexed operator)` and `SKUActiveToggled(uint256 indexed skuId, bool active, address indexed operator)` with NatSpec `@param` per field; diamond-aware `supportsInterface` triple-OR override copied from GNUSLifecycle lines 45-48. No magic numbers (name constants per project rules); Solidity ^0.8.19.
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enum NetworkScope" contracts/gnus-ai/GNUSLicensingTypes.sol` and `grep -c "priceUsd" contracts/gnus-ai/GNUSLicensingTypes.sol` returns 0
    - `grep -q "gnus.ai.licensing.storage" contracts/gnus-ai/GNUSLicensingStorage.sol`
    - `grep -q "Only creator or admin" contracts/gnus-ai/GNUSLicensing.sol`
    - Compiles clean with `npx hardhat compile`; GNUSLicensing bytecode ≤ 24,576 bytes (check artifact `. bytecode.length / 2`)
  </acceptance_criteria>
  <done>Three files compile; SKU struct carries exactly the seven D-04 fields; CRUD role-gated; events emitted with operator index.</done>
</task>

<task type="auto">
  <name>Task 2: Diamond config registration at versions["2.6"] (D-15)</name>
  <files>diamonds/GeniusDiamond/geniusdiamond.config.json</files>
  <read_first>diamonds/GeniusDiamond/geniusdiamond.config.json (GNUSLifecycle / GNUSLifecycleMint entries ~lines 128-144 and current max priority), .planning/phases/14-private-network-ai-licensing/14-PATTERNS.md (config pattern)</read_first>
  <action>
Add a `"GNUSLicensing"` entry following the exact GNUSLifecycle pattern: next free priority after the current maximum, `"versions": { "2.6": { "fromVersions": [0.0, 2.4, 2.5] } }`. NEVER a 2.7 key (D-15). Then run the repo's existing diamond-abi/typechain regeneration pipeline (same command prior phases used after config changes — check package.json scripts; typically the compile/deploy prep task chain). GNUSLicensingPurchase gets its own entry in plan 14-03 — do not add it here.
  </action>
  <verify>
    <automated>grep -A4 '"GNUSLicensing"' diamonds/GeniusDiamond/geniusdiamond.config.json | grep -q '2.6' && ! grep -q '2\.7' diamonds/GeniusDiamond/geniusdiamond.config.json && echo CONFIG-OK</automated>
  </verify>
  <acceptance_criteria>
    - geniusdiamond.config.json contains `"GNUSLicensing"` with `versions["2.6"]` and fromVersions [0.0, 2.4, 2.5]
    - No `2.7` string anywhere in the config
    - ABI/typechain regeneration pipeline ran without error
  </acceptance_criteria>
  <done>GNUSLicensing registered at 2.6; ABI regenerated; no protocolVersion bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→registry facet | SKU mutation crosses trust boundary — must be role-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Elevation of Privilege | configureSKU / setSKUActive | mitigate | CREATOR_ROLE or DEFAULT_ADMIN_ROLE gate, "Only creator or admin" revert |
| T-14-02-02 | Tampering | SKU pricing data | mitigate | role gate + SKUConfigured event with indexed operator for audit |
| T-14-02-03 | DoS | facet bytecode size | mitigate | EIP-170 check in acceptance criteria; split further rather than trampoline (D-16) |
</threat_model>

<verification>
- `npx hardhat compile` clean; config greps pass; existing full suite unaffected (no behavior change yet).
</verification>

<success_criteria>
LIC-03 registry surface exists, compiles, and is registered at 2.6 with types/contracts ready for plan 14-03.
</success_criteria>

<output>
Create `.planning/phases/14-private-network-ai-licensing/14-02-SUMMARY.md` when done
</output>
