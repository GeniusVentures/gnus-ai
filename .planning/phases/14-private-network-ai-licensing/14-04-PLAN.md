---
phase: 14-private-network-ai-licensing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GNUSBridge.sol
  - test/unit/GNUSBridgePolicy.test.ts
autonomous: true
requirements: []  # LIC-05 covered by 14-03; this plan implements D-23 (expiry gate) + D-24 (SOULBOUND bridge amendment) for traceability

must_haves:
  truths:
    - "An expired SOULBOUND credit holder cannot bridgeOut (revert before limiter charge)"
    - "A CREATOR_ROLE or ADMIN caller CAN bridgeOut unexpired SOULBOUND credits (D-24 operator-mediated mint→bridge)"
    - "A non-privileged SOULBOUND holder still cannot bridgeOut"
    - "Expired EVM burns and settlements remain permitted (Phase 13 D5 carve-out untouched)"
    - "ISSUER_ONLY / CONTROLLED_RESALE still revert"
    - "Expiry reaches SuperGenius via the existing bridge attestation's EVM RPC lookup only — no bridgeOut event/message change (D-21)"
  artifacts:
    - path: "contracts/gnus-ai/GNUSBridge.sol"
      provides: "D-23/D-24 _enforceBridgePolicy gate"
      contains: "expired"
    - path: "test/unit/GNUSBridgePolicy.test.ts"
      provides: "D-24/D-23 behavior matrix"
  key_links:
    - from: "contracts/gnus-ai/GNUSBridge.sol"
      to: "GNUSLifecycleStorage.holderExpiresAt"
      via: "PerHolder expiry check in bridge policy"
      pattern: "holderExpiresAt"
---

<objective>
Amend Phase 13 D7 inside `GNUSBridge._enforceBridgePolicy`: permit SOULBOUND bridgeOut for CREATOR_ROLE/ADMIN callers while unexpired (D-24), and reject any expired holder's bridgeOut (D-23 gate) — covering PerTokenId `validUntil` and PerHolder `holderExpiresAt`.

Purpose: Unblocks the D-19/D-21/D-22 transport model (credits in device wallets bridgeOut to become SG timed UTXOs) while keeping non-privileged soulbound lockout and the expired-burn settlement carve-out intact.
Output: Modified gate in GNUSBridge.sol + extended policy test matrix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-private-network-ai-licensing/14-CONTEXT.md
@.planning/phases/14-private-network-ai-licensing/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-24/D-23 gate in _enforceBridgePolicy + test matrix</name>
  <files>contracts/gnus-ai/GNUSBridge.sol, test/unit/GNUSBridgePolicy.test.ts</files>
  <read_first>contracts/gnus-ai/GNUSBridge.sol (bridgeOut ordering 232-277 — call site at ~251 runs _enforceBridgePolicy BEFORE limiter charge and _burn; _enforceBridgePolicy 304-332; burn path 178-186), contracts/gnus-ai/GNUSLifecyclePolicy.sol (burn carve-out 186-190 — DO NOT TOUCH), contracts/gnus-ai/GNUSLifecycleMint.sol (line 155 "Sale ended" expiry-check analogue), contracts/gnus-ai/GNUSLifecycle.sol (expiry views 127-154, local _CREATOR_ROLE line 36), test/unit/GNUSBridgePolicy.test.ts (Phase 13 D7 test block)</read_first>
  <behavior>
    - Expired PerTokenId holder bridgeOut reverts; expired PerHolder holder (holderExpiresAt[id][sender] passed) bridgeOut reverts.
    - Unexpired SOULBOUND bridgeOut by CREATOR_ROLE or ADMIN caller succeeds.
    - Unexpired SOULBOUND bridgeOut by a non-privileged holder reverts ("Policy-bound token cannot bridge in v1").
    - ISSUER_ONLY and CONTROLLED_RESALE still revert unchanged.
    - Regression: expired burn / settleExpired still succeeds (policy burn carve-out untouched); reverted bridge consumes no limiter allowance (limiter state unchanged after revert).
  </behavior>
  <action>
Modify only the SOULBOUND fallthrough of `_enforceBridgePolicy` (GNUSBridge.sol ~330-331). Replace the unconditional revert for SOULBOUND with: allow when caller holds `DEFAULT_ADMIN_ROLE` or CREATOR_ROLE (local `_CREATOR_ROLE` constant, import-free — GeniusAccessControl is already a base) AND the token is unexpired for that holder: PerTokenId — revert if `nft.validUntil != 0 && block.timestamp >= nft.validUntil`; PerHolder — revert if `GNUSLifecycleStorage.layout().holderExpiresAt[id][sender] != 0 && block.timestamp >= holderExpiresAt[id][sender]` (mirrors "Sale ended" at GNUSLifecycleMint.sol:155); expiration-mode None passes. Error strings as named constants ("License expired" style), no magic strings. ISSUER_ONLY / CONTROLLED_RESALE keep the existing revert. Do NOT move the call site at line ~251 (must stay before limiter + _burn, Pitfall 2) and do NOT touch GNUSLifecyclePolicy's always-permitted burn branch. Then extend GNUSBridgePolicy.test.ts per the behavior block, adding to the existing Phase 13 D7 block style.
  </action>
  <verify>
    <automated>npx hardhat test test/unit/GNUSBridgePolicy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Expired-holder and non-privileged-soulbound bridgeOut tests assert reverts; privileged unexpired passes
    - Limiter-unchanged assertion present after a reverted bridgeOut
    - `grep -c "GNUSLifecyclePolicy" contracts/gnus-ai/GNUSBridge.sol` unchanged in intent — no edits to the policy library file itself (`git diff --stat contracts/gnus-ai/GNUSLifecyclePolicy.sol` empty)
    - GNUSBridge bytecode ≤ 24,576 bytes (baseline 22,711 B after Phase 13 — record new size in SUMMARY)
    - Full `npx hardhat test` at known baseline (only the known-stale GNUSControlStorage chainID failure)
  </acceptance_criteria>
  <done>D-24/D-23 gate live with full matrix green; expired-burn carve-out and limiter ordering regression-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| holder→bridgeOut | untrusted sender crossing into bridge policy; expiry + role are the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Elevation of Privilege | SOULBOUND bridge allowance | mitigate | restricted to CREATOR_ROLE/ADMIN (D-24); non-privileged reverts unchanged |
| T-14-04-02 | Tampering | expired value transport to SG | mitigate | D-23 expiry gate covering both PerTokenId validUntil and PerHolder holderExpiresAt, checked pre-limiter |
| T-14-04-03 | DoS | limiter griefing via revert | mitigate | gate ordering kept before checkAndRecordWithdraw (assert limiter unchanged on revert) |
| T-14-04-04 | Tampering | expired burn/settlement path | accept-by-design | Phase 13 D5 carve-out intentionally permits expired burns (ops settlement) — regression-tested, not "fixed" |
</threat_model>

<verification>
- `npx hardhat test test/unit/GNUSBridgePolicy.test.ts` green; full suite at known baseline; bytecode size recorded.
</verification>

<success_criteria>
D-24 amendment and D-23 gate implemented and proven, with zero regression to Phase 13 bridge policy behavior.
</success_criteria>

<output>
Create `.planning/phases/14-private-network-ai-licensing/14-04-SUMMARY.md` when done
</output>
