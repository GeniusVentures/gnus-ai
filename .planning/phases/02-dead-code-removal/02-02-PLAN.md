---
phase: 02-dead-code-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/GeniusAI.sol
  - contracts/gnus-ai/GeniusAIStorage.sol
  - diamonds/GeniusDiamond/geniusdiamond.config.json
  - diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json
  - test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json
  - test/unit/GeniusAI.test.ts
  - test/unit/GeniusAIStorage.test.ts
  - docs/GeniusAI.md
  - docs/GeniusAIStorage.md
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "GeniusAI.sol and GeniusAIStorage.sol no longer exist in contracts/gnus-ai/"
    - "GeniusAI facet entry is removed from all active diamond configs"
    - "Hardhat compilation succeeds after removal and cache cleanup"
    - "TypeChain types no longer export GeniusAI"
  artifacts:
    - path: "contracts/gnus-ai/GeniusAI.sol"
      provides: "DELETED — formerly GeniusAI escrow facet"
      must_not_exist: true
    - path: "contracts/gnus-ai/GeniusAIStorage.sol"
      provides: "DELETED — formerly GeniusAI storage library"
      must_not_exist: true
    - path: "diamonds/GeniusDiamond/geniusdiamond.config.json"
      provides: "Primary diamond config — GeniusAI entry removed"
      not_contains: '"GeniusAI"'
    - path: "diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json"
      provides: "Override diamond config — GeniusAI entry removed"
      not_contains: '"GeniusAI"'
    - path: "test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json"
      provides: "Test diamond config — GeniusAI entry removed"
      not_contains: '"GeniusAI"'
  key_links:
    - from: "diamonds/GeniusDiamond/geniusdiamond.config.json"
      to: "none"
      via: "facet removal"
      pattern: '"GeniusAI" not present in facets object'
    - from: "npx hardhat compile"
      to: "typechain-types/"
      via: "regeneration"
      pattern: "No GeniusAI export in typechain-types/index.ts"
---

<objective>
Remove the GeniusAI facet entirely from the codebase. The GeniusAI escrow functionality has been moved to the SuperGenius chain and is no longer needed in these contracts.

Purpose: Eliminate dead code that increases compilation time, audit surface, and maintenance burden. The GeniusAI facet was the only consumer of the `AIProcessingJob` struct and escrow logic.

Output: GeniusAI.sol and GeniusAIStorage.sol deleted. GeniusAI entries removed from 3 diamond config files. Test files, docs, artifacts, and typechain types cleaned up. Full compilation verified.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dead-code-removal/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete GeniusAI source files, tests, and docs</name>
  <files>contracts/gnus-ai/GeniusAI.sol, contracts/gnus-ai/GeniusAIStorage.sol, test/unit/GeniusAI.test.ts, test/unit/GeniusAIStorage.test.ts, docs/GeniusAI.md, docs/GeniusAIStorage.md</files>
  <read_first>
  - contracts/gnus-ai/GeniusAI.sol — confirm no other contracts import this file
  - contracts/gnus-ai/GeniusAIStorage.sol — confirm the AIProcessingJob struct and library are only used by GeniusAI.sol
  - test/unit/GeniusAI.test.ts — identify test file to delete
  - test/unit/GeniusAIStorage.test.ts — identify test file to delete
  </read_first>
  <action>
Delete the GeniusAI dead code. No Solidity contract imports GeniusAI.sol or GeniusAIStorage.sol — this is confirmed by `grep -rn "GeniusAI" --include="*.sol" contracts/` returning only self-references. The escrow has been moved to SuperGenius chain.

Delete these files (use `rm`):
1. `contracts/gnus-ai/GeniusAI.sol` — escrow facet (40 lines)
2. `contracts/gnus-ai/GeniusAIStorage.sol` — storage library with AIProcessingJob struct (39 lines)
3. `test/unit/GeniusAI.test.ts` — unit tests for GeniusAI
4. `test/unit/GeniusAIStorage.test.ts` — unit tests for GeniusAIStorage (if exists)
5. `docs/GeniusAI.md` — documentation
6. `docs/GeniusAIStorage.md` — documentation (if exists)

After deletion, run `npx hardhat clean` to purge stale compilation artifacts and cache entries for these files. This removes `artifacts/contracts/gnus-ai/GeniusAI.sol/`, `artifacts/contracts/gnus-ai/GeniusAIStorage.sol/`, and `cache/solidity-files-cache.json` GeniusAI entries.

Do NOT touch: `coverage/` directory (coverage HTML reports), `.vscode/slither-results.json` (will be stale but non-blocking), `diamonds/GeniusDiamond/archive/` (historical configs — preserved as-is), `diamonds/GeniusDiamond/deployments/` (runtime state — updated on redeploy only).
  </action>
  <verify>
    <automated>test ! -f contracts/gnus-ai/GeniusAI.sol && test ! -f contracts/gnus-ai/GeniusAIStorage.sol && test ! -f test/unit/GeniusAI.test.ts && echo "PASS: Source files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/gnus-ai/GeniusAI.sol` does not exist
    - `contracts/gnus-ai/GeniusAIStorage.sol` does not exist
    - `test/unit/GeniusAI.test.ts` does not exist
    - `artifacts/contracts/gnus-ai/GeniusAI.sol/` directory does not exist (cleared by `npx hardhat clean`)
  </acceptance_criteria>
  <done>All GeniusAI source files, tests, and docs are deleted; hardhat cache is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove GeniusAI from all active diamond configs</name>
  <files>diamonds/GeniusDiamond/geniusdiamond.config.json, diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json, test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json</files>
  <read_first>
  - diamonds/GeniusDiamond/geniusdiamond.config.json — lines 62-69 contain the GeniusAI facet entry (priority 70, deployInit GeniusAI_Initialize)
  - diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json — lines 58-64 contain the GeniusAI facet entry
  - test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json — lines 52-58 contain the GeniusAI facet entry
  </read_first>
  <action>
Remove the `GeniusAI` facet entry from all three active diamond configuration files. Each config file contains a `"facets"` JSON object with a `"GeniusAI"` key — remove the entire key-value pair including the trailing comma from the preceding entry.

**File 1: `diamonds/GeniusDiamond/geniusdiamond.config.json`** (lines 62-69)
Remove the entire block:
```json
    "GeniusAI": {
      "priority": 70,
      "versions": {
        "0.0": {
          "deployInit": "GeniusAI_Initialize()"
        }
      }
    },
```
Note the trailing comma after the closing `}` — remove it along with the block. The preceding entry (`ERC1155ProxyOperator`) must end without a trailing comma after `GeniusAI` is removed.

**File 2: `diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json`** (lines 58-64)
Same removal pattern.

**File 3: `test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json`** (lines 52-58)
Same removal pattern.

After editing each file, validate the JSON is still valid with `node -e "JSON.parse(require('fs').readFileSync('{file}', 'utf8'))"`. The JSON must parse without errors — pay attention to trailing commas in the facet object before and after the removed entry.

Configs NOT modified: `diamonds/GeniusDiamond/archive/` (historical), `diamonds/GeniusDiamond/deployments/` (runtime state), any other `.json` files in the diamond directory.
  </action>
  <verify>
    <automated>bash test/unit/cleanup-02-02.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"GeniusAI"' diamonds/GeniusDiamond/geniusdiamond.config.json` returns 0
    - `grep -c '"GeniusAI"' diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json` returns 0
    - `grep -c '"GeniusAI"' test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json` returns 0
    - `node -e "JSON.parse(require('fs').readFileSync('diamonds/GeniusDiamond/geniusdiamond.config.json', 'utf8'))"` succeeds
    - `node -e "JSON.parse(require('fs').readFileSync('diamonds/GeniusDiamond/geniusdiamond-erc1155override.config.json', 'utf8'))"` succeeds
    - `node -e "JSON.parse(require('fs').readFileSync('test-assets/test-diamonds/GeniusDiamond/geniusdiamond.config.json', 'utf8'))"` succeeds
  </acceptance_criteria>
  <done>All three diamond configs have GeniusAI entries removed and remain valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Recompile, verify typechain regeneration, and validate full cleanup</name>
  <files>typechain-types/ (regenerated), artifacts/ (regenerated), cache/ (regenerated)</files>
  <read_first>
  - hardhat.config.ts — confirm compiler settings and typechain configuration
  - package.json — check hardhat and typechain scripts available
  </read_first>
  <action>
Regenerate compilation artifacts and typechain types after GeniusAI removal. Run in sequence:

1. `npx hardhat clean` — ensures no stale artifacts remain (belt-and-suspenders with Task 1's clean)
2. `npx hardhat compile` — full recompilation of all contracts, generates new artifacts and typechain types
3. Verify GeniusAI is gone from generated output:
   - `grep -r "GeniusAI" typechain-types/` should return nothing (or only 0-match)
   - `grep -r "GeniusAI" artifacts/` should return nothing
   - `grep "GeniusAI" cache/solidity-files-cache.json` should return nothing

After compile succeeds, run the bash verification script to confirm full cleanup:
4. `bash test/unit/cleanup-02-02.test.sh` — verifies no GeniusAI references remain in source files, configs, or generated output

If the test script does not exist yet, create `test/unit/cleanup-02-02.test.sh` as a Wave 0 scaffold with grep assertions for: GeniusAI.sol absence, GeniusAIStorage.sol absence, config entries absence, typechain exports absence, hardhat compile success.

If compilation fails with errors, debug: check for remaining GeniusAI imports in other Solidity files (should be none per research), check config JSON validity, check for stale cache. Run `npx hardhat clean` again if needed.
  </action>
  <verify>
    <automated>npx hardhat compile && bash test/unit/cleanup-02-02.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0 with no errors
    - `grep -r "GeniusAI" typechain-types/ 2>/dev/null` returns no matches (or `grep -c` returns 0)
    - `grep "GeniusAI" cache/solidity-files-cache.json 2>/dev/null` returns no matches
    - `bash test/unit/cleanup-02-02.test.sh` exits 0 with all PASS lines
  </acceptance_criteria>
  <done>Full recompilation succeeds; typechain types, artifacts, and cache contain zero GeniusAI references; bash verification script passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diamond config→Facet registry | GeniusAI facet removal must not leave dangling references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Diamond config JSON | mitigate | Validate all three configs are valid JSON after edit; ensure preceding facet entry has no trailing comma after comma removal |
| T-02-04 | Information Disclosure | Stale typechain exports | mitigate | Verify `grep -r GeniusAI typechain-types/` returns zero matches after recompile; stale ABI could confuse auditors |
| T-02-SC | Tampering | npm installs | accept | No new packages installed — Solidity deletion + config edit only |
</threat_model>

<verification>
Run `bash test/unit/cleanup-02-02.test.sh` after Task 3. This script must verify:
1. GeniusAI.sol and GeniusAIStorage.sol do not exist
2. No "GeniusAI" string in any of the three config files
3. `npx hardhat compile` succeeds
4. No GeniusAI exports in typechain-types/

Create the TDD script if it does not exist (MISSING → Wave 0 scaffold).
</verification>

<success_criteria>
- [ ] `contracts/gnus-ai/GeniusAI.sol` deleted
- [ ] `contracts/gnus-ai/GeniusAIStorage.sol` deleted
- [ ] Test files and docs for GeniusAI deleted
- [ ] Three diamond config files: GeniusAI entries removed, valid JSON
- [ ] `npx hardhat compile` succeeds
- [ ] Zero GeniusAI references in typechain-types/, artifacts/, cache/
- [ ] Bash verification script passes
</success_criteria>

<output>
Create `.planning/phases/02-dead-code-removal/02-02-SUMMARY.md` when done
</output>
