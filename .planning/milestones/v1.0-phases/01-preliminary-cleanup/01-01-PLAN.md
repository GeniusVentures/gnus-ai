---
phase: 01-preliminary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/gnus-ai/DiamondInitFacet.sol
  - contracts/gnus-ai/GNUSWithdrawLimiterStorage.sol
  - contracts/gnus-ai/ERC20TransferBatch.sol
  - contracts/gnus-ai/GNUSBridge.sol
  - contracts/gnus-ai/GNUSERC1155MaxSupply.sol
  - contracts/gnus-ai/GNUSControl.sol
  - contracts/gnus-ai/GeniusAIStorage.sol
  - contracts/gnus-ai/GNUSNFTFactoryStorage.sol
  - contracts/gnus-ai/libraries/TransferHelper.sol
  - contracts/gnus-ai/GNUSNFTFactory.sol
  - contracts/gnus-ai/GeniusAI.sol
  - contracts/gnus-ai/GNUSControlStorage.sol
  - contracts/gnus-ai/GNUSContractAssets.sol
  - contracts/gnus-ai/GeniusDiamond.sol
  - contracts/gnus-ai/GNUSNFTCollectionName.sol
  - contracts/gnus-ai/GeniusOwnershipFacet.sol
  - contracts/gnus-ai/GNUSConstants.sol
  - contracts/gnus-ai/ERC1155ProxyOperator.sol
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "DiamondInitFacet.sol no longer imports hardhat/console.sol or calls console.log()"
    - "Init observability is maintained via the existing InitLog event emission"
    - "All production contracts in contracts/gnus-ai/ use pragma solidity ^0.8.19; (18 files standardized from ^0.8.0 or ^0.8.2)"
    - "Compiler warnings for mismatched pragmas are resolved"
  artifacts:
    - path: "contracts/gnus-ai/DiamondInitFacet.sol"
      provides: "Diamond initialization without dev-only console import"
      contains: "pragma solidity ^0.8.19;"
      lacks: "hardhat/console.sol"
      lacks: "console.log"
    - path: "contracts/gnus-ai/GNUSWithdrawLimiterStorage.sol"
      provides: "Withdraw limiter storage"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/ERC20TransferBatch.sol"
      provides: "Batch token transfers"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSBridge.sol"
      provides: "Cross-chain bridge"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSERC1155MaxSupply.sol"
      provides: "ERC1155 supply enforcement"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSControl.sol"
      provides: "Protocol controls"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GeniusAIStorage.sol"
      provides: "AI escrow storage"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSNFTFactoryStorage.sol"
      provides: "NFT factory storage"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/libraries/TransferHelper.sol"
      provides: "Transfer utility library"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSNFTFactory.sol"
      provides: "NFT factory"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GeniusAI.sol"
      provides: "AI escrow"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSControlStorage.sol"
      provides: "Control storage"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSContractAssets.sol"
      provides: "Asset recovery"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GeniusDiamond.sol"
      provides: "Diamond proxy"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSNFTCollectionName.sol"
      provides: "Collection metadata"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GeniusOwnershipFacet.sol"
      provides: "Ownership facet"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/GNUSConstants.sol"
      provides: "Shared constants"
      contains: "pragma solidity ^0.8.19;"
    - path: "contracts/gnus-ai/ERC1155ProxyOperator.sol"
      provides: "Marketplace proxy operator"
      contains: "pragma solidity ^0.8.19;"
  key_links:
    - from: "contracts/gnus-ai/DiamondInitFacet.sol"
      to: "InitLog event (line 30)"
      via: "emit InitLog(sender, ...)"
      pattern: "emit InitLog"
---

<objective>
Remove the development-only `hardhat/console.sol` import and `console.log()` call from `DiamondInitFacet.sol` (DEBT-02), and standardize all production Solidity pragmas in `contracts/gnus-ai/` to `^0.8.19` (DEBT-03).

Purpose: Eliminate dev tooling from production contracts and create a consistent compiler target. The existing `InitLog` event already provides initialization observability — no replacement logic needed.

Output: 18 modified `.sol` files with consistent `pragma solidity ^0.8.19;` and zero `console` references.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@contracts/gnus-ai/DiamondInitFacet.sol
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove console.log from DiamondInitFacet.sol and standardize pragmas across all contracts</name>
  <files>
    contracts/gnus-ai/DiamondInitFacet.sol
    contracts/gnus-ai/GNUSWithdrawLimiterStorage.sol
    contracts/gnus-ai/ERC20TransferBatch.sol
    contracts/gnus-ai/GNUSBridge.sol
    contracts/gnus-ai/GNUSERC1155MaxSupply.sol
    contracts/gnus-ai/GNUSControl.sol
    contracts/gnus-ai/GeniusAIStorage.sol
    contracts/gnus-ai/GNUSNFTFactoryStorage.sol
    contracts/gnus-ai/libraries/TransferHelper.sol
    contracts/gnus-ai/GNUSNFTFactory.sol
    contracts/gnus-ai/GeniusAI.sol
    contracts/gnus-ai/GNUSControlStorage.sol
    contracts/gnus-ai/GNUSContractAssets.sol
    contracts/gnus-ai/GeniusDiamond.sol
    contracts/gnus-ai/GNUSNFTCollectionName.sol
    contracts/gnus-ai/GeniusOwnershipFacet.sol
    contracts/gnus-ai/GNUSConstants.sol
    contracts/gnus-ai/ERC1155ProxyOperator.sol
  </files>
  <behavior>
    - Test 1: DiamondInitFacet.sol contains zero occurrences of "console" (grep returns empty)
    - Test 2: DiamondInitFacet.sol uses `pragma solidity ^0.8.19;`
    - Test 3: All 18 .sol files in contracts/gnus-ai/ (including libraries/) use `pragma solidity ^0.8.19;` exactly — no other pragma versions remain
    - Test 4: GeniusAccessControl.sol and GNUSWithdrawLimiter.sol already have ^0.8.19 — they are unchanged and still verify correctly
  </behavior>
  <action>
    **DEBT-02 — Remove console.log from DiamondInitFacet.sol:**
    1. READ the file first to see current state
    2. Remove line 4: `import "hardhat/console.sol";`
    3. Remove line 46: `console.log("DiamondInitFacet: diamondInitialize250 called");`
    4. The `InitLog` event at line 30 is already emitted at line 48 (`emit InitLog(sender, "diamondInitialize Function called");`) — no replacement logging is needed; observability is already in place
    5. Do NOT add new events or modify any other code

    **DEBT-03 — Standardize all pragmas to ^0.8.19 (per D-03, CONVENTIONS.md line 60: production contracts use ^0.8.19):**
    6. For each of these 18 files, change the pragma line from their current version to `pragma solidity ^0.8.19;`:
       - DiamondInitFacet.sol: `^0.8.2` → `^0.8.19`
       - GNUSWithdrawLimiterStorage.sol: `^0.8.2` → `^0.8.19`
       - ERC20TransferBatch.sol: `^0.8.2` → `^0.8.19`
       - GNUSBridge.sol: `^0.8.2` → `^0.8.19`
       - GNUSERC1155MaxSupply.sol: `^0.8.2` → `^0.8.19`
       - GNUSControl.sol: `^0.8.2` → `^0.8.19`
       - GeniusAIStorage.sol: `^0.8.2` → `^0.8.19`
       - GNUSNFTFactoryStorage.sol: `^0.8.2` → `^0.8.19`
       - TransferHelper.sol: `^0.8.2` → `^0.8.19`
       - GNUSNFTFactory.sol: `^0.8.2` → `^0.8.19`
       - GeniusAI.sol: `^0.8.2` → `^0.8.19`
       - GNUSControlStorage.sol: `^0.8.2` → `^0.8.19`
       - GNUSContractAssets.sol: `^0.8.2` → `^0.8.19`
       - GeniusDiamond.sol: `^0.8.2` → `^0.8.19`
       - GNUSNFTCollectionName.sol: `^0.8.2` → `^0.8.19`
       - GeniusOwnershipFacet.sol: `^0.8.0` → `^0.8.19`
       - GNUSConstants.sol: `^0.8.2` → `^0.8.19`
       - ERC1155ProxyOperator.sol: `^0.8.2` → `^0.8.19`
    7. Do NOT touch GeniusAccessControl.sol or GNUSWithdrawLimiter.sol — they are already at `^0.8.19`
    8. Do NOT change any non-pragma lines in any file
    9. Run Solidity compilation after all changes: `yarn hardhat compile`

  </action>
  <verify>
    <automated>grep -rn "console" contracts/gnus-ai/DiamondInitFacet.sol && echo "FAIL: console found" || echo "PASS: no console"</automated>
    <automated>grep -rn "pragma solidity" contracts/gnus-ai/ | grep -v "^0.8.19" && echo "FAIL: non-standard pragma found" || echo "PASS: all pragmas ^0.8.19"</automated>
    <automated>yarn hardhat compile</automated>
  </verify>
  <done>
    1. `grep -c "console" contracts/gnus-ai/DiamondInitFacet.sol` returns 0
    2. `grep -c "pragma solidity" contracts/gnus-ai/*.sol contracts/gnus-ai/libraries/*.sol` returns 20 matches (18 changed + 2 already correct), all showing `^0.8.19;`
    3. `yarn hardhat compile` exits 0 with no warnings about mismatched pragmas
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                   |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Solidity compile target | Changing pragma from ^0.8.0/^0.8.2 to ^0.8.19 shifts compiler version expectations — must verify no breaking language changes |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                                                                                                                                           |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-01   | Information Disclosure | DiamondInitFacet.sol | mitigate    | Remove `console.log()` — prevents accidental logging in production deployments where Hardhat console could leak initialization state. Replace with existing `InitLog` event which is a proper on-chain log with access control semantics. |
| T-01-02   | Tampering              | All 18 .sol files    | mitigate    | Pragma change only — no logic modified. Compiler version bump from 0.8.0/0.8.2 to 0.8.19 has no breaking Solidity changes between these minor versions (all 0.8.x). Full compilation (`yarn hardhat compile`) verifies no regressions.    |
| T-01-03   | Denial of Service      | Compilation pipeline | mitigate    | If any contract inadvertently uses a feature deprecated between 0.8.2 and 0.8.19, the compilation gate (`yarn hardhat compile`) catches it. Post-change compilation must pass with zero errors.                                           |

</threat_model>

<verification>
1. `grep -rn "console" contracts/gnus-ai/DiamondInitFacet.sol` — MUST return empty
2. `grep -rn "pragma solidity" contracts/gnus-ai/ | grep -v "0.8.19"` — MUST return empty
3. `yarn hardhat compile` — MUST exit 0
</verification>

<success_criteria>

- DiamondInitFacet.sol: zero `console` references, pragma at `^0.8.19`
- All 18 targeted .sol files (excluding GeniusAccessControl.sol and GNUSWithdrawLimiter.sol which are already at ^0.8.19): pragma reads `^0.8.19;`
- Hardhat compilation passes with zero errors
  </success_criteria>

<output>
Create `.planning/phases/01-preliminary-cleanup/01-01-SUMMARY.md` when done
</output>
