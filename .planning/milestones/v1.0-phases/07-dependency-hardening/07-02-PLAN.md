---
phase: 07-dependency-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env # untracked (gitignored) — token storage only; no tracked file changes in this plan
autonomous: false
requirements: [DEP-01] # enables the D-08 full security gate that certifies the DEP-01 hardening arc
user_setup:
  - service: snyk
    why: "yarn snyk:test sub-command of the D-08 security gate"
    env_vars:
      - name: SNYK_TOKEN
        source: "app.snyk.io → Account Settings → API token"
  - service: socket
    why: "yarn socket:scan sub-command of the D-08 security gate"
    env_vars:
      - name: SOCKET_CLI_API_TOKEN
        source: "socket.dev → Settings → API Tokens"
must_haves:
  truths:
    - "semgrep, osv-scanner, and git-secrets binaries respond on PATH (brew-installed after owner legitimacy approval)"
    - "git-secrets AWS patterns are registered in the gnus-ai repo config"
    - ".env carries SNYK_TOKEN and SOCKET_CLI_API_TOKEN keys with non-empty values and is git-ignored"
  artifacts:
    - path: ".env"
      provides: "Local token storage for snyk + socket sub-commands"
      contains: "SNYK_TOKEN / SOCKET_CLI_API_TOKEN keys"
  key_links:
    - from: "package.json socket:scan script"
      to: ".env"
      via: "grep SOCKET_CLI_API_TOKEN .env substitution"
      pattern: "SOCKET_CLI_API_TOKEN"
---

<objective>
Unblock the full D-08 security gate: install the three missing local CLIs (owner-legitimacy-gated) and acquire the two missing API tokens (human-gated).

Purpose: 07-RESEARCH Pitfall 2 — `yarn security-check` physically cannot run today (5 of 7 sub-commands blocked). Planning correction: snyk 1.1301.2 and @socketsecurity/cli 1.1.51 ALREADY exist in node_modules (verified: ./node_modules/.bin/snyk --version works) — only semgrep, osv-scanner, git-secrets need installing, and only the two tokens need human acquisition. D-08 forbids narrowing the gate; this plan is what makes "no narrowing" achievable.
Output: Working binaries + registered patterns + tokens in .env (never committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-dependency-hardening/07-RESEARCH.md
@.planning/phases/07-dependency-hardening/07-VALIDATION.md

<interfaces>
<!-- Verified current state (planning session 2026-08-27). -->

Already present and working (NO install needed):
  node_modules/.bin/snyk   → snyk 1.1301.2 (package.json devDependency, exact pin) — blocked only on SNYK_TOKEN
  node_modules/.bin/socket → @socketsecurity/cli 1.1.51 (devDependency) — blocked only on SOCKET_CLI_API_TOKEN
  /opt/homebrew/bin/slither → 0.11.5 (working, verified by research)

Missing (brew route; research marked all [ASSUMED] because slopcheck was not run):
  semgrep      → upstream github.com/returntocorp/semgrep
  osv-scanner  → upstream github.com/google/osv-scanner
  git-secrets  → upstream github.com/awslabs/git-secrets

npm "semgrep@1.0.0" devDependency is an inert stub (no bin, empty package) — 07-01 removes it; the real CLI comes from brew and resolves via PATH.

package.json scripts consumed downstream (07-03):
  "snyk:test":       snyk test --all-projects --exclude=yarn.lock --severity-threshold=medium
  "socket:scan":     SOCKET_CLI_API_TOKEN=$(grep SOCKET_CLI_API_TOKEN .env | cut -d'=' -f2) npx socket ci
  "osv:scan":        osv-scanner --lockfile=yarn.lock --format=json
  "semgrep:scan":    semgrep scan --config .semgrep.yml --exclude-rule weak-encryption --error
  "git-secrets:scan": git secrets --scan

.env key audit (2026-08-27): SNYK_TOKEN and SOCKET_CLI_API_TOKEN are ABSENT.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner approval of the three brew tool installs (supply-chain legitimacy gate)</name>
  <files>none (dossier presented in-session; no repo file changes)</files>
  <read_first>07-RESEARCH.md "Package Legitimacy Audit" table (the [ASSUMED] dispositions this checkpoint discharges)</read_first>
  <action>
Automate everything automatable BEFORE pausing: run "brew info semgrep osv-scanner git-secrets", retain the output at /tmp/07-02-brew-dossier.txt, extract each formula's homepage/source field, and cross-check it against the official upstream repositories (github.com/returntocorp/semgrep, github.com/google/osv-scanner, github.com/awslabs/git-secrets). Present the dossier and pause for the owner's blocking approval — no install may run before the resume signal.
  </action>
  <what-built>
A verification dossier for the three CLI installs the audit gate needs. Claude assembles it first:
1. Run "brew info semgrep osv-scanner git-secrets" and record the homepage/source each formula points to.
2. Cross-check against the official upstreams: semgrep → github.com/returntocorp/semgrep; osv-scanner → github.com/google/osv-scanner; git-secrets → github.com/awslabs/git-secrets.
3. Confirm none of the three is satisfied by a suspicious/proxy formula (the research legitimacy audit marked all three [ASSUMED] for this reason — the blocking gate exists so a human eyeballs the registry entries before code lands on the machine).
Note: snyk and socket need NO install — their CLIs already ship as exact-pinned devDependencies in node_modules (verified working). Only tokens remain for those (Task 3).
  </what-built>
  <how-to-verify>
1. Review the dossier Claude presents (brew info output + upstream repo URLs).
2. Optionally spot-check one upstream in a browser (e.g., github.com/awslabs/git-secrets).
3. Confirm the formulas resolve to the official upstreams, not third-party forks.
  </how-to-verify>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && brew info semgrep osv-scanner git-secrets > /tmp/07-02-brew-dossier.txt && grep -c 'semgrep' /tmp/07-02-brew-dossier.txt</automated>
    Dossier file exists and names all three formulas; the cross-check summary maps each formula to its official upstream.
  </verify>
  <acceptance_criteria>
    - /tmp/07-02-brew-dossier.txt exists and covers semgrep, osv-scanner, git-secrets
    - Each formula's source is mapped to the official upstream repo in the presented dossier
    - Zero installs executed before the owner's "approved" resume signal
  </acceptance_criteria>
  <done>Owner approval recorded; dossier retained; the three installs are cleared to proceed in Task 2.</done>
  <resume-signal>Type "approved" to proceed with the installs, or name a formula to reject/investigate further</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install semgrep, osv-scanner, git-secrets; register AWS secret patterns</name>
  <files>.git/config (untracked repo-local pattern registration via git-secrets — no tracked file changes)</files>
  <read_first>07-RESEARCH.md "Supporting" table + "Environment Availability", package.json security scripts</read_first>
  <action>
1. "brew install semgrep osv-scanner git-secrets" (formula names verified against brew info in Task 1; if a formula name differs on this machine, resolve the correct name via "brew search" and record it — do not install an unvetted substitute).
2. Version probes: "semgrep --version", "osv-scanner --version", and "git secrets --help" (git-secrets has no --version flag) — each must exit 0.
3. Register the AWS credential patterns for git-secrets IN THE GNUS-AI REPO: run "git secrets --register-aws" from the repo root (writes to the repo's git config — untracked, safe). If the repo uses a .git file (submodule gitdir indirection), git-secrets resolves it correctly; verify with "git secrets --list" showing added prohibited patterns.
4. Do NOT wire any hooks ("git secrets --install" stays unused — the repo uses husky; only the scan sub-command matters for the D-08 gate).
5. Smoke only the binaries in this task — the full scans and their dispositions belong to 07-03.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && semgrep --version && osv-scanner --version && git secrets --help > /dev/null && echo GITSECRETS-OK && git secrets --list | head -5</automated>
    Each version probe prints; GITSECRETS-OK prints; git secrets --list shows registered prohibited patterns.
  </verify>
  <acceptance_criteria>
    - semgrep --version, osv-scanner --version exit 0 with plausible version strings
    - git secrets --help exits 0 and git secrets --list shows AWS prohibited patterns registered in this repo
    - No git-secrets hooks installed (husky remains the only hook mechanism)
    - Installs happened only after the Task 1 blocking approval
  </acceptance_criteria>
  <done>All three missing CLIs functional on PATH; AWS patterns registered; the seven D-08 sub-commands are now locally runnable modulo tokens.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Owner acquires SNYK_TOKEN and SOCKET_CLI_API_TOKEN into .env</name>
  <files>.env</files>
  <read_first>.env (existing key names only — never print values), package.json socket:scan script (the .env grep dependency)</read_first>
  <action>
Run the safety pre-flight first: "git check-ignore -q .env && echo IGNORED" — it MUST print IGNORED; if it does not, STOP and surface (tokens must never land in a tracked file — threat T-07-06). Then pause for the owner to perform the two unautomatable steps: create the Snyk API token (app.snyk.io → Account Settings → API token) and the Socket token (socket.dev → Settings → API Tokens), appending SNYK_TOKEN=... and SOCKET_CLI_API_TOKEN=... to gnus-ai/.env themselves. On resume, verify key presence by name only.
  </action>
  <what-built>
Claude prepares and verifies the safe landing zone, then the owner performs the only two steps that cannot be automated (credential issuance lives behind account dashboards):
1. Snyk: app.snyk.io → Account Settings → API token (create/reveal).
2. Socket: socket.dev → Settings → API Tokens (create token).
Claude's pre-flight (already done before pausing): "git check-ignore -q .env && echo IGNORED" — .env must be proven git-ignored BEFORE any token is written, so credentials can never enter a commit (Information Disclosure threat T-07-06). If the check fails, STOP and surface — do not write tokens to a tracked file.
Owner action: append the two lines to gnus-ai/.env (SNYK_TOKEN=... and SOCKET_CLI_API_TOKEN=...). Never paste token values into chat, plan files, or commits.
  </what-built>
  <how-to-verify>
1. Confirm .env is git-ignored (Claude shows the git check-ignore proof).
2. Create the two tokens in the respective dashboards.
3. Append SNYK_TOKEN and SOCKET_CLI_API_TOKEN lines to gnus-ai/.env yourself (or tell Claude the values are placed).
4. Claude then verifies presence WITHOUT printing values: grep -c '^SNYK_TOKEN=..' .env → 1 and grep -c '^SOCKET_CLI_API_TOKEN=..' .env → 1, plus git status showing .env untracked.
  </how-to-verify>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && git check-ignore -q .env && echo IGNORED && grep -c '^SNYK_TOKEN=..' .env && grep -c '^SOCKET_CLI_API_TOKEN=..' .env && git status --porcelain .env | wc -l</automated>
    IGNORED, then 1, then 1, then 0 (key names asserted; values never printed; .env not staged).
  </verify>
  <acceptance_criteria>
    - git check-ignore proves .env untracked BEFORE any token was written
    - SNYK_TOKEN and SOCKET_CLI_API_TOKEN keys present with non-empty values
    - Token values never printed to chat, logs, plan files, or commits
    - Any refusal is recorded as an explicit D-08 blocker, never a silent skip
  </acceptance_criteria>
  <done>Both token keys live in the git-ignored .env; every D-08 sub-command is now executable locally with no narrowing.</done>
  <resume-signal>Type "tokens placed" once both keys exist in .env, or state which token is unobtainable (a refusal escalates as a D-08 blocker — it must never become a silent skip)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homebrew/PyPI registry → local machine | brew formulas deliver third-party executable code (legitimacy-gated below) |
| owner dashboards → .env | Credentials cross from external SaaS into a local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | brew installs of semgrep/osv-scanner/git-secrets | mitigate | Blocking-human legitimacy checkpoint (Task 1) before any install; formulas cross-checked against official upstream repos; research audit table drove the gate ([ASSUMED] dispositions) |
| T-07-06 | Information Disclosure | tokens written to a tracked file | mitigate | git check-ignore -q .env MUST pass before any token lands; values never printed to chat/logs/commits; verification greps assert key presence, not values |
| T-07-07 | Elevation of Privilege | git-secrets hook installation | mitigate | Only patterns registered (--register-aws); --install explicitly NOT used — husky stays the repo's only hook mechanism |
| T-07-08 | Tampering | token-less gate narrowing | accept (escalation path) | If a token is unobtainable, the blocker surfaces to the owner (D-08 forbids silent narrowing); CI (07-03) is designed to stay green without secrets — local D-08 is the strict path |
</threat_model>

<verification>
Task 1 blocking approval recorded; Task 2 binary probes; Task 3 key-presence greps (names only). All deterministic.
</verification>

<success_criteria>
- Three CLIs functional; AWS patterns registered; both token keys present in a git-ignored .env
- Every D-08 sub-command is now locally executable — 07-03 can run the complete gate with no narrowing
</success_criteria>

<output>
Create `.planning/phases/07-dependency-hardening/07-02-SUMMARY.md` when done
</output>
