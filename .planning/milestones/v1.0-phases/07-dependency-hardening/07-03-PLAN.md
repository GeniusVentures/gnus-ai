---
phase: 07-dependency-hardening
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - .planning/STATE.md
  - .github/workflows/security-audit.yml
autonomous: true
requirements: [DEP-01] # the D-08 gate certifies the hardened dependency tree; STATE 09-05 assigned Phase 7 the audit-gate wiring
must_haves:
  truths:
    - "Every one of the seven yarn security-check sub-commands has been executed locally with a written disposition (result, class, evidence)"
    - "slither reports exactly the 3 known Phase-9 false positives and yarn slither:scan --fail-high exits 0"
    - "yarn audit exits 0 (advisory fixes hold)"
    - ".github/workflows/security-audit.yml exists, parses as YAML, and runs the tokenless gate (immutable install, npm audit, slither --fail-high) with snyk/socket steps conditional on secret presence"
    - "D-08: the COMPLETE seven-sub-command security gate runs (audit, snyk, socket, osv, semgrep, slither, git-secrets) plus yarn test and yarn forge:test across the phase — no narrowing"
    - "D-09: audit failures are fixed at root cause in the OWNING phase or a gap-fix plan — never silently patched inside Phase 7 (routing events recorded, execution stops)"
  artifacts:
    - path: ".github/workflows/security-audit.yml"
      provides: "CI security-audit gate (STATE 09-05 wiring ownership)"
      contains: "yarn install --immutable"
    - path: ".planning/STATE.md"
      provides: "Phase 7 Decisions Logged (07-03) disposition record"
      contains: "### Phase 7 Decisions Logged (07-03)"
  key_links:
    - from: ".github/workflows/security-audit.yml"
      to: "package.json scripts"
      via: "yarn audit / yarn slither:scan / yarn snyk:test invocations"
      pattern: "yarn (audit|slither:scan|snyk:test)"
    - from: ".github/workflows/security-audit.yml"
      to: "GitHub secrets SNYK_TOKEN / SOCKET_CLI_API_TOKEN"
      via: "job-env presence probes (HAS_SNYK / HAS_SOCKET) gating conditional steps"
      pattern: "secrets\\.SNYK_TOKEN"
---

<objective>
Execute the complete D-08 security gate sub-command by sub-command with written dispositions, then wire the gate into CI as a GitHub Actions workflow (owner ruling 2026-08-27: CI is in-scope; STATE 09-05 assigned Phase 7 this wiring).

Purpose: The audit's value is that it is the complete gate — no silent narrowing. The disposition record is also the committed baseline the CI tolerances reference.
Output: Disposition table in STATE.md; security-audit.yml running the tokenless gate hard and the secret-gated scanners conditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/07-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-dependency-hardening/07-RESEARCH.md
@.planning/phases/07-dependency-hardening/07-PATTERNS.md

<interfaces>
<!-- The D-08 gate composition (package.json line 54, verbatim) -->
"security-check": "yarn audit && yarn snyk:test && yarn socket:scan && yarn osv:scan && yarn semgrep:scan && yarn slither:scan && yarn git-secrets:scan"

Individual scripts (package.json):
  "audit":           yarn npm audit --severity moderate
  "snyk:test":       snyk test --all-projects --exclude=yarn.lock --severity-threshold=medium
  "socket:scan":     SOCKET_CLI_API_TOKEN=$(grep SOCKET_CLI_API_TOKEN .env | cut -d'=' -f2) npx socket ci
  "osv:scan":        osv-scanner --lockfile=yarn.lock --format=json
  "semgrep:scan":    semgrep scan --config .semgrep.yml --exclude-rule weak-encryption --error
  "slither:scan":    slither . --config-file slither.config.json
  "git-secrets:scan": git secrets --scan

Known slither baseline (STATE.md 09-05, re-verified by research full-tree run 2026-08-27):
  exactly 3 findings, all dispositioned false positives:
    weak-prng          @ GNUSWithdrawLimiterStorage.calculateCurrentBin (deterministic bin indexing)
    erc721-interface   @ GNUSBridge.approve (intentional ERC-20 facade)
    erc721-interface   @ GNUSBridge.transferFrom (intentional ERC-20 facade)
  slither 0.11.5 exits 255 whenever findings print; --fail-high/--fail-medium/--fail-none flags verified present; --triage-mode does NOT exist in 0.11.5.

CI convention source (PATTERNS, from _vendor-diamondslab workflows): runs-on ubuntu-latest; permissions: contents: read; actions/checkout@v4; actions/setup-node@v4 node-version '24' + cache: 'yarn'; corepack enable (packageManager yarn@4.10.3); concurrency group with cancel-in-progress; repo scripts over restated tool commands; NEVER the legacy NPM_TOKEN/registry-url/--ignore-scripts publish pattern; semgrep/semgrep-action is deprecated upstream — use the CLI natively.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute all seven D-08 sub-commands locally and write the disposition record into STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>package.json security scripts (lines 35-55), .planning/STATE.md (09-05 section + decisions-log format), 07-RESEARCH.md Pattern 2 + Pitfalls 3/4/5, 07-VALIDATION.md known baselines</read_first>
  <action>
Run each sub-command INDIVIDUALLY (never piped — Pitfall 5; redirect output to /tmp/07-03-<name>.log for the record and check the exit code explicitly). Required outcomes:
1. "yarn audit" → exit 0 (07-01's advisory fixes hold). Non-zero here = regression: STOP and route per D-09.
2. "yarn snyk:test" → completes with token from 07-02. Capture the summary. Any medium+ vulnerability is a D-09 routing event: record it, STOP, report — do NOT auto-upgrade dependencies to chase snyk findings (that is the exact scope explosion the owner bounded).
3. "yarn socket:scan" → completes. socket ci reports policy alerts (install scripts, typosquat heuristics) — disposition each alert class in the record; alerts are advisories, not failures.
4. "yarn osv:scan" → JSON output. Its advisory space should be a subset of what npm audit already covered (same yarn.lock); anything genuinely NEW gets its own disposition row (route per D-09 if CVE-class).
5. "yarn semgrep:scan" → FIRST-EVER run (binary installed in 07-02). Capture the complete findings list — this becomes the committed baseline. Triage each finding against the three custom rules in .semgrep.yml (diamond-selector-collision, unsafe-external-call, insecure-private-key); suspected-real hits are D-09 routing events, not in-phase fixes.
6. "yarn slither:scan > /tmp/07-03-slither-raw.log 2>&1" — EXACTLY this filename (the verify block greps /tmp/07-03-slither-raw.log for the FP count; a differently-named log makes that grep silently no-op behind its 2>/dev/null) → exit 255 is EXPECTED with exactly the 3 known Phase-9 false positives listed in the interfaces block (same detectors, same sites). A 4th finding = D-09 route. Then prove the severity gate: "yarn slither:scan --fail-high" must exit 0 (no HIGH findings). Do NOT add // slither-disable pragmas to contract source — this phase writes no contract code.
7. "yarn git-secrets:scan" → exit 0 (AWS patterns registered in 07-02). Any hit is a critical stop: potential committed credential.
8. Aggregate honesty note for the record: the chained "yarn security-check" command chain-stops at slither:scan (exit 255) because slither 0.11.5 counts the 3 dispositioned FPs against its default exit code and has no triage mode. Per-sub-command execution + written dispositions = the D-08 record; the severity gate is expressed via --fail-high.
9. Append a "### Phase 7 Decisions Logged (07-03)" section to STATE.md (format matches the Phase 15 sections): one disposition table with columns sub-command | result | class | evidence, plus bullets for the slither exit-code mechanics, the semgrep first-baseline capture, and the CI semgrep promotion follow-up — tracked in STATE, not only in a workflow comment: "Promote the CI semgrep step from continue-on-error advisory to hard gate (drop continue-on-error in .github/workflows/security-audit.yml) once the Task 1 local first-run baseline is confirmed stable across runs." Do NOT touch the STATE.md frontmatter progress block (GSD-managed).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && unset npm_config_prefix && yarn audit > /tmp/07-03-audit.log 2>&1; echo "AUDIT_EXIT=$?" && yarn slither:scan --fail-high > /tmp/07-03-slither-failhigh.log 2>&1; echo "SLITHER_FAILHIGH_EXIT=$?" && grep -c 'weak-prng' /tmp/07-03-slither-raw.log && grep -c 'Phase 7 Decisions Logged (07-03)' .planning/STATE.md && grep -c 'continue-on-error' .planning/STATE.md</automated>
    AUDIT_EXIT=0; SLITHER_FAILHIGH_EXIT=0; the raw slither log (/tmp/07-03-slither-raw.log — created verbatim by action step 6; a missing file now fails the chain instead of silently no-oping) shows weak-prng ≥1; STATE.md contains the new decisions section AND the CI semgrep promotion follow-up (continue-on-error ≥1).
  </verify>
  <acceptance_criteria>
    - All seven sub-commands individually executed with logs retained under /tmp and exit codes recorded
    - The raw slither log is written to exactly /tmp/07-03-slither-raw.log — the filename the FP-count grep targets
    - Disposition table present in STATE.md under "Phase 7 Decisions Logged (07-03)" with one row per sub-command
    - The CI semgrep promotion-to-hard-gate follow-up is recorded as a bullet in the STATE.md decisions section (not only a workflow comment)
    - yarn audit exits 0; slither findings are exactly the 3 Phase-9 FPs; slither --fail-high exits 0; git-secrets exits 0
    - No new finding was silently patched, waived, or suppressed — every unexpected result is a recorded routing event
    - STATE.md frontmatter untouched
  </acceptance_criteria>
  <done>The complete D-08 gate has run end-to-end locally with a written disposition for every sub-command, and the baseline for CI tolerances is committed to the planning record — including the tracked semgrep promotion follow-up.</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/security-audit.yml (tokenless-hard gate, secret-conditional scanners)</name>
  <files>.github/workflows/security-audit.yml</files>
  <read_first>07-PATTERNS.md section ".github/workflows/security-audit.yml (NEW)" including both vendored workflow excerpts, 07-RESEARCH.md "CI workflow skeleton" + "State of the Art" (semgrep-action deprecated), package.json scripts + packageManager field</read_first>
  <action>
Create the workflow at gnus-ai/.github/workflows/security-audit.yml (gnus-ai is its own GitHub repo — workflows live HERE, never in the outer TokenContracts repo; no .github/workflows directory exists yet, create it). Concrete specification:
- name: security-audit; on: push, pull_request, workflow_dispatch.
- concurrency: group "security-audit-${{ github.ref }}", cancel-in-progress: true.
- permissions: contents: read (least privilege — nothing publishes, so NO id-token: write).
- Single job "audit", runs-on: ubuntu-latest, job-level env defining the secret-presence probes exactly as: HAS_SNYK: ${{ secrets.SNYK_TOKEN != '' }} and HAS_SOCKET: ${{ secrets.SOCKET_CLI_API_TOKEN != '' }} (job-level env may reference secrets; step-level "if" reads job env — this is the documented mechanism that keeps the gate green without secrets and stricter with them; on fork PRs secrets are empty and the steps skip by design).
- Steps in order:
  1. actions/checkout@v4
  2. actions/setup-node@v4 with node-version: '24' and cache: 'yarn'
  3. "corepack enable" (packageManager field resolves yarn 4.10.3)
  4. "yarn install --immutable" — the DEP-01 determinism gate in CI (hard fail)
  5. "yarn npm audit --severity moderate" — hard gate; exit 0 is guaranteed by 07-01, so red here correctly signals a newly-published advisory
  6. actions/setup-python@v5 with python-version: '3.12', then "pip install slither==0.11.5" (exact parity with the local gate version)
  7. Foundry toolchain: "curl -L https://foundry.paradigm.xyz | bash" then "foundryup", with ~/.foundry/bin added to PATH (slither drives forge build for this repo)
  8. "yarn slither:scan --fail-high" — hard gate; the same invocation proven exit-0 locally in Task 1 (yarn run appends the flag to the repo script)
  9. "pip install semgrep", then "yarn semgrep:scan" with continue-on-error: true plus actions/upload-artifact@v4 (if: always()) of the scan output — ADVISORY BOOTSTRAP: the first CI semgrep baseline; include a comment in the workflow stating the promotion-to-hard-gate condition (once the 07-03 local baseline is confirmed stable across runs, drop continue-on-error). This is the ONLY non-hard step — never a bare "|| true" without a committed baseline. The same promotion condition is tracked in STATE.md by Task 1 step 9 so it survives as a follow-up beyond the workflow comment.
  10. Snyk step: "if: env.HAS_SNYK == 'true'", env SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}, run "yarn snyk:test"
  11. Socket step: "if: env.HAS_SOCKET == 'true'", env SOCKET_CLI_API_TOKEN: ${{ secrets.SOCKET_CLI_API_TOKEN }}, run "npx socket ci" — NOT "yarn socket:scan", because the repo script hard-substitutes the token from a ".env" grep that does not exist on a runner; document this one-line deviation in a workflow comment.
- FORBIDDEN anywhere in the file (grep gates below enforce): pull_request_target, id-token: write, NPM_TOKEN, registry-url, --ignore-scripts.
- No "yarn test"/"yarn forge:test" in CI — Foundry tests need the local-network harness (07-RESEARCH responsibility map keeps the test gate local for this phase).
- Pre-merge honesty: the executor cannot observe a live Actions run (no push during execution). Structural verification below + the local-equivalent command proofs from Task 1 are the pre-merge evidence; the first live run happens on the phase PR.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/TokenContracts/gnus-ai && /usr/bin/ruby -ryaml -e 'YAML.load_file(".github/workflows/security-audit.yml"); puts "YAML-OK"' && grep -c 'contents: read' .github/workflows/security-audit.yml && grep -c 'concurrency' .github/workflows/security-audit.yml && grep -c "node-version: '24'" .github/workflows/security-audit.yml && grep -c "cache: 'yarn'" .github/workflows/security-audit.yml && grep -c -- '--immutable' .github/workflows/security-audit.yml && grep -c -- '--fail-high' .github/workflows/security-audit.yml && grep -c 'continue-on-error: true' .github/workflows/security-audit.yml ; grep -Ec 'pull_request_target|id-token|NPM_TOKEN|registry-url|ignore-scripts' .github/workflows/security-audit.yml</automated>
    Expected outputs in order: YAML-OK, 1 (permissions), ≥1 (concurrency), 1 (node 24), 1 (yarn cache), ≥1 (immutable), ≥1 (fail-high), exactly 1 (the sole advisory step), 0 (forbidden patterns).
  </verify>
  <acceptance_criteria>
    - File parses as YAML (system ruby YAML.load_file exits 0)
    - permissions: contents: read present; no id-token grant
    - concurrency group with cancel-in-progress present
    - Node 24 + cache: 'yarn' + corepack + yarn install --immutable sequence present
    - Hard gates: npm audit (post-fix exit-0) and slither --fail-high; exactly one continue-on-error step (semgrep bootstrap) with its promotion condition documented in a comment and tracked in STATE.md (Task 1 step 9)
    - HAS_SNYK/HAS_SOCKET job-env presence probes gate the snyk/socket steps; socket uses "npx socket ci" with the documented rationale
    - Zero occurrences of pull_request_target, id-token, NPM_TOKEN, registry-url, ignore-scripts
    - CI slither pinned to 0.11.5 (parity with the local gate that produced the disposition baseline)
  </acceptance_criteria>
  <done>The audit gate is wired into CI per the mined conventions: green without secrets, stricter with them, every tolerance documented — STATE 09-05's assignment is discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI runner | Untrusted code changes run on the gate's infrastructure |
| GitHub secrets → workflow env | Credentials cross into runner processes only when present |
| scanner registries → findings | External advisory databases drive gate verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Elevation of Privilege | CI workflow | mitigate | push/pull_request only — NEVER pull_request_target (fork code never gets secret access); permissions locked to contents: read; concurrency cancels superseded runs |
| T-07-10 | Information Disclosure | secrets in workflow | mitigate | Tokens flow only via env mapping; presence-probed (HAS_*) so absence skips cleanly; no step echoes token values; fork PRs get empty secret context by GitHub design |
| T-07-11 | Tampering | slither gate bypass | mitigate | No "|| true" and no source pragmas; severity expressed via --fail-high (verified flag), with the 3-FP baseline recorded in STATE.md and the exit-255 mechanics documented |
| T-07-12 | Tampering | semgrep advisory step | mitigate | Single continue-on-error step with artifact upload and a written promotion condition — a bootstrap, not a silent pass; count enforced by grep gate (exactly 1); the promotion is also tracked as a STATE.md follow-up so it cannot live only in a workflow comment |
| T-07-13 | Tampering | scanner finding drift (snyk/socket/osv) | accept | External databases drift daily by nature; dispositions record the phase-time truth, unexpected findings route per D-09 rather than being absorbed |
| T-07-14 | Denial of Service | CI runtime cost | accept | foundryup + solc + forge build is minutes, not hours; concurrency group bounds duplicate runs |

<verification>
Task 1 exit-code proofs (audit=0, slither --fail-high=0, git-secrets=0, 3-FP identity against the pinned /tmp/07-03-slither-raw.log) + STATE.md section/promotion greps; Task 2 YAML parse + convention greps + forbidden-pattern zero-count.
</verification>

<success_criteria>
- Seven dispositions written; no silent narrowing; no new finding absorbed
- Workflow structurally valid, conventions enforced, secrets mechanism documented and grep-verified; semgrep promotion tracked in STATE
</success_criteria>

<output>
Create `.planning/phases/07-dependency-hardening/07-03-SUMMARY.md` when done
</output>
