---
phase: 07-dependency-hardening
plan: "01"
subsystem: infra
tags: [yarn4, supply-chain, dependency-pinning, eslint, hardhat-multichain, npm-audit]

# Dependency graph
requires:
  - phase: 15-secure-bridgein
    provides: phase-exit test baselines (Hardhat + Foundry) this plan proves neutrality against
provides:
  - Deterministic contracts-starter build input (DEP-01 pin, both manifests, immutable-install green)
  - An audit sub-gate that exits 0 with zero advisories and zero waivers (owner ruling 2026-08-27 satisfied)
  - Renamed @diamondslab/hardhat-multichain plugin with proven task registration and retargeted imports
  - eslint 10.9.1 toolchain with byte-identical lint behavior vs the 9.39.5 capture
affects: [07-02, 07-03, 07-04, dependency-maintenance, devcontainer-parity]

# Tech tracking
tech-stack:
  added:
    - "@eslint/js 10.0.1 (direct dep; first-party, previously transitive via eslint 9)"
    - "@eslint/eslintrc 3.3.6 (direct dep; first-party, already in lockfile graph)"
  patterns:
    - "Yarn 4 git pin via #commit=<sha> keyword form; lockfile only ever regenerated by yarn install"
    - "Plugin rename blast-radius check: grep old package name across sources before removing it"
    - "Lint toolchain bump gated on a byte-identical --fix-dry-run capture (pre/post diff = proof)"

key-files:
  created: []
  modified:
    - package.json
    - yarn.lock
    - .devcontainer/config/package.json (nested submodule)
    - hardhat.config.ts
    - scripts/setup/RPCDiamondDeployer.ts
    - 22 test/ files + test/utils/test-template.ts + 5 test-suites/deployment files (import retarget)

key-decisions:
  - "Zero-drift pin (D-02/D-03): #commit=bf67b736ad5fa3366551f599e204784856fb3069 appended in root + devcontainer manifests; yarn re-keyed exactly 2 descriptor lines with resolution+checksum byte-identical"
  - "Owner escalation 1 (checkpoint): eslint major jump 9.39.5 -> 10.9.1 approved — the whole 9.x line is registry-EOL so 9.39.5 could not clear the advisory"
  - "Owner escalation 2 (checkpoint): @typescript-eslint 8.51.0 -> 8.68.0 (Option A) approved — 8.51.0 is API-incompatible with eslint 10 (FlatESLint extends undefined); A+ declined so eslint-plugin-promise stays 7.2.1 with a residual peer warning (same class as pre-existing hardhat/@types/chai YN0060s)"
  - "Fresh Hardhat baseline captured as 665/2/1 (deterministic across 2 runs; failure set identical-in-kind) — STATE.md's 661 figure is stale, matching 07-RESEARCH Pitfall 6 which records the orchestrator's 665 observation"
  - "Full yarn test-multichain forked-network run dispositioned out of scope per plan; npx hardhat help test-multichain registration + config-load smoke is the coverage"

patterns-established:
  - "Lint-bump proof: capture --fix-dry-run output pre-bump, require byte-identical post-bump before accepting any toolchain change"

requirements-completed: [DEP-01]

# Metrics
duration: 46min
completed: 2026-08-27
---

# Phase 7 Plan 1: DEP-01 Pin + In-Phase Advisory Fixes Summary

**contracts-starter pinned to bf67b736 with a zero-drift 2-line lockfile re-key, both deprecation advisories eliminated in-phase (multichain rename + eslint 10.9.1 via two owner-approved escalations), audit gate exit 0, and the full matrix proven neutral at the documented baselines**

## Performance

- **Duration:** 46 min wall clock (spans two owner checkpoints)
- **Started:** 2026-08-27T20:49:36Z
- **Completed:** 2026-08-27T21:35:21Z
- **Tasks:** 3/3 (Task 2 landed as two commits — partial + completion — around the owner escalations)
- **Files modified:** 31 in gnus-ai root + 1 in the nested .devcontainer submodule

## Accomplishments

- DEP-01 closed: both manifests pinned, `yarn install --immutable` green, lockfile diff exactly the 2 descriptor lines with byte-identical resolution + checksum (zero drift, D-02)
- `yarn npm audit --severity moderate` exits **0 with zero advisories and zero waivers** — the owner's 2026-08-27 ruling is satisfied in full
- hardhat-multichain 1.0.6 → @diamondslab/hardhat-multichain 1.1.0 rename with all 27 consuming source files retargeted and the `test-multichain` task registration proven intact
- eslint 9.39.2 → 10.9.1 with @typescript-eslint 8.68.0, proven behavior-identical by byte-diff of the pre/post `--fix-dry-run` captures (56 pre-existing findings, exit 1 both sides)
- Inert semgrep npm stub removed (zero-imports precondition gate passed)
- Full matrix: Hardhat 665 passing / 2 pending / 1 failing (only GNUSControlStorage chainID), Foundry 215 passed / 2 failed / 3 skipped (only the Phase 08.1 Safe setUp reverts), multichain smoke exit 0

## Task Commits

Each task was committed atomically:

1. **Task 1: Pin contracts-starter (root + devcontainer) + lockfile determinism** — `75ebe8c` (chore) + `.devcontainer` submodule `b1f1dd3`
2. **Task 2 (partial): multichain rename + import retarget + semgrep stub removal + eslint 9.39.5** — `839e05a` (chore)
3. **Task 2 (completion): eslint 10.9.1 + typescript-eslint 8.68.0 + @eslint/* declarations + devcontainer mirror** — `5c7de56` (chore) + `.devcontainer` submodule `812ae69`
4. **Task 3: verification-only gate — no file modifications, no commit** (per plan)

**Plan metadata:** (this commit)

## Files Created/Modified

- `package.json` — pin + @diamondslab rename + eslint 10.9.1 + @typescript-eslint 8.68.0 + @eslint/* declarations + semgrep stub removed
- `yarn.lock` — regenerated only by `yarn install` (pin hunk isolated in `75ebe8c`; advisory hunks in `839e05a`/`5c7de56`)
- `hardhat.config.ts` — plugin import renamed
- `.devcontainer/config/package.json` — pin + mirrored eslint resolution (nested submodule commits `b1f1dd3`, `812ae69`)
- `scripts/setup/RPCDiamondDeployer.ts` + 22 `test/**` files + 5 `test-suites/deployment/**` files — multichain import specifier retarget

## Decisions Made

- Kept the pin hunk isolated in its own commit before any advisory edits (per plan) so the zero-drift diff stayed verifiable
- eslint escalation path taken only through blocking checkpoints; each attempt's failure mode was captured as evidence (config-load crash, then FlatESLint API crash) before escalation
- `.devcontainer` handled as a nested submodule: changes land inside `diamonds-devcontainer` (develop branch) + gitlink bumps in gnus-ai — matching the repo's established `a5d0b2f`/`f8e7c90` pattern
- Recorded 665/2/1 as the fresh authoritative Hardhat baseline (see Deviations #6) rather than silently matching the stale 661

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 27-file import retarget for the multichain rename**
- **Found during:** Task 2
- **Issue:** Plan expected only the hardhat.config.ts:213 comment + doc text to reference the old name after the rename; 23 source files (18 test/unit, 3 test/integration, test/deployment, test/utils, 5 test-suites/deployment) plus RPCDiamondDeployer.ts import `{ multichain }` / the plugin from `'hardhat-multichain'` — unresolvable once the old package left node_modules; the whole Task 3 matrix would have failed at load
- **Fix:** Mechanical specifier rename to `'@diamondslab/hardhat-multichain'` (renamed package exports the identical `multichain` symbol; verified in its dist/index.d.ts)
- **Files modified:** 27 source files
- **Verification:** zero residual quoted old-name imports; full matrix at baseline
- **Committed in:** `839e05a`

**2. [Rule 1 - Bug] Declared the config's undeclared imports (@eslint/js, @eslint/eslintrc)**
- **Found during:** Task 2 (eslint 10 proof)
- **Issue:** committed `eslint.config.mjs` imports `@eslint/js` + `@eslint/eslintrc` without declaring them; they resolved only via eslint 9's transitive hoisting. eslint 10 dropped `@eslint/js` from its own tree → `ERR_MODULE_NOT_FOUND` at config load
- **Fix:** declared `@eslint/js@10.0.1` + `@eslint/eslintrc@3.3.6` as exact-pinned direct deps (first-party eslint-org packages; 3.3.6 already resolved in the lockfile, 10.0.1 is the eslint-10 line of @eslint/js — note it publishes at major boundaries, there is no 10.9.1)
- **Files modified:** package.json, yarn.lock
- **Verification:** config loads; dry-run proof identical; audit exit 0
- **Committed in:** `5c7de56`

**3. [Repo mechanics] .devcontainer is a nested submodule, not a plain directory**
- **Found during:** Task 1 (first commit attempt failed: "Pathspec ... is in submodule '.devcontainer'")
- **Issue:** plan listed `.devcontainer/config/package.json` as an ordinary file; it lives in the `diamonds-devcontainer` submodule (branch develop)
- **Fix:** pin + eslint mirror landed as submodule commits (`b1f1dd3`, `812ae69`) with gitlink bumps in the gnus-ai root commits — the repo's established pattern
- **Verification:** `git submodule status` clean; both submodule commits on develop

**4. [Owner escalation 1 — checkpoint] eslint major jump to 10.9.1**
- **Found during:** Task 2
- **Issue:** eslint 9.39.5 (max <10) still carried the deprecation — the entire 9.x line is registry-EOL; the plan forbids major jumps on executor discretion
- **Fix:** blocking checkpoint issued; owner approved "approved eslint 10.9.1"; landed with the plugin-compat proof (install exit 0, `npx eslint --version` v10.9.1, byte-identical `--fix-dry-run` on hardhat.config.ts + GNUSBridge.test.ts vs the 9.39.5 capture)
- **Committed in:** `5c7de56`

**5. [Owner escalation 2 — checkpoint] @typescript-eslint 8.51.0 → 8.68.0**
- **Found during:** Task 2 (escalation 1's proof)
- **Issue:** typescript-eslint 8.51.0 is API-incompatible with eslint 10 (`@typescript-eslint/utils/dist/ts-eslint/eslint/FlatESLint.js:12 — TypeError: Class extends value undefined`); fixing requires a plugin bump — owner territory per coordinator instruction
- **Fix:** blocking checkpoint issued with the full 7-plugin registry matrix; owner approved Option A (plain): plugin+parser → 8.68.0, @eslint/* declarations, eslint-plugin-promise stays 7.2.1
- **Committed in:** `5c7de56`

**6. [Observation — recorded, not fixed] Hardhat baseline is 665/2/1, not 661/2/1**
- **Found during:** Task 3
- **Issue:** plan/STATE pin 661 passing; two fresh runs both report 665 passing / 2 pending / 1 failing with the failure set EXACTLY the documented known-stale one (GNUSControlStorage "should return initial protocol info", chainID 31337 vs 0) and nothing new failing
- **Disposition:** 07-RESEARCH.md Pitfall 6 already records the orchestrator observing 665 vs STATE's 661 — the STATE figure is stale record-keeping, not a regression from this plan (test files unchanged except import specifiers; no conditional test gating found; provider maps feed fixtures, not enumeration). Recorded here as the authoritative fresh baseline for 07-03/07-04 gates; no test was modified

**7. [Plan arithmetic] yarn.lock grep count is 3, not the predicted 2**
- The pattern also matches the pre-existing `resolution:` line (which already carried `#commit=bf67b736…` pre-pin — that byte-identity IS the zero-drift proof). Substantive gates (0 non-contracts-starter diff lines, 0 checksum drift) all held.

---

**Total deviations:** 5 auto-fixed/mechanics (1× Rule 3, 1× Rule 1, 1× repo mechanics, 2 recorded observations) + 2 owner-approved escalations via blocking checkpoints
**Impact on plan:** All deviations were forced by the plan's own gates; no scope creep. The two escalations are the owner's explicit decisions, taken only after evidence-producing failed proofs.

## Issues Encountered

- `npx` initially resolved eslint from the parent TokenContracts directory (harness resets cwd between calls) and pulled eslint@10.9.1 into the npx cache while running against the wrong directory — harmless (`--fix-dry-run`, cache-only) and it pre-verified 10.9.1 executes
- One transient `YN0008: secp256k1@npm:4.0.4 must be rebuilt` notice on the final install (dependency-tree change side effect); install exited 0 and both test suites ran green — no action needed
- Husky hooks are inert in this clone (`core.hooksPath` unset) — pre-existing environmental state documented in 07-RESEARCH Pitfall 2; commits therefore ran without lint-staged side effects

## Proof Ledger

| Gate | Result |
| ---- | ------ |
| `grep -c '#commit=bf67b736…'` package.json / yarn.lock / .devcontainer | 1 / 3 / 1 (3rd lockfile hit = pre-existing resolution line; see Deviation 7) |
| `git diff yarn.lock` (pin commit) | exactly 2 descriptor lines; resolution + checksum byte-identical |
| `yarn install --immutable` | exit 0 (IMMUTABLE-OK) |
| Advisory re-capture pre-fix | exactly the 2 planning-time IDs, no delta |
| `yarn npm audit --severity moderate` (unpiped) | **exit 0, "No audit suggestions"** |
| eslint proof | v10.9.1; `--fix-dry-run` byte-identical to 9.39.5 capture (56 pre-existing findings, exit 1 both) |
| `yarn test` | 665 passing / 2 pending / 1 failing (only GNUSControlStorage chainID); deterministic across 2 runs |
| `yarn forge:test` | 215 passed / 2 failed / 3 skipped — failures are exactly SafeSingleShotUpgrade.t.sol + SafeDiamondCut.t.sol setUp reverts |
| `npx hardhat help test-multichain` | exit 0; "Launches multiple forked Hardhat networks" printed once |

## Known Stubs

None — no stub patterns introduced.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: supply-chain | package.json | `@eslint/js@10.0.1` + `@eslint/eslintrc@3.3.6` promoted from transitive to direct deps (T-07-SC claimed "no new packages enter"; both were already in the pre-change lockfile graph and are first-party eslint-org packages, but the direct-declaration surface is new and recorded for the verifier) |

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 07-02 prerequisites (tool installs + tokens) are the next gate's blockers, unchanged
- 07-03/07-04 gates should use **665/2/1** as the Hardhat baseline (this SUMMARY is the evidence record); 215/2/3 unchanged
- eslint 10 landed with identical behavior; the repo's lint findings remain 56 pre-existing errors on the two representative files — a lint-debt cleanup is future maintenance, out of scope here

## Self-Check: PASSED

- SUMMARY file exists at `.planning/phases/07-dependency-hardening/07-01-SUMMARY.md`
- Commits verified in git log: `75ebe8c`, `839e05a`, `5c7de56` (root) + `b1f1dd3`, `812ae69` (.devcontainer submodule)
- ROADMAP.md Phase 7 row reads `1/4 | In Progress`; REQUIREMENTS.md DEP-01 checkbox `[x]` + traceability row `Complete`
- Task 3 gate evidence: Hardhat 665/2/1 (two deterministic runs), Foundry 215/2/3 (only the Phase 08.1 Safe setUp reverts), multichain help smoke exit 0

---
*Phase: 07-dependency-hardening*
*Completed: 2026-08-27*
