# Test Suites

This directory contains test suites for the deployment of the Genius Diamond contracts that
are not included in the main test directory. This is because they require a specific deployment environment and setup that is not present in the standard test configuration.
